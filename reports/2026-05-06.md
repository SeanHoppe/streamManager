# Daily task log — 2026-05-06

Cycle: v1.8. Branch: `main` (P1 + P1a + P1c merged via PR #94).

---

## State at session start

| Item | Status |
|---|---|
| P1 content-detection wiring (`governance._evaluate_inner_core`) | ✅ merged #93 |
| P1a soak prompt coverage (prose-pattern widening) | ✅ merged |
| P1c-B deliberative+destructive hybrid prompts in `_L2_L3_TRIGGER` | ✅ merged #94 |
| Alignment-eval `--ci-gate` (report: `alignment-eval-20260505T181101Z`) | ✅ 0 FR-OG-7 regressions |
| `cli_dispatch_fallback_ms` p95 > 0 in any soak | ❌ 0.00 in all runs |

### Lever-dormant root cause (found 2026-05-06 morning)

- Hybrid prompts at soak positions 5 (`"Wondering whether to delete the old metrics table..."`) and 55 (`"Not sure whether to drop the staging table..."`) both fire `_looks_ambiguous_block=True` (unit tests confirm).
- BUT Haiku returns confidence ≥ 0.70 on these mild deliberative phrases → `BRIDGE_L4_FALLBACK_CONFIDENCE` floor (0.70) never crossed → no Sonnet retry → `cli_dispatch_fallback_ms` = 0.
- P1c-B theory was wrong: deliberative framing makes LLMs MORE confident, not less.
- "Should I force push..." (pos ≥ 60) not even emitted in the first 60-event sequence (seed=4242).

**P2a is NOT closed. P2 is BLOCKED until a soak shows non-zero `cli_dispatch_fallback_ms`.**

---

## Today's work order

### ☐ P2a — Fix fallback-trigger corpus (ACTIVE BLOCKER)

**Goal:** soak shows `cli_dispatch_fallback_ms` p95 > 0 AND ≥ 1 `governance_fallback_routed` envelope.

**Fix 1 (preferred — extend soak corpus):** replace/add 1-2 `_L2_L3_TRIGGER` payloads with content that genuinely causes Haiku to return confidence < 0.70. Candidates that should produce borderline verdicts:
- Mixed-intent plans touching protected symbols (e.g. "drop the cli_pool table and rebuild the pool from scratch — it keeps deadlocking")
- Commands framed as executed (not deliberative) that are ambiguous but not obviously BLOCK-worthy (so Haiku returns ~0.50-0.65, not 0.95)
- Multi-step plans with contradictory safety signals (allow + destroy in same sentence)

**Do NOT** use fix 2 (lower `BRIDGE_L4_FALLBACK_CONFIDENCE` floor) unless fix 1 exhausted — floor change risks over-firing in production.

After fix: re-run short soak (~12 min, `--cli-pool-size 2`). Verify `cli_dispatch_fallback_ms` p95 > 0.

Prompt: `docs/prompts/v1.8-orchestration/phase-2a-soak-prompt-coverage.md`

---

### ☐ P2 — v1.8 ship-gate + ADR-5 + lever-effect measurement (BLOCKED on P2a)

**Pre-conditions:**
- [ ] P2a merged to `main`, short re-soak shows `cli_dispatch_fallback_ms` p95 > 0

**Steps (mirror v1.7 P3):**

1. **Tier-3 ship-gate soak** — `python tools/soak_driver.py --cli-pool-size 2`, ~32 min, `run_in_background` + `ScheduleWakeup`. Report: `reports/soak-<UTC>Z.md`. Verify all 3 ALLOW breakout blocks present; `cli_dispatch_fallback_ms` p95 > 0; fire rate < 30%.
2. **Alignment-eval gate** — `python tools/alignment_eval.py --ci-gate`. Report: `reports/alignment-eval-<UTC>Z.md`. FR-OG-7 row gate must exit 0.
3. **ADR-5 v1.8 baseline** — append `## v1.8 ship-gate baseline` to `docs/adr/ADR-5-latency-budget.md`. Append-only. Annotate v1.7 §"Caveats" lever bullet.
4. **Lever-effect §"Caveats"** — fallback fire rate (n + %); `cli_pool_send_ms` p95 vs v1.7 baseline (5128.96 ms); ALLOW p95 vs v1.7 (5.13 s); L4 alignment p95 vs v1.7 (13.41 s); lever falsification re-check.
5. **LM p95 trend** — vs v1.7 baseline 11.95 s. Re-open watch if ≥ 18 s + >1 s magnitude.
6. **CHANGELOG** — append `## [1.8.0]`. Mirror v1.7.0 style.
7. **Tag** — `v1.8.0` on merge commit, push to origin.

v1.7 baseline refs: `reports/soak-20260505T125741Z.md`, `reports/alignment-eval-20260505T124519Z.md`.

Prompt: `docs/prompts/v1.8-orchestration/phase-2-ship-gate-finalize.md`

---

## Mint-new-phase triggers today

| Trigger | Action |
|---|---|
| P2a re-soak still 0 fire rate | Falsification confirmed → v1.9 backlog item; v1.8.0 ships as latency no-op |
| P2 soak fire rate > 30% | Mint `phase-2c-fallback-floor-tuning.md` |
| P2 `cli_pool_send_ms` p95 no drop despite non-zero fires | v1.9 backlog: Haiku TTFT not faster than Sonnet on this prompt distribution |
| P2 alignment-eval FR-OG-7 gate fires | ABORT merge; debug `requires_alignment` routing precedence |
| P2 LM p95 ≥ 18 s + >1 s margin | Mint `phase-2b-lm-regression-triage.md` |
| PR reviewer blockers | Mint `phase-2d-ship-pr-fixups.md` |
| Tag push failure | Mint `phase-2e-tag-recovery.md` |

---

## Key numbers (v1.7 ship-gate baseline)

| Metric | v1.7 baseline | v1.8 target |
|---|---|---|
| Overall p95 | 9.277 s | ≤ budget |
| ALLOW p95 | 5.13 s | ↓ if lever fires |
| L4 alignment p95 | 13.41 s | stable (Sonnet-only) |
| LM categorize p95 | 11.95 s | stable (< 18 s ceiling) |
| `cli_pool_send_ms` p95 | 5128.96 ms | ↓ if lever fires |
| `cli_dispatch_fallback_ms` p95 | 0.00 ms | **> 0 required** |
| Fallback fire rate | 0% | > 0%, < 30% |

---

## Completed today

- [x] Session-start state audit (lever-dormant root cause identified)
- [x] Daily task file created (`reports/2026-05-06.md`)
