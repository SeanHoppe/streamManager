# v2.5 backlog

Carry-forwards from v2.5.1 P2 ship-gate (2026-05-20) into v2.6. v2.5
was a **consolidation cycle** (P0 + P2 BLOCK + v2.5.1 P1 corrective +
v2.5.1 P2 ship); net production-bucket LOC = **0** vs cycle-tip
`634e9d1d982a3b6071bfe78c369c4995419e2d44` (Amendment C binding gate,
manual bucket-scoped diff — `src/ + tests/ + tools/ + dashboard/`).
`WIRED_LEVER_LEDGER_COUNT` held at **1** (production scope, v2.3 Seed
6 JsonlTailWorker wire unchanged) per Seed v2.4-H production-scope-
canonical binding.

Severity emoji convention is **frozen at original review time** per
the `docs/v1.3-backlog.md` convention — re-triage by adding a v2.6
task, not by editing the emoji.

---

## Carry-forwards from v2.5 (dispositions at v2.5.1 P2 ship-gate)

Per `docs/v2.5-next-steps.md` §"v2.5 P2 ship-gate close-out" and
`docs/prompts/v2.5.1-corrective/phase-2-shipgate-refire.md` §S10/S11.
Items surfaced through the v2.5 cycle and the v2.5.1 corrective phase
that v2.6 inherits:

1. ⏸ **Seed v2.4-C → renames Seed v2.5-C (🟡 Path-D synthetic-fixture
   P5 implementation)** — DEFERRED v2.6 per consolidation cycle choice
   (recorded at v2.5 P0 PR #185 + v2.5.1 P2 ship-gate this PR).
   Phase-5 prompt re-mint to reflect Amendment D gate-split (v10.1-
   mode vs v10.3-mode entry conditions) also defers to v2.6 P0
   alongside the implementation. ~600 LOC requires feature
   classification per Amendment A. Source: `docs/v2.4-next-steps.md`
   §"Seed v2.4-C"; issue #177 §"Disposition options" Path D;
   `project_v10_p5_gate_deadlock.md`.

2. ⏸ **Seed v2.4-E (🟢 overall p95 watch)** — WATCH continues into
   v2.6. v2.5.1 P2 re-measure result populated post-S2:
   p95 = ___ s (vs v2.4 P2 = 10.518 s, v2.3 = 10.584 s). NOT ≤ 8.2 s
   closure threshold. Carry forward to v2.6 ship-gate as advisory;
   if v2.6 holds or improves further, evaluate downgrade or closure.
   Source: `reports/soak-20260520T*.md` §"Per-band latency"
   (populated at S2 close).

3. ⏸ **Seed v2.4-F (🟡 L4 + LM categorize p95 small-n watch)** —
   WATCH continues into v2.6. v2.5.1 P2 re-measure: L4 ___ s (n=__),
   LM ___ s (n=__) — populated post-S2. Downgrade candidate if one
   more cycle holds. Source: same soak report §"Per-band latency".

4. ⏸ **Seed v2.4-G → renames Seed v2.5-G (🔴 CLI governance timeout
   audit)** — PROMOTED 🟡 → 🔴 at v2.5 P0 PR #185 with measurement-
   protocol stance per J2 audit `docs/seed-v2.4-g-cli-timeout-audit.md`
   §"Recommendation". Three-step protocol:

   1. **Instrument first** — add per-run wall-clock timing to
      alignment-eval row runner (~30 LOC tooling); no FROZEN-surface
      touches.
   2. **Then tighten** — pick `TIMEOUT_SECONDS` value in 30–45 s range
      based on measured eval p99.
   3. **Then split** — `BRIDGE_CLI_TIMEOUT` (prod) +
      `BRIDGE_CLI_TIMEOUT_EVAL` (eval override) env-readable.

   Implementation **DEFERS to v2.6** because ~30 LOC in `tools/`
   trips consolidation gate. `src/stream_manager/cli_governance.py:49`
   `TIMEOUT_SECONDS = 25.0` STAYS FROZEN through v2.5.1. **Seed
   v2.5-A (below) is fresh empirical evidence reinforcing the
   promotion stance** — v2.5.1 P1 n=6 measurement showed 100%
   Sonnet timeout on row `frog7-wirecli-module-10`, the strongest
   single-row signal yet.

---

## NEW v2.5.1 P2 ship-gate seeds (filed at this PR)

5. 🟡 **Seed v2.5-A — `frog7-wirecli-module-10` 100% Sonnet timeout
   opacity.** v2.5.1 P1 n=6 re-measure at
   `reports/alignment-eval-20260520T092222Z.md` row 10 recorded
   `cli governance timeout (>25.0s); degrading` on **6/6** Sonnet runs
   (100% rate). The row's "stable NONE" Sonnet majority is therefore a
   100% timeout-degradation artefact, **NOT** content drift. This
   single-row evidence drove the v2.5.1 P1 verdict-A classification
   (3 of the 4 n=3 stable-failures destabilised at n=6; the one row
   that stayed stable-divergent — row 10 — was 100% timeout-attributable).

   **Status:** measurement-blind spot; resolves when Seed v2.5-G
   timeout-instrumentation lands. Carry to v2.6 alongside Seed v2.5-G.
   No standalone fix path (instrumentation is the prerequisite to
   diagnose what underlying content drift, if any, sits behind the
   timeout-degraded read).

   **Why this is a seed, not just an annotation on Seed v2.5-G:**
   when Seed v2.5-G's instrumentation lands at v2.6, the close-out
   path for v2.5-A is "re-measure row 10 with instrumented runner;
   verdict = content-drift OR timeout-attributable." Tracking it
   independently lets that re-measure ship as a v2.6 evidence item
   without re-litigating the Seed v2.5-G instrumentation scope.

   Source: `docs/v2.5.1-sonnet-floor-investigation.md` §"n=6 re-measure
   summary" + §"Row-level dispositions".

6. ⏸ **Seed v2.5-B (subsumed by v2.5.1 P1 verdict A)** — the v2.5 P2
   Sonnet floor breach (`pass_rate=0.7895`) was investigated at v2.5.1
   P1 and classified as a **measurement artefact** (verdict A). n=6
   re-measure cleared the floor at 0.9375. No carry-forward; this row
   stays as historical record only. Resolution shipped as the new
   stability-window rule (`feedback_alignment_eval_stability_window.md`)
   which mandates `--runs 6` near the floor — replaces the implicit
   `--runs 3` default for ship-gate use at the prompt-template level.

---

## Carry-forwards from v2.4 (still unresolved into v2.6)

Promotion-criterion-bound + demand-bound carry-forwards from v2.4
ship-gate that did not fire in v2.5 (Amendment E EXEMPT). All five
remain in `docs/v2.5-next-steps.md` §"Seeds v2.4-I..N" verbatim; no
edits to existing emoji per frozen-emoji rule.

7. ⏸ Seed v2.4-I (🟢 project-context rank + 400-token budget
   invariant test) — promotion-criterion-bound. Trigger has NOT
   fired in v2.5.
8. ⏸ Seed v2.4-J (🟢 learn-mode ALLOW-promotion regression coverage)
   — promotion-criterion-bound. Trigger has NOT fired in v2.5.
9. ⏸ Seed v2.4-K (🟢 topology-discovery coverage) — promotion-
   criterion-bound. Trigger has NOT fired in v2.5.
10. ⏸ Seed v2.4-L (🟢 out-of-scope guard scans) — promotion-criterion-
    bound. Trigger has NOT fired in v2.5.
11. ⏸ Seed v2.4-M (🟢 "governs messages, not transitions" boundary
    regression) — promotion-criterion-bound. Trigger has NOT fired
    in v2.5.
12. ⏸ Seed v2.4-N (🟡 remote-CLI monitoring extension) — demand-
    bound. No concrete use case surfaced in v2.5.

---

## ADR-18 Rule 5 — backlog hard cap (Amendment E reading at v2.6 P0)

**Post-v2.5.1 P2 ship reading (2026-05-20, reconciled at this PR).**
This file held **5 cap-counted seeds** (Seeds v2.4-Q + v2.4-C / E / F
/ G) at v2.5 P0 entry. v2.5.1 P2 ship-gate disposition:

- Seed v2.4-Q → CLOSED RECOVERED at n=6 0.9375; no carry.
- Seed v2.4-C, v2.4-E, v2.4-F, v2.4-G → carry unchanged (renamed
  to v2.5-C / v2.5-G; v2.4-E and v2.4-F retain ID-stable v2.4
  designations).
- NEW Seed v2.5-A surfaced at v2.5.1 P1 → carries to v2.6.

Effective cap-counted reading at **v2.6 P0 = 5** (4 v2.4-cycle
deferrals carrying + 1 NEW v2.5.1 P2 seed) + **6 EXEMPT**
(v2.4-I..N) per ADR-18 Amendment E §"Self-application".

**Net change from v2.5 P0 entry:** −1 (Seed v2.4-Q closed) +1
(Seed v2.5-A new) = **5 cap-counted, unchanged**.

If v2.6 grows the count further the operator should either
explicitly accept another cap-bump (precedent: v2.2 P0 1 → 6; v2.3
P0 6 → 11) or invoke Amendment E EXEMPT classification where
applicable. Operator-accepted cap-bumps remain documented in
`docs/v2.6-task-plan.md` §"Cycle-discipline guardrails" rather than
re-amending ADR-18.

## Refs

- `docs/v2.5-task-plan.md` §"P2 ship-gate BLOCK" + §"P2 close-out"
  (P2 close-out section appended this PR).
- `docs/v2.5-next-steps.md` §"v2.5 P2 ship-gate close-out".
- `docs/v2.4-backlog.md` (predecessor cycle backlog).
- `docs/v2.5.1-sonnet-floor-investigation.md` (v2.5.1 P1 verdict A).
- `docs/prompts/v2.5.1-corrective/phase-1-sonnet-floor-investigation.md`.
- `docs/prompts/v2.5.1-corrective/phase-2-shipgate-refire.md`.
- `reports/soak-20260520T*.md` (this PR Tier-3 soak; populated at
  S2 close).
- `reports/alignment-eval-20260520T092222Z.{md,json}` (n=6 evidence
  cited at S4 path-1).
- `docs/adr/ADR-18-mvp-surface-freeze.md` §Amendments A–E.
- `feedback_alignment_eval_stability_window.md` (n=6 mandate memory).
