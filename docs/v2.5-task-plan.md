# v2.5 task plan — consolidation cycle (Seed v2.4-G promotion + v2.4-backlog reconciliation)

**Cycle theme: consolidation.** Operator-declared at P0 fire 2026-05-19.
Net-LOC target ≤ **0** vs cycle-tip (v2.5 P0-merge SHA
`634e9d1d982a3b6071bfe78c369c4995419e2d44`), production bucket binding
per ADR-18 Amendment A.
Anchor per **Amendment C** = cycle-tip, NOT predecessor tag (v2.4.0 =
`08eb71d` retained as narrative comparator only).

**Cycle-type framing.** v2.4 was consolidation (cycle-tip bucket-scoped
LOC = 0; Amendments D + E + #111 close + Sonnet-DIP investigation).
v2.5 repeats consolidation — alternation hygiene is the *target* but
not law (ADR-18 Amendment A §"Default lean rationale" permits two
consolidations in a row when the lever-wire backlog has no ready
candidate). Seed v2.4-C (Path-D P5, ~600 LOC) is the obvious lever-
wire candidate, but is not staged for v2.5; deferring it another cycle
is the conservative call.

Headline scope this cycle:

1. **Seed v2.4-G promotion to 🔴** with measurement-protocol stance.
   Audit doc `docs/seed-v2.4-g-cli-timeout-audit.md` (J2 output)
   recommends PROMOTE TO 🔴 with three-step protocol: (1) instrument
   per-run wall-clock in alignment-eval row runner (~30 LOC tooling);
   (2) tighten timeout to 30–45 s band once instrumentation lands;
   (3) split `TIMEOUT_SECONDS` into prod / eval env-readable
   constants. **Implementation defers to v2.6** because ~30 LOC in
   `tools/` would trip consolidation gate (production-bucket ≤ 0).
   v2.5 P0 records the promotion + protocol; v2.6 P0/P1 implements.

2. **v2.4-backlog.md reconciliation** — Seed v2.4-O marked RESOLVED
   PR #184 (`9ca5226`); cap-counted reading corrected 7 → 5 (Q +
   C/E/F/G post-#184). Closes the stale-doc condition flagged by
   v2.5 P0 skeleton DoD item §"v2.4 carry-forward dispositions
   table reconciled".

Path-D synthetic-fixture P5 implementation (Seed v2.4-C, ~600 LOC)
**DEFERS to v2.6** (becomes Seed v2.5-C) per consolidation cycle
choice. Phase-5 prompt re-mint to reflect Amendment D gate-split
(v10.1-mode vs v10.3-mode) also defers.

**P1/P2 fire-order (recorded at P0).** Per `docs/v2.5-next-steps.md`
fire-order table with consolidation-cycle dispositions applied:

1. P0 cycle frame (this PR) — Seed v2.4-G promotion record + v2.4-
   backlog reconciliation + drafts this task-plan + drafts v2.5-
   next-steps.
2. **Seed v2.4-G — CLI timeout audit follow-up** — instrumentation
   **DEFERRED to v2.6** (production-bucket ≤ 0 binding). v2.5 P0
   records promotion to 🔴; v2.6 P0/P1 mints prompt + implements.
3. **Seed v2.4-C — Path-D synthetic-fixture P5 impl** — **DEFERRED
   to v2.6** (consolidation cycle; ~600 LOC requires feature
   classification per Amendment A).
4. P2 ship-gate finalize. Prompt: `docs/prompts/v2.5-orchestration/
   phase-2-ship-gate-finalize.md` (TBD mint ahead-of-fire per v2.4
   PR #182 precedent).

**Phase count.** P0 (frame) + **0 work phases** + P2 (ship-gate) =
**2 numbered phases**, **0 work phases**. ADR-18 Rule 4 caps work-
phase count at ≤ 3 — v2.5 sits well under at 0. Pure docs-frame +
docs-ship-gate cycle.

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3 numbered phases. v2.5 numbered-phase span =
P0 → P2. v10.x slot also blocked on #112 (v10 P5) which remains
blocked at the **implementation side** (Seed v2.4-C deferred again
in v2.5). #131 remains gated.

**Companion docs:**

- `docs/v2.5-next-steps.md` — **the comparison anchor**. P2
  ship-gate walks this file row-by-row.
- `docs/v2.4-backlog.md` — reconciled this PR (Seed v2.4-O resolved
  + cap-counted 7 → 5).
- `docs/seed-v2.4-g-cli-timeout-audit.md` — J2 evidence audit for
  the Seed v2.4-G promotion decision.
- `docs/v10-mvp-status.md` — J1 refresh (#111 closed + Amendment D
  v10.1/v10.3 gate split + corpus 240 → 360).
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendments A–E (no new
  amendment minted this cycle).
- `docs/v2.4-next-steps.md` — predecessor cycle's comparison anchor.
- `docs/v2.4-task-plan.md` — predecessor cycle's task plan.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — no new FR row this cycle.
- `docs/adr/ADR-5-latency-budget.md` — v2.3 baseline current; v2.5
  P2 may skip baseline append per consolidation + LOC ≈ 0 + no
  latency lever wired (matches v2.4 P2 §S7 default).
- `docs/adr/ADR-17-soak-tiers.md` — Tier 3 ship-gate at P2.

## Operator decisions recorded at P0 fire (2026-05-19)

Per `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md` skeleton
operator-fill blocks:

1. **Cycle type: CONSOLIDATION.** Default lean accepted. Seed v2.4-C
   Path-D P5 not staged for v2.5; no other lever-wire candidate
   sequenced. Alternation hygiene relaxed per Amendment A §"Default
   lean rationale" (two consolidations permitted when lever-wire
   backlog has no ready candidate). Net LOC ≤ 0 vs cycle-tip per
   Amendment C.

2. **`WIRED_LEVER_LEDGER_COUNT` posture: HOLD at 1 (production
   scope, v2.3 Seed 6 JsonlTailWorker production wire unchanged).**
   No new lever wired this cycle. Soak-scope ledger (`{}` in
   `tools/soak_driver.py`) remains internal artefact per Seed
   v2.4-H production-scope-canonical binding.

3. **Rule 5 backlog cap reading: ACCEPT 5-CAP** (Amendment E §"Self-
   application", entry 2026-05-19, post-PR #184). Cap-counted = 5
   (Seed v2.4-Q + Seeds v2.4-C / E / F / G). Exempt = 6 (Seeds
   v2.4-I..N, promotion-criterion-bound + demand-bound). Total
   open = 11 seeds entering v2.5 P0. No Rule 5 clarification
   amendment minted this cycle.

   **Reconciliation note.** `docs/v2.4-backlog.md` §"Rule 5 backlog
   hard cap" reading was stale at "7 cap-counted" because PR #184
   resolution of Seeds v2.4-O + v2.4-P had not been folded back into
   the backlog file. Reconciled this PR (see §P0 file changes).

4. **Seed v2.4-G promotion: PROMOTE TO 🔴** with measurement-protocol
   stance. Per J2 audit `docs/seed-v2.4-g-cli-timeout-audit.md`
   §"Recommendation":
   - Promote 🟡 → 🔴 at v2.5 P0 (this decision).
   - Three-step protocol: (1) instrument per-run wall-clock in
     alignment-eval row runner (~30 LOC tooling); (2) tighten
     `TIMEOUT_SECONDS` to 30–45 s band once instrumentation lands
     measured eval p99; (3) split `TIMEOUT_SECONDS` into
     `BRIDGE_CLI_TIMEOUT` (prod) + `BRIDGE_CLI_TIMEOUT_EVAL` (eval
     override).
   - Implementation **DEFERS to v2.6** because ~30 LOC in `tools/`
     trips consolidation gate (production-bucket ≤ 0 binding).
   - v2.5 P0 records the promotion; no code change this cycle.
     v2.6 P0/P1 mints prompt + implements step (1).
   - Evidence hooks: v2.3 row-08 `frog7-lifecycle-bridge-08` 3×
     NONE (deterministic CLI-degrade fingerprint); v2.4 P2 row-10
     `frog7-wirecli-module-10` 2× NONE (partial echo); aggregate
     sonnet NONE rate 6/96 = 6.25% upper bound at v2.4 P2; soak
     path comfortable (degrade_count=0 across 5 consecutive soaks,
     max 18.266 s vs 25.0 s ceiling).
   - `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS =
     25.0` STAYS FROZEN through v2.5 per ADR-18 surface freeze.

5. **Seed v2.4-C deferral: DEFER another cycle.** Path-D synthetic-
   fixture P5 implementation (~600 LOC) requires feature
   classification per Amendment A. Coupled with §"Cycle type" =
   consolidation. Seed v2.4-C becomes **Seed v2.5-C** in
   `docs/v2.5-backlog.md` (minted at P2 ship-gate). Phase-5 prompt
   re-mint to reflect Amendment D gate-split also defers to v2.6
   P0 alongside the implementation.

6. **Seed v2.4-Q carry-forward: FREEZE-on-content carries.** Sonnet
   alignment 0.8261 (v2.4 P2) still in 0.80–0.85 band; root cause
   not yet isolated. Watch fires at v2.5 P2 ship-gate. If 5th-cycle
   measurement holds in 0.80–0.85 band with same row-set
   composition, mint row-level disposition per Seed v2.4-D §"v2.5
   follow-ups" item 2 (row-16 `ambig-block-reset-tag-16` INTERVENE
   → BLOCK golden update OR REQUIREMENTS amendment to accept
   BLOCK). No P0-time action.

7. **Amendment C cycle-tip anchor verbatim citation.** v2.5 P2
   ship-gate prompt cites
   `634e9d1d982a3b6071bfe78c369c4995419e2d44..HEAD` for the
   consolidation-gate net-LOC measurement. Diff command:

   ```
   git diff 634e9d1d982a3b6071bfe78c369c4995419e2d44..HEAD --stat -- src tests tools dashboard
   ```

   Predecessor-tag narrative diff `08eb71d..HEAD` (same bucket) is
   reported alongside for cycle-impact context but does NOT gate.

8. **Memory pre-flight (Amendment B).** Performed 2026-05-19 against
   current `main` at `9ca5226` (PR #184 merged). Stamp below.

## Memory pre-flight stamp (Amendment B)

Pre-flight performed 2026-05-19 against current `main` at `9ca5226`
(PR #184 squash-merge). 5 load-bearing memories verified per v2.5 P0
cycle-frame skeleton minimum re-read list:

| Memory | Status | Action |
|--------|--------|--------|
| `feedback_certportal_dev_firewall.md` | FRESH (verified `.claude/settings.local.json` deny patterns + `CLAUDE.md` firewall language unchanged) | None |
| `feedback_no_self_monitor.md` | FRESH (polarity-flip rule + `BRIDGE_SM_PROJECT_SLUGS` filter pattern unchanged) | None |
| `project_v24_cycle_close.md` | FRESH (minted at v2.4 P2 ship-gate 2026-05-19; includes Seeds v2.4-O + v2.4-P RESOLVED PR #184 closing facts) | None |
| `feedback_glob_narrowing_no_op.md` | FRESH (PR #184 cassette; `git clean -df` pattern + S1.1 `--untracked-files=no` filter recorded) | None |
| `feedback_cycle_tolerance_masks_bugs.md` | FRESH (Seed v2.4-O closure case study; both-tolerance unit test discipline recorded) | None |

7-day age warning on `feedback_certportal_dev_firewall.md` +
`feedback_no_self_monitor.md` per memory system reminder — text body
re-verified against current repo state; firewall + polarity-flip
patterns unchanged; no update required.

## Cycle-discipline guardrails

- **LOC budget (Amendment A + Amendment C).** Anchor = cycle-tip
  (v2.5 P0-merge SHA `634e9d1d982a3b6071bfe78c369c4995419e2d44`).
  Consolidation gate: **net ≤ 0** across production bucket (`src/`
  + `tests/` + `tools/` + `dashboard/`). Docs bucket advisory per
  Amendment A. BLOCK at any positive production-bucket delta.

- **Deletion-offset posture.** v2.5 P0 carries zero production-
  bucket delta (docs-only frame). No deletion offset required at
  P0; consolidation gate hygiene confirmed at P2 ship-gate via
  bucket-scoped `git diff <P0-merge>..HEAD -- src tests tools
  dashboard`.

- **Rule 5 backlog cap.** Effective cap reading = **5** (cap-
  counted: Seed v2.4-Q + Seeds v2.4-C / E / F / G). 6 exempt
  carry-forwards (v2.4-I..N) per Amendment E §"Self-application".
  v2.5 cycle work that opens any NEW backlog item beyond these 5
  must close or graduate ≥ 1 existing seed before merging — OR
  add the new seed as cap-counted (no further amendment required;
  the 2-cycle drift clock starts at first carry-forward).

- **Rule 6 memory pre-flight.** Performed at P0 (above stamp). All
  5 load-bearing memories FRESH; no updates required in this PR.

- **Lever ledger.** Entering posture 1 (production scope, v2.3
  Seed 6 JsonlTailWorker wire). Target end posture **unchanged**
  (consolidation; no lever change). Seed v2.4-H scope binding
  remains in force (production scope canonical).

- **Surface freeze.** ADR-18 Rule 1 in force. No v2.5 P0 prompt
  touches FROZEN surface; `src/stream_manager/cli_governance.py:49`
  `TIMEOUT_SECONDS = 25.0` stays frozen through v2.5 (instrumentation
  defers v2.6 per Seed v2.4-G measurement protocol).

## PHASE P0 — this PR (docs frame)

Files changed (repo-tracked):

- `docs/v2.5-task-plan.md` (NEW; this file).
- `docs/v2.5-next-steps.md` (NEW; the comparison anchor).
- `docs/v2.4-backlog.md` (RECONCILED — Seed v2.4-O marked RESOLVED
  PR #184; cap-counted reading 7 → 5).
- `docs/seed-v2.4-g-cli-timeout-audit.md` (NEW; J2 audit; carried
  over from working tree).
- `docs/v10-mvp-status.md` (REFRESHED; J1 — #111 closed at v2.4 P0
  + Amendment D v10.1/v10.3 gate split + corpus 240 → 360).
- `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md` (NEW;
  J3 skeleton ahead-of-fire mint).

Files NOT changed (per ADR-18 surface freeze + cycle-discipline):

- `src/stream_manager/cli_governance.py` — `TIMEOUT_SECONDS = 25.0`
  FROZEN; Seed v2.4-G instrumentation defers v2.6.
- All FROZEN surface — untouched per ADR-18 Rule 1.
- `tools/`, `tests/` production-bucket paths — zero delta this PR.

Files held outside this PR (session-thread artifacts, optional
companion commit):

- `docs/2026-05-19-status.md` — operator status snapshot.
- `docs/2026-05-19-task-list.md` — agent-dispatchable jobs ledger
  for this thread.
- `docs/2026-05-19-implementation-notes.md` — running notes on
  design / deviations / tradeoffs.

Repo-tracked LOC delta: docs only.

ADR-18 Amendment B self-applies: this PR carries the memory pre-
flight stamp.

ADR-18 Amendment C self-applies: cycle-tip anchor =
`634e9d1d982a3b6071bfe78c369c4995419e2d44` (v2.5 P0-merge SHA,
backfilled post-merge).

ADR-18 Amendment E self-applies: cap-counted = 5 reading recorded
above; verbatim external-trigger citations live in
`docs/v2.5-next-steps.md` (mirrors v2.4 pattern).

## PHASE P1 — none (pure consolidation cycle)

v2.5 has **zero work phases**. Operator may surface a v2.5 P1
candidate during the cycle (e.g. mint Seed v2.4-G follow-up prompt
for v2.6 use), but no work phase is planned at P0 fire. Seed v2.4-G
measurement-protocol implementation is the obvious P1 candidate but
its ~30 LOC tooling defers to v2.6 per consolidation gate.

## PHASE P2 — ship-gate finalize

Prompt: `docs/prompts/v2.5-orchestration/phase-2-ship-gate-
finalize.md` (TBD mint ahead-of-fire per v2.4 PR #182 precedent).

P2 procedure mirrors v2.4 P2 pattern:

- Tier-3 soak (Amendment C cycle-tip anchor binding).
- Alignment-eval `--ci-gate` with Sonnet-DIP re-measure (Seed
  v2.4-Q watch).
- ADR-5 baseline append: SKIPPED default (consolidation + LOC ≈ 0
  + no latency lever wired). Operator override at P2 fire if
  signal moves.
- v10 P4 corpus piggyback (Run 5) if `BRIDGE_RL_LOGGER_ENABLED=1`.
- `docs/v2.5-backlog.md` mint at P2 (carry-forwards to v2.6).

## DoD (this PR)

- [x] `docs/v2.5-task-plan.md` minted with all 8 operator decisions
      recorded.
- [x] `docs/v2.5-next-steps.md` minted as comparison anchor.
- [x] `docs/v2.4-backlog.md` reconciled (Seed v2.4-O RESOLVED PR
      #184; cap-counted 7 → 5).
- [x] Memory pre-flight stamp included (above).
- [x] Stale memories: NONE (all 5 FRESH).
- [x] §"Cycle-tip anchor" template recorded; SHA populated post-
      merge in follow-up commit.
- [x] §"v2.4 carry-forward dispositions" table reconciled against
      `docs/v2.4-backlog.md` ground truth (post-#184).
- [x] Cycle type = consolidation; explicit deletion-offset posture
      recorded (zero production-bucket delta at P0; survey at P2).
- [x] Seed v2.4-Q carry-forward logged in `docs/v2.5-next-steps.md`
      §"Carry-forwards" with FREEZE-on-content + v2.5 P2 watch
      trigger.
- [ ] Single PR `chore(v2.5-p0):` against `main` (trivially true
      at merge).

## Refs

- `docs/v2.5-next-steps.md` (this PR, the comparison anchor).
- `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md` (J3
  skeleton; this PR fills the decision blocks via this task-plan).
- `docs/seed-v2.4-g-cli-timeout-audit.md` (J2 evidence audit).
- `docs/v10-mvp-status.md` (J1 ledger refresh).
- `docs/adr/ADR-18-mvp-surface-freeze.md` Amendments A / B / C /
  D / E.
- `docs/v2.4-task-plan.md` (predecessor cycle task plan).
- `docs/v2.4-next-steps.md` (predecessor cycle anchor).
- `docs/v2.4-backlog.md` (reconciled this PR).
- `project_v24_cycle_close.md` (v2.4 cycle-close memory).
- Issue #112 (v10 P5 phase; remains blocked on Path-D P5 impl
  deferred to v2.6).
- Issue #131 (v10.x cycle frame; remains gated).
