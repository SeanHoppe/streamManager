# v1.9 task plan

**Companion docs:**
- `docs/v1.9-backlog.md` — seed backlog from v1.8 ship-gate (Haiku fastpath confidence floor, external session registry, Learn Mode JSONL expansion, background task token registry; CLI pool sizing >2 + PPP audit harness carry-forwards)
- `docs/v2.0-backlog.md` — items deferred out of v1.9 scope (created in P0; two carry-forward stubs)

**Reference docs (always available, never inlined):**
- `REQUIREMENTS.md` — current spec
- `docs/adr/ADR-5-latency-budget.md` — latency budget; v1.8 ship-gate baseline at HEAD. v1.8 §"Caveats" is the load-bearing reference for P1 scope (Haiku always returns confidence ≥ 0.70 on ambiguous-block content; two P2a corpus-fix attempts both failed; root cause: Haiku consistently returns high-confidence verdicts on short destructive commands; verdict-based trigger is the next lever).
- `docs/adr/ADR-17-soak-tiers.md` — three-tier soak model
- `docs/v1.8-backlog.md` — predecessor (closed-out: v1.8 cycle)
- `docs/v1.8-task-plan.md` — format template
- `reports/soak-20260506T101746Z.md` — v1.8 ship-gate report (32.1 min, `--cli-pool-size 2`, baseline for v1.9 lever-effect comparison: overall p95 7.612 s, ALLOW p95 6.48 s, L4 alignment p95 11.85 s, LM p95 13.30 s, `cli_pool_send_ms` p95 6470.35 ms, `cli_dispatch_fallback_ms` p95 0.00 ms, fallback fire rate 0%)
- `reports/alignment-eval-20260506T113450Z.md` — v1.8 alignment-eval `--ci-gate` baseline (sonnet 0.913, haiku 0.90, 0 FR-OG-7 regressions, exit 0)
- `src/stream_manager/cli_governance.py` — retry path with confidence-based trigger; `BRIDGE_L4_FALLBACK_CONFIDENCE` floor (0.70); `governance_fallback_routed` envelope; v1.9 P1 changes the trigger mechanism here.
- `src/stream_manager/governance.py` — pre-routing call site at `_evaluate_inner_core`; v1.8 P1 wired `is_ambiguous_block` / `is_hitl_synthesis` content-detection here; v1.9 P1 does NOT modify this file.
- `src/stream_manager/model_router.py` — band → model mapping; `RoutingDecision.fallback_model_id`; unchanged in v1.9 P1.
- Memory: `feedback_subagent_stale_mental_model.md`, `feedback_no_self_monitor.md`, `feedback_cli_over_sdk.md`, `feedback_soak_cli_pool_flag.md`, `feedback_cassette_must_cover_new_envelopes.md`, `feedback_cross_pr_seam_review.md`, `feedback_subagent_escape_hatches.md`, `feedback_subagent_long_task_abandonment.md`, `feedback_monitoring_live_sessions.md`, `project_v12_cycle_close.md` (ADR-17 soak-tier ref), `project_learn_mode.md` (Learn Mode design)

**Format:** mirrors `docs/v1.8-task-plan.md` style — each phase block is self-contained, hand verbatim to a fresh Claude Code session via the matching prompt under `docs/prompts/v1.9-orchestration/`.

**Prior baseline:** v1.8.0 ship (content-detection wiring merged PR #93 + PR #94). Soak source of truth: `reports/soak-20260506T101746Z.md`. Overall p95 7.612 s, ALLOW p95 6.48 s, L4 alignment p95 11.85 s (n=4), LM p95 13.30 s, `cli_pool_send_ms` p95 6470.35 ms. Major finding: **lever still DORMANT**. `is_ambiguous_block` / `is_hitl_synthesis` are correctly computed and routed to Haiku-first path (wiring correct), but Haiku returns confidence ≥ 0.70 on all tested ambiguous-block content (2/2 soak events), so the `BRIDGE_L4_FALLBACK_CONFIDENCE` floor (0.70) is never crossed. Two corpus-fix attempts (P1c-B deliberative questions, P2a imperative declarative forms) both produced 0 fire rate. Root cause: Haiku gives high-confidence verdicts (BLOCK/SUGGEST/INTERVENE at ≥ 0.70) on short destructive commands. Outstanding work: replace confidence-based retry trigger with verdict-based trigger (if Haiku returns BLOCK/INTERVENE on ambiguous-block content, auto-retry with Sonnet); re-measure lever effect.

---

## CRITICAL: do-not-touch list (v1.1 + v1.2 + v1.3 + v1.4 + v1.5 + v1.6 + v1.7 + v1.8 protected symbols)

These symbols are load-bearing and MUST NOT be modified by any v1.9 phase. Diff PR head vs base on hot files before merge (memory: `feedback_subagent_stale_mental_model.md`).

| Symbol / file | Owning task | Cycle |
|---|---|---|
| `cli_pool`, `CliPool`, `CliWorker`, `CliWorker.send`, `.bridge/cli-pool.pids` | Task J | v1.1 |
| `_install_lazy_hydrator`, `GovernanceEngine.hydrated`, `tools/perf_probe.py` | Task I | v1.1 |
| `_run_sse`, `_consume_sse_stream`, `transport` kwarg, `/api/commands/stream` | Task K | v1.1 |
| `hitl_pending.matched_hash`, `dispatch_resolution` matched_hash read | Task L | v1.1 |
| `wirecli.py` module + `WireProtocolError`/`WireSchemaVersionError`/`WireTransportError` | Task N | v1.1 |
| `tools/soak_driver.py` `--cli-replay` flag, `--cli-pool-size` flag | Task A | v1.2 |
| `LifecycleBridge` + `/api/lifecycle/jobs` + dashboard lifecycle pane | Task C | v1.2 |
| `_VALID_TRANSPORTS = frozenset({"sse"})`, long-poll removal | Task D | v1.2 |
| `Transport = Literal["wirecli"]`, `_JSON_REMOVED_MSG`, json transport refusal | Task E | v1.2 |
| Learn Mode advisory bias surface: `_consult_learn_mode_bias`, `_emit_learn_mode_bias_applied`, `bias_consult` timing key, `bias_hint` HITL column | v1.3 P5d | v1.3 |
| `engine._last_phase_timings_ms` v1.4 keys (`inbound_publish`, `evaluate_inner`, `bias_consult`, `hitl_classify_trigger`, `hitl_route`, `record_decision`, `alert_publish`, `total`) — **extend, never remove or rename** | v1.4 instrumentation | v1.4 |
| `engine._last_phase_timings_ms` v1.5 sub-phase keys (`og7_check`, `fast_precheck`, `graph_classify`, `hydrator_state_read`, `routing_dispatch`) — **extend, never remove or rename** | v1.5 P1 | v1.5 |
| `engine._sub_timings_in_flight` transient slot mechanism — additive use only | v1.5 P1 | v1.5 |
| `engine._last_phase_timings_ms` v1.6 CLI residue keys (`cli_setup_ms`, `cli_dispatch_ms`, `cli_pool_acquire_ms`, `cli_pool_send_ms`, `cli_parse_ms`) — **extend, never remove or rename** | v1.6 P1 | v1.6 |
| `CliGovernor.evaluate` `sub_timings: dict | None = None` kwarg + per-key population | v1.6 P1 | v1.6 |
| `tools/soak_driver.py` `_ALLOW_PHASE_ORDER` — extend, never reorder existing entries | v1.4 + v1.5 + v1.6 + v1.7 + v1.8 | v1.4/v1.5/v1.6/v1.7/v1.8 |
| `tools/soak_driver.py` `_format_allow_phase_breakout`, `### ALLOW publish-path phase breakout (v1.4)`, `### ALLOW _evaluate_inner sub-phase breakout (v1.5)`, `### ALLOW _evaluate_inner CLI residue breakout (v1.6)` blocks — keep emitting unchanged | v1.4 + v1.5 + v1.6 | v1.4/v1.5/v1.6 |
| Cassette+beacon DRY single source: `tools/cassette_record.py::_LM_DIALOGUE_PAIRS` and `tests/beacons/learn_mode_categorizer.jsonl` | v1.4 PR #79 | v1.4 |
| `CliGovernor.evaluate` outer signature (positional `content`, kwargs `model_id`, `sub_timings`, `fallback_model_id`) — **extend with optional kwargs only**, never reorder branches or alter return-type contract | Task J + v1.1 NFR-M2 + v1.7 P2 | v1.1 + v1.7 |
| `model_router.route` priority order (L4 alignment first, then L0, L1, L2, L3) — **extend within an existing band, never reorder bands** | NFR-M1-M5 | v1.1 |
| `model_router.RoutingDecision` field set (`layer`, `model_id`, `fallback_model_id`) — **extend with new fields only** | v1.7 P2 | v1.7 |
| `model_router.ConvergenceMonitor` (NFR-M4) | NFR-M4 | v1.1 |
| `engine._last_phase_timings_ms` v1.7 fallback key (`cli_dispatch_fallback_ms`) — **extend, never remove or rename** | v1.7 P2 | v1.7 |
| `cli_governance.FALLBACK_CONFIDENCE_ENV = "BRIDGE_L4_FALLBACK_CONFIDENCE"`, `FALLBACK_CONFIDENCE_DEFAULT = 0.70`, `_fallback_confidence_floor()`, `_publish_bus_message`, `_parse_envelope_with_meta`, `_evaluate_once` — **extend with optional kwargs; never remove or rename** | v1.7 P2 | v1.7 |
| Bus envelope types `governance_fallback_routed`, `governance_envelope_missing_confidence` metadata schema — **extend metadata only; never rename or remove fields** | v1.7 P2 | v1.7 |
| `tests/golden/l4_alignment.jsonl` + `tools/alignment_eval.py` runner | v1.7 P1 | v1.7 |
| `governance._evaluate_inner_core` content-detection helpers: `_looks_ambiguous_block(content)` (verify exact name by grep), `is_ambiguous_block` / `is_hitl_synthesis` flag computation + kwargs at the `route(...)` call site — **extend pattern list only; never change flag semantics or route() kwarg names** | v1.8 P1 | v1.8 |
| `tests/test_governance_content_detection.py` (40 passing scenarios) — **new tests only; never modify existing scenario inputs or expected outputs** | v1.8 P1 | v1.8 |
| `tools/soak_driver.py` `_L2_L3_TRIGGER` list (8+ items after v1.8 P2a extension) — **extend, never remove or reorder existing entries** | v1.8 P2a | v1.8 |

**Rule:** if a v1.9 phase needs to modify any row above, the modification must be additive (new key, new column, new optional kwarg, new sub-band that does not change priority order) — never replace the existing surface.

---

## Task summary

| Phase | Title | Effort | Depends on | Action prompt |
|---|---|---|---|---|
| P0 | Cycle frame (THIS doc + `v2.0-backlog.md` + five phase prompts) | ¼ session | — | `phase-0-cycle-frame.md` |
| P1 | Verdict-based fallback trigger (replace confidence-floor retry trigger with verdict-based in `cli_governance.py`) | ½ session | P0 | `phase-1-verdict-based-fallback.md` |
| P2 | External session registry + background task token registry (new `session_watcher.py`, bus envelopes, dashboard panels) | 1 session | P0 | `phase-2-session-watcher.md` |
| P3 | Learn Mode JSONL source expansion (configurable external session sources in `learn_mode.py`) | ½ session | P0 | `phase-3-learn-mode-sources.md` |
| P4 | v1.9 ship-gate + ADR-5 v1.9 baseline + lever-effect measurement | ½ session | P1 + P2 + P3 merged | `phase-4-ship-gate-finalize.md` |

**Order:** P0 lands first. P1, P2, P3 are independent — they may land in any order after P0, or in parallel across sessions. P4 cuts only after all three are merged; runs the ship-gate soak and measures whether the verdict-based trigger finally activates the lever.

**Abandonment rule:** if P1 verdict-based fallback still produces 0 fire rate on the soak (i.e. Haiku never returns BLOCK/INTERVENE on ambiguous-block content), document the falsification and promote option 4 (expand the `is_ambiguous_block` heuristic via empirical corpus study) to a v1.9 follow-up before opening v2.0.

If P1 alignment-eval `--ci-gate` regresses on FR-OG-7 rows (impossible by construction — `requires_alignment` keeps them on Sonnet), ABORT the change and debug routing precedence.

---

## PHASE P0 — Cycle frame

**Pain point:** v1.8 cycle closed with the lever-still-dormant finding documented in ADR-5 §"v1.8 ship-gate baseline" + 5 new seed items in `docs/v1.9-backlog.md`, but no task plan, no v2.0 deferred-items file, and no orchestration prompts.

**References:**
- `docs/v1.8-task-plan.md` — format template
- `docs/v1.9-backlog.md` — seeded post-v1.8 (5 items)
- `reports/soak-20260506T101746Z.md` — v1.8 ship-gate (baseline context for P1)

**Do-not-touch guard:** P0 is docs only. Zero code edits.

**Scope:**
1. Create `docs/v1.9-task-plan.md` (THIS doc).
2. Create `docs/v2.0-backlog.md` seed file (carry-forward stubs for CLI pool sizing >2 + PPP audit harness).
3. Create `docs/prompts/v1.9-orchestration/phase-{0,1,2,3,4}-*.md` — five prompt files.
4. Confirm `docs/v1.9-backlog.md` exists and is unchanged (frozen-emoji rule).

**DOD:**
- [ ] All four artifact groups created
- [ ] PR scope is docs-only (no `src/`, `tests/`, `tools/`, `dashboard/` hunks)
- [ ] Single PR against `main`

**Action prompt:** `docs/prompts/v1.9-orchestration/phase-0-cycle-frame.md`

---

## PHASE P1 — Verdict-based fallback trigger

**Pain point:** v1.8 ADR-5 §"Caveats" confirms the confidence-floor trigger is permanently ineffective on the current content shape: Haiku consistently returns confidence ≥ 0.70 on short destructive commands, so `BRIDGE_L4_FALLBACK_CONFIDENCE` (0.70 default) is never crossed. Two corpus-fix attempts failed to change this. The trigger mechanism itself must change.

**References:**
- `docs/v1.9-backlog.md` §"Haiku fastpath confidence floor" option 3 — verdict-based trigger design rationale
- `src/stream_manager/cli_governance.py` — retry trigger at `_evaluate_once` or equivalent; `BRIDGE_L4_FALLBACK_CONFIDENCE` floor constants; `governance_fallback_routed` envelope emission
- `src/stream_manager/governance.py` — `is_ambiguous_block` / `is_hitl_synthesis` computation (DO NOT TOUCH — v1.8 protected)
- `src/stream_manager/model_router.py` — `RoutingDecision.fallback_model_id` (DO NOT TOUCH)
- `tests/test_governance_fallback_routing.py` — extend with verdict-mode scenarios
- `reports/soak-20260506T101746Z.md` — v1.8 baseline (0% fire rate; target: > 0% in v1.9)

**Do-not-touch guard:** P1 touches ONLY `src/stream_manager/cli_governance.py` + `tests/test_governance_fallback_routing.py`. All v1.7 + v1.8 protected symbols in `cli_governance.py` must stay (especially `FALLBACK_CONFIDENCE_ENV`, `FALLBACK_CONFIDENCE_DEFAULT`, `_fallback_confidence_floor()`, `_evaluate_once` signature, `governance_fallback_routed` existing metadata fields). The `governance_fallback_routed` envelope metadata schema may be extended (add `trigger_mode` field) but no field renamed or removed.

**Scope:**
1. `src/stream_manager/cli_governance.py` — add verdict-based retry trigger:
   - New constants: `BRIDGE_L4_FALLBACK_MODE_ENV = "BRIDGE_L4_FALLBACK_MODE"`, `FALLBACK_MODE_DEFAULT = "verdict"`.
   - Retry logic: when `fallback_model_id is not None`, mode `"verdict"` → fire if `primary_verdict in {"BLOCK", "INTERVENE"}`; mode `"confidence"` → original `primary_confidence < _fallback_confidence_floor()` behavior (backward-compat).
   - `FALLBACK_CONFIDENCE_ENV`, `FALLBACK_CONFIDENCE_DEFAULT`, `_fallback_confidence_floor()` remain unchanged and functional in confidence mode.
   - Extend `governance_fallback_routed` envelope metadata: add `trigger_mode: str` field (value `"verdict"` or `"confidence"`) — additive only.
2. `tests/test_governance_fallback_routing.py` — extend (do not modify existing tests):
   - Ambiguous-block + Haiku returns BLOCK → retry fires in verdict mode (default).
   - Ambiguous-block + Haiku returns INTERVENE → retry fires in verdict mode.
   - Ambiguous-block + Haiku returns ALLOW/SUGGEST → retry does NOT fire in verdict mode.
   - `BRIDGE_L4_FALLBACK_MODE=confidence` → original confidence-floor behavior preserved.
   - `governance_fallback_routed` envelope includes `trigger_mode` field.
3. Alignment-eval `--ci-gate` — re-run; expect exit 0 (FR-OG-7 rows unchanged — `requires_alignment` keeps them on Sonnet; retry trigger change is downstream of routing, so FR-OG-7 rows never reach Haiku-first path).
4. Local soak smoke — short run (`--total-seconds 300 --cli-pool-size 2`) with destructive-content L2/L3 items. Verify `cli_dispatch_fallback_ms` p95 > 0 AND at least one `governance_fallback_routed` envelope with `trigger_mode="verdict"` emitted.

**DOD:** see `docs/prompts/v1.9-orchestration/phase-1-verdict-based-fallback.md`.

**Action prompt:** `docs/prompts/v1.9-orchestration/phase-1-verdict-based-fallback.md`

---

## PHASE P2 — External session registry + background task token registry

**Pain point:** UC-01 (2026-05-06) required 7 manual steps to answer "is the certPortal Matt QA agent healthy?" because SM has zero visibility into external `claude -p` subprocesses. Session state is discoverable but SM has no ingestion mechanism.

**References:**
- `docs/v1.9-backlog.md` §"External session registry" (AC-1) + §"Background task token registry" (AC-3)
- `docs/use-cases/uc-01-external-session-monitoring.md` — full incident + acceptance criteria
- Memory: `feedback_monitoring_live_sessions.md` — session discovery via `~/.claude/sessions/`, PID validation, 0-byte task file ≠ hung
- Memory: `feedback_no_self_monitor.md` — NEVER register SM's own sessions

**Do-not-touch guard:** P2 adds new files only (`src/stream_manager/session_watcher.py`). No changes to governance engine, `learn_mode.py`, `cli_governance.py`, `model_router.py`, `governance.py`, or any protected symbol. New bus envelopes are session-lifecycle only — they do not appear in the cassette decision-output path; cassette schema unchanged. Verify by grep after implementation.

**Scope:**
1. `src/stream_manager/session_watcher.py` (NEW):
   - Poll `~/.claude/sessions/` every 5 s for JSON/JSONL files (configurable; default on).
   - On new/changed file: read `pid`, `sessionId`, `cwd`, `entrypoint`; cross-check liveness with `kill -0 <pid>`.
   - Emit `external_session_registered` bus envelope on first liveness confirmation (metadata: `sessionId`, `pid`, `cwd`, `entrypoint`, `registered_at`).
   - Emit `external_session_exited` bus envelope when liveness check fails (metadata: `sessionId`, `pid`, `exit_time`).
   - Self-monitor guard: never register a session whose `cwd` resolves to the SM working directory or whose `entrypoint` matches SM's own CLI invocation pattern (per `feedback_no_self_monitor.md`).
   - Background task token extraction: when a JSONL entry contains `backgroundTaskId`, record token + output file path + originating session + start time. Poll output file size every 30 s; emit `bg_task_output_ready` bus envelope when size transitions from 0 → non-zero (metadata: `taskId`, `output_path`, `originating_session`, `start_time`, `ready_at`).
2. Dashboard panel — active external sessions: liveness indicator, sessionId, cwd, last-seen. Pending bg tokens: token, output_path, age, size.
3. Tests:
   - Unit tests for session discovery (mock `~/.claude/sessions/` directory).
   - Liveness check: live PID passes, dead PID emits `external_session_exited`.
   - Self-monitor guard: SM's own session is silently skipped, not registered.
   - `bg_task_output_ready` emitted when file transitions from 0 bytes.
   - No envelope emitted for 0-byte file until non-zero transition.
4. Confirm `tools/cassette_record.py` and `tools/soak_driver.py` are unaffected (session-lifecycle envelopes do not appear in the decision-output cassette path — verify by grep: `external_session` / `bg_task_output_ready` must not appear in cassette-related code).

**DOD:** see `docs/prompts/v1.9-orchestration/phase-2-session-watcher.md`.

**Action prompt:** `docs/prompts/v1.9-orchestration/phase-2-session-watcher.md`

---

## PHASE P3 — Learn Mode JSONL source expansion

**Pain point:** Learn Mode (`v1.3`) ingests only the Desktop session JSONL. certPortal oversight agents (Dave/Jen/Jason/Matt/Oliver/Michael) produce rich PASS/FAIL reasoning across hundreds of turns per day — exactly the signal SM's categoriser should learn from — but the ingest pipeline never sees it.

**References:**
- `docs/v1.9-backlog.md` §"Learn Mode JSONL expansion" (AC-2)
- `docs/use-cases/uc-01-external-session-monitoring.md` §AC-2
- `docs/learn-mode-design.md` (or `docs/smartai.md`) — Learn Mode design
- Memory: `feedback_no_self_monitor.md` — if source label matches SM-internal session, REJECT (never eval-feedback-loop)
- Memory: `project_learn_mode.md` — Learn Mode design overview

**Do-not-touch guard:** P3 touches ONLY `src/stream_manager/learn_mode.py`. The v1.3 protected symbols (`_consult_learn_mode_bias`, `_emit_learn_mode_bias_applied`, `bias_consult` timing key, `bias_hint` HITL column) are untouched. P3 extends the ingest side only; the bias advisory surface is unchanged.

**Scope:**
1. `src/stream_manager/learn_mode.py` — add `learn_sources` config:
   - Config structure: list of `{path_glob: str, label: str}`. Each entry names a labelled source of JSONL dialogue to ingest.
   - Default: empty list (opt-in; Desktop session JSONL path is unchanged, unaffected).
   - Ingest: for each source, tail-watch the JSONL glob for new entries. Tag each turn with `source_label` before categorisation so oversight-agent patterns are attributed separately from SM-governance patterns.
   - Self-monitor guard: if any resolved path matches the SM-internal JSONL session path, REJECT that source entry and log a warning (per `feedback_no_self_monitor.md` — no eval feedback loops).
   - Gatekeeper: `source_label` attribute on categorised turns propagates to the `hitl_overrides` WAL table (new nullable column `source_label` — additive).
2. `docs/learn-mode-design.md` (or `docs/smartai.md`) — update to document `learn_sources` config field.
3. Tests:
   - Source config: valid path_glob + label → turns tagged correctly.
   - Self-monitor guard: SM-internal path → rejected with warning; no turns ingested.
   - Source isolation: turns from source A and source B categorised independently; labels present in WAL table.
   - Empty `learn_sources` (default): existing Desktop session ingest path unchanged.

**DOD:** see `docs/prompts/v1.9-orchestration/phase-3-learn-mode-sources.md`.

**Action prompt:** `docs/prompts/v1.9-orchestration/phase-3-learn-mode-sources.md`

---

## PHASE P4 — v1.9 ship-gate + ADR-5 v1.9 baseline + lever-effect measurement

**Pain point:** P1 changes the retry trigger mechanism for the first time since the lever was wired in v1.7 P2; this is the first ship-gate that could show a non-zero `cli_dispatch_fallback_ms` p95. P2 adds a background observation subsystem that must not regress the governance hot path. P4 confirms all three land cleanly together.

**Do-not-touch guard:** P4 is finalize-only — soak run, alignment-eval gate, report, ADR-5 re-baseline, CHANGELOG, tag. Zero code hunks. `git --no-pager diff origin/main..HEAD -- src tools` must be empty.

Per memory `feedback_no_self_monitor.md`: do NOT run governance over SM's own JSONL/bus during the ship-gate.

Per memory `feedback_soak_cli_pool_flag.md`: **always** pass `--cli-pool-size 2`.

Per memory `feedback_subagent_long_task_abandonment.md`: the 32-min soak MUST run from the main session via `run_in_background` + `ScheduleWakeup`. NEVER from a subagent.

**Scope (mirror v1.8 P2 §1–§7):**

1. **Tier-3 ship-gate soak** — `python tools/soak_driver.py --cli-pool-size 2`. Match the v1.8 ship-gate parameter set (32-min wall-clock, ~ALLOW n=49, L2/L3 n=7+, L4 n=4+, LM n=10). Critical check: `cli_dispatch_fallback_ms` p95 > 0 (proves verdict-based trigger fires for the first time). If 0: STOP and mint `phase-4a-fallback-still-dormant.md` (Haiku never returned BLOCK/INTERVENE on soak content — falsification of verdict-based approach; promote option 4 corpus expansion to v1.9 follow-up).
2. **Alignment-eval gate** — `python tools/alignment_eval.py --ci-gate`. Attach report. Ship-gate FAILS if FR-OG-7 rows regress.
3. **ADR-5 v1.9 baseline** — append `## v1.9 ship-gate baseline`. Update Status header. Append-only. Required subsections: Source / Date / Ship SHA / Driver / Runtime / Events / Verdict; Latency targets (overall, n=60); Per-band split; ALLOW _evaluate_inner CLI residue breakout (6 rows — `cli_dispatch_fallback_ms` now expected non-zero for first time); Fallback routing summary (v1.9 measurement: trigger mode = verdict); Alignment-eval result; Delta vs v1.8 (`reports/soak-20260506T101746Z.md`); Budget; Caveats.
4. **Lever-effect measurement** (§"Caveats", data-driven only):
   - Fallback fire rate: share of ambiguous-BLOCK / HITL-synthesis L4 events triggering Sonnet retry. Target: > 0% (proves mechanism change works).
   - `cli_pool_send_ms` p95 trend vs v1.8 6470.35 ms. Earned improvement only if fire rate > 0% AND p95 drops; v1.8's +26% swing was upstream variance.
   - ALLOW p95 trend vs v1.8 6.48 s. Haiku TTFT on non-ambiguous content should be unchanged; any ALLOW p95 change is upstream variance.
   - L4 alignment p95 trend vs v1.8 11.85 s. Should be unchanged (FR-OG-7 still on Sonnet only).
   - Lever-falsification re-check: if fire rate > 0% AND `cli_pool_send_ms` p95 drops materially → lever effect confirmed; document magnitude. If fire rate > 0% but p95 does NOT drop → Haiku TTFT not faster than Sonnet's on this content; document and promote Haiku fastpath scope reduction to v2.0 backlog.
5. **LM p95 trend** — re-check vs v1.8 13.30 s ceiling (< 18 s). If ≥ 18 s + magnitude > 1 s: re-open watch as v2.0 backlog item; mint `phase-4b-lm-regression-triage.md`.
6. **CHANGELOG** — append `## [1.9.0]` section. Cover: P1 verdict-based fallback (PR #<m>), P2 session watcher (PR #<n>), P3 Learn Mode sources (PR #<p>), ship-gate verdict, lever-effect Δ, ADR-5 cross-link.
7. **Tag** — tag `v1.9.0` on the merge commit; push to origin.

**Cross-PR seam audit (memory: `feedback_cross_pr_seam_review.md`):** verify full chain: v1.8 P1 content-detection wiring (governance `is_ambiguous_block` → route() → `fallback_model_id`) ↔ v1.9 P1 verdict trigger (`cli_governance._evaluate_once` BLOCK/INTERVENE check) ↔ v1.9 P4 soak block (`cli_dispatch_fallback_ms` row, first non-zero) ↔ ADR-5 v1.9 fallback routing summary. Any mismatch (fallback fires but `cli_dispatch_fallback_ms` = 0, or envelope count > 0 but p95 = 0): STOP before tag.

**DOD:** see `docs/prompts/v1.9-orchestration/phase-4-ship-gate-finalize.md`.

**Action prompt:** `docs/prompts/v1.9-orchestration/phase-4-ship-gate-finalize.md`

---

## Convention

- Every phase block in this plan stands alone — copy-paste verbatim into a fresh Claude Code session via the matching action prompt under `docs/prompts/v1.9-orchestration/`.
- All phases land in **single-PR-per-phase** branches; no multi-feature PRs.
- v1.9 protected symbols are added to the do-not-touch list above as they ship.
- Memory feedback file references are absolute (full path / canonical name) so a fresh session can resolve them without scrollback.
