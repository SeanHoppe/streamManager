# v2.3 task plan — feature cycle (JsonlTailWorker wiring + soak hardening)

**Cycle theme: feature.** Operator-declared at P0 mint 2026-05-17.
Soft net-LOC target ≤ **1500** per ADR-18 Amendment A; BLOCK at 1.5×
= 2250. Anchor per **Amendment C** = cycle-tip (v2.3 P0-merge SHA),
NOT predecessor tag.

**Cycle-type framing.** v2.2 was consolidation (−6 LOC vs cycle-tip;
landed gap-4 API-timeout invariant + ADR-18 Amendments A/B/C). v2.3
returns to feature per ADR-18 cycle-alternation hygiene. **At least
one lever wired** — Seed 6 (JsonlTailWorker production wiring) is
the standing candidate; `WIRED_LEVER_LEDGER_COUNT` 0 → 1.

**P1 fire-order (recorded at P0).** Per `docs/v2.3-next-steps.md`
fire-order table:

1. P0 cycle frame (this PR).
2. **Seed 3 — Soak-driver PYTHONPATH fix** (🔴; ~5-30 LOC additive).
   Prompt: `docs/prompts/v2.3-orchestration/task-soak-driver-
   pythonpath-fix.md`.
3. **Seed 4 — Soak-summary dual-anchor block** (🟡; ~130 LOC
   additive tooling). Prompt: `docs/prompts/v2.3-orchestration/
   task-amendment-soak-summary-dual-anchor.md`. Closes ADR-18
   Amendment A L388 acceptance item + introduces Amendment C
   cycle-tip rendering.
4. **Seed 5 — v10 P4 corpus-fill continuation** (🔴; BG fire; ~0
   LOC). Path A (re-soak) default. Prompt: `docs/prompts/v2.3-
   orchestration/task-v10-p4-corpus-fill-continuation.md`.
5. **Seed 6 — JsonlTailWorker production wiring** (🟡; ~40 LOC
   production-bucket + ~40 LOC test). The **lever wire** for v2.3
   feature classification. Prompt: `docs/prompts/v2.3-orchestration/
   task-jsonltailworker-wiring.md`.
6. P2 ship-gate finalize. Prompt: `docs/prompts/v2.3-orchestration/
   phase-2-ship-gate-finalize.md`.

**Phase count.** P0 (frame) + **1 work phase P1 with 4 parallel
prompts** + P2 (ship-gate) = **3 numbered phases**, **1 work phase**.
ADR-18 Rule 4 caps the work-phase count at ≤ 3 per cycle — v2.3
sits well under at 1 work phase. The 4 P1 prompts are siblings under
the same work-phase umbrella, parallel-eligible (Seed 3 + Seed 4 may
land in either order; Seed 5 fires BG; Seed 6 independent).

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3 numbered phases. v2.3 numbered-phase span =
P0 → P2. v10.x slot remains gated on v10 P4 corpus-fill clearing
200 episodes — Seed 5 (BG fire) advances that gate in parallel.

**Companion docs:**

- `docs/v2.3-next-steps.md` — **the comparison anchor**. P2 S9
  walks this file row-by-row.
- `docs/adr/ADR-18-mvp-surface-freeze.md` — no new amendment
  planned at P0 (Rule 5 cap-bump 6 → 11 recorded inline below per
  operator).
- `docs/v2.2-backlog.md` — 11 open seeds carry forward.
- `docs/v2.2-task-plan.md` — consolidation-cycle predecessor.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — Seed 6 wiring adds NO new FR row (worker
  built since v1.3; FR-LM-* / FR-PPP-* cover existing surface).
- `docs/adr/ADR-5-latency-budget.md` — v2.2 ship-gate baseline
  current; v2.3 P2 appends new baseline.
- `docs/adr/ADR-17-soak-tiers.md` — Tier 3 ship-gate at P2.
- `dashboard/server.py` — Seed 6 wires at `_startup_cli_pool`.
- `src/stream_manager/jsonl_tail.py` — Seed 6 imports
  `JsonlTailWorker` + `set_active_worker`.
- `tools/soak_driver.py` — Seed 3 fix (L1243 + L1574) +
  Seed 4 dual-anchor block.

## Operator decisions recorded at P0 fire (2026-05-17)

Per `docs/prompts/v2.3-orchestration/phase-0-cycle-frame.md`
§"Operator decisions to record at P0 fire":

1. **Cycle type:** **FEATURE.** Default lean (alternation hygiene
   post-v2.2 consolidation).
2. **`WIRED_LEVER_LEDGER_COUNT` posture:** Currently 0. v2.3 wires
   ≥ 1 lever via Seed 6 (JsonlTailWorker `.start()` at dashboard
   startup). Target end-of-cycle count: **0 → 1.** **LANDED v2.3
   P1 Seed 4 (this section).** Ledger increment = 1 (JsonlTailWorker
   wired at `dashboard/server.py` `_startup_cli_pool` peer to
   `session_watcher`); stop wired at `_shutdown_cli_pool_event`.
   Test: `tests/test_dashboard_startup_jsonl_tail.py` PASSES (2/2).
3. **ADR-18 Rule 5 backlog cap:** **ACCEPT CAP-BUMP 6 → 11.**
   Matches v2.2 P0 precedent (1 → 6). Recorded in §"Cycle-
   discipline guardrails" below. No ADR-18 edit at P0; if v2.3
   work opens additional seeds beyond 11, operator must either
   accept another cap-bump or mint a Rule 5 amendment to codify
   the cycle-handoff exemption pattern.
4. **v10 freeze-lift overlap:** v2.3 has 4 P1 phases + P0 + P2 = 6
   phases. v10.x cycle remains blocked per #131 trigger cond 2
   throughout v2.3 P0–P3. Trigger cond 3 (v10 P4 corpus ≥ 200
   episodes) is advanced by Seed 5 in parallel.

## Memory pre-flight stamp (Amendment B)

Pre-flight performed 2026-05-17 against current `main` at
`aae298b` (PR #170 merged).

| Memory | Status | Action |
|--------|--------|--------|
| `project_v22_cycle_close.md` | FRESH (just-minted at v2.2 P2) | None |
| `project_v10_p4_hold_lifted.md` | **STALE** ("0 rows") | UPDATED 2026-05-17: now reads "60 rows post-piggyback" + reference to v2.3 prompt |
| `project_v10_rl_track.md` | FRESH (no v10 surface change since 2026-05-08) | None |
| `feedback_soak_cli_pool_flag.md` | FRESH (rule still binds; "flip default to 2" note advisory) | None |
| `feedback_no_self_monitor.md` | FRESH (polarity-flip rule current) | None |
| `MEMORY.md` index entry for `project_v10_p4_hold_lifted` | UPDATED to reflect 60/200 state | DONE |

## Cycle-discipline guardrails

- **LOC budget (Amendment A + Amendment C).** Anchor = cycle-tip
  (P0-merge SHA, populated post-merge of this PR). Soft target
  ≤ 1500 LOC across 3-bucket scope (`src/`, `tests/`, `docs/`).
  BLOCK threshold = 1.5× = 2250. Production bucket (`src/` +
  `dashboard/`) load-bearing; test + docs advisory per Amendment A.
- **Rule 5 backlog cap.** **Cap-bumped to 11 at P0** (operator
  acceptance recorded). v2.3 cycle work that opens any NEW backlog
  item beyond 11 must close or graduate ≥ 1 existing seed before
  merging — OR escalate to Rule 5 amendment.
- **Rule 6 memory pre-flight.** Performed at P0 (above stamp).
  Stale memory updated in same PR before P0 merge.
- **Lever ledger.** Entering posture 0; target end posture 1.
  Wire count tracked at P2 ship-gate against `WIRED_LEVER_LEDGER_
  COUNT` per ADR-18 Rule 7.
- **Surface freeze.** ADR-18 Rule 1 in force. FROZEN scope per
  ADR-18 Rule 1 table is the **pre-CLI seam** specifically:
  `governance._evaluate_inner_core` + helpers, `model_router.py`
  band-routing surface, `cli_governance.py` `CliWorker.send`
  contract. Entire modules are NOT wholesale FROZEN — only the
  named seam. No v2.3 prompt touches FROZEN surface; Seed 6 wires
  `JsonlTailWorker` at `dashboard/server.py` startup (peer to
  session_watcher), not at the pre-CLI seam.

## PHASE P0 — this PR (docs frame)

Files changed (repo-tracked):
- `docs/v2.3-task-plan.md` (new; this file).

Files changed (local-only auto-memory, NOT in this PR — per
Amendment B pre-flight; absolute paths kept for operator/reviewer
verifiability only):
- `~/.claude/projects/C--Users-SeanHoppe-vs-streamManager/memory/project_v10_p4_hold_lifted.md` (UPDATED).
- `~/.claude/projects/C--Users-SeanHoppe-vs-streamManager/memory/MEMORY.md` (UPDATED).

Repo-tracked LOC delta: docs only.

ADR-18 Amendment B self-applies: this PR carries the memory pre-
flight stamp + the stale-memory update.

## PHASE P1 — task prompts (parallel-eligible)

See fire-order table above. Each P1 task is a discrete PR off
`main` post-P0.

## PHASE P2 — ship-gate finalize

Prompt: `docs/prompts/v2.3-orchestration/phase-2-ship-gate-
finalize.md`. Fires after all required P1 PRs merge.

## DoD (this PR)

- [ ] `docs/v2.3-task-plan.md` minted with all 4 operator
      decisions recorded.
- [ ] Memory pre-flight stamp included.
- [ ] Stale memory updated (project_v10_p4_hold_lifted.md +
      MEMORY.md index).
- [ ] Single PR `feat(v2.3-p0):` against `main`.

## Refs

- `docs/v2.3-next-steps.md` (PR #170, the comparison anchor).
- `docs/prompts/v2.3-orchestration/phase-0-cycle-frame.md`.
- `docs/adr/ADR-18-mvp-surface-freeze.md` Amendments A / B / C.
- `project_v22_cycle_close.md`.
