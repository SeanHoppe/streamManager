# v1.5 backlog

Seed list of items deferred out of v1.4 scope. Each item links back to
the originating phase or ADR section.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v1.5 task,
not by editing the emoji.

---

## Carry-forward from v1.4 ship-gate (ADR-5 v1.4 baseline §"Caveats")

- 🟡 **`_evaluate_inner` sub-phase instrumentation**
  v1.4 ship-gate (`reports/soak-20260504T182027Z.md`) disproved the
  v1.3.1 publish-path I/O hypothesis: ALLOW p95 = 7.57 s, of which
  `inbound_publish` p95 = 0.43 ms and `record_decision` p95 = 0.11 ms.
  **100% of the ALLOW tail is now inside `_evaluate_inner`** and opaque
  to the soak report. v1.5 should add sub-phase wall-clock timings on
  `engine._last_phase_timings_ms` for the components inside
  `_evaluate_inner`:
  - `og7_check` — FR-OG-7 maturity gate
  - `fast_precheck` — L0/L1 short-circuit path
  - `graph_classify` — decision-graph classify call
  - `hydrator_state_read` — lazy-hydrator state lookup
  - `routing_dispatch` — final L0→L4 dispatch + verdict assembly
  Then extend `tools/soak_driver.py` to emit a
  `### ALLOW _evaluate_inner sub-phase breakout (v1.5)` block analogous
  to the v1.4 publish-path block, so the next ship-gate attributes the
  ~7.5 s tail to a specific component before any tightening of the
  ALLOW p95 budget is attempted.
  Originating: ADR-5 v1.4 ship-gate baseline §"Caveats" — `_evaluate_inner`
  is now the load-bearing tail.

- 🔵 **LM (categorize) p95 trend watch**
  v1.4 ship-gate measured LM p95 = 19.26 s (n=10), vs v1.3.1 = 15.39 s.
  +3.87 s is within sample noise at n=10 and the budget is ≤ 25 s, but
  the trend is worth a re-measure. v1.5 ship-gate should keep an eye on
  this row; if the next measurement also lands above 18 s, open a v1.5
  task to investigate Sonnet upstream queueing or local categorizer
  worker contention. No action required if the next sample retreats.
  Originating: ADR-5 v1.4 ship-gate baseline §"Caveats".

---

## Convention

Items here are seeded from v1.4 phase scoping and the v1.4 ship-gate
ADR-5 §"Caveats" block. When picked up in a v1.5 task, link the new
task ID and the originating phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
