# 2026-05-21 task list

> **Source:** post-v2.6.0 ship state (`c3a964c`, PR #198 merged 2026-05-20; tag pushed 2026-05-21). Anchored against `docs/v2.6-backlog.md` §"Carry-forwards from v2.6" + `docs/v10-mvp-status.md` last refresh 2026-05-19.
>
> **Pipeline:** Each row is either **AGENT-DISPATCHABLE** (a subagent can land it now without operator gating) or **OPERATOR-BOUND** (documented; not spawned). Agent-dispatchable rows spawn in this session and track to landing in [`docs/2026-05-21-implementation-notes.md`](2026-05-21-implementation-notes.md).
>
> **Predecessor:** [`docs/2026-05-19-task-list.md`](2026-05-19-task-list.md) (3 jobs landed: J1 v10-mvp-status refresh, J2 seed-v2.4-g audit, J3 v2.5 P0 skeleton). [`docs/2026-05-19-implementation-notes.md`](2026-05-19-implementation-notes.md) is aspirational baseline for shape + per-job structure + landing-log format.
>
> **Lifetime:** Discardable at v2.7 P0 cycle frame fire.

---

## Scoping rationale

v2.6.0 shipped 2026-05-20 (`c3a964c`, PR #198). Cycle was feature: Seed v2.5-G step (1) wall-clock instrumentation landed at PR #196 (`7220b33`), lever ledger bumped 1 → 2 (production), first wire since v2.3 Seed 6. Seed v2.5-A closed CONTENT-DRIFT pre-execution at PR #197. NEW Seeds v2.6-A (content drift) + v2.6-A-T (timeout boundary) spawned. Sonnet n=6 0.9412 + Haiku 1.0 + regression_rows=[].

Entering v2.7: **6 cap-counted seeds** (v2.6-A + v2.6-A-T + v2.6-C + v2.4-E + v2.4-F + v2.6-G) + **6 EXEMPT** (v2.4-I..N) = 12 open. Δ +1 cap-counted vs v2.6 P0 entry.

Operator-bound v2.7 P0 cycle-type decision pending. The agent-dispatchable subset reduces to **four concrete prep artifacts** that bound the v2.7 P0 fire surface without requiring the operator's cycle-type call:

1. **Refresh `docs/v10-mvp-status.md`** — corpus 360 → 608 episodes (+248 since last refresh; v2.5 + v2.5.1 + v2.6 piggyback Runs 5 + 6 + 7 absorbed). Head-of-chain renames v2.4-C → v2.5-C → v2.6-C (4th-consecutive deferral). Aggregate completeness narrative unchanged at ~80%; corpus-growth row + deferral-count row stale.
2. **Author Seed v2.6-G step (2) timeout-tighten audit** — evidence prep for the operator's v2.7 P0 "fire step (2) cap-tighten?" decision. Measured Sonnet endpoint p99 = **25.048 s** (n=192, Sonnet) from v2.6 P1 PR #196 instrumentation now in hand; J2 audit (v2.5 P0) recommended band 30–45 s. Doc selects a candidate cap value + records trade-offs (boundary safety margin vs eval-runtime budget) for the operator's promote-to-FIRE decision. Does NOT change `TIMEOUT_SECONDS` (FROZEN per ADR-18 surface-freeze).
3. **Author Seed v2.6-A row-10 n=12 re-measure protocol** — design doc for the v2.7 P2 instrumented re-measure that decides golden-update (a) vs DIP-watch hold (b) per `docs/v2.6-backlog.md` §"Seed v2.6-A". Specifies n=12 fixture, runner invocation, decision tree, expected runtime, and gate-condition stability check. Evidence prep for v2.7 P2 disposition.
4. **Mint v2.7 PM-mint bundle** — `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` (P0 skeleton) + `docs/v2.7-next-steps.md` (comparison anchor). PR #191 precedent (v2.6 PM-mint bundled both files in one PR). Skeleton bounds the P0 decision surface (cycle-type call; cap-counted 6 + EXEMPT 6; Seed v2.6-G step (2)+(3) fire decision; Seed v2.6-A re-measure decision; Seed v2.6-C 4th-defer decision; Seed v2.4-E/F watch dispositions). No operator decision pre-empted.

Everything else (v2.7 P1 work-phase prompt mints, P2 ship-gate mint, latency-lever scope decision, PR #154 sign-off) is gated on the operator's v2.7 P0 fire or on a future ship-gate run and therefore not dispatchable in this session.

---

## Agent-dispatchable jobs (spawned this session)

| Job | Output | Bind | Spawn status | Land status | Result PR / commit | Notes ref |
| --- | ------ | ---- | ------------ | ----------- | ------------------ | --------- |
| **J1** | Refresh `docs/v10-mvp-status.md` (corpus 360 → 608 with v2.5/v2.5.1/v2.6 Run 5/6/7 absorption; head-of-chain rename v2.4-C → v2.6-C; deferral-count bump to 4 consecutive; aggregate completeness narrative unchanged) | Docs-only | DISPATCHED | ✅ LANDED | working-tree edit (+21/−14 net +7) | [§J1](#j1-refresh-docsv10-mvp-statusmd) |
| **J2** | New `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` (Sonnet p99 25.048s cited; candidate cap value within 30-45s band; safety-margin vs eval-runtime tradeoff; v2.7 P0 promote-to-FIRE evidence block) | Docs-only | DISPATCHED | ✅ LANDED | working-tree new file (272 lines; primary cap 30 s) | [§J2](#j2-author-seed-v26-g-step-2-timeout-tighten-audit) |
| **J3** | New `docs/seed-v2.6-a-row10-remeasure-protocol.md` (n=12 design + runner invocation + decision tree for v2.7 P2 golden-update vs DIP-watch hold) | Docs-only | DISPATCHED | ✅ LANDED | working-tree new file (220 lines; ≥9/12 gate) | [§J3](#j3-author-seed-v26-a-row-10-re-measure-protocol) |
| **J4** | New `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` (P0 skeleton; mirrors v2.6 P0 shape) + new `docs/v2.7-next-steps.md` (PM-mint comparison anchor; mirrors v2.6 next-steps) | Docs-only | DISPATCHED | ✅ LANDED | working-tree new files (617 + 495 lines) + new dir | [§J4](#j4-mint-v27-pm-mint-bundle) |

---

## Operator-bound items (documented, not spawned)

These cannot be dispatched by an agent dev session. Listed so the v2.7 P0 fire can pick them up.

| # | Title | Bind | Source |
| - | ----- | ---- | ------ |
| O1 | **Fire v2.7 P0 cycle frame** — cycle-type decision (feature vs consolidation) | Operator only | post-v2.6 cycle close; carry from v2.6 backlog |
| O2 | **Seed v2.6-G step (2) timeout-tighten fire/defer at v2.7 P0** — promote to FIRE iff cycle = feature; J2 audit provides evidence | Operator at P0 fire (J2 provides evidence) | `docs/v2.6-backlog.md` §"Seed v2.6-G" item 5 |
| O3 | **Seed v2.6-G step (3) env-split fire/defer at v2.7 P0** — bundle same phase as step (2) OR carry independently | Operator at P0 fire | `docs/v2.6-backlog.md` §"Seed v2.6-G" item 5 |
| O4 | **Seed v2.6-A golden-update vs DIP-watch hold** — decided at v2.7 P2 post n=12 re-measure | Operator at v2.7 P2 (J3 provides protocol) | `docs/v2.6-backlog.md` §"Seed v2.6-A" item 6 |
| O5 | **Seed v2.6-C 4th-consecutive deferral call** — defer to v2.8 OR fire at v2.7 if cycle = feature with budget | Operator at v2.7 P0 | `docs/v2.6-backlog.md` §"Seed v2.6-C" item 1 |
| O6 | **Seed v2.4-E p95 watch close-vote** — closes if v2.7 P2 reading ≤ 8.2 s; else continues 🟢 | Operator at v2.7 P2 | `docs/v2.6-backlog.md` §"Seed v2.4-E" item 2 |
| O7 | **Seed v2.4-F L4 half close-vote** — closes if v2.7 P2 reading ≤ 17 s; promotes 🔴 if ≥ 22 s | Operator at v2.7 P2 | `docs/v2.6-backlog.md` §"Seed v2.4-F" item 4 |
| O8 | **PR #154 sign-off** — still prefixed `DRAFT` per `gh pr view 154`; flip + merge, re-mark `isDraft=true`, or close | Operator only | carry from `docs/2026-05-19-implementation-notes.md` Q-4 |
| O9 | **v10 chain unblock** — Seed v2.6-C Path-D synthetic-fixture P5 implementation (~600 LOC); unblocks #112 → #131 → #124 + #125 | Operator at v2.7 P0 if cycle = feature with budget | `docs/v10-mvp-status.md` §4 (held-chain map) |

---

## Per-job details

### J1 — refresh `docs/v10-mvp-status.md`

**Why now.** The status doc was last refreshed 2026-05-19 at corpus = 360 episodes (post v2.4 P2 Run 4). v2.5 P2 (Run 5), v2.5.1 P2 (Run 6), v2.6 P2 (Run 7) piggyback runs absorbed +248 episodes total (360 → 408 → 548 → 608). Head-of-chain rename v2.4-C → v2.5-C → v2.6-C; deferral count entering v2.7 = 4 consecutive (v2.4 + v2.5 + v2.5.1 + v2.6). No surface change; pure ledger refresh.

**Out-of-scope (do not touch).** Anything inside `src/stream_manager/`, `tools/`, `dashboard/`. Pure docs-only ledger refresh. ADR-18 surface-freeze still applies — no code edits.

**Acceptance.** Diff shows only `docs/v10-mvp-status.md` modified. §3 P4 corpus row updated with three new piggyback entries (Run 5 v2.5 P2, Run 6 v2.5.1 P2, Run 7 v2.6 P2). §4 held-chain map renames Seed v2.4-C → Seed v2.6-C; deferral-count narrative updated (4 consecutive). Header `Last refresh` line updated to 2026-05-21. §12 cross-refs append v2.5/v2.5.1/v2.6 cycle-close memories + v2.6 backlog. Aggregate completeness narrative remains ~80% (no phase status flips this cycle).

---

### J2 — author Seed v2.6-G step (2) timeout-tighten audit

**Why now.** v2.6 P1 PR #196 (`7220b33`) landed the instrumentation step (1) and surfaced authoritative per-run wall-clock distributions. Sonnet n=192, p99 = **25.048 s**, max = 25.063 s (50 ms above the current `TIMEOUT_SECONDS = 25.0` cap). J2 (v2.5 P0) audit recommended band 30–45 s; measured p99 now lands at the upper bound of the boundary-sensitivity zone. The step (2) value-selection audit is the evidence package the operator reads before deciding "promote step (2) to FIRE at v2.7 P0?" Predecessor `docs/seed-v2.4-g-cli-timeout-audit.md` is the structural template.

**Inputs.**
- `src/stream_manager/cli_governance.py:49` — current `TIMEOUT_SECONDS = 25.0` value + surrounding context. FROZEN; cite line only, do not edit.
- `reports/alignment-eval-20260520T205842Z.{md,json}` — v2.6 P2 n=6 alignment-eval with NEW wall-clock distributions (this is the canonical step (2) input).
- `reports/seed-v2.5-a/alignment-eval-20260520T172054Z.{md,json}` — v2.6 P2 S6.5 instrumented n=6 row-10 re-measure (single-row p99 = 24.96 s, max = 25.047 s).
- `docs/seed-v2.4-g-cli-timeout-audit.md` — predecessor audit (J2 from v2.5 P0); cite as template + cite the 30–45 s recommended band.
- `tools/alignment_eval.py` — instrumentation surface from PR #196 (cite as the measurement source; do not edit).

**Out-of-scope (do not touch).** `src/stream_manager/cli_governance.py` — FROZEN. `tools/alignment_eval.py` — last touched at PR #196; cite the per-run timing surface but do not edit. `tests/`, `dashboard/`. Audit is read-only.

**Acceptance.** New file `docs/seed-v2.6-g-step2-timeout-tighten-audit.md`. Sections: §Current state (cite `cli_governance.py:49`, current `TIMEOUT_SECONDS = 25.0`); §Measured distribution (Sonnet + Haiku p50/p95/p99/max from v2.6 P2 n=6 reading); §Boundary analysis (the 50 ms gap between p99 25.048 s and cap 25.0; what this means for content-classification stability at the boundary); §Cap-value selection (3–5 candidates evaluated, e.g. 28 s / 30 s / 35 s / 40 s / 45 s; for each: safety margin, eval-runtime impact, false-timeout reduction estimate); §Recommendation (specific cap value + rationale; explicit acknowledgement of remaining v2.6-A-T timeout-boundary watch); §v2.7 P0 question (verbatim decision block operator pastes into cycle-frame decision log); §Step (3) env-split coupling note (whether to bundle same phase as step (2) or carry independently). 200–300 lines.

---

### J3 — author Seed v2.6-A row-10 re-measure protocol

**Why now.** Seed v2.6-A (filed at v2.6 P2 S6.5) classifies row 10 `frog7-wirecli-module-10` as content-divergent: sonnet_majority = INTERVENE (4/6) ≠ golden expected SUGGEST. Disposition options (a) golden-update + (b) DIP-watch hold + (c) re-measure-at-n=12 then decide. Recommended path = (c). The re-measure protocol doc specifies the n=12 fixture, runner invocation, decision tree, and stability-check gate so that the v2.7 P2 re-measure can fire deterministically without operator re-design.

**Inputs.**
- `docs/seed-v2.5-a-row10-diagnosis.md` §"Disposition" 2 — option (a)/(b)/(c) text + verdict-matrix row 3 selection.
- `reports/seed-v2.5-a/alignment-eval-20260520T172054Z.{md,json}` — v2.6 P2 n=6 instrumented sample (the baseline reading; cite the run distribution + p99 + 1/6 timeout count).
- `tools/alignment_eval.py` — instrumented runner from PR #196 (cite the per-run timing + sonnet_runs surface).
- `tests/golden/l4_alignment.jsonl:10` — golden row reference (cite the `expected_verdict=SUGGEST` line). Do NOT edit golden in this audit; golden update is the operator decision after the re-measure lands.

**Out-of-scope (do not touch).** Golden files. `src/stream_manager/`. Audit is design-only.

**Acceptance.** New file `docs/seed-v2.6-a-row10-remeasure-protocol.md`. Sections: §Background (cite v2.6 P2 S6.5 n=6 reading: INTERVENE 4/6 vs SUGGEST golden); §Re-measure design (n=12 single-row fixture; specify runner invocation, env vars, output path); §Stability check (criterion for "stable INTERVENE majority" vs "still unstable"; ≥ 9/12 INTERVENE → STABLE-CONTENT-DRIFT path; 6–8/12 INTERVENE → still-unstable carry); §Decision tree (a-stable → golden update path; b-unstable → DIP-watch hold; b-still-100%-timeout → escalate Seed v2.6-A-T per v2.6-G step (2) coupling); §Expected runtime (n=12 × Sonnet p50 ~16s = ~3.2 min minimum; budget headroom); §v2.7 P2 fire conditions (pre-conditions: Seed v2.6-G step (2) cap value chosen at P1 OR not yet bound; if not yet bound, re-measure under current cap = 25 s); §v2.7 P2 question (verbatim decision block); §Coupling with Seed v2.6-A-T (timeout-boundary watch closes iff cap tighten lands + re-measure shows ≥ 2 s margin under new cap). 150–220 lines.

---

### J4 — mint v2.7 PM-mint bundle

**Why now.** Precedent: PR #191 (v2.6 PM-mint) bundled `docs/v2.6-next-steps.md` + `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` in one PR. PR #178 (v2.4 PM-mint) used the same shape. v2.7 PM-mint bundle:
- `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` — P0 skeleton; mirrors `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` shape. Decision blocks UNCHECKED (operator fills at fire time).
- `docs/v2.7-next-steps.md` — comparison anchor; mirrors `docs/v2.6-next-steps.md` shape. Per-seed disposition tracker with check-row markers; ADR-18 hygiene block; ship-gate close-out skeleton (filled at v2.7 P2 close).

**Anchor.** `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` (P0 skeleton) + `docs/v2.6-next-steps.md` (comparison anchor). Match shape, section headings, decision-block format. Substitute v2.6 → v2.7 throughout. Preserve §"Memory pre-flight (Rule 6)" + §"Cycle-type call" + §"Rule 5 cap-counted reading" + §"Canonical S2 env block" + §"Alignment-eval n=6 stability rule" + §"Phase prompt stubs" sections.

**Required content (P0 skeleton).**
- §"Branch + base" — base `main` at `c3a964c` (v2.6.0 ship); branch `feat/v2.7-p0-cycle-frame` (feature default) OR `chore/v2.7-p0-cycle-frame` (consolidation override).
- §"Memory pre-flight (Rule 6 — ADR-18 Amendment B)" — minimum re-read list updated with v2.6 close memory (`project_v26_cycle_close.md`) + retained: firewall, polarity-flip, n=6 stability window, cli-pool-size flag, glob narrowing.
- §"Cycle-type call" — decision-block UNCHECKED. Default-lean **TBD** (alternation hygiene: v2.6 was feature; v2.7 consolidation would alternate. But v2.6-G step (2) is ready and v2.6-C carries 4-cycle deferral pressure — both feature triggers. Operator decides at fire time).
- §"Rule 5 cap-counted reading" — cap-counted 6 (v2.6-A + v2.6-A-T + v2.6-C + v2.4-E + v2.4-F + v2.6-G) + EXEMPT 6 (v2.4-I..N). Cite Amendment E.
- §"Seed v2.6-G step (2) fire decision" — decision-block UNCHECKED; references J2 audit as evidence input; couples to cycle-type call.
- §"Seed v2.6-G step (3) env-split fire decision" — decision-block UNCHECKED; MAY bundle step (2) OR carry independently.
- §"Seed v2.6-C deferral/fire decision" — decision-block UNCHECKED; 4th-consecutive defer call.
- §"Seed v2.6-A re-measure decision" — decision-block UNCHECKED; references J3 protocol; fires at v2.7 P2 by default but P0 confirms.
- §"Seed v2.6-A-T carry-forward" — re-state coupling with v2.6-G step (2) close-out.
- §"Seed v2.4-E + v2.4-F latency-regression posture" — re-state watch dispositions (E continues 🟢; F L4 half DOWNGRADE-TOWARD-CLOSE).
- §"v2.6 carry-forward dispositions" — quick-ref table (6 cap-counted + 6 EXEMPT rows).
- §"ADR-18 reference block" — Amendments A / B / C / D / E with one-line summaries + file:line cites.
- §"Canonical S2 env block" — copy verbatim from v2.6 P0 (BRIDGE_RL_LOGGER_ENABLED + BRIDGE_LOC_PATHSPEC).
- §"Alignment-eval n=6 stability rule (S5 procedure)" — encode the branching rule; v2.7 P2 entry condition: prior cycle is v2.6, Sonnet pass_rate = 0.9412 (n=6) → 0.9412 ≥ 0.85 → default `--runs 3` UNLESS escape-hatch conditions fire.
- §"Phase prompt stubs" — v2.7 P1 (if any; depends on §"Seed v2.6-G step (2) fire decision") + P2 ship-gate.
- §"DoD" — match v2.6 P0 DoD checklist shape.

**Required content (next-steps comparison anchor).**
- §"Fire-order" table — placeholder rows for P0 + (optional) P1 + P2 ship-gate.
- §"Seeds" — per-seed entry for each of 6 cap-counted + 6 EXEMPT. Each entry includes Source, Fact, Disposition v2.7 (default-lean OR TBD), Compare-back row marker (blank `[ ]` checkbox + landed marker template).
- §"P0 frame — operator-bound decisions" — decision-block checklist (UNCHECKED at PM-mint; matches P0 skeleton decision surface).
- §"Cycle-type fallback (consolidation override)" — same shape as v2.6 P0 §"Cycle-type fallback".
- §"Compare-back protocol (per-landing)" — same 5-step protocol.
- §"v2.7 P2 ship-gate close-out" — empty table skeleton (filled at v2.7 P2 close).
- §"NEW v2.7 ship-gate seeds" — empty section skeleton.
- §"ADR-18 hygiene at mint time" — match shape.
- §"Refs" — point at v2.6 next-steps as predecessor, v2.6 backlog, ADR-18, J2/J3 outputs from this session.

**Out-of-scope.** Do not invent v2.7 phase scope. Cycle-type decision belongs to operator; the prompt's job is to bound the decision surface, not pre-empt it. Decision blocks UNCHECKED.

**Acceptance.** Two new files (`docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` + `docs/v2.7-next-steps.md`) + 1 new directory. Each file matches predecessor shape. Diff scoped to those two paths. Operator can pull P0 trigger as-is after filling decision blocks.

---

## Landing flow

```
[J1, J2, J3, J4] spawned in parallel
    │
    ▼
each agent writes its artifact directly (docs-only; no PR per job)
    │
    ▼
this thread verifies landing (Read each artifact; confirm size / sections / refs)
    │
    ▼
this thread updates task-list rows above (spawn → land → "✅")
    │
    ▼
this thread closes implementation-notes with deviations / open questions / follow-ups
    │
    ▼
operator inherits: 4 docs (1 modified + 3 new) + 1 new directory; 9 operator-bound items queued for v2.7 P0 fire
```

---

## Refs

- [`docs/2026-05-19-task-list.md`](2026-05-19-task-list.md) — predecessor shape baseline.
- [`docs/2026-05-19-implementation-notes.md`](2026-05-19-implementation-notes.md) — predecessor notes baseline (aspirational; D-1..D-9 patterns).
- [`docs/2026-05-21-implementation-notes.md`](2026-05-21-implementation-notes.md) — this session's running notes.
- [`docs/v2.6-backlog.md`](v2.6-backlog.md) — carry-forward ground truth (v2.6-A + v2.6-A-T + v2.6-C + v2.4-E + v2.4-F + v2.6-G + v2.4-I..N).
- [`docs/v2.6-next-steps.md`](v2.6-next-steps.md) — comparison anchor for v2.6 cycle (closes at v2.6 P2).
- [`docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md`](prompts/v2.6-orchestration/phase-0-cycle-frame.md) — J4 P0 skeleton structural anchor.
- [`docs/v10-mvp-status.md`](v10-mvp-status.md) — J1 target.
- [`docs/seed-v2.5-a-row10-diagnosis.md`](seed-v2.5-a-row10-diagnosis.md) — v2.6 P2 S6.5 verdict + Seed v2.6-A + v2.6-A-T mint origin.
- [`docs/seed-v2.4-g-cli-timeout-audit.md`](seed-v2.4-g-cli-timeout-audit.md) — J2 v2.5 P0 audit (J2 this session is a step-(2) successor).
- [`docs/adr/ADR-18-mvp-surface-freeze.md`](adr/ADR-18-mvp-surface-freeze.md) — Amendments A–E.
