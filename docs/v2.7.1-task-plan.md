# v2.7.1 task plan — corrective sub-cycle (post-v2.7 P3 BLOCK)

**Sub-cycle theme: corrective.** Minted 2026-05-22 after v2.7 P3 ship-
gate BLOCKED at S4 CI gate (1 FR-OG-7 regression on
`frog7-phase-timings-keys-05` + 4-row cap-clip artefact corpus-wide).
v2.7.0 NOT tagged.

**Precedent.** v2.5.1 corrective sub-cycle (v2.5 P2 BLOCKED at Sonnet
0.7895 n=3 floor breach; v2.5.1 P1 re-eval n=6 → Sonnet 0.9375 verdict
A = measurement artefact; v2.5.1 ship at PR #75 `ad372d7`). v1.3.1
also a `<base>.x.1` precedent (v1.3.0 feature-complete but never
soaked → v1.3.1 corrective + ship).

**Cycle-tip anchor (ADR-18 Amendment C).** Cycle-tip remains
`4902cca440b33c14fddd9357116923ae5fe1fa4b` (v2.7 P0 merge). v2.7.1
sub-cycle inherits the v2.7 anchor; no re-pin. v2.7.1 ship tag = first
actual ship of v2.7 work (v2.7.0 never minted).

**WIRED_LEVER_LEDGER posture entering v2.7.1.** **3 production / 0
soak.** P1 wire (Seed v2.6-G step (2) cap 25→30) already shipped via
PR #203 `28a89c4`. v2.7.1 corrective does NOT bump the ledger — it
ships the existing v2.7 work past a corrected gate.

## Carry-forward state from v2.7 P3 BLOCK

| Seed                 | Status entering v2.7.1                                                                                                                |
|----------------------|---------------------------------------------------------------------------------------------------------------------------------------|
| v2.7-B (NEW)         | Row `frog7-phase-timings-keys-05` Haiku-stabilisation regression. **v2.7.1 P1 fires the J3 n=12 re-measure** (re-eval).                |
| v2.7-A-CLIP          | Corpus-wide cap-clip artefact (4 rows ≥ 50 % timeout). **v2.7.1 P1 records the n=12 broader re-measure as a sibling output** at P1 fire. |
| v2.6-A               | Carry from v2.7 P2; still blocked on cap-band escalation or step (3) env-split. Will re-evaluate at v2.7.1 P2 ship-gate.               |
| v2.6-A-T             | Carry from v2.7 P2; margin −0.092 s at v2.7 P2; same as v2.6-A status.                                                                |
| v2.6-G step (3)      | **PROMOTED candidate** — structural unblocker for v2.7-A-CLIP + v2.6-A-T. Operator decides v2.7.1 vs v2.8 fire.                       |

## Phases

### P1 — row-05 Haiku-stabilisation investigation (J3 n=12 re-eval)

Prompt: `docs/prompts/v2.7.1-corrective/phase-1-row-05-haiku-
investigation.md`.

Scope:
- n=12 single-row alignment-eval re-measure on
  `frog7-phase-timings-keys-05` (mirror Seed v2.6-A J3 protocol shape).
- Sonnet-only (`--candidate-only-control`) PLUS Haiku-only secondary
  measurement (the regression is on the Haiku side).
- Decision tree:
  - **Hatch A — measurement artefact.** If Haiku n=12 majority ≠
    ALLOW (e.g. mixed or SUGGEST majority), the n=6 stable ALLOW was
    a sampling artefact (n=6 escape-hatch did not fire because Haiku
    was unanimous, but unanimity at n=6 ≠ unanimity at n=12). →
    v2.7.1 P2 ship-gate re-fire with --runs 6, expect row-05 to
    flip back to unstable+excluded.
  - **Hatch B — Haiku server-side drift confirmed.** If Haiku n=12
    is also 12/12 ALLOW, the regression is real. → operator picks at
    P2: (i) golden recalibration (downgrade row 05 to model_floor=
    sonnet but explicit `haiku_known_diverge=ALLOW` tag bypassing
    haiku_regression_frog7 gate); (ii) Sonnet-only gate (skip Haiku
    on this row); (iii) accept the regression as a real product
    finding requiring Haiku model_floor upgrade work in v2.8+.
  - **Hatch C — verdict diversity.** If Haiku n=12 produces some
    new pattern (e.g. 4/12 ALLOW + 8/12 SUGGEST), spawn refined seed
    Seed v2.7-B-N for v2.8.

LOC budget: 0 src/tests/tools/dashboard production-bucket (re-measure
only, no production code change unless operator picks golden
recalibration at P2 — then +1/-1 on `tests/golden/l4_alignment.jsonl`
row 5).

Lever-ledger: HOLD at 3/0 production through P1.

### P2 — ship-gate re-fire + v2.7.1 tag

Prompt: `docs/prompts/v2.7.1-corrective/phase-2-shipgate-refire.md`
(mint at P1 merge per v2.5.1 PR #75 cadence — predecessor mints P2
prompt after P1 verdict).

Scope mirrors v2.7 P3 (S1–S13), with these adjustments folded post-
P1 verdict:
- S4 alignment-eval `--runs 6` (hatch (3) still bound; v2.6 P2
  baseline 47 % > 25 % unchanged).
- S4 row exclusions (hatch (4)): apply to all 4 cap-clip rows
  identified at v2.7 P3 (`frog7-cli-pool-acquire-01`,
  `frog7-cli-replay-flag-02`, `frog7-matched-hash-column-03`,
  `frog7-wirecli-module-10`) + (conditional) row 05 per P1 verdict.
- S9 tag = **v2.7.1** (not v2.7.0).
- S11 close memory: `project_v27_cycle_close.md` records v2.7 +
  v2.7.1 jointly (similar to v2.5.1 memory pattern).
- S13 v2.8 P0 skeleton mint.

WIRED_LEVER_LEDGER posture exiting P2: **3 production / 0 soak**
(unchanged — v2.7.1 is corrective ship of existing wire, no new
lever).

## Refs

- `docs/v2.7-task-plan.md` §"P3 close-out — BLOCKED 2026-05-22" —
  source of this sub-cycle.
- `docs/v2.7-next-steps.md` §"Fire-order" row 5 — BLOCK marker.
- `reports/alignment-eval-20260522T064433Z.{md,json}` — S4 failing
  output.
- `docs/prompts/v2.5.1-corrective/phase-1-sonnet-floor-investigation.md`
  — v2.5.1 P1 precedent prompt shape.
- `docs/prompts/v2.5.1-corrective/phase-2-shipgate-refire.md` —
  v2.5.1 P2 precedent prompt shape.
- `project_v25_cycle_close.md` — v2.5.1 ship close memory (records
  the sub-cycle ship vs the never-tagged base cycle).
- `feedback_alignment_eval_stability_window.md` — n=6 mandate + hatch
  rules (re-applied at v2.7.1 P2).
