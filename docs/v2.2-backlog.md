# v2.2 backlog

Seed list of items deferred out of v2.1 scope. v2.2 inherits ADR-18
cycle-discipline rules in full force.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v2.2 task, not
by editing the emoji.

---

## Seed items

### 🟡 Remote-CLI monitoring — extend SM beyond local JSONL tail

**Surfaced 2026-05-08** during issue #128 (PPP probe transport) review.
SM today reaches CLI instances exclusively via local-disk JSONL tail
(`src/stream_manager/jsonl_tail.py`) + local SQLite WAL bus
(`src/stream_manager/message_bus.py`). Bus = local file path. JSONL paths =
local-disk. No HTTP / network surface for SM monitoring.

PPP audit harness (v2.1 P1–P3) audits the SM↔stream binding for streams
SM already reaches; it does NOT extend reach. Question raised at #128
review: can SM monitor remote CLI instances? Answer: not today, no
matter which probe transport is chosen. Out of v2.1 scope. Track here.

**Three implementation sketches (decision belongs to whichever cycle
picks this up):**

- **Option A — JSONL sync.** Remote box rsyncs / Syncthing / SMB mounts
  its `~/.claude/projects/` to a path SM tails locally. Cheapest. Latency
  bounded by sync cadence. Multi-host = N sync paths under one local SM.
- **Option B — Remote bus bridge.** Remote box runs lightweight publisher
  that ships its bus envelopes over SSE / WebSocket to local SM. Re-uses
  ADR-14 SSE shape. Authentication + back-pressure design needed.
- **Option C — Distributed SM agents + roll-up.** Per-host SM instance,
  central dashboard aggregates bus state. Heaviest. Operationally most
  flexible but introduces multi-process governance state coherence
  problems (which agent is authoritative on cross-host patterns?).

**Promotion criterion:** only promote to a v2.x P1 if a concrete
remote-monitoring use case is on deck (e.g. CI runner observation,
multi-developer SM-as-service, remote pair-programming). Speculative
demand alone does not promote.

**Cross-cutting risks (sketch — not load-bearing til promotion):**
- Network surface = new attack surface. ADR review required.
- HMAC seam currently single-process; remote bridge multiplies the
  secret-distribution problem.
- `provenance_assertions` (v2.1 P1) is local SQLite; remote write
  semantics undefined.
- `feedback_no_self_monitor.md` filter widens — SM-watching-SM-on-other-host
  is a new failure mode.

**Refs:**
- Issue #128 (probe transport decision; recorded transport ≠ monitoring transport).
- v2.1 P1 prompt: `docs/prompts/v2.1-orchestration/phase-1-ppp-stream-disambiguation.md`.
- ADR-14 (SSE transport shape — re-usable for Option B).
- `src/stream_manager/jsonl_tail.py`, `src/stream_manager/message_bus.py`.
- Memory `feedback_no_self_monitor.md`.

---

## Carry-forwards from v2.1

(populated at v2.1 P4 ship-gate)

---

## INTENT.md gap-analysis seeds (pre-cycle, 2026-05-16)

Surfaced by `docs/intent-todo-gap-2026-05-16.md` gap-synthesis pass.
Five seeds queued for graduation at v2.2 P0 cycle frame mint. Each
seed below carries a stated promotion criterion; speculative demand
alone does NOT promote. **Operator must explicitly accept the cap-
bump at P0** (backlog grows 1 → 6).

### 🟢 Project-context rank + 400-token budget invariant test

**INTENT ref.** §"Project context loading" — rank order INTENT >
REQUIREMENTS > CLAUDE.md > README; 10 s debounce; 400-token budget.

**Promotion criterion.** Any PR touches `project_context.py` OR
INTENT-priority field rank changes OR alignment dip recurs and root
cause traces to context-loading drift.

### 🟢 Learn-mode ALLOW-promotion regression coverage

**INTENT ref.** §"What governance should learn" — `pip install -e
".[dev]"`, `pytest`, `ruff check`, `mypy` "should promote to ALLOW
patterns quickly".

**Promotion criterion.** Learn Mode telemetry shows promotion count
drop ≥ 30% week-over-week OR a routine command (e.g. `pytest`) is
reported failing to promote after the documented threshold.

### 🟢 Topology-discovery coverage

**INTENT ref.** §"What this project is" — SM "discovers Desktop's
sub-agent topology via hybrid metadata + pattern inference"; unknown-
agent fallback rule.

**Promotion criterion.** New unknown-agent pattern surfaces in
production (i.e. `agent_profiles` row with `role = "unknown"`
persists > 1 cycle) OR a known-agent profile mis-classifies in a
ship-gate run.

### 🟢 Out-of-scope guard scans

**INTENT ref.** §"Out of scope" — transport/ modification, plaintext
session-token storage, SQLite bus DB exfil.

**Promotion criterion.** Security review surfaces a concrete attack-
vector PoC against one of the three OR a related CVE-class issue
filed.

### 🟢 "Governs messages, not transitions" boundary regression

**INTENT ref.** §"Sub-agent governance principles" — "Pipeline ordering
remains with Desktop orchestration. SM governs messages, not
transitions."

**Promotion criterion.** A todo / PR review surfaces a code path
where SM gates one agent on another's completion OR `governance.py`
review surfaces transition-gating helpers.

---

## ADR-18 Rule 5 — backlog hard cap

This file currently holds **1** seed item pre-graduation +
**5** seeds queued pre-cycle (gap-analysis 2026-05-16) = **6**
post-graduation. ADR-18 Rule 5 caps backlog growth per cycle; v2.2
cycle work that opens new items must close or graduate at least one
seed. Re-triage at v2.2 P0 cycle frame; operator decision required
at P0 to accept the pre-cycle cap-bump 1 → 6 OR to compress some
gaps into phase items / reject as out-of-INTENT.
