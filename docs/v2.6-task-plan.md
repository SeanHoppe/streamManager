# v2.6 task plan — feature cycle (Seed v2.5-G step-(1) lever-wire + Seed v2.5-A diagnosis unblock)

**Cycle theme: feature.** Operator-declared at P0 fire 2026-05-20. Soft
LOC target ≤ **1500** vs cycle-tip (v2.6 P0-merge SHA
`084137dfc8823ae5eac84755581fc0aeed6342db`), production bucket binding
per ADR-18 Amendment A 3-bucket measurement. BLOCK at 1.5× = **2250**.
Anchor per **Amendment C** = cycle-tip, NOT predecessor tag (v2.5.1 =
`c1e9070` retained as narrative comparator only).

**Cycle-type framing.** v2.5 + corrective v2.5.1 was consolidation (3 of
last 4 cycles consolidation: v2.2 / v2.4 / v2.5; v2.3 = feature +461
LOC). v2.6 flips back to feature — alternation hygiene argues feature,
and Seed v2.5-G measurement-protocol step (1) is the obvious lever-wire
candidate after staying dormant 1 cycle on consolidation gate. ADR-18
Amendment A §"Default lean rationale" permits the flip; pre-authorized
operator signal at PM-mint preamble cited Seed v2.5-G + Seed v2.5-A
2-for-1 as the lever-wire fit. Final cycle-type call recorded at this
P0 fire.

Headline scope this cycle:

1. **Seed v2.5-G step (1) instrumentation** — per-run wall-clock
   timing in alignment-eval row runner (~30 LOC `tools/` addition).
   Production-bucket LOC under Amendment A 3-bucket measurement;
   qualifies as lever-wire on its own (env-readable split optional
   same phase, NOT required). Fires as v2.6 P1. No FROZEN-surface
   touch; `src/stream_manager/cli_governance.py:49`
   `TIMEOUT_SECONDS = 25.0` stays frozen until step (2) at v2.7+
   per the measurement-protocol stance.

2. **Seed v2.5-A diagnosis unblock** — resolves in lockstep with
   Seed v2.5-G fire. Instrumented row runner re-measures row
   `frog7-wirecli-module-10` at v2.6 P2 ship-gate; verdict =
   content-drift OR timeout-attributable. No standalone fix path.

Path-D synthetic-fixture P5 implementation (Seed v2.5-C, ~600 LOC)
**DEFERS to v2.7** (becomes Seed v2.6-C in v2.6 backlog at P2 mint).
3rd consecutive deferral; lever-budget bound — single-lever fire this
cycle keeps the feature-cycle envelope tight (~30 LOC vs ~630 if both
fired). Phase-5 prompt re-mint per Amendment D gate-split also defers.

**P1/P2 fire-order (recorded at P0).** Per `docs/v2.6-next-steps.md`
fire-order table with feature-cycle dispositions applied:

1. P0 cycle frame (this PR) — operator-decisions recorded + this
   task-plan minted + `docs/v2.6-next-steps.md` decision blocks
   ticked.
2. **v2.6 P1 — Seed v2.5-G step (1) instrumentation.** Prompt:
   `docs/prompts/v2.6-orchestration/phase-1-cli-timeout-
   instrumentation.md` (TBD mint ahead-of-fire per v2.4 PR #182 /
   v2.5.1 PR #188 precedent). Scope: per-run wall-clock timing in
   alignment-eval row runner; ~30 LOC tooling; no FROZEN-surface
   touch.
3. **Seed v2.5-C Path-D P5 — DEFERRED to v2.7** (3rd consecutive
   deferral; lever-budget bound).
4. **v2.6 P2 — ship-gate finalize.** Prompt: `docs/prompts/v2.6-
   orchestration/phase-2-ship-gate-finalize.md` (TBD mint ahead-of-
   fire). S4 alignment-eval branches on n=3 vs n=6 per
   `feedback_alignment_eval_stability_window.md`: prior cycle
   v2.5.1 P2 Sonnet `pass_rate=0.9375` ≥ 0.85 → default `--runs 3`
   applies (subject to unstable-count + CLI-timeout-rate escape
   hatches per skeleton §"Alignment-eval n=6 stability rule").
   Seed v2.4-F latency-regression re-measure fires at this phase
   (verdict: 🔴 promote vs sample-variance dismissal).

**Phase count.** P0 (frame) + **1 work phase** (P1 Seed v2.5-G
instrumentation) + P2 (ship-gate) = **3 numbered phases**, **1 work
phase**. ADR-18 Rule 4 caps work-phase count at ≤ 3 — v2.6 sits well
under at 1.

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3 numbered phases. v2.6 numbered-phase span = P0 →
P2. v10.x slot also blocked on #112 (v10 P5) which remains blocked
at the **implementation side** (Seed v2.5-C deferred again in v2.6).
#131 remains gated.

**Companion docs:**

- `docs/v2.6-next-steps.md` — **the comparison anchor**. P2
  ship-gate walks this file row-by-row.
- `docs/v2.5-backlog.md` — backlog ground-truth at v2.5.1 close
  (verified reconciled at this PR; no drift vs P0 skeleton table).
- `docs/seed-v2.4-g-cli-timeout-audit.md` — J2 evidence audit for
  the Seed v2.5-G measurement-protocol stance (carry from v2.5 P0).
- `docs/v2.5.1-sonnet-floor-investigation.md` — v2.5.1 P1 verdict-A
  investigation; row-10 100% timeout evidence anchors Seed v2.5-A.
- `docs/v10-mvp-status.md` — J1 ledger (no refresh required this
  P0; v2.5.1 reading current).
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendments A–E (no new
  amendment minted this cycle).
- `docs/v2.5-task-plan.md` — predecessor cycle task plan.
- `docs/v2.5-next-steps.md` — predecessor cycle anchor.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — no new FR row this cycle.
- `docs/adr/ADR-5-latency-budget.md` — v2.3 baseline current; v2.6
  P2 may append baseline if latency lever wired (Seed v2.5-G step-
  (1) is tooling-only; no latency surface change — operator decides
  at P2 fire).
- `docs/adr/ADR-17-soak-tiers.md` — Tier 3 ship-gate at P2.

## Operator decisions recorded at P0 fire (2026-05-20)

Per `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` skeleton
operator-fill blocks:

1. **Cycle type: FEATURE.** Default-lean accepted per PM-mint preamble
   pre-authorization. Pre-auth signal: Seed v2.5-G + Seed v2.5-A
   2-for-1 lever-wire candidate; consolidation-streak 3 of last 4
   cycles argues alternation hygiene. Soft LOC ≤ 1500 vs cycle-tip
   per Amendment A; BLOCK at 1.5× = 2250.

2. **`WIRED_LEVER_LEDGER_COUNT` posture: WIRE +1 at v2.6 P1.** Seed
   v2.5-G step (1) per-run wall-clock instrumentation in `tools/`
   qualifies as lever-wire under Amendment A 3-bucket production
   measurement. Target end posture: **2 production / 0 soak** (v2.3
   Seed 6 JsonlTailWorker wire unchanged + Seed v2.5-G step (1)
   wire NEW). Soak-scope ledger remains internal artefact per Seed
   v2.4-H production-scope-canonical binding.

3. **Rule 5 backlog cap reading: ACCEPT 5-CAP** (Amendment E §"Self-
   application"). Cap-counted = 5 (Seed v2.5-A + Seeds v2.5-C +
   v2.4-E + v2.4-F + v2.5-G). Exempt = 6 (Seeds v2.4-I..N,
   promotion-criterion-bound + demand-bound). Total open = 11 seeds
   entering v2.6 P0. No Rule 5 clarification amendment minted this
   cycle. Reading verified against `docs/v2.5-backlog.md`
   §"ADR-18 Rule 5 — backlog hard cap"; no drift.

4. **Seed v2.5-G fire decision: FIRE step (1) this cycle.** Default-
   lean accepted. Per J2 audit `docs/seed-v2.4-g-cli-timeout-audit.md`
   §"Recommendation":
   - Step (1) — instrument per-run wall-clock in alignment-eval row
     runner (~30 LOC tooling); env-readable
     `BRIDGE_CLI_TIMEOUT` + `BRIDGE_CLI_TIMEOUT_EVAL` split optional
     same phase (operator decides at v2.6 P1 mint).
   - Step (2) timeout-tighten DEFERS to v2.7+ pending measured eval
     p99 from step (1).
   - Step (3) env-split MAY land same phase as step (1) OR defer to
     v2.7+; operator decides at P1 mint.
   - `src/stream_manager/cli_governance.py:49`
     `TIMEOUT_SECONDS = 25.0` STAYS FROZEN through v2.6 per ADR-18
     surface freeze.
   - Resolves Seed v2.5-A row-10 `frog7-wirecli-module-10` 100%
     timeout opacity in lockstep at v2.6 P2 ship-gate re-measure
     with instrumented runner.

5. **Seed v2.5-C deferral: DEFER another cycle.** 3rd consecutive
   deferral. Lever-budget bound — single-lever fire this cycle
   keeps the feature-cycle LOC envelope tight (~30 LOC vs ~630 if
   bundled with Seed v2.5-C ~600 LOC Path-D P5). Pre-auth signal
   cited Seed v2.5-G + Seed v2.5-A 2-for-1 as the lever-wire fit,
   NOT both work phases. Seed v2.5-C becomes **Seed v2.6-C** in
   `docs/v2.6-backlog.md` (minted at v2.6 P2 ship-gate). Phase-5
   prompt re-mint per Amendment D gate-split also defers to v2.7
   P0 alongside the implementation. Re-evaluate at v2.7 P0 with
   v10.3 stochastic-propensities status in hand.

6. **Seed v2.5-A carry-forward: WATCH 🟡 resolves in lockstep with
   Seed v2.5-G fire.** v2.5.1 P1 n=6 evidence: row `frog7-wirecli-
   module-10` 6/6 `cli governance timeout (>25.0s); degrading`
   (100% rate). The row's "stable NONE" Sonnet majority is 100%
   timeout-degradation artefact, NOT content drift. Close-out path:
   instrumented row runner re-measures row 10 at v2.6 P2 ship-gate;
   verdict = content-drift OR timeout-attributable. No standalone
   fix path; no P0-time action beyond the carry-forward logging.

7. **Amendment C cycle-tip anchor verbatim citation.** v2.6 P2
   ship-gate prompt cites
   `084137dfc8823ae5eac84755581fc0aeed6342db..HEAD` for the
   feature-cycle net-LOC gate measurement. Diff command:

   ```
   git diff 084137dfc8823ae5eac84755581fc0aeed6342db..HEAD --stat -- src tests tools dashboard
   ```

   Predecessor-tag narrative diff `c1e9070..HEAD` (same bucket) is
   reported alongside for cycle-impact context but does NOT gate.

8. **Memory pre-flight (Amendment B).** Performed 2026-05-20 against
   current `main` at `2f051fd` (PR #192 squash-merge — PM-mint
   review fold). Stamp below.

## Memory pre-flight stamp (Amendment B)

Pre-flight performed 2026-05-20 against current `main` at `2f051fd`
(PR #192 squash-merge). 6 load-bearing memories verified per v2.6 P0
cycle-frame skeleton minimum re-read list:

| Memory | Status | Action |
|--------|--------|--------|
| `feedback_certportal_dev_firewall.md` | FRESH (verified `.claude/settings.local.json` deny patterns + `CLAUDE.md` firewall language unchanged) | None |
| `feedback_no_self_monitor.md` | FRESH (polarity-flip rule + `BRIDGE_SM_PROJECT_SLUGS` filter pattern unchanged in CLAUDE.md) | None |
| `project_v25_cycle_close.md` | FRESH (minted at v2.5.1 P2 ship-gate 2026-05-20; includes Seed v2.4-Q CLOSED RECOVERED + NEW Seed v2.5-A facts) | None |
| `feedback_alignment_eval_stability_window.md` | FRESH (minted at v2.5.1 P1; n=6 mandate rule recorded; verified body matches `--runs 6` branching in P0 skeleton §"Alignment-eval n=6 stability rule") | None |
| `feedback_soak_cli_pool_flag.md` | FRESH (`--cli-pool-size 2` flag still required; cited in P0 skeleton §"Canonical S2 env block" invocation pattern unchanged) | None |
| `feedback_glob_narrowing_no_op.md` | FRESH (PR #184 cassette; `git clean -df` pattern + tracked-status filter recorded; pattern unchanged) | None |

Memory-system 7-day age warning applies to
`feedback_certportal_dev_firewall.md` + `feedback_no_self_monitor.md`
per running memory hygiene — text body re-verified against current
repo state; firewall + polarity-flip patterns unchanged; no update
required.

## Cycle-discipline guardrails

- **LOC budget (Amendment A + Amendment C).** Anchor = cycle-tip
  (v2.6 P0-merge SHA `084137dfc8823ae5eac84755581fc0aeed6342db`).
  Feature gate: **soft ≤ 1500 / BLOCK at 1.5× = 2250** across
  production bucket (`src/` + `tests/` + `tools/` + `dashboard/`).
  Docs bucket advisory per Amendment A 3-bucket measurement.

- **Production-bucket commitment.** v2.6 P0 (this PR) carries zero
  production-bucket delta (docs-only frame). P1 lever-wire commitment:
  Seed v2.5-G step (1) ~30 LOC tooling addition in `tools/`
  (production bucket). P2 ship-gate adds zero production-bucket
  delta beyond the P1 work-phase scope.

- **Rule 5 backlog cap.** Effective cap reading = **5** (cap-counted:
  Seeds v2.5-A + v2.5-C + v2.4-E + v2.4-F + v2.5-G). 6 exempt carry-
  forwards (v2.4-I..N) per Amendment E §"Self-application". v2.6
  cycle work that opens any NEW backlog item beyond these 5 must
  close or graduate ≥ 1 existing seed before merging — OR add the
  new seed as cap-counted (no further amendment required; 2-cycle
  drift clock starts at first carry-forward).

- **Rule 6 memory pre-flight.** Performed at P0 (above stamp). All
  6 load-bearing memories FRESH; no updates required in this PR.

- **Lever ledger.** Entering posture **1 production / 0 soak** (v2.3
  Seed 6 JsonlTailWorker wire). Target end posture **2 production /
  0 soak** at v2.6 P2 close (Seed v2.5-G step (1) wire NEW; v2.3
  Seed 6 wire unchanged). Seed v2.4-H scope binding remains in force
  (production scope canonical).

- **Surface freeze.** ADR-18 Rule 1 in force.
  `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS = 25.0`
  STAYS FROZEN through v2.6 (Seed v2.5-G step (2) timeout-tighten
  defers to v2.7+ pending measured eval p99 input from step (1)).
  All other FROZEN surface untouched.

## PHASE P0 — this PR (docs frame)

Files changed (repo-tracked):

- `docs/v2.6-task-plan.md` (NEW; this file).
- `docs/v2.6-next-steps.md` (UPDATED — P0 frame decision blocks
  ticked per operator picks above; was minted at PR #191 PM-mint).
- `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` (UPDATED
  — operator-decision blocks ticked + operator-pick rationale lines
  filled; was skeleton-minted at PR #191 + review-folded at PR #192).

Files NOT changed (per ADR-18 surface freeze + cycle-discipline):

- `src/stream_manager/cli_governance.py` — `TIMEOUT_SECONDS = 25.0`
  FROZEN; Seed v2.5-G step (2) timeout-tighten defers v2.7+.
- All FROZEN surface — untouched per ADR-18 Rule 1.
- `tools/`, `tests/` production-bucket paths — zero delta this PR.
  (Seed v2.5-G step (1) instrumentation lands at v2.6 P1, NOT this
  P0 PR.)

Repo-tracked LOC delta: docs only.

ADR-18 Amendment B self-applies: this PR carries the memory pre-
flight stamp (above).

ADR-18 Amendment C self-applies: cycle-tip anchor =
`084137dfc8823ae5eac84755581fc0aeed6342db` (v2.6 P0-merge SHA,
backfilled post-merge).

ADR-18 Amendment E self-applies: cap-counted = 5 reading recorded
above; verbatim external-trigger citations live in
`docs/v2.6-next-steps.md` (PR #191 PM-mint pre-satisfied).

## PHASE P1 — Seed v2.5-G step (1) instrumentation

Prompt: `docs/prompts/v2.6-orchestration/phase-1-cli-timeout-
instrumentation.md` (TBD mint ahead-of-fire per v2.4 PR #182 /
v2.5.1 PR #188 precedent).

Scope:

- Per-run wall-clock timing instrumentation in alignment-eval row
  runner (`tools/alignment_eval.py` or equivalent path; identified
  at mint time).
- ~30 LOC tooling addition (production bucket under Amendment A
  3-bucket measurement); qualifies as lever-wire on its own.
- Env-readable split (step 3): `BRIDGE_CLI_TIMEOUT` (prod) +
  `BRIDGE_CLI_TIMEOUT_EVAL` (eval override) — operator decides at
  P1 mint whether to bundle same phase OR defer to v2.7+.
- NO FROZEN-surface touch; `src/stream_manager/cli_governance.py:49`
  `TIMEOUT_SECONDS = 25.0` STAYS FROZEN.
- Step (2) timeout-tighten DEFERS to v2.7+ (requires measured eval
  p99 from step (1) as input).

DoD at P1 (preview):

- Per-run wall-clock emitted in alignment-eval JSON report (key
  TBD at mint).
- Row-10 `frog7-wirecli-module-10` re-measure prepared as v2.6 P2
  ship-gate close-out evidence for Seed v2.5-A diagnosis verdict.
- Lever ledger updated 1 → 2 (production).

### P1 ledger (on merge)

LANDED PR #___, +70 LOC tooling (`tools/alignment_eval.py` net,
+77/−7) / +84 LOC tests (`tests/test_alignment_eval_timing.py`
new) / docs delta minimal (this ledger row only). Production-bucket
net add **147 LOC** (≤ strict 150 LOC cap, ≤ feature soft 1500 LOC
cycle target). Step (3) env-split DEFERRED v2.7+ per
`docs/prompts/v2.6-orchestration/phase-1-cli-timeout-
instrumentation.md` §"Optional step (3) env-split" mint-default
disposition. ZERO touch on
`src/stream_manager/cli_governance.py` (FROZEN-surface untouched
per ADR-18 Rule 1). WIRED_LEVER_LEDGER posture **2 production / 0
soak** (v2.3 Seed 6 JsonlTailWorker wire unchanged + Seed v2.5-G
step (1) wire NEW).

## PHASE P2 — ship-gate finalize

Prompt: `docs/prompts/v2.6-orchestration/phase-2-ship-gate-
finalize.md` (TBD mint ahead-of-fire per v2.4 PR #182 / v2.5.1
PR #188 precedent).

P2 procedure mirrors v2.5.1 P2 pattern:

- S1 wipe + S1.1 assertion.
- S2 Tier-3 soak (Amendment C cycle-tip anchor binding) — canonical
  env block per P0 skeleton §"Canonical S2 env block" exports
  `BRIDGE_RL_LOGGER_ENABLED=1` + `BRIDGE_LOC_PATHSPEC=src/,tests/,
  tools/,dashboard/`. `--cli-pool-size 2` mandatory.
- S3 invariant + dual-anchor.
- S4 alignment-eval — `--runs 3` default (prior cycle v2.5.1 P2
  Sonnet `pass_rate=0.9375` ≥ 0.85); escape-hatches per skeleton
  §"Alignment-eval n=6 stability rule" trigger n=6 if unstable-
  count or row-level CLI-timeout-rate exceed thresholds.
- S5 LOC delta verify (Amendment C cycle-tip; dual-anchor with
  predecessor-tag narrative `c1e9070..HEAD`).
- S6 lever-ledger bump 1 → 2 (production; Seed v2.5-G step (1)).
- S6.5 Seed v2.5-A diagnosis verdict — instrumented row-10 re-
  measure: content-drift OR timeout-attributable.
- S7 ADR-5 baseline append — operator decides at P2 fire (Seed
  v2.5-G step (1) is tooling-only; no latency-surface change argues
  SKIP; operator may override if signal moves).
- S8 CHANGELOG append.
- S9 tag v2.6.0.
- S10 compare-back — walk `docs/v2.6-next-steps.md` row-by-row.
- S11 close memory — mint `project_v26_cycle_close.md`.
- S12 lifetime — default; v2.6 prompts persist as historical record.
- S13 mint v2.7 P0 frame ahead-of-fire (PM-mint precedent).
- Seed v2.4-F latency-regression re-measure verdict (🔴 promote vs
  sample-variance dismissal) decided this phase.
- Seed v2.4-E overall-p95 re-measure (downgrade candidate vs
  carry).
- v10 P4 corpus piggyback (Run 7+) if `BRIDGE_RL_LOGGER_ENABLED=1`.
- `docs/v2.6-backlog.md` mint at P2 (carry-forwards to v2.7).

## DoD (this PR)

- [x] `docs/v2.6-task-plan.md` minted with all 8 operator decisions
      recorded.
- [x] `docs/v2.6-next-steps.md` decision blocks ticked per operator
      picks (was pre-minted at PR #191 PM-mint as the comparison
      anchor).
- [x] `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md`
      decision-block ticks + operator-pick rationale lines filled.
- [x] `docs/v2.5-backlog.md` reconciled (no drift vs §"v2.5 carry-
      forward dispositions" table in P0 skeleton).
- [x] Memory pre-flight stamp included (above).
- [x] Stale memories: NONE (all 6 FRESH).
- [x] §"Cycle-tip anchor" template recorded; SHA populated post-
      merge in follow-up commit per v2.5 PR #186 precedent.
- [x] §"v2.5 carry-forward dispositions" table verified at P0 fire
      against `docs/v2.5-backlog.md` ground truth (no drift).
- [x] Cycle type = feature; explicit lever-wire commitment recorded
      (Seed v2.5-G step (1) at v2.6 P1).
- [x] Seed v2.5-A + Seed v2.5-G coupling logged in
      `docs/v2.6-next-steps.md` §"Carry-forwards" (resolves in
      lockstep at v2.6 P2 ship-gate).
- [ ] Single PR `chore(v2.6-p0):` against `main` (trivially true at
      merge).

## Refs

- `docs/v2.6-next-steps.md` (the comparison anchor; pre-minted at
  PR #191 PM-mint, updated this PR).
- `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` (skeleton
  pre-minted at PR #191 + review-folded at PR #192; decision blocks
  filled this PR).
- `docs/seed-v2.4-g-cli-timeout-audit.md` (J2 evidence audit; Seed
  v2.5-G measurement-protocol stance, renamed from v2.4-G).
- `docs/v2.5.1-sonnet-floor-investigation.md` (v2.5.1 P1 verdict-A;
  Seed v2.5-A row-10 100% timeout evidence).
- `docs/v2.5-backlog.md` (backlog ground truth at v2.5.1 P2 close;
  reconciled this PR — no drift).
- `docs/adr/ADR-18-mvp-surface-freeze.md` Amendments A / B / C / D
  / E.
- `docs/v2.5-task-plan.md` (predecessor cycle task plan).
- `docs/v2.5-next-steps.md` (predecessor cycle anchor).
- `project_v25_cycle_close.md` (v2.5.1 cycle close memory).
- `feedback_alignment_eval_stability_window.md` (n=6 mandate memory).
- `feedback_certportal_dev_firewall.md` (dev firewall).
- `feedback_no_self_monitor.md` (polarity-flip rule).
- `feedback_soak_cli_pool_flag.md` (`--cli-pool-size 2` required).
- `feedback_glob_narrowing_no_op.md` (PR #184 cassette).
- Precedent fire PR: PR #185 (v2.5 P0 fire — default-lean
  consolidation; v2.6 fires default-lean feature).
- Precedent ahead-of-fire prompt mints: PR #182 (v2.4 P2 ship-gate),
  PR #188 (v2.5.1 P1 corrective).
- PR #191 (v2.6 PM-mint — P0 skeleton + next-steps minted).
- PR #192 (v2.6 PM-mint review fold — 5 caveman-review findings).
