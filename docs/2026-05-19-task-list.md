# 2026-05-19 task list

> **Source:** [`docs/2026-05-19-status.md`](2026-05-19-status.md) §"What's next (indexed)" + §"Operator-bound items" + §"Agent-dispatchable items (post-v2.5 P0)".
>
> **Pipeline:** Each row below is either **AGENT-DISPATCHABLE** (a subagent can land it now without operator gating) or **OPERATOR-BOUND** (documented for awareness; not spawned). Agent-dispatchable rows are spawned in this session and tracked to landing in [`docs/2026-05-19-implementation-notes.md`](2026-05-19-implementation-notes.md).
>
> **Lifetime:** Discardable at v2.5 P0 cycle frame mint.

---

## Scoping rationale

The 2026-05-19 status doc lists 6 "What's next" items + 5 operator-bound items + 2 agent-dispatchable items. The agent-dispatchable items reduce to **three concrete prompt mints / doc authors** that prep the v2.5 P0 cycle-frame fire without requiring the operator's cycle-type decision:

1. **Refresh `docs/v10-mvp-status.md`** — issue #111 closed at v2.4 P0, Amendment D split into v10.1 / v10.3 modes, corpus 240 → 360 piggyback; doc-only ledger refresh, no operator gating.
2. **Author Seed v2.4-G CLI governance timeout audit doc** — evidence prep for the operator's "promote v2.4-G to 🔴?" decision at v2.5 P0; reads `src/stream_manager/cli_governance.py:49` against Sonnet endpoint p99 latency observed in v2.4 P2 soak/alignment runs; does NOT change `TIMEOUT_SECONDS` (FROZEN per ADR-18 surface-freeze).
3. **Mint v2.5 P0 cycle-frame skeleton prompt** — `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md` with cycle-type decision placeholders, Rule 5 cap-counted reading (5 cap-counted + 6 EXEMPT post-PR #184), Seed v2.4-G promotion question, Seed v2.4-C deferral/fire decision. Skeleton fires when operator pulls the trigger.

Everything else (Path-D synthetic-fixture P5 implementation, v2.5 P2 ship-gate fire, Seed v2.4-Q row-level disposition, p95 / L4 / LM closure votes) is gated on the operator's v2.5 P0 mint or on a future ship-gate run and therefore not dispatchable in this session.

---

## Agent-dispatchable jobs (spawned this session)

| Job | Output | Bind | Spawn status | Land status | Result PR / commit | Notes ref |
| --- | ------ | ---- | ------------ | ----------- | ------------------ | --------- |
| **J1** | Refresh `docs/v10-mvp-status.md` (#111 closed at v2.4 P0; Amendment D v10.1/v10.3 gate split; corpus 240 → 360; head-of-chain now Seed v2.4-C Path-D) | Docs-only | DISPATCHED | ✅ LANDED | working-tree edit (+53/−22 net +31) | [§J1](#j1-refresh-docsv10-mvp-statusmd) |
| **J2** | New `docs/seed-v2.4-g-cli-timeout-audit.md` (audit of `cli_governance.py:49` `TIMEOUT_SECONDS = 25.0` against Sonnet endpoint p99 latency from v2.4 P2 soak + alignment runs; recommendation for v2.5 P0 promotion-to-🔴 decision) | Docs-only | DISPATCHED | ✅ LANDED | working-tree new file (289 lines) | [§J2](#j2-author-seed-v24-g-cli-timeout-audit) |
| **J3** | New `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md` (skeleton mirroring `docs/prompts/v2.4-orchestration/phase-0-cycle-frame.md` shape; cycle-type decision placeholders; cap-counted reading; Seed v2.4-G promotion question; Seed v2.4-C deferral/fire decision; Amendment D + E cross-refs) | Docs-only | DISPATCHED (1st crash → retry) | ✅ LANDED on retry | working-tree new file (315 lines) + new dir | [§J3](#j3-mint-v25-p0-cycle-frame-skeleton-prompt) |

---

## Operator-bound items (documented, not spawned)

These cannot be dispatched by an agent dev session. Listed here so the v2.5 P0 fire can pick them up.

| # | Title | Bind | Source |
| - | ----- | ---- | ------ |
| O1 | **Fire v2.5 P0 cycle frame** — cycle-type decision (feature vs consolidation) | Operator only | status doc §"Operator-bound items" |
| O2 | **Seed v2.4-G promotion to 🔴 at v2.5 P0** | Operator at P0 fire (J2 provides evidence) | status doc §"What's next" #4 |
| O3 | **Seed v2.4-Q row-level disposition** — if 5th-cycle Sonnet 0.80–0.85 band holds with same row-set composition at v2.5 P2 | Operator at v2.5 P2 ship-gate | status doc §"What's next" #2 |
| O4 | **PR #154 sign-off** — title still prefixed `DRAFT`; either flip + merge, re-mark `isDraft=true`, or close | Operator only | status doc §3.1 |
| O5 | **v2.3 / v2.4 phase fires** (post-mint) — Path-D synthetic-fixture P5 implementation, v10.x cycle frame fire (#131), L4 fallback wire (#124), Ridge-Q DR restore (#125) | Operator gates each | status doc §"Cross-cutting" |

---

## Per-job details

### J1 — refresh `docs/v10-mvp-status.md`

**Why now.** The status doc's Bucket 2 records two v10-chain closures since the open-tasks-2026-05-16 anchor was written: issue #111 closed at v2.4 P0 (PR #179, `b35e982`) and corpus advanced 240 → 360 (v2.4 P2 piggyback Run 4). Amendment D (v10.1-mode vs v10.3-mode gate split) reshapes the gate logic that the ledger was anchored against. Without a refresh, the ledger references stale gate conditions and reports unreached phases that have in fact been re-scoped.

**Out-of-scope (do not touch).** Anything inside `src/stream_manager/`, `tools/`, `dashboard/`. Pure docs-only ledger refresh. ADR-18 surface-freeze still applies — no code edits.

**Acceptance.** Diff shows only `docs/v10-mvp-status.md` modified. v10 P4 (#111) marked closed with PR + SHA citation. v10 P5 entry reflects Amendment D gate split. Corpus row reads "360 episodes (160-row margin over 200-gate)". Reference chain re-anchored to point at Seed v2.4-C Path-D synthetic-fixture as head-of-chain.

---

### J2 — author Seed v2.4-G CLI timeout audit

**Why now.** Status doc §"What's next" #4 recommends Seed v2.4-G promotion to 🔴 at v2.5 P0 per Seed v2.4-D investigation. The audit doc is the evidence package the operator reads before making that promotion call. No code change; `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS = 25.0` is FROZEN under ADR-18 surface-freeze and stays frozen until v2.5 P0 disposition.

**Inputs.**
- `src/stream_manager/cli_governance.py:49` — current `TIMEOUT_SECONDS` value + surrounding context.
- `reports/soak-20260519T085540Z.md` — v2.4 P2 Tier-3 soak; extract any CLI timeout / panic / degrade observations.
- `reports/alignment-eval-20260519T101249Z.{md,json}` — v2.4 P2 alignment-eval; per Seed v2.4-D §"v2.5 follow-ups": row-08 `frog7-lifecycle-bridge-08` 3× NONE empirical hook; 2 fresh CLI timeouts observed during rows 14 / 15 / 17 / 18.
- `reports/sonnet-dip-v23-v24.md` — Seed v2.4-D investigation; root-causes the v2.3 dip as denominator inflation + 1 CLI-degrade row + pre-v2.3 row-16 drift.
- Past soak summaries if needed for cross-cycle p99 trend.

**Out-of-scope (do not touch).** `src/stream_manager/cli_governance.py` — FROZEN. `tools/`, `dashboard/`, any test file. Audit is read-only.

**Acceptance.** New file `docs/seed-v2.4-g-cli-timeout-audit.md`. Sections: §Current state (cite line + value), §Evidence (the row-08 hook + the row-14/15/17/18 timeouts + soak observations), §Sonnet endpoint p99 estimate (from soak runs; report n and confidence), §Recommendation (FREEZE / promote-to-🔴 / no-action; if promote, the proposed `TIMEOUT_SECONDS` value), §v2.5 P0 question (verbatim block the operator can paste into the cycle-frame decision log). 150–250 lines.

---

### J3 — mint v2.5 P0 cycle-frame skeleton prompt

**Why now.** Precedent: PR #182 minted `phase-2-ship-gate-finalize.md` ahead of fire; PR #181 minted the Sonnet-DIP investigation prompt ahead of v2.4 P1. Minting the v2.5 P0 cycle-frame skeleton ahead of fire is the same pattern — the operator pulls the trigger when ready; the skeleton just bounds the decision surface and pre-loads the cap-counted reading.

**Anchor.** `docs/prompts/v2.4-orchestration/phase-0-cycle-frame.md` — match shape, section headings, decision-block format. Substitute v2.4 → v2.5 / v2.5 throughout. Preserve §"Operator decisions" + §"Pre-flight" + §"Cycle-type call" + §"Rule 5 cap-counted reading" + §"Phase prompt stubs" sections.

**Required content.**
- §"Cycle-type call" — feature vs consolidation; note that a Seed v2.4-C Path-D fire (~600 LOC) requires feature classification per Amendment A.
- §"Rule 5 cap-counted reading" — 5 cap-counted (Seed v2.4-Q + Seeds v2.4-C / E / F / G) + 6 EXEMPT (Seeds v2.4-I..N). Cite Amendment E.
- §"Seed v2.4-G promotion question" — operator decision-block; references J2 audit doc as evidence input.
- §"Seed v2.4-C deferral/fire decision" — operator decision-block; fire iff cycle type = feature.
- §"Seed v2.4-Q carry-forward" — re-state FREEZE-on-content; watch fires at v2.5 P2.
- §"v2.4 carry-forward dispositions" — Seed v2.4-E / F watch continues; Seed v2.4-G promotion per the audit doc verdict.
- §"ADR-18 reference block" — Amendments A / B / C / D / E with one-line summaries.
- §"Phase prompt stubs" — list of v2.5 phase prompts that will need minting (P1 if any; P2 ship-gate finalize follows v2.4 pattern).
- §"Memory pre-flight" — invokes Rule 6 (Amendment B) re-read of memory before P0 fire.

**Out-of-scope.** Do not invent v2.5 phase scope. The cycle-type decision belongs to the operator; the prompt's job is to bound the decision surface, not pre-empt it.

**Acceptance.** New file `docs/prompts/v2.5-orchestration/phase-0-cycle-frame.md`. Diff scoped to that one path + (if needed) one new index entry. Matches v2.4 P0 prompt shape. Operator can pull trigger as-is.

---

## Landing flow

```
[J1, J2, J3] spawned in parallel
    │
    ▼
each agent writes its artifact directly (docs-only, no PR per job)
    │
    ▼
this thread verifies landing (Read each artifact; confirm size / sections / refs)
    │
    ▼
this thread updates task-list rows above (spawn status → land status → "✅")
    │
    ▼
this thread closes implementation-notes with deviations / open questions / follow-ups
    │
    ▼
operator inherits: 3 docs ready for review + 5 operator-bound items queued for v2.5 P0 fire
```

---

## Refs

- [`docs/2026-05-19-status.md`](2026-05-19-status.md) — source status snapshot.
- [`docs/2026-05-19-implementation-notes.md`](2026-05-19-implementation-notes.md) — running notes on design / deviations / tradeoffs.
- [`docs/v2.4-backlog.md`](v2.4-backlog.md) — Seed v2.4-{C,E,F,G,I..N,O,P,Q} catalogue.
- [`docs/prompts/v2.4-orchestration/phase-0-cycle-frame.md`](prompts/v2.4-orchestration/phase-0-cycle-frame.md) — J3 anchor.
- [`docs/adr/ADR-18-mvp-surface-freeze.md`](adr/ADR-18-mvp-surface-freeze.md) — Amendments A–E.
