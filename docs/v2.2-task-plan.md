# v2.2 task plan — consolidation cycle (gap-4 invariant + ship-gate)

**Cycle theme: consolidation.** Net-LOC delta target: **≤ 0** per
ADR-18 Rule 3 (consolidation arm; unchanged by v2.2 P0 Amendment A
which only adds a feature-cycle soft target). Two work phases +
P0 docs frame = ADR-18 Rule 4 phase budget with 2 phases of slack.

**Cycle-type framing.** v2.1 was the feature cycle (+3874 LOC, PPP
audit harness end-to-end). v2.0 was the deletion-positive
consolidation cycle (−1031 LOC, Haiku + verdict-fallback rips).
v2.2 returns to consolidation per ADR-18 cycle-alternation hygiene.
No new levers wired. `WIRED_LEVER_LEDGER_COUNT` stays at 0;
DORMANT-N gate stays inert.

**P1 lever pivot (recorded at P0).** Operator declined gap-1
(cadence FR) + gap-2 (sub-agent scope-escalation FR) — both are
feature-gated (gap-1 promotion criterion = "cycle type = feature";
gap-2 additionally requires ADR-18 Rule 1 carve-out for FROZEN
`governance.py`). Both deferred to v2.3. Gap-3 (dashboard
regression watch) also deferred — additive tests would overshoot
consolidation LOC ≤ 0 budget without offsetting deletion. Gap-4
(API-timeout invariant test + Tier-3 ledger column) folds as P1
with a mandatory pre-P1 deletion offset survey to keep cycle
LOC ≤ 0.

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3. v2.2 = 2 work phases (P1 + ship-gate) keeps the
v10.x slot free for #112-close + #131 mint as soon as corpus-fill
completes.

**Companion docs:**

- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendment A
  (feature-cycle LOC soft target) + Amendment B (memory pre-
  flight Rule 6) land in this P0 PR.
- `docs/v2.2-backlog.md` — 1 → 6 seeds at P0 mint (operator
  accepted cap-bump per phase-0 §"Operator decisions"). Gaps
  5–9 graduate to backlog with promotion criteria stated.
- `docs/v2.1-backlog.md` §"Carry-forwards from v2.1" — 4 cycle-
  handoff items inherited at v2.2 P0; disposition recorded below.
- `docs/v2.0-task-plan.md` — consolidation-cycle format template.
- `docs/intent-todo-gap-2026-05-16.md` — gap synthesis pass;
  dispositions recorded at this P0 mint.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — gap-4 adds NO new FR row (the invariant is
  already INTENT §"Safety priorities" #5; gap-4 adds a regression
  test for the existing safety property).
- `docs/adr/ADR-5-latency-budget.md` — gap-4 P1 reads the live
  `BRIDGE_FALLBACK_LATENCY_BUDGET_MS` constant; ADR-5 baseline
  ~11 s p95 per `project_v17_cycle_close.md`.
- `docs/adr/ADR-17-soak-tiers.md` (+ Tier 1.5 amendment from v2.0
  P2) — Tier 3 ship-gate at P2.
- `src/stream_manager/cli_governance.py` — gap-4 P1 fault-injection
  site (subprocess shim).
- `src/stream_manager/governance.py` — FROZEN; gap-4 reads the
  live latency-budget constant for the regression test, does NOT
  modify the seam.
- `src/stream_manager/model_router.py` — FROZEN; same as above.
- `tools/soak_driver.py` — gap-4 adds one observability line
  (`[soak] invariant-degrade canary: PASS/FAIL`) to the closing
  block.
- Memory: `project_v21_cycle_close.md` (template), `project_v17_
  cycle_close.md` (ADR-5 latency baseline anchor),
  `feedback_cassette_must_cover_new_envelopes.md`,
  `feedback_subagent_long_task_abandonment.md`,
  `feedback_monitoring_live_sessions.md`,
  `project_v10_p4_hold_lifted.md`.

**Format:** mirrors `docs/v2.0-task-plan.md` (consolidation-cycle
predecessor). Each phase block is self-contained, hand verbatim to
a fresh Claude Code session via the matching prompt under
`docs/prompts/v2.2-orchestration/`.

**Prior baseline:** v2.1.0 ship at `8303f38`. Soak source of truth:
`reports/soak-20260511T173516Z.md` (v2.1 ship-gate, 31.8 min,
`--cli-pool-size 2`, `--ppp-auto-probe` default-on). Lever ledger
empty (DORMANT-N gate inert).

---

## Pre-flight stamp (Rule 6 self-application)

Memories verified against current repo state 2026-05-16:

| Memory | Stamp | Notes |
|---|---|---|
| `project_v21_cycle_close.md` | fresh | 4 days old; v2.1.0 facts (tag `8303f38`, +3874 LOC, alignment dip row IDs, JsonlTailWorker dormancy) match `docs/v2.1-backlog.md` + git log. |
| `project_v10_p4_hold_lifted.md` | fresh | 5 days old; Q4 lift fact + corpus-fill blocker match `todo.md` §v10 + `docs/jobs/MASTER.md` #111 row (`READY corpus-gated`). |
| `project_v10_rl_track.md` | not re-read this PR | indexed; v10 track scope unchanged by v2.x main cycle. |
| `feedback_cassette_must_cover_new_envelopes.md` | fresh | "Convention → enforced rule" matches PR #159 (cassette CI guard LANDED 2026-05-16 per `todo.md` L65-71). |
| `MEMORY.md` index | fresh | All cited entries present. |

INTENT.md pre-flight (Rule 6 explicit extension):

| Section | Stamp | Notes |
|---|---|---|
| §"Current cycle posture / v2.x main cycle" | updated-in-this-PR | "v2.2 cycle frame NOT yet minted" → minted (this PR). |
| §"ADR-18 governance regime" | updated-in-this-PR | Rule 3 line gains Amendment A pointer; new Rule 6 line. |
| §"Authoritative status references / MASTER.md" | updated-in-this-PR | Gap-12 single-line strike (parenthetical removed; MASTER.md row for #111 reconciled at PR #158). |
| §"Authoritative status references / v2.1-backlog carry-forwards" | unchanged | Pointer stays accurate; the bullet IS the carry-forward source-of-truth. |

---

## Cycle-discipline guardrails (in force)

- **ADR-18 Rule 1 — surface freeze.** FROZEN list inherits
  unchanged. NO FROZEN edits this cycle (gap-4 is read-only against
  `governance.py` / `model_router.py` constants).
- **ADR-18 Rule 2 — DORMANT-N falsify-before-extend.** Inert
  (`WIRED_LEVER_LEDGER_COUNT` = 0).
- **ADR-18 Rule 3 — net LOC ≤ 0.** Consolidation arm. Gap-4 adds
  ~80 LOC (~70 tests + ~10 driver); P1 MUST find ≥ 80 LOC deletion
  offset in `src/` + `tests/` + `tools/` + `dashboard/` to satisfy
  the cap. Survey runs pre-P1.
- **ADR-18 Rule 4 — phase budget.** 2 work phases (P1, P2-ship-
  gate) + P0 frame = within 3-phase cap.
- **ADR-18 Rule 5 — backlog cap.** Operator accepted 1 → 6 seed
  growth at P0 (gaps 5–9 graduate). v2.2 cycle work that opens
  any NEW seed beyond gap synthesis must close or graduate ≥ 1
  existing seed before merging.
- **ADR-18 Rule 6 (NEW this PR) — memory pre-flight.** This PR
  self-applies; stamp block above.

---

## PHASE P0 — cycle frame (THIS PR)

**Status: open.**

**Deliverables:**

1. ADR-18 §"Amendments" entries for v2.2 P0 Amendment A (LOC soft
   target, Rule 3 extension) + v2.2 P0 Amendment B (Rule 6 memory
   pre-flight). Closes #130 + #133.
2. `docs/v2.2-task-plan.md` (this file).
3. `docs/prompts/v2.2-orchestration/phase-1-gap-4-api-timeout-
   invariant.md` — P1 phase prompt stub.
4. `docs/prompts/v2.2-orchestration/phase-2-ship-gate-finalize.md`
   — ship-gate phase prompt stub.
5. Memory `project_v22_cycle_frame.md` minted; indexed in
   `MEMORY.md`.
6. INTENT.md edits: gap-10 Option B (LOC ceiling → pointer),
   gap-12 strike (MASTER.md parenthetical), §"Current cycle
   posture" updated.
7. `docs/v2.2-backlog.md` carry-forwards section added; gaps 5–9
   stamped GRADUATED at this P0.
8. `todo.md` cycle-gate item struck (DONE); sticking-point #6
   (v2.2 cycle type) → RESOLVED.
9. Per-gap dispositions stamped on gap-1..gap-12 prompt files.
10. `docs/prompts/operator-decisions-2026-05-16.md` deleted (per
    its lifetime rule when v2.2 P0 mints).

**LOC impact P0:** 0 src/tests/tools/dashboard (docs-only).

---

## PHASE P1 — gap-4 API-timeout invariant test + deletion offset

**Status: blocked on this P0 merge.**

**Prompt:** `docs/prompts/v2.2-orchestration/phase-1-gap-4-api-
timeout-invariant.md`.

**Deliverables:**

1. `tests/test_api_timeout_invariant.py` covering CLI-timeout +
   API-500 fault classes; both assert `OBSERVE` verdict + bounded
   latency below the LIVE `BRIDGE_FALLBACK_LATENCY_BUDGET_MS`
   constant (pull from `governance.py` / `model_router.py` at
   test runtime — do NOT hard-code).
2. `tools/soak_driver.py` extension: invariant-degrade canary
   computed during Tier-3 soak; one closing-block line
   `[soak] invariant-degrade canary: PASS/FAIL`.
3. Tier-3 ship-gate ledger gains an `invariant-degrade` column
   (in addition to existing latency + alignment).
4. **Deletion offset survey + execution.** Pre-P1 grep for dead /
   unused code in `src/` + `tests/` + `tools/` + `dashboard/`.
   Surface candidates in P1 PR body. Net cycle LOC at ship-gate
   MUST be ≤ 0. If survey yields < 80 LOC candidates, escalate at
   P1 PR review: either (a) defer gap-4 to v2.3, or (b) operator
   override per Amendment A (consolidation cycles do NOT have an
   override mechanism per current Rule 3 — would require its own
   amendment).

**LOC estimate P1:** +80 tests/driver, ≥ −80 deletion = net ≤ 0.

**ADR-18 posture:** EXPERIMENTAL test surface; ratchet to EVOLVING
after first stable cycle. No FROZEN edits.

---

## PHASE P2 — ship-gate finalize

**Status: blocked on P1 merge.**

**Prompt:** `docs/prompts/v2.2-orchestration/phase-2-ship-gate-
finalize.md`.

**Deliverables:**

1. Tier-3 soak fired (`tools/soak_driver.py --cli-pool-size 2
   --ppp-auto-probe --total-seconds 1800 --interval-seconds 20`)
   — verdict PASS.
2. New invariant-degrade canary line in soak summary fires PASS
   (gap-4 P1 deliverable validated end-to-end).
3. Alignment-eval `--ci-gate` exit 0; alignment dip from v2.1
   reproduced or recovered — operator records disposition at ship
   per v2.1-backlog §"Alignment-recovery investigation" options
   (a/b/c).
4. `docs/adr/ADR-5-latency-budget.md` — append §"v2.2 ship-gate
   baseline" section.
5. `CHANGELOG.md` — append `## [2.2.0]` section.
6. v2.2.0 tag at ship.
7. Cycle-close memory `project_v22_cycle_close.md` minted.
8. LOC delta confirmed ≤ 0 vs `8303f38` across
   `src/`+`tests/`+`tools/`+`dashboard/`.

**LOC impact P2:** docs-only + tag.

---

## Carry-forwards from v2.1 (v2.2 P0 dispositions)

Per `docs/v2.1-backlog.md` §"Carry-forwards from v2.1":

1. **🟢 `JsonlTailWorker.start()` production wiring.** **DEFER
   to v2.3.** ~30 LOC non-additive runtime-shape change; folds
   naturally if v2.3 is feature cycle (`dashboard/server.py`
   startup hook is a feature surface, not consolidation scope).
2. **🟢 Soak-summary probe-emit counter.** **LANDED 2026-05-16**
   (PR #163, chore branch `chore/soak-summary-probe-counter`).
   No v2.2 action.
3. **🟡 Sonnet 0.95 → 0.8636 alignment dip.** Step 1 row-level
   audit **LANDED PR #161 2026-05-16**. Step 2 (cassette replay)
   remains operator-bound. **No v2.2 phase action;** ship-gate
   P2 records disposition (a/b/c per audit DOD) based on whether
   the dip reproduces on fresh-CLI control.
4. **🟢 Feature-cycle LOC ceiling.** **CLOSED at v2.2 P0**
   (Amendment A this PR). Issue #130 closes on merge.

---

## INTENT.md gap-analysis dispositions (12 gaps)

Per `docs/intent-todo-gap-2026-05-16.md`. Each gap's individual
prompt under `docs/prompts/v2.2-orchestration/gap-*.md` gets a
disposition stamp at the top of its file in this PR.

| Gap | Disposition | Action |
|---|---|---|
| 1 Cadence FR | **DEFERRED to v2.3** | Feature-gated; cycle = consolidation. |
| 2 Sub-agent scope-escalation FR | **DEFERRED to v2.3** | Feature-gated + Rule 1 carve-out gated. |
| 3 Dashboard regression watch | **DEFERRED to v2.3** | Either cycle type technically OK, but LOC ≤ 0 budget can't absorb without offset. Re-triage at v2.3 P0. |
| 4 API-timeout invariant test | **FOLDED v2.2 P1** | ADR-18-clean; ~80 LOC test + ~10 driver; mandatory deletion offset survey. |
| 5 Context-rank + 400-tok budget | **GRADUATED to v2.2-backlog** | Promotion-gated (promotion criteria stated in gap-5 prompt). |
| 6 Learn-mode ALLOW-promotion | **GRADUATED to v2.2-backlog** | Promotion-gated. |
| 7 Topology-discovery coverage | **GRADUATED to v2.2-backlog** | Promotion-gated. |
| 8 Out-of-scope guard scans | **GRADUATED to v2.2-backlog** | Promotion-gated (security-class). |
| 9 Messages-not-transitions boundary | **GRADUATED to v2.2-backlog** | Promotion-gated. |
| 10 LOC ceiling asymmetry | **LANDED — Option B** | INTENT pointer → "feature-cycle LOC ceiling (see #130)"; symmetry restored via cross-link. |
| 11 WIRED_LEVER_LEDGER_COUNT surface | **LANDED — NO-OP** | Consolidation cycle wires zero levers; counter stays at 0; gate stays inert. |
| 12 MASTER.md stale ref in INTENT | **LANDED** | Single-line strike; parenthetical removed. MASTER.md row for #111 verified `READY corpus-gated` per PR #158. |

---

## Cross-references

- ADR-18: `docs/adr/ADR-18-mvp-surface-freeze.md`.
- Prior cycle close: `CHANGELOG.md` §"[2.1.0]", tag `8303f38`,
  memory `project_v21_cycle_close.md`.
- v2.1 backlog: `docs/v2.1-backlog.md`.
- v2.2 backlog: `docs/v2.2-backlog.md`.
- Companion v10 status: `docs/v10-mvp-status.md`.
- v10 chain status: `project_v10_p4_hold_lifted.md`.
