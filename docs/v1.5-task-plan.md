# v1.5 task plan

**Companion docs:**
- `docs/v1.5-backlog.md` — seed backlog from v1.4 ship-gate ADR-5 §"Caveats"
- `docs/v1.6-backlog.md` — items deferred out of v1.5 scope (created in P0; populated only if v1.5 ship-gate surfaces new caveats)

**Reference docs (always available, never inlined):**
- `REQUIREMENTS.md` — current spec
- `docs/adr/ADR-5-latency-budget.md` — latency budget; v1.4 ship-gate baseline at HEAD `8b50f47`. v1.4 §"Caveats" is the load-bearing reference for P1 scope.
- `docs/adr/ADR-17-soak-tiers.md` — three-tier soak model (v1.2 Task A)
- `docs/v1.4-backlog.md` — predecessor (closed-out: M1–M5 of v1.4 cycle)
- `docs/v1.3-task-plan.md` — format template
- `reports/soak-20260504T182027Z.md` — v1.4 ship-gate report (32-min, `--cli-pool-size 2`, source for v1.5 P1 scope and the diagnosing finding that disproved the v1.3.1 publish-path I/O hypothesis)
- Memory: `feedback_subagent_stale_mental_model.md`, `feedback_no_self_monitor.md`, `feedback_cli_over_sdk.md`, `feedback_soak_cli_pool_flag.md`, `feedback_cassette_must_cover_new_envelopes.md`, `feedback_cross_pr_seam_review.md`, `feedback_subagent_escape_hatches.md`

**Format:** mirrors `docs/v1.3-task-plan.md` style — each phase block is self-contained, hand verbatim to a fresh Claude Code session via the matching prompt under `docs/prompts/v1.5-orchestration/`.

**Prior baseline:** v1.4.0 ship (commit `8b50f47`, tagged 2026-05-04). Soak source of truth: `reports/soak-20260504T182027Z.md` — overall p95 8.178 s (−2.26 s vs v1.3.1), ALLOW p95 7.572 s. Major finding: v1.3.1 publish-path I/O hypothesis DISPROVED — 100% of ALLOW tail is inside `_evaluate_inner` (publish + record < 1 ms total).

---

## CRITICAL: do-not-touch list (v1.1 + v1.2 + v1.3 + v1.4 protected symbols)

These symbols are load-bearing and MUST NOT be modified by any v1.5 phase. Diff PR head vs base on hot files before merge (memory: `feedback_subagent_stale_mental_model.md`).

| Symbol / file | Owning task | Cycle |
|---|---|---|
| `cli_pool`, `CliPool`, `CliWorker`, `.bridge/cli-pool.pids` | Task J | v1.1 |
| `_install_lazy_hydrator`, `GovernanceEngine.hydrated`, `tools/perf_probe.py` | Task I | v1.1 |
| `_run_sse`, `_consume_sse_stream`, `transport` kwarg, `/api/commands/stream` | Task K | v1.1 |
| `hitl_pending.matched_hash`, `dispatch_resolution` matched_hash read | Task L | v1.1 |
| `EngineRegistry.start_refresh/stop_refresh/refresh_all/refresh_status`, `last_refresh_ts`, `_refresh_timer` | Task M | v1.1 |
| `wirecli.py` module + `WireProtocolError`/`WireSchemaVersionError`/`WireTransportError` | Task N | v1.1 |
| `tools/soak_driver.py` `--cli-replay` flag, `--cli-pool-size` flag | Task A | v1.2 |
| `LifecycleBridge` + `/api/lifecycle/jobs` + dashboard lifecycle pane | Task C | v1.2 |
| `_VALID_TRANSPORTS = frozenset({"sse"})`, long-poll removal | Task D | v1.2 |
| `Transport = Literal["wirecli"]`, `_JSON_REMOVED_MSG`, json transport refusal | Task E | v1.2 |
| Learn Mode advisory bias surface: `_consult_learn_mode_bias`, `_emit_learn_mode_bias_applied`, `bias_consult` timing key, `bias_hint` HITL column | v1.3 P5d | v1.3 |
| `engine._last_phase_timings_ms` dict — existing keys (`inbound_publish`, `evaluate_inner`, `bias_consult`, `hitl_classify_trigger`, `hitl_route`, `record_decision`, `alert_publish`, `total`) — **extend, never remove or rename** | v1.4 instrumentation | v1.4 |
| `tools/soak_driver.py` `_ALLOW_PHASE_ORDER` — extend, never reorder existing entries | v1.4 instrumentation | v1.4 |
| `tools/soak_driver.py` `_format_allow_phase_breakout`, `### ALLOW publish-path phase breakout (v1.4)` block — keep emitting unchanged for back-compat with v1.4 baseline diffs | v1.4 instrumentation | v1.4 |
| Cassette+beacon DRY single source: `tools/cassette_record.py::_LM_DIALOGUE_PAIRS` and `tests/beacons/learn_mode_categorizer.jsonl` consume the same canonical pair list | v1.4 PR #79 | v1.4 |

**Rule:** if a v1.5 phase needs to modify any row above, the modification must be additive (new key, new column, new method) — never replace the existing surface.

---

## Task summary

| Phase | Title | Effort | Depends on | Action prompt |
|---|---|---|---|---|
| P0 | Cycle frame (THIS doc + `v1.6-backlog.md` + three phase prompts) | ¼ session | — | `phase-0-cycle-frame.md` |
| P1 | `_evaluate_inner` sub-phase instrumentation | 1 session | P0 | `phase-1-evaluate-inner-instrumentation.md` |
| P2 | v1.5 ship-gate + ADR-5 v1.5 baseline + LM trend re-check | ½ session | P1 merged | `phase-2-ship-gate-finalize.md` |

**Order:** P0 lands first (this doc + v1.6 seed). P1 lands directly on `main`. P2 cuts after P1 merges; runs the ship-gate, appends ADR-5 v1.5 baseline, tags v1.5.0.

---

## PHASE P0 — Cycle frame

**Pain point:** v1.5 cycle starts with `docs/v1.5-backlog.md` already seeded but no task plan, no v1.6 deferred-items file, and no orchestration prompts.

**References:**
- `docs/v1.3-task-plan.md` — format template
- `docs/v1.5-backlog.md` — seed item list
- `reports/soak-20260504T182027Z.md` — v1.4 ship-gate (sourcing the P1 scope)

**Do-not-touch guard:** P0 is docs only. Zero code edits.

**Scope:**
1. Create `docs/v1.5-task-plan.md` (THIS doc).
2. Create `docs/v1.6-backlog.md` seed file (empty items list, convention block only).
3. Create `docs/prompts/v1.5-orchestration/phase-{0,1,2}-*.md` — three prompt files.

**DOD:**
- [ ] All three artifact groups created
- [ ] PR scope is docs-only (no `src/`, `tests/`, `tools/`, `dashboard/` hunks)
- [ ] Single PR against `main`

**Action prompt:** `docs/prompts/v1.5-orchestration/phase-0-cycle-frame.md`

---

## PHASE P1 — `_evaluate_inner` sub-phase instrumentation

**Pain point:** v1.4 ship-gate disproved the v1.3.1 publish-path hypothesis. ALLOW p95 7.572 s; `inbound_publish` p95 = 0.43 ms, `record_decision` p95 = 0.11 ms. **100% of the ALLOW tail is inside `_evaluate_inner`** and opaque to the soak report. Without sub-phase attribution, the ALLOW p95 budget cannot be tightened defensibly.

**References:**
- `docs/adr/ADR-5-latency-budget.md` v1.4 §"Caveats"
- `src/stream_manager/governance.py` — `_evaluate_inner` (line ~366), `_evaluate_inner_core` (line ~782), v1.4 timing capture sites (lines 249–363)
- `tools/soak_driver.py` — `_ALLOW_PHASE_ORDER` (line 415), `_format_allow_phase_breakout` (line 427)

**Scope:**
1. Add five sub-phase wall-clock keys on `engine._last_phase_timings_ms` for ALLOW envelopes:
   - `og7_check` — `_check_fr_og7` call
   - `fast_precheck` — precheck path inside `_evaluate_inner_core`
   - `graph_classify` — decision-graph classify call inside `_evaluate_inner_core`
   - `hydrator_state_read` — hydrated-state reads consumed during evaluate
   - `routing_dispatch` — `route(...)` + `_apply_profile_constraints` tail
2. Extend `_ALLOW_PHASE_ORDER` (no reorder).
3. Add `### ALLOW _evaluate_inner sub-phase breakout (v1.5)` block in soak report; keep the v1.4 publish-path block intact.
4. Two new tests (verdict-equality regression + report-block render).

**Verdict-path invariant:** engine edits are additive only (`_t = _pc(); ... ; timings[...] = ...`). Verdicts must be byte-identical to v1.4. Regression test required.

**No new bus envelopes.** Cassette/beacon coverage unaffected (memory: `feedback_cassette_must_cover_new_envelopes.md` — verify by grep, not by extension).

**DOD:** see `docs/prompts/v1.5-orchestration/phase-1-evaluate-inner-instrumentation.md`.

---

## PHASE P2 — v1.5 ship-gate + ADR-5 v1.5 baseline + LM trend re-check

**Pain point:** P1 ships sub-phase instrumentation without a measured ship-gate; ADR-5 still references v1.4 as current; LM (categorize) p95 trend (15.39 s → 19.26 s across v1.3.1 → v1.4) is a watch item that needs a third measurement.

**References:**
- `docs/adr/ADR-5-latency-budget.md` v1.4 ship-gate baseline § (template for v1.5 §)
- `docs/adr/ADR-17-soak-tiers.md` — Tier-3 soak parameters
- `reports/soak-20260504T182027Z.md` — v1.4 ship-gate driver invocation (replicate exactly for v1.5)

**Scope:**
1. Run Tier-3 soak with `--cli-pool-size 2` (mandatory per memory `feedback_soak_cli_pool_flag.md`). Match v1.4 driver invocation. Produce `reports/soak-<UTC>Z.md` containing BOTH the v1.4 publish-path block AND the new v1.5 `_evaluate_inner` sub-phase block.
2. Append `## v1.5 ship-gate baseline` to `docs/adr/ADR-5-latency-budget.md`. Update the ADR header `Status:` line. Append-only — do NOT touch prior baseline sections.
3. Document the v1.5 LM p95 measurement in §"Caveats":
   - ≤ 18 s: close the v1.4 LM trend watch.
   - > 18 s: open a v1.6 backlog item ("🟡 LM (categorize) p95 sustained-elevation investigation").
4. Append `## [1.5.0]` section to `CHANGELOG.md`.
5. Tag `v1.5.0` on the merge commit.

**Cross-PR seam audit (memory: `feedback_cross_pr_seam_review.md`):** verify P1 writer (engine `_last_phase_timings_ms` keys) ↔ P2 reader (soak report block + ADR-5 table) agreement on all five v1.5 keys before merge. `hydrator_state_read` may legitimately render n=0 if the hydrator path does not fire under the soak workload — note in §"Caveats" if so.

**Subagent escape-hatch detection (memory: `feedback_subagent_escape_hatches.md`):** if any subagent return uses "deferred to a follow-up" phrasing, demand authorization or in-PR landing.

**DOD:** see `docs/prompts/v1.5-orchestration/phase-2-ship-gate-finalize.md`.

---

## Convention

Severity emoji on backlog items is set when the item lands and is NOT re-triaged by edit (per `docs/v1.3-backlog.md` convention).
