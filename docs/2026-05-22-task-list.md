# 2026-05-22 task list — v2.7.1 corrective sub-cycle launch

> **Source:** v2.7 P3 ship-gate BLOCKED at PR #207 (S4 CI gate FAIL on
> `frog7-phase-timings-keys-05` Haiku-stabilisation regression +
> corpus-wide cap-clip artefact confirmation). v2.7.0 NOT tagged.
> Companion: [`docs/2026-05-22-status.md`](2026-05-22-status.md).
>
> **Pipeline:** Mirror of `docs/2026-05-21-task-list.md` shape —
> agent-dispatchable prep artefacts + operator-bound fire decisions.
> Each row is **PM (prompt-mint)** = mintable now in this session, OR
> **FIRE** = operator-only at trigger time.
>
> **Lifetime:** Discardable at v2.7.1 P3 ship-gate merge (or v2.7.0
> retro-tag if PR #207 fold path is chosen).
>
> **Predecessor precedent:** v2.5 → v2.5.1 corrective sub-cycle
> (PR #188 BLOCK record + PR #189 corrective P1 + PR #190 ship-gate
> finalize verdict-A). v2.7.1 follows the same shape.

---

## Session-monitoring caveat (before any fire)

Per [[feedback_session_monitor_target.md]]: before attaching Monitor or
tail to any live session, enumerate `~/.claude/sessions/`, exclude self,
pick the most-active non-self candidate, and **lock the chosen
`{pid, sessionId, cwd}` triple for the entire run**. Do not silently
re-select mid-run.

**Snapshot at task-list mint (2026-05-22):**

| PID    | sessionId    | cwd                                          | status | role          |
| ------ | ------------ | -------------------------------------------- | ------ | ------------- |
| 27984  | `3cadde0d…`  | `C:\Users\SeanHoppe\vs\streamManager`        | busy   | **self**      |
| 7924   | `55614961…`  | `C:\Users\SeanHoppe\vs\certPortal\oversight` | idle   | non-self, idle |
| 4916   | `3c741c79…`  | `C:\Users\SeanHoppe\vs\certPortal\oversight` | busy   | non-self, most active (`codify-dave-eod-bundle-policy`) |

**Note (firewall):** PID 4916 cwd is under
`C:\Users\SeanHoppe\VS\certPortal\`. Per
[[feedback_certportal_dev_firewall.md]], do **NOT** attach Monitor or
read its JSONL transcript from this SM dev session. Surface to operator;
recommend operator open the monitor inside that repo's own claude
session if needed.

For v2.7.1 work, the target session to monitor is **self (PID 27984)**
— the SM-side fire chain. No external session attachment required.

---

## Scoping rationale

v2.7 P3 produced two distinct corrective surfaces:

1. **`frog7-phase-timings-keys-05` Haiku-stabilisation regression** —
   new row instability under `--runs 6`; needs n=12 re-eval per J3
   protocol pattern.
2. **Corpus-wide cap-clip artefact confirmation** — Seed v2.7-A-CLIP
   elevated from "hypothesised P2" to "confirmed P3"; multiple rows
   show p99 at-or-near new 30 s cap.

v2.7.1 P1 handles surface (1). Surface (2) feeds into the Hatch A/B/C
decision at v2.7.1 P2 and ultimately into the v2.8 Convergence-cycle
proposal in `docs/2026-05-22-status.md` §"Proposal".

**Sub-cycle phase count.** v2.5.1 precedent = P1 corrective + P2 ship-
gate finalize (2 work phases). v2.7.1 mirrors: P1 (J3-style re-eval on
row 05) + P2 (Hatch-driven disposition + ship-gate re-fire). No new P0
frame; v2.7 P0 cycle-frame decisions inherit.

---

## Agent-dispatchable PM jobs (mintable this session)

| Job | Output                                                                              | Bind                 | Spawn status   |
| --- | ----------------------------------------------------------------------------------- | -------------------- | -------------- |
| **J1** | New `docs/seed-v2.7-b-row05-haiku-protocol.md` (n=12 re-eval design; Haiku-stabilisation decision tree) | Docs-only | ✅ LANDED (220 lines) |
| **J2** | New `docs/prompts/v2.7.1-corrective/phase-1-row-05-haiku-investigation.md` (P1 prompt) | Docs-only | ⛔ **RETRACTED** — operator's PR #207 minted authoritative version; my draft deleted |
| **J3** | New `docs/prompts/v2.7.1-corrective/phase-2-ship-gate-finalize.md` (P2 stub; Hatch A/B/C; ship-gate re-fire) | Docs-only | ✅ LANDED (714 lines) — fills gap operator left |
| **J4** | New `docs/v2.7.1-task-plan.md` + `docs/v2.7.1-next-steps.md` | Docs-only | ⛔ **PARTIAL** — task-plan RETRACTED (PR #207 authoritative); next-steps KEPT (571 lines, no operator equivalent) |
| **J5** | New `docs/seed-v2.7-a-clip-corpus-protocol.md` (corpus-wide cap-clip re-measure at eval-cap=60s; v2.8 evidence prep) | Docs-only | ✅ LANDED (298 lines) |

**Retractions caused by parallel-operator collision** — see status doc
§"Parallel-operator collision". Operator opened PR #207 at 07:06:58Z
mid-session, minting authoritative v2.7.1 task-plan + P1 prompt before
my J-jobs spawned. J2 + J4 (task-plan portion) deleted; J1 + J3 + J5
+ J4 (next-steps portion) survive after rename to operator's naming
convention (`Seed v2.7-B`, `v2.7.1-corrective/`).

---

## Operator-bound FIRE items (queue for next session)

| #   | Item                                                                                   | Trigger                          |
| --- | -------------------------------------------------------------------------------------- | -------------------------------- |
| F1  | **Review / merge PR #207 disposition** — fold-as-corrective OR merge-as-BLOCK-record + fresh branch | First action of next session    |
| F2  | **Fire v2.7.1 P1** — J3-style n=12 re-eval on `frog7-phase-timings-keys-05`            | After F1 disposition            |
| F3  | **Hatch A/B/C decision at v2.7.1 P2** — based on J2 output verdict matrix              | After F2 lands                  |
| F4  | **Fire v2.7.1 P2 ship-gate re-fire** — S1–S13 procedure with corrective fold           | After F3 picks hatch            |
| F5  | **v2.7.0 or v2.7.1 tag decision** — v1.3.1 / v2.5.1 precedent says v2.7.1 tag, not v2.7.0 | At F4 ship-gate                  |
| F6  | **v2.8 P0 mint** — Convergence-cycle frame (Path-D + step (3) + Seed v2.7-A-CLIP corpus measure) per status doc §"Proposal" | Bundled at F4 ship-gate OR dedicated PM-mint |

---

## Per-job details

### J1 — `docs/seed-v2.7-b-row05-haiku-protocol.md`

**Anchor:** `docs/seed-v2.6-a-row10-remeasure-protocol.md` (v2.6 J3
output; n=12 single-row design + decision tree + runtime estimate +
stability-check gate). Reuse structure verbatim; substitute row-10 →
row-05; substitute Sonnet-content-drift verdict tree → Haiku-stabilisation
verdict tree.

**Required sections:**

1. **Background** — cite v2.7 P3 S4 CI gate FAIL reading on
   `frog7-phase-timings-keys-05` (Haiku unstable under `--runs 6`).
2. **Re-measure design** — n=12 single-row fixture; runner invocation
   `python -m tools.alignment_eval --golden <row-05 fixture> --runs 12
   --candidate-only-control --report-only --reports-dir
   reports/seed-v2.7-a-row05`.
3. **Stability check** — Haiku majority verdict + stability boolean per
   J3 §"Stability check" pattern; ≥ 9/12 stable → Hatch A; 6–8/12 → Hatch
   B; < 6/12 → Hatch C.
4. **Decision tree (Hatch A/B/C):**
   - **Hatch A** (≥ 9/12 stable) — golden re-baseline + ship-gate re-fire.
   - **Hatch B** (6–8/12 stable) — per-row exclusion per
     [[feedback_alignment_eval_stability_window.md]] hatch (4); re-fire
     with row-05 dropped from gate denominator.
   - **Hatch C** (< 6/12 stable) — escalate to corpus-wide cap-clip
     investigation; promote Seed v2.7-A-CLIP to v2.8 P1 candidate; do
     NOT re-fire v2.7.1 ship-gate.
5. **Expected runtime** — n=12 × Haiku p50 (cite measured) ≈ 2–3 min
   single-row add-on within v2.7.1 P2 alignment-eval window.
6. **Coupling with Seed v2.7-A-CLIP** — explicit cross-ref.
7. **v2.7.1 P2 question** — verbatim decision block for operator to paste
   into v2.7.1 P2 fire PR body.

**Acceptance.** 150–220 lines (mirror J3 v2.6 sizing). Diff = single new
file.

---

### J2 — `docs/prompts/v2.7.1-corrective/phase-1-row-05-haiku-investigation.md`

**Anchor:** `docs/prompts/v2.7-orchestration/phase-2-seed-v2.6-a-row10-remeasure.md`
(v2.7 P2 prompt). Match shape; substitute row-10 → row-05; cite J1
protocol as evidence source.

**Required content:**

- §"Branch + base" — base `main` after PR #207 merge; branch
  `feat/v2.7.1-p1-row05-remeasure`.
- §"Memory pre-flight (Amendment B)" — light re-verify; cite
  [[feedback_alignment_eval_stability_window.md]] +
  [[feedback_session_monitor_target.md]] as load-bearing for the n=12
  fire.
- §"Scope" — n=12 re-eval invocation (per J1 §"Re-measure design"); zero
  production-bucket touch unless Hatch A golden re-baseline fires.
- §"DoD" — n=12 report sidecar emitted; stability-check verdict recorded;
  Hatch A/B/C selection written into v2.7.1 P2 prompt entry block.

**Acceptance.** Single new file. Operator can fire P1 directly after PR
#207 disposition.

---

### J3 — `docs/prompts/v2.7.1-corrective/phase-2-ship-gate-finalize.md`

**Anchor:** `docs/prompts/v2.7-orchestration/phase-3-ship-gate.md` (v2.7
P3 stub) + `docs/prompts/v2.6-orchestration/phase-2-ship-gate-finalize.md`
(v2.6 P2 full template).

**Required content:**

- §"P1 → P2 verdict-bridge" — Hatch A/B/C disposition table (mirrors v2.7
  P3 §"P2 → P3 verdict-bridge" shape but driven by J1 stability-check).
- §"Scope (S1–S13)" — same ship-gate procedure shape; carry the
  cycle-tip anchor decision (re-anchor at v2.7.1 P0 OR retain v2.7 P0's
  `4902cca`?). Default: **retain `4902cca`** (sub-cycle inherits parent
  cycle's anchor per v2.5.1 precedent; cycle-tip is `4902cca` for both
  v2.7 P3 and v2.7.1 P2).
- §"S4 alignment-eval invocation" — `--runs 6` baseline (hatch (3) still
  bound); Hatch B path adds per-row exclusion of row-05; Hatch A path
  retests after golden re-baseline.
- §"S9 tag" — **v2.7.1 tag** (NOT v2.7.0; v1.3.1 / v2.5.1 precedent).
- §"S13 mint" — Convergence-cycle v2.8 P0 frame (per status doc
  §"Proposal").

**Acceptance.** Single new file. Stub-and-parameter shape; final fold
post-P1 verdict at the v2.7.1 P2 fire-PR.

---

### J4 — `docs/v2.7.1-task-plan.md` + `docs/v2.7.1-next-steps.md`

**Anchors:** `docs/v2.5.1-task-plan.md` (or equivalent v2.5.1
sub-cycle plan) + `docs/v2.7-task-plan.md`.

**Required content (task plan):**

- §"Sub-cycle framing" — corrective; inherits v2.7 P0 cycle-type
  decisions; cycle-tip anchor `4902cca`; soft LOC ≤ 1500 (cumulative
  with v2.7 P1 cycle delta).
- §"PHASE P1" — J1 protocol invocation + Hatch-decision capture.
- §"PHASE P2" — ship-gate re-fire with Hatch-driven scope.
- §"P2 close-out" — placeholder (filled at v2.7.1 P2 fire).

**Required content (next-steps):**

- §"Fire-order" table — P1 (row-05 re-eval) + P2 (ship-gate re-fire).
- §"Seeds" — re-record v2.6-A + v2.6-A-T + v2.7-A-CLIP + v2.6-C carry-
  forwards; mark Seed v2.7-B (NEW at v2.7 P3 BLOCK).
- §"Operator-bound decisions" — Hatch A/B/C selection + tag pick.

**Acceptance.** Two new files. Diff scope to those two paths.

---

### J5 — `docs/seed-v2.7-a-clip-corpus-protocol.md`

**Anchor:** `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` (v2.6 J2)
+ J1 protocol style.

**Required content:**

- §"Background" — Seed v2.7-A-CLIP elevation (v2.7 P2 hypothesis → v2.7
  P3 corpus-wide confirmation); J2 audit's clipped-distribution origin
  documented.
- §"Corpus-wide re-measure design** — full v2.6/v2.7 alignment-eval
  golden corpus (n=32 rows); per-row n ≥ 6 ideally n=12; **eval-cap
  widened to 60 s** (requires Seed v2.6-G step (3) env-split landing OR
  one-off `TIMEOUT_SECONDS` override).
- §"Output** — per-row Sonnet p50/p95/p99/max distribution under wide
  cap; cap-clip diagnosis (rows where p99 ≥ old cap value).
- §"v2.8 Convergence-cycle coupling" — re-measure feeds the
  Convergence-cycle (Path-D + step (3) + corpus re-measure) bundling
  decision per status doc §"Proposal".
- §"Fire conditions** — NOT at v2.7.1 (out of scope); v2.8 P1 candidate
  contingent on step (3) env-split shape.

**Acceptance.** 200–300 lines. Single new file. **Does NOT fire** in
v2.7.1; pure evidence-prep for v2.8 P0 cycle-type decision.

---

## POC test scenarios (≤ 60–90 s per scenario)

Each scenario validates a specific corrective-path assertion **before**
firing the heavier v2.7.1 P1 n=12 single-row re-eval (which is itself
≈ 2–3 min, inside the soft 60–90 s but not redundant). Scenarios run
against the production-bucket code state at PR #207 HEAD.

### TS-1 — golden-row sanity (≤ 15 s)

**Purpose:** Confirm `frog7-phase-timings-keys-05` golden fixture is
present + well-formed + parseable by alignment-eval runner.

**Command:**

```powershell
python -m tools.alignment_eval --golden tests/golden/l4_alignment.jsonl --runs 1 --filter-row frog7-phase-timings-keys-05 --report-only --reports-dir reports/ts-1
```

**Pass:** Runner exits 0; sidecar emitted; row-05 verdict recorded
(any verdict value — content does not gate here).

**Fail action:** Inspect golden file shape vs runner expectations
before mint J1.

---

### TS-2 — Haiku-only re-eval at n=3 (≤ 60 s)

**Purpose:** Confirm Haiku-stabilisation issue reproduces under a
shorter sample; validates hatch surface assumption before n=12 commit.

**Command:**

```powershell
python -m tools.alignment_eval --golden tests/golden/l4_alignment.jsonl --runs 3 --filter-row frog7-phase-timings-keys-05 --candidate-only-control --report-only --reports-dir reports/ts-2
```

**Pass:** Haiku stability boolean recorded; verdict counts present in
sidecar `haiku_runs` field.

**Hatch-A-likely signal:** Haiku majority + stable=true at n=3 → row may
have been transiently unstable at v2.7 P3 S4; n=12 confirms direction.

**Hatch-B/C-likely signal:** Haiku unstable at n=3 → mirrors P3 reading;
n=12 will resolve.

**Fail action:** Runner crash or empty sidecar — STOP, mint runner-
hardening issue before J1.

---

### TS-3 — cap-clip surface probe (≤ 90 s)

**Purpose:** Confirm cap-clip artefact is detectable in corpus reading
without firing the full corpus re-measure (J5 is the full investigation
later).

**Command:**

```powershell
python -m tools.alignment_eval --golden tests/golden/l4_alignment.jsonl --runs 3 --report-only --reports-dir reports/ts-3
```

(Full corpus, n=3 — quick smoke; expected ≤ 90 s under current 30 s
cap with worst-case per-row 30 s × 3 runs ≈ 90 s if one row times out
each run; central tendency much faster.)

**Pass:** Sidecar `sonnet_runs` per-row distribution emitted; any row
with p99 ≥ 29 s flags cap-clip presence.

**Cap-clip confirmed signal:** ≥ 2 rows show p99 ≥ 29 s → corroborates
P3 finding without requiring full J5 re-measure.

**Cap-clip absent signal:** All rows p99 < 29 s → P3 finding was row-05-
local, NOT corpus-wide; Seed v2.7-A-CLIP re-investigated (possible
de-escalation back to "hypothesised" status).

**Fail action:** Runner timeout > 120 s overall — investigate `--cli-
pool-size` flag presence per [[feedback_soak_cli_pool_flag.md]].

---

### TS-4 — session-monitor lock-in (≤ 30 s)

**Purpose:** Validate [[feedback_session_monitor_target.md]] discovery
flow works against the live PID landscape.

**Command (PowerShell):**

```powershell
Get-ChildItem $HOME/.claude/sessions/*.json | ForEach-Object {
  $j = Get-Content $_.FullName | ConvertFrom-Json
  [PSCustomObject]@{
    Pid = $j.pid
    Status = $j.status
    UpdatedAt = $j.updatedAt
    Cwd = $j.cwd
    Self = ($j.sessionId -eq $env:CLAUDE_SESSION_ID)
  }
} | Sort-Object UpdatedAt -Descending | Format-Table -AutoSize
```

**Pass:** Table emits ≥ 1 row; self-row marked; non-self candidates
ranked by `UpdatedAt` desc; firewalled cwds visible.

**Lock-in rule:** Operator selects the chosen `{Pid, sessionId, cwd}`
and pins to v2.7.1 fire-PR body §"Monitor target".

**Fail action:** If no non-self busy candidate exists, v2.7.1 fire runs
without external session attachment (this is the normal SM-side case).

---

### TS-5 — PR #207 state read (≤ 10 s)

**Purpose:** Confirm PR #207 is in expected BLOCKED state before any
v2.7.1 work fires.

**Command:**

```bash
gh pr view 207 --json number,title,state,isDraft,headRefName,mergeable
```

**Pass:** `state == "OPEN"`, `headRefName == "ship/v2.7-p3-ship-gate"`,
`mergeable` reflects any merge-conflict state.

**Action on result:**

- `OPEN` + no conflict → operator picks fold-corrective vs merge-as-
  block-record path.
- `OPEN` + conflict → resolve conflict (likely cycle-tip anchor drift)
  before v2.7.1 P1 fires.
- `MERGED` already → v2.7.1 fires fresh-branch path; no fold.
- `CLOSED` (unexpected) → STOP; investigate.

---

## Success criteria (this task list)

- [ ] All 5 PM jobs (J1–J5) landed as docs-only artefacts in working
      tree.
- [ ] All 5 POC test scenarios (TS-1 .. TS-5) documented (commands +
      pass / fail conditions).
- [ ] PM-mint bundle PR opened (or working tree staged for operator
      review).
- [ ] Operator-bound FIRE queue (F1 .. F6) handed off.
- [ ] Session-monitor target snapshot recorded above (already done).
- [ ] No reads or writes to `C:\Users\SeanHoppe\VS\certPortal\` (firewall
      holds throughout this session).

---

## Refs

- `docs/2026-05-22-status.md` — companion status doc; §"Convergence
  cycle" proposal feeds J5 + F6.
- `docs/2026-05-21-task-list.md` — predecessor task list (J1–J4
  shape source).
- `docs/2026-05-21-implementation-notes.md` — predecessor notes baseline.
- `docs/v2.7-task-plan.md` + `docs/v2.7-next-steps.md` — parent cycle
  ledger; v2.7.1 inherits cycle-tip anchor `4902cca`.
- `docs/seed-v2.6-a-row10-remeasure-protocol.md` — J1 structural anchor
  (v2.6 J3 output).
- `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` — J5 structural
  anchor (v2.6 J2 output).
- `docs/prompts/v2.7-orchestration/phase-2-seed-v2.6-a-row10-remeasure.md`
  — J2 structural anchor.
- `docs/prompts/v2.7-orchestration/phase-3-ship-gate.md` — J3
  structural anchor.
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendments A–E (inherits;
  no new amendment in v2.7.1).
- `feedback_alignment_eval_stability_window.md` — hatch (3) / hatch (4)
  source.
- `feedback_session_monitor_target.md` — session-monitor lock-in rule
  (memory minted this session).
- `feedback_certportal_dev_firewall.md` — firewall (PID 4916 cwd
  blocked).
- v2.5 → v2.5.1 precedent: PR #188 (BLOCK record), PR #189 (corrective
  P1), PR #190 (ship-gate finalize verdict-A).

---

# Goal append — v10 MVP gauge + POC subagent fleet for live non-SM session monitoring

> **Appended 2026-05-22 in response to `/goal` directive:** scope `docs/v10-mvp-status.md`,
> `docs/2026-05-22-guage-notes.md`, `docs/2026-05-22-status.md`, plus commits, identify how
> close we are to v10 MVP 100%, then design POC (minimum) that can monitor non-SM session,
> provide real-time data + real-time suggestions — i.e. SM + v10 intent. Coordinator
> subagent + agent jobs identified.
>
> **Post-write state (newer than the v2.7.1 corrective body above):** v2.7.1 shipped
> 2026-05-22 (PR #210, tag `5e971b4`). v2.8 P0 cycle frame minted (PR #211 `70e23e5`,
> backfill PR #212 `1f17cbc`). **v2.8 P1-prep minted Path-D synthetic-fixture P5 prompt
> (PR #213 `c5080b7`)** at `docs/prompts/v2.8-orchestration/phase-1-seed-v2.6-c-path-d.md`.
> Branch `feat/v2.8-p1-path-d` open; working-tree files `rl/shadow.py` + `rl/stop_conditions.py`
> already drafted ahead of P1 fire-PR. **6th-consecutive Seed v2.6-C defer averted by v2.8
> Convergence-cycle FEATURE pick.**

---

## §1 — v10 MVP gauge (where we sit)

### Phase ledger snapshot (from `docs/v10-mvp-status.md` §2 + commits since)

| Phase | Status | Weight |
|---|---|---|
| P0 + P0a (design + reconcile) | ✅ SHIPPED | 100% |
| P1 (episode logging) | ✅ SHIPPED (PR #121 + JsonlTailWorker production wire PR #174) | 100% |
| P2 (corpus augmentation) | ✅ SHIPPED | 100% |
| P3 (OPE 5-stage gauntlet) | ⚠ SHIPPED, 2 known gaps (#124 stage-1 ADVISORY + #125 DR-alias-to-IPS) | 85% |
| P4 (bandit trainer + #111 close) | ✅ SHIPPED (v2.3 PR #176 + v2.4 P0 PR #179) | 100% |
| P5 (shadow A/B + ship criteria) | 🔄 **IN FLIGHT v2.8 P1 Path-D** (synthetic-fixture; ~+600 LOC `rl/` + tests; v10.1-mode entry gate) | 0% → expected 100% post P1 merge |

**Aggregate read.** Pre v2.8 P1 fire: ~80% (foundation-weighted). Post Path-D land:
~92% (P5 implementation lands; #112 unblocks; #124/#125 stay blocked-on-#131 = ADR-18
freeze-lift cycle). MVP 100% binds on:

1. **Seed v2.6-C Path-D P5** lands at v2.8 P1 fire (head-of-chain).
2. **#112 closes** — 3 consecutive Tier-3 shadows pass ship criteria (post Path-D, requires
   stochastic propensity rollout = v10.3 mode; under v10.1-mode the synthetic-fixture run
   validates infrastructure but criteria-checker exit code is informational).
3. **#131 cycle frame mints** — ADR-18 freeze-lift for `model_router.route` (3 trigger
   conditions: #112 closed + v2.x slot open + #124/#125 still open).
4. **#124 + #125 land** in the freeze-lift cycle (un-ADVISORY stage-1 + restore Ridge-Q DR).

### Velocity verdict (anchored to `docs/2026-05-22-status.md` §"velocityGuage")

| Path | v10 MVP 100% timeline | Notes |
|---|---|---|
| Best (Convergence cycle ships P1+P2+P3 clean) | **2 cycles ≈ 2–6 days** | v2.8 lands Path-D + step (3) env-split + cap-clip re-measure; v2.9 = shadow stabilization + freeze-lift bundled |
| Nominal | 4 cycles ≈ 4–10 days | step (3) or shadow-stabilization slips a cycle |
| Pessimistic | 6–8 cycles ≈ 2–4 weeks | LM p95 OQ-2 corrective sub-cycle + cap-clip pre-Path-D blocking |

**Single-number read:** ~2 feature cycles to v10 MVP 100% under best case; current cycle
(v2.8) is firing the binding phase NOW. POC for non-SM session monitoring is **structurally
ready** (JsonlTailWorker production-wired since v2.3; learn-mode + governance + dashboard
all shipping); what's missing is **end-to-end smoke validation** that the full pipeline
delivers real-time data + suggestions against a live non-SM session.

---

## §2 — POC scope: monitor non-SM session, surface real-time data + suggestions

### Existing surface inventory (already in production)

| Surface | Path | Wired? | Role in POC |
|---|---|---|---|
| JsonlTailWorker | `src/stream_manager/jsonl_tail.py` + `dashboard/server.py:292-331` startup | ✅ v2.3 PR #174 | Tails target non-SM project JSONL; emits `desktop_prompt` / `user_reply` envelopes to bus |
| Polarity-flip self-monitor refusal | `src/stream_manager/jsonl_tail.py:178` + `rl/episode_logger.py:45,110` | ✅ v2.3 | Refuses SM-own session at wire site (loud-fail per `feedback_no_self_monitor.md`) |
| Learn-mode categorizer | `src/stream_manager/learn_mode.py` + `learn_categorizer.py` | ✅ v1.3 / v1.9 P3 | Out-of-band Sonnet categorizes turns → patterns table |
| Governance evaluate | `src/stream_manager/governance.py` + `cli_governance.py` | ✅ v1.x baseline | L1–L4 ladder → verdict (ALLOW/SUGGEST/INTERVENE/BLOCK/AMBIGUOUS) |
| Decision suggestions | `src/stream_manager/decision_suggestions.py` | ✅ v1.x | Advisory bias surface read at decide-time |
| HITL gate | `src/stream_manager/hitl.py` | ✅ v1.0–v1.2 | Sync/async HITL prompt + `hitl_overrides` persistence |
| Session watcher | `src/stream_manager/session_watcher.py` | ✅ v1.9 P2 | Peer at dashboard startup; tracks live session metadata |
| Cross-session hydrator | `src/stream_manager/cross_session_hydrator.py` | ✅ v1.0–v1.2 K | Surfaces cross-session flags via HITL gate |
| Dashboard server | `dashboard/server.py` + `dashboard/static/index.html` | ✅ v1.x | Real-time decision feed + audit rows |
| Episode logger (RL) | `rl/episode_logger.py` | ✅ v10 P1 | Writes to `rl_episodes.db` (corpus 777 episodes post v2.7.1 Run 9) |
| Shadow recorder (RL P5) | `rl/shadow.py` (working-tree) | 🔄 v2.8 P1 in flight | Non-invasive A/B candidate eval → `rl_shadow.db` |
| Stop conditions checker | `rl/stop_conditions.py` (working-tree) | 🔄 v2.8 P1 in flight | 6 pre-registered ship criteria evaluator |

**Read.** The wire is complete. The POC is not a new build — it is **smoke-validation of
the existing pipeline against a live non-SM session, end-to-end, with operator-facing
real-time surfaces verified rendering**.

### POC end-to-end happy path

```
operator-local non-SM Claude CLI session (e.g. certPortal)
        │  writes ~/.claude/projects/<encoded-slug>/<sessionId>.jsonl
        ▼
JsonlTailWorker  (slug = BRIDGE_PROJECT_SLUG; self-refusal active)
        │  emits desktop_prompt / user_reply envelopes
        ▼
MessageBus (SQLite WAL)
        │
        ├─► learn_categorizer (out-of-band Sonnet → patterns)
        │       │
        │       ▼
        │   patterns table (advisory bias source)
        │
        ├─► governance.evaluate (L1–L4 ladder reads patterns)
        │       │
        │       ▼
        │   decisions table + decision_suggestions surface
        │
        └─► (v2.8 P1) ShadowRecorder.on_governance_decision
                │  non-invasive ≤ 50 ms p95
                ▼
            rl_shadow.db (candidate A/B for future v10.3 writeback)

        ▼
dashboard/server.py  ─► dashboard/static/index.html
        │  SSE/WebSocket real-time feed
        ▼
operator sees: live decision row + advisory bias used + suggestion text
```

### POC gap map (what's not validated end-to-end yet)

| Gap | Description | Severity |
|---|---|---|
| G1 | No documented smoke run that proves a live non-SM turn surfaces in dashboard within latency budget | **BLOCKER** |
| G2 | `BRIDGE_PROJECT_SLUG` + `BRIDGE_SM_PROJECT_SLUGS` env mandate (v2.8 P0 §"Monitor target") never fired against a live target outside soak context | **BLOCKER** |
| G3 | `decision_suggestions` real-time surface to dashboard view never explicitly verified for a live non-SM-driven decision (only verified in soak fixtures + cassette replay) | HIGH |
| G4 | Path-D ShadowRecorder non-invasion invariant (≤ 50 ms p95) never measured against live envelopes — only synthetic-fixture pytest at v2.8 P1 DoD | MEDIUM (Path-D itself is v10.1-mode infrastructure-only) |
| G5 | No reproducible operator runbook for `start-monitoring <non-SM-project>` ↔ `stop-monitoring` lifecycle | MEDIUM |
| G6 | Dashboard front-end (`index.html`) audit-row rendering for `learn-mode-biased` decisions never visually verified against live tail | MEDIUM |
| G7 | Firewall reaffirmation: runtime monitoring of certPortal is allowed (Q-D BOUND v2.8 P0); dev-session reads of certPortal repo are NOT (per `feedback_certportal_dev_firewall.md`) — POC must not blur this | **HARD INVARIANT** |

---

## §3 — Subagent fleet for POC

### Design principles (binding)

1. **One coordinator owns the verdict.** Subagents return artefacts + PASS/FAIL rows; the coordinator gates ship/no-ship.
2. **No subagent runs > 5 min Bash.** Per `feedback_subagent_long_task_abandonment.md`. Long-running tail / soak attaches stay with main thread (`run_in_background` + `ScheduleWakeup`).
3. **No subagent reads certPortal repo files.** Per `feedback_certportal_dev_firewall.md`. Runtime monitoring of certPortal sessions != dev-side reading of certPortal repo. Reads of `~/.claude/projects/C--Users-SeanHoppe-VS-certPortal/<sessionId>.jsonl` for live-tail probing are admissible (that's session transcript surface, not repo source). Reads of `C:\Users\SeanHoppe\VS\certPortal\**` repo paths are denied.
4. **No subagent fires SM governance against SM-self session.** Polarity flip per `feedback_no_self_monitor.md`. Self-detection happens at wire site; subagent verifies by inspecting the loud-fail log line, not by reading SM session.
5. **Subagent scope is bounded by file:line touch surface.** Each agent's `tools:` line names only what it needs; no agent gets `*`.
6. **Coordinator may NOT escape-hatch ("deferred to follow-up").** Same rule as robin.

### Fleet roster

| # | Subagent | Role | Tools | Owner of |
|---|---|---|---|---|
| **C0** | **`poc-coordinator`** (NEW — recommended) | Orchestrator. Reads goal, sequences C1..C9 dispatch, collects PASS/FAIL rows, produces POC ship/no-ship verdict. Final report to main thread. | `Read`, `Glob`, `Grep`, `Bash` (≤ 60s commands), `Write` (POC verdict report only) | POC verdict (`reports/poc-live-monitor-<UTC>.md`) |
| **C1** | `session-target-scout` | Enumerate `~/.claude/sessions/` + `~/.claude/projects/`; exclude SM-self per `feedback_session_monitor_target.md`; rank by busy + recent `updatedAt`; return locked `{pid, sessionId, cwd, projectSlug}` triple for non-SM target. **Refuses firewalled cwd** = no-go for certPortal repo paths (but certPortal projects' JSONL transcripts at `~/.claude/projects/C--Users-SeanHoppe-VS-certPortal/*.jsonl` ARE admissible as session-transcript surface; firewall is about `C:\Users\SeanHoppe\VS\certPortal\**` repo source). | `Read`, `Glob`, `Bash` (`Get-ChildItem`-class only) | Lock-in `{pid, sessionId, cwd, projectSlug}` |
| **C2** | `env-bootstrap-validator` | Validate operator's `BRIDGE_*` env per v2.8 P0 §"Monitor target" + v2.8 P1 §"Q-D monitor target carryover". Confirms: `BRIDGE_PROJECT_SLUG` = encoded non-SM dir; `BRIDGE_SM_PROJECT_SLUGS` ∪ `streamManager` ∪ 12 encoded SM dirs; `BRIDGE_SM_SELF_SESSION_ID` = current SM session; `BRIDGE_PROJECTS_DIR` = `~/.claude/projects`. Reads dashboard log for `jsonl_tail: started (... slug=<target> ...)` line. **Refuses if SM-slug appears in `BRIDGE_PROJECT_SLUG`** (polarity flip). | `Read`, `Glob`, `Grep`, `Bash` (env-print + log-tail only) | Bootstrap PASS/FAIL row + env snapshot |
| **C3** | `tail-emitter-prober` | Confirms JsonlTailWorker emits `desktop_prompt` / `user_reply` envelopes against the locked target slug. Reads `MessageBus` SQLite for envelopes whose `metadata.source_label` matches target. Verifies self-refusal log present for any incidentally-touched SM slug. ≤ 60s observation window. | `Read`, `Grep`, `Bash` (`sqlite3 -readonly`) | Envelope-emission PASS/FAIL + per-source-slug counts |
| **C4** | `governance-trace-verifier` | For 1 captured turn from C3 output, traces: envelope → `governance.evaluate` → decisions table row → `decision_suggestions` advisory bias attached. Reads `MessageBus` decisions table; confirms decision_id present + verdict band + bias provenance. | `Read`, `Grep`, `Bash` (`sqlite3 -readonly`) | Trace PASS/FAIL + decision_id |
| **C5** | `dashboard-surface-prober` | Hits dashboard SSE/WebSocket endpoint at `dashboard/server.py`; confirms the C4 decision_id surfaces in the live feed within budget (target p95 ≤ 5 s tail-to-surface). Reads `dashboard/static/index.html` to verify renderable shape. Does NOT modify dashboard. | `Read`, `Bash` (`curl` / `Invoke-WebRequest`, single-request) | Surface PASS/FAIL + latency reading |
| **C6** | **`robin`** (EXISTING, reuse for P5 shadow side) | Per `.claude/agents/robin.md`. Verifies v2.8 P1 Path-D ShadowRecorder against synthetic-fixture invariants (non-invasion ≤ 50 ms p95 over ≥ 1000 envelopes; `rl_shadow.db` rows match expected schema; production verdict byte-identity with vs without shadow). Read-only against `rl_shadow.db`. **Does NOT fire Tier-3 soak** (main-thread-only). | (per `robin.md`) | Shadow-side PASS/FAIL verdict |
| **C7** | `learn-mode-bias-prober` | Reads `patterns` table; confirms at least one `desktop_prompt` / `user_reply` pair from target session produced a categorizer write (out-of-band Sonnet). Verifies bias provenance threads through to a downstream governance decision (sample n=1 sufficient for POC). Decay-clock state read-only. | `Read`, `Grep`, `Bash` (`sqlite3 -readonly`) | Bias-applied PASS/FAIL + pattern_id |
| **C8** | `firewall-auditor` | Walks subagent tool-call log + file-read log for any path matching `C:\Users\SeanHoppe\VS\certPortal\**` or repo-source patterns. Confirms zero dev-side firewall breach. Confirms `~/.claude/projects/C--Users-SeanHoppe-VS-certPortal/*.jsonl` reads (if any) stay session-transcript-only. Hard FAIL on any breach. | `Read`, `Grep`, `Glob` | Firewall PASS/FAIL audit row |
| **C9** | `e2e-smoke-runner` | Coordinator-dispatched final smoke: prompts operator (via `AskUserQuestion`) to send one turn in the locked non-SM session, then runs C3→C4→C5 in sequence with timing instrumentation. Returns end-to-end latency p50/p95 + per-stage breakdown. ≤ 90 s wall-clock. | `Read`, `Bash` (≤ 60 s probes), `Grep` | E2E latency reading + per-stage timing |

**Coordinator pick:** option chosen = **NEW `poc-coordinator` subagent** (not extending robin).

Reason: robin's scope is v10 P1–P5 phase verification (RL track testing lifecycle). POC
coordinator owns a **product-level smoke** (live-tail → governance → dashboard), which
spans surfaces outside robin's mandate (learn-mode, dashboard render). Mixing concerns
would muddy robin's verdict semantics. Robin stays the right pick for C6 specifically.

### `poc-coordinator` agent stub (drop into `.claude/agents/poc-coordinator.md` at fire)

```yaml
---
name: poc-coordinator
description: Owns end-to-end POC verdict for SM live-monitoring of a non-SM session. Sequences C1..C9 dispatch (session-target-scout, env-bootstrap-validator, tail-emitter-prober, governance-trace-verifier, dashboard-surface-prober, robin shadow side, learn-mode-bias-prober, firewall-auditor, e2e-smoke-runner). Aggregates PASS/FAIL rows into a single ship/no-ship verdict for the operator. Refuses to fire Tier-3 soaks (main-thread-only). Refuses certPortal repo reads (firewall). Refuses SM-self governance (polarity flip).
tools: Read, Glob, Grep, Bash, Write
model: sonnet
---

You are poc-coordinator, the live-monitoring POC ship-gate orchestrator.

## Mission
Ship a POC report proving SM can monitor a non-SM Claude CLI session and surface
real-time governance data + suggestions, end-to-end, within latency budget.

## Hard boundaries (same as robin)
1. NEVER read C:\Users\SeanHoppe\VS\certPortal\** repo paths. Runtime tail of that
   project's session transcripts under ~/.claude/projects/ is admissible.
2. NEVER fire Tier-3 soaks or >5min Bash. Escalate to main thread.
3. NEVER attach against SM-self session. Polarity flip refusal.
4. NEVER relax pre-registered ship criteria from rl/stop_conditions.py.
5. NEVER write to rl_episodes.db / rl_shadow.db. Read-only.
6. NEVER escape-hatch ("deferred to follow-up", "out of scope").

## Workflow
1. Read this task list §3 fleet roster.
2. Dispatch C1 first; lock the {pid, sessionId, cwd, projectSlug} triple.
3. Dispatch C8 firewall-auditor in parallel with all others (continuous audit).
4. Sequence C2 → C3 → C4 → C5 (each gates the next).
5. Dispatch C6 (robin) + C7 in parallel after C4 PASS.
6. Dispatch C9 e2e-smoke-runner last; requires operator turn (use AskUserQuestion).
7. Aggregate into reports/poc-live-monitor-<UTC>.md with per-subagent row.

## Output
| Subagent | Verdict | Evidence | Remediation if FAIL |
| ... |

## Verdict
SHIP if all rows PASS. NO-SHIP if any FAIL (cite blocker subagent + remediation).
```

### Fire order (operator decides timing)

1. **F-POC-0** — Operator mints `.claude/agents/poc-coordinator.md` from the stub above (single-file PR, docs-only — outside v2.8 P1 scope per `do-not-touch guard`).
2. **F-POC-1** — Operator (or main thread) dispatches `poc-coordinator` with this task list path + the goal sentence as context.
3. **F-POC-2** — Coordinator sequences subagents per §"Workflow"; main thread acts as relay for `AskUserQuestion` at C9.
4. **F-POC-3** — Coordinator emits `reports/poc-live-monitor-<UTC>.md`; operator reviews; ships POC tag (e.g. `poc-live-monitor-v0.1`) on PASS.
5. **F-POC-4** — POC ship feeds back into v10 MVP gauge: confirms end-to-end pipeline works against the surface that v10 P5 shadow recorder rides; closes G1–G6 from §2 gap map.

### Coupling with v2.8 cycle

- **POC does NOT block v2.8 P1 Path-D fire.** Path-D's synthetic-fixture P5 is offline (no live tail required per v2.8 P1 prompt §"Q-D monitor target carryover" L283-289: "Path-D synthetic-fixture P5 implementation in this PR does NOT require dashboard runtime OR Tier-3 soak").
- **POC runs orthogonal to v2.8 P1.** Once Path-D lands, C6 robin re-runs with the shipped `rl/shadow.py` against the synthetic corpus; C6 also reads `rl_shadow.db` populated by live envelopes if the operator opts to enable shadow recording during the POC C9 smoke.
- **POC validates the v10 production-runtime delivery vehicle.** v10 MVP 100% requires this pipeline working end-to-end; even with #112+#131+#124+#125 still blocked, the POC ship demonstrates SM achieves the intent (governance over non-SM sessions in real time).

### DoD — POC ship

- [ ] `.claude/agents/poc-coordinator.md` minted (single-file PR).
- [ ] Coordinator-dispatched run produces `reports/poc-live-monitor-<UTC>.md` with all 9 subagent rows.
- [ ] **C1**: locked `{pid, sessionId, cwd, projectSlug}` recorded; non-SM; non-firewalled (cwd may be under certPortal *project* dir for JSONL purposes — that's session-transcript, not repo source).
- [ ] **C2**: dashboard log line `jsonl_tail: started (... slug=<target> ...)` confirmed; SM-slug refused at wire site (negative test).
- [ ] **C3**: ≥ 1 `desktop_prompt` envelope from target slug observed in MessageBus within 60 s window.
- [ ] **C4**: governance decision row tied to that envelope; verdict band recorded; bias provenance attached.
- [ ] **C5**: dashboard SSE/WebSocket feed surfaces the decision_id; tail-to-surface p95 ≤ 5 s.
- [ ] **C6** (robin): Path-D shadow path PASS on synthetic fixtures (non-invasion ≤ 50 ms p95 + schema + byte-identity).
- [ ] **C7**: at least one categorizer write to `patterns` table from target turn observed; advisory bias attached to a downstream decision.
- [ ] **C8**: firewall audit zero-breach (no read of `C:\Users\SeanHoppe\VS\certPortal\**` repo paths).
- [ ] **C9**: end-to-end smoke against an operator-supplied live turn; p95 latency reading captured; per-stage breakdown attached.
- [ ] No `--no-verify`, no FROZEN-surface edits, no `>5min` Bash from any subagent.
- [ ] POC verdict line: `SHIP` (all PASS) or `NO-SHIP <blocker>` (≥ 1 FAIL).

---

## §4 — INTENT.md conformance: what "100%" actually means

> Operator amendment 2026-05-22: `INTENT.md` is the load-bearing "what is SM"
> doc and the authoritative anchor for what MVP 100% must demonstrate. The
> v10 phase ledger (§1) tracks the RL-companion track's % completion; INTENT.md
> defines the **product-level shape SM must hit** to count as "done". POC ship
> binds on BOTH.

### INTENT.md → POC DoD mapping

| INTENT.md section | What 100% looks like | POC validation point | Subagent owner |
|---|---|---|---|
| §"What this project is" | SM is the **project manager layer** between Desktop sub-agent orchestration and CLI executor; reads governed project `*.md`; discovers Desktop sub-agent topology (Prompt Constructor, Developer, Code Reviewer, Tester, others) via hybrid metadata + pattern inference; governs each sub-agent **independently** per role | POC target session MUST be one running Desktop sub-agent orchestration (not raw chat). C1 must record observed sub-agent topology in the locked-target record. | **C1** (extend) |
| §"Plan alignment + cadence" | SM enforces (a) does this orchestration prompt move toward stated requirements? (b) is the session making forward progress? — beyond raw safety | C4 governance trace must surface (a) `alignment_score` field on the decision row + (b) cadence band; if absent, governance is shipping safety-only and INTENT is under-served | **C4** (extend) |
| §"Safety priorities" 1–5 (absolute) | (1) destructive shell BLOCK; (2) force-push to protected branches INTERVENE; (3) `eval(`/`exec(` INTERVENE; (4) credential shapes BLOCK; (5) API timeout degrades to OBSERVE (never stalls bridge) | Negative-test pack: 5 synthetic prompts (one per priority) injected via operator's non-SM session (or fixture replay); each MUST hit the stated verdict. API-timeout test forces `cli_governance` timeout and confirms decision row = OBSERVE, not stall. | **C10** (NEW — see below) |
| §"Out of scope" (do not auto-allow) | Network mods to `transport/`, plaintext session-token storage, exfiltration of SQLite bus DB | C8 firewall-auditor already covers DB-exfil shape; extend to grep subagent tool-log for any `transport/` write. | **C8** (extend) |
| §"Hot zones" | `governance.py`, `message_bus.py`, `project_context.py` — protected | C8 confirms zero subagent diff against these (POC is read-only). | **C8** (extend) |
| §"Sub-agent governance principles" | Independent per-role governance; reviewer SUGGEST-scope only (CLI exec from reviewer → BLOCK); developer near hot files → GUIDE→INTERVENE; unknown agents → standard engine until pattern inference resolves | C4 must show per-sub-agent role binding in decision metadata (`agent_role`, `agent_profile`); reviewer-vs-developer rows for same prompt class must produce different verdicts. | **C4** (extend) + **C10** |
| §"Project context loading" | INTENT > REQUIREMENTS > CLAUDE.md > README ranking; 10 s debounce on monitored-file change; 400-token budget; mid-session refresh | C11 (NEW — see below) modifies one governed-project `INTENT.md` mid-session, confirms reload within 10 s + new context surfaces in next decision. | **C11** (NEW — see below) |
| §"UI / HITL principles" | Monitor-first 3-frame dashboard (Interactive REPL / Sub-Agents / Background Jobs); only `desktop_pause` / negative regression / static-rule fire auto-foregrounds; HITL ON = ranked-option list from memory + persist pick; HITL OFF = proposed answer read-only with per-card opt-in; color + label paired badges | C5 dashboard-surface-prober extended: confirms 3-frame layout, paired badges, HITL ranked-option list rendered when applicable. | **C5** (extend) |
| §"Intentional non-goals (v1)" | Multi-tenant cloud, replacing Claude Code permission model, IDE/terminal multiplexer, non-Claude CLI tools | POC ships against single-host single-operator + only Claude CLI. C0 coordinator confirms in verdict report. | **C0** |
| §"What governance should learn from this project" (self-reference) | Routine commands (`pip install -e ".[dev]"`, `pytest`, `ruff`, `mypy`) → ALLOW patterns; feature-branch commits routine; force-push main NOT | POC validates the **non-SM project's** equivalent INTENT-derived "routine" list lands in patterns table for that project — not SM's. | **C7** (extend) |

### New subagents (C10 + C11)

| # | Subagent | Role | Tools |
|---|---|---|---|
| **C10** | `safety-priority-injector` | Fires the 5-row negative-test pack (one per INTENT §"Safety priorities" rule) via cassette-replay OR via operator-supplied live prompts; reads governance decision rows; confirms each prompt produced the stated verdict band. Also drives the role-binding probe (reviewer-vs-developer same-prompt-different-verdict). Does NOT exfiltrate live credentials — uses synthetic shape (`SECRET_KEY=AKIAFAKE123...`) only. | `Read`, `Grep`, `Bash` (`sqlite3 -readonly`; cassette-record/replay invocations ≤ 60 s) |
| **C11** | `context-reload-prober` | Modifies one governed-project `INTENT.md` (operator-supplied path under a non-SM project root — NOT certPortal repo paths; admissible targets: any operator-owned non-SM, non-firewalled project). Confirms (a) reload within 10 s debounce, (b) ranked excerpt budget ≤ 400 tokens, (c) new content surfaces in next decision's `alignment_context`. Reverts the edit at end of probe. | `Read`, `Write` (one specific file, revertible), `Grep`, `Bash` (≤ 60 s) |

Both subagents fire under `poc-coordinator` orchestration; **C10 runs in parallel with
C7; C11 runs after C5 PASS** so the dashboard-side reload signal can be observed.

### Revised POC DoD (additive)

In addition to the §3 DoD checklist:

- [ ] **INTENT-§"Plan alignment + cadence"**: C4 surfaces `alignment_score` + cadence band on at least one live decision row.
- [ ] **INTENT-§"Safety priorities"**: C10's 5-row negative-test pack PASS (one per priority).
- [ ] **INTENT-§"Safety priorities" #5**: C10 API-timeout simulation produces OBSERVE, NOT stall (forwarding latency p95 ≤ `BRIDGE_FALLBACK_LATENCY_BUDGET_MS = 45_000` ms per `src/stream_manager/latency_budgets.py`).
- [ ] **INTENT-§"Sub-agent governance principles"**: C4 + C10 show role-bound divergence (same prompt, reviewer vs developer roles → different verdicts).
- [ ] **INTENT-§"Project context loading"**: C11 confirms 10 s debounce reload + 400-token budget enforcement + INTENT > REQUIREMENTS > CLAUDE.md > README rank order.
- [ ] **INTENT-§"UI / HITL principles"**: C5 confirms 3-frame monitor-first layout + paired label+color badges + HITL ranked-option rendering when applicable.
- [ ] **INTENT-§"Out of scope"**: C8 audits subagent tool-log for any `transport/` write OR plaintext session-token persistence shape OR off-host SQLite bus copy.
- [ ] **INTENT-§"What governance should learn"**: C7 confirms target non-SM project's routine commands graduate to ALLOW patterns within POC window (or document the patterns-decay clock state if too short).

### POC ship verdict — revised single-line read

`SHIP` requires **BOTH**: (a) §3 9-subagent pipeline PASS (end-to-end live monitoring works) **AND** (b) §4 INTENT.md conformance PASS (SM behaves the way INTENT.md says it must). Either side FAIL → `NO-SHIP <blocker>`.

A POC ship under both means SM has demonstrated the **product-level shape INTENT.md
defines**, end-to-end, against a live non-SM session. Combined with v2.8 P1 Path-D
landing (v10 P5 infrastructure under v10.1-mode), the v10 MVP gauge moves from
~80% → ~95% effective coverage. The remaining ~5% binds on #112 (v10.3-mode shadow
stabilization across 3 Tier-3 soaks) + #131 freeze-lift + #124/#125 — those are
**post-MVP refinement** per `docs/v10-mvp-status.md` §2 ("v10.1+ is post-MVP
refinement"), not POC blockers.

### Refs (INTENT amendment)

- `INTENT.md` — entire doc; load-bearing for POC DoD §4.
- `docs/v10-mvp-status.md` §2 ("v10.1+ is post-MVP refinement").
- `src/stream_manager/project_context.py` — INTENT-rank loader (hot zone per INTENT.md §"Hot zones").
- `src/stream_manager/latency_budgets.py` — `BRIDGE_FALLBACK_LATENCY_BUDGET_MS = 45_000` per INTENT §"Safety priorities" #5.
- `src/stream_manager/agent_registry.py` — sub-agent role binding (per INTENT §"Sub-agent governance principles").
- `dashboard/static/index.html` — 3-frame monitor-first surface per INTENT §"UI / HITL principles".
- `docs/adr/ADR-9-hitl-as-mode.md` — HITL ON/OFF semantics per INTENT §"UI / HITL principles".

---

### Out of scope for POC (explicit)

- Live propensity-aware shadow A/B against a deployed candidate threshold. (Requires v10.3 mode; Path-D under v10.1-mode is infrastructure-only.)
- ADR-18 freeze-lift on `model_router.route` (#131). (Bound to a separate cycle.)
- HITL auto-resolve at high confidence. (Locked out v1.3+; bias never bypasses HITL gate per `docs/learn-mode-design.md` §2.3.)
- Multi-user pattern propagation. (Locked out v1.3 §2.7.)
- Tier-3 soak validation of live-tail under load. (Main-thread-owned; not POC scope.)

### Refs (POC)

- `docs/v10-mvp-status.md` §2 phase ledger + §4 held-chain map.
- `docs/2026-05-22-status.md` §"velocityGuage" + §"Proposal — Convergence cycle".
- `docs/2026-05-22-guage-notes.md` §DD-2 MVP anchor + §OQ-3 horizon.
- `docs/v2.8-task-plan.md` + `docs/prompts/v2.8-orchestration/phase-1-seed-v2.6-c-path-d.md` — Path-D in-flight detail.
- `docs/learn-mode-design.md` §3.1 data flow + §7 `learn_sources` config (v1.9 P3) + §7.2.1 production wiring env contract.
- `src/stream_manager/jsonl_tail.py` + `dashboard/server.py:292-331` (JsonlTailWorker production wire site).
- `rl/shadow.py` + `rl/stop_conditions.py` (working-tree Path-D drafts).
- `.claude/agents/robin.md` — C6 anchor + coordinator-design contrast.
- `feedback_session_monitor_target.md` — C1 lock-in rule.
- `feedback_no_self_monitor.md` — C2/C3 polarity-flip refusal.
- `feedback_certportal_dev_firewall.md` — C8 audit rule.
- `feedback_subagent_long_task_abandonment.md` — `>5min` Bash refusal across all subagents.
- `feedback_alignment_eval_stability_window.md` — applies to any C9 smoke that hits the alignment-eval surface.
- `feedback_parallel_operator_state.md` — coordinator must `git fetch` + `gh pr list` before dispatching docs-mint subagents.
- ADR-5 latency budget (NFR-P2: p50 ≤ 7 s, p95 ≤ 15 s) — C5/C9 tail-to-surface budget anchor.
- ADR-18 — surface-freeze posture; new modules at birth = EVOLVING.
- ADR-19 — `learn-patterns-canonical-split` (audit/canonical two-table split for `patterns`).
