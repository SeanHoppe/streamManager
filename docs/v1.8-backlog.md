# v1.8 backlog

Seed list of items deferred out of v1.7 scope.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v1.8 task,
not by editing the emoji.

---

## Seed items

_None at v1.7 P0 cycle frame. Items will be appended by v1.7 P3 ship-gate
finalize if the soak / alignment eval / LM watch produce new caveats per
the data-driven mint rules in `docs/v1.7-task-plan.md` PHASE P3._

Carry-forward candidates (un-promoted from v1.7 — the v1.7 cycle did not
land them; re-open here only if v1.7 P3 explicitly defers):

- 🟢 CLI pool sizing >2 — concurrent-burst fallback lever (v1.7 backlog
  item 2). Stays in `docs/v1.7-backlog.md` until promoted; carries forward
  unchanged unless P3 surfaces acquire-time elevation.
- 🟢 PPP audit harness — Provenance Probe Protocol (v1.7 backlog item 4).
  Blocked-by sync-comms v1.0 HITL panel landing first; carries forward.

---

## Convention

When picked up in a v1.8 task, link the new task ID and the originating
phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
