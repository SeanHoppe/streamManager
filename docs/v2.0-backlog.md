# v2.0 backlog

Seed list of items deferred out of v1.9 scope.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v2.0 task,
not by editing the emoji.

---

## Seed items

### 🟢 CLI pool sizing >2 — concurrent-burst fallback lever

**Carry-forward from v1.9.** v1.7 P3 disposition: NOT promoted. v1.8 P2
disposition: NOT promoted. v1.9 P0 disposition: NOT promoted
(deprioritised vs. verdict-based fallback trigger, session watcher, and
Learn Mode source expansion). Still deferred.

---

### 🟢 PPP audit harness — Provenance Probe Protocol

**Carry-forward from v1.9.** Blocked-by sync-comms v1.0 HITL panel
landing first. No change in disposition.

---

## Convention

When picked up in a v2.0 task, link the new task ID and the originating
phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
