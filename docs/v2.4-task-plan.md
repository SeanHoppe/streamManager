# v2.4 task plan — consolidation cycle (Amendment D + Amendment E + #111 close)

**Cycle theme: consolidation.** Operator-declared at P0 mint
2026-05-19. Net-LOC target ≤ **0** vs cycle-tip (v2.4 P0-merge SHA),
docs bucket advisory per ADR-18 Amendment A. Anchor per
**Amendment C** = cycle-tip, NOT predecessor tag (v2.3.0 = `b00473d`
retained as narrative comparator only).

**Cycle-type framing.** v2.3 was feature (+461 LOC at cycle-tip;
JsonlTailWorker production wiring; lever ledger 0 → 1). v2.4 returns
to consolidation per ADR-18 cycle-alternation hygiene. Headline
scope this cycle:

1. **ADR-18 Amendment D** — v10 P5 entry-gate split (closes #177).
   Docs-only relocation from staging draft `docs/adr/ADR-18-
   amendment-d-draft.md` (deleted this PR) into ADR-18 §Amendments.
2. **ADR-18 Amendment E** — Rule 5 cycle-handoff exemption for
   externally-triggered seeds. Docs-only.
3. **Issue #111 close** — P4 trainer DOD met at `cf7d003` (PR
   #176); gate-semantics follow-up tracked in #177.
4. **Seed v2.4-D Sonnet-DIP investigation** — fires P1 (3 cycles
   of alignment-eval data now available).

Path-D synthetic-fixture P5 implementation (Seed v2.4-C, ~600 LOC)
**DEFERS to v2.5** per consolidation cycle choice.

**P1 fire-order (recorded at P0).** Per `docs/v2.4-next-steps.md`
fire-order table with consolidation-cycle dispositions applied:

1. P0 cycle frame (this PR) — mints Amendment D + Amendment E +
   closes #111 + drafts this task-plan.
2. **Seed v2.4-D — Sonnet-DIP investigation** (🟡; report-only,
   no surface change). Prompt: `docs/prompts/v2.4-orchestration/
   task-sonnet-dip-investigation.md` (TBD mint at P0/P1 fire).
3. **Seed v2.4-C — Path-D synthetic-fixture P5 impl** —
   **DEFERRED to v2.5** (consolidation cycle).
4. **Seed v2.4-G — CLI governance timeout audit** —
   **DEFERRED to v2.5** (consolidation cycle per fire-order
   row 6 "FIRE P1 IF feature; DEFER ELSE").
5. P2 ship-gate finalize. Prompt: `docs/prompts/v2.4-orchestration/
   phase-2-ship-gate-finalize.md`.

**Phase count.** P0 (frame) + **1 work phase P1 (Sonnet-DIP
investigation only)** + P2 (ship-gate) = **3 numbered phases**,
**1 work phase**. ADR-18 Rule 4 caps work-phase count at ≤ 3 —
v2.4 sits well under at 1.

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3 numbered phases. v2.4 numbered-phase span =
P0 → P2. v10.x slot also blocked on #112 (v10 P5) which is now
unblocked at the **docs side** by Amendment D landing here but
remains blocked at the **implementation side** (Seed v2.4-C
deferred). #131 remains gated.

**Companion docs:**

- `docs/v2.4-next-steps.md` — **the comparison anchor**. P2
  ship-gate walks this file row-by-row.
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendment D + E
  appended this PR (§Amendments).
- `docs/v2.3-next-steps.md` — feature-cycle predecessor.
- `docs/v2.3-task-plan.md` — feature-cycle predecessor task plan.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — Amendment D adds NO new FR row (v10 EXPERIMENTAL
  track per ADR-18 row table L71).
- `docs/adr/ADR-5-latency-budget.md` — v2.3 ship-gate baseline
  current; v2.4 P2 may append new baseline IF consolidation cycle
  with LOC delta ≈ 0 and no latency lever wired → may skip per
  P0 cycle-frame prompt §"ADR-5 freshness".
- `docs/adr/ADR-17-soak-tiers.md` — Tier 3 ship-gate at P2.

## Operator decisions recorded at P0 fire (2026-05-19)

Per `docs/prompts/v2.4-orchestration/phase-0-cycle-frame.md`
§"Operator decisions to record at P0 fire" and
`docs/v2.4-next-steps.md` §"P0 frame — operator-bound decisions":

1. **Cycle type: CONSOLIDATION.** Default lean (alternation hygiene
   post-v2.3 feature +461 LOC). Net LOC ≤ 0 vs cycle-tip per
   Amendment C. Path-D P5 implementation (Seed v2.4-C, ~600 LOC)
   defers to v2.5 as a consequence.

2. **`WIRED_LEVER_LEDGER_COUNT` posture: HOLD at 1 (production
   scope) / 0 (soak scope).** No new lever wired this cycle. The
   production-scope-vs-soak-scope split (artifact of v2.3 Seed 6
   FastAPI startup wiring) carries forward to Seed v2.4-H lever-
   ledger scope clarification at P2 ship-gate. ADR-18
   `WIRED_LEVER_LEDGER_COUNT: 0` HTML comment + `tools/soak_driver.py`
   `WIRED_LEVER_LEDGER: dict = {}` both remain — soak scope unchanged.
   v2.4 P2 ship-gate prompt binds which scope counts for close-
   memory `WIRED_LEVER_LEDGER_COUNT` field.

3. **ADR-18 Amendment D mint: LAND IN P0 PR BODY.** Body relocated
   from staging `docs/adr/ADR-18-amendment-d-draft.md` (deleted)
   into ADR-18 §Amendments. Matches Amendments A/B/C precedent.
   Amendment text per issue #177 disposition unchanged. Cycle-
   conditional acceptance items (`rl/bandit.py`, `rl/cli/train.py`,
   etc.) marked **DEFERRED v2.5** because consolidation cycle does
   not implement Path-D P5.

4. **ADR-18 Rule 5 backlog cap: MINT AMENDMENT E (cycle-handoff
   exemption).** Operator chose amendment over cap-bump 11 → 14.
   Amendment E formalizes that promotion-criterion-bound + demand-
   bound carry-forwards are exempt from the 2-cycle cap if their
   external trigger has not yet fired. Self-application at v2.4
   P0: 14 open seeds = **8 cap-counted** (NEW v2.4-A..H) +
   **6 EXEMPT** (v2.4-I..M INTENT-graduated, v2.4-N demand-bound).
   Effective cap reading at v2.4 P0 = 8. Under the prior 11-cap.

5. **Amendment C cycle-tip anchor verbatim citation.** v2.4 P2
   ship-gate prompt cites `<v2.4 P0-merge SHA full 40-char>..HEAD`
   for the consolidation-gate net-LOC measurement. SHA populated
   post-merge of this PR. Diff command:

   ```
   git diff <v2.4-P0-merge-SHA>..HEAD --stat -- src tests tools dashboard
   ```

   Predecessor-tag narrative diff `b00473d..HEAD` is reported
   alongside for cycle-impact context but does NOT gate.

6. **Memory pre-flight (Amendment B).** Performed 2026-05-19 against
   current `main` at `b5a2ed5` (PR #178 merged). Stamp below.

7. **Issue #111 close.** Closed at P0 alongside this PR with
   `gh issue close 111 --reason completed` and comment linking
   PR #176 (cf7d003) merge + #177 gate-semantics follow-up. Per
   Seed v2.4-B housekeeping disposition.

## Memory pre-flight stamp (Amendment B)

Pre-flight performed 2026-05-19 against current `main` at
`b5a2ed5` (PR #178 merged).

| Memory | Status | Action |
|--------|--------|--------|
| `project_v23_cycle_close.md` | FRESH (minted at v2.3 P2 ship-gate 2026-05-17) | None |
| `project_v10_p5_gate_deadlock.md` | FRESH (minted 2026-05-18 in PR #178; verified against live-train artifacts + bandit.py L23/L99-101 + train.py L120-123/L221; already records Amendment D as the filed resolution path) | None — Amendment D acceptance item "status → AMENDMENT-LANDED" DEFERRED v2.5 (bundled with Path-D P5 implementation PR) |
| `project_v10_rl_track.md` | **STALE** (10 days old; "Next gate: v2.0 ship tag → ..." outdated since v2.0/v2.1/v2.2/v2.3 all shipped + P4 just landed) | UPDATED 2026-05-19: appended note recording P0–P4 shipped, P5 BLOCKED on Amendment D + pointer to [[project_v10_p5_gate_deadlock]] / [[project_v10_p4_hold_lifted]] |
| `project_v10_p4_hold_lifted.md` | **STALE** (says "60 rows", "operator must run ≥2 more Tier-3 soaks", P4 not shipped) | UPDATED 2026-05-19: P4 SHIPPED at cf7d003 PR #176; corpus CLEARED at 240; P5 BLOCKED on Amendment D; #118 schema-parity test reference retained |
| `feedback_soak_cli_pool_flag.md` | FRESH (verified `tools/soak_driver.py` L1523 retains `--cli-pool-size` default=0) | None |
| `feedback_no_self_monitor.md` | FRESH (verified `rl/episode_logger.py` L45 + L96-105 retain `BRIDGE_SM_PROJECT_SLUGS` + `BRIDGE_SM_SELF_SESSION_ID` polarity-flip filter) | None |
| `MEMORY.md` index entries | FRESH | None |

## Cycle-discipline guardrails

- **LOC budget (Amendment A + Amendment C).** Anchor = cycle-tip
  (v2.4 P0-merge SHA, populated post-merge of this PR).
  Consolidation gate: **net ≤ 0** across production bucket (`src/`
  + `dashboard/` + `tools/` + `tests/`). Docs bucket advisory per
  Amendment A. BLOCK at any positive production-bucket delta.

- **Rule 5 backlog cap.** **Amendment E minted at P0.** Effective
  cap reading = **8** (cap-counted: Seeds v2.4-A..H). 6 exempt
  carry-forwards (v2.4-I..N) per Amendment E §"Self-application".
  v2.4 cycle work that opens any NEW backlog item beyond these
  8 must close or graduate ≥ 1 existing seed before merging —
  OR add the new seed as cap-counted (no further amendment
  required; the 2-cycle drift clock starts at first carry-forward).

- **Rule 6 memory pre-flight.** Performed at P0 (above stamp).
  2 stale memories updated in same PR before P0 merge.

- **Lever ledger.** Entering posture 1 (production scope, v2.3
  Seed 6 JsonlTailWorker wire). Target end posture **unchanged**
  (consolidation; no lever change). Seed v2.4-H locks scope
  semantics at P2 ship-gate.

- **Surface freeze.** ADR-18 Rule 1 in force. No v2.4 P0 prompt
  touches FROZEN surface; Amendment D body relocates ADR text
  only (docs-only), Amendment E adds Rule 5 addendum (docs-only),
  #111 closes via `gh` (no surface change), task-plan + next-
  steps update (docs-only).

## PHASE P0 — this PR (docs frame)

Files changed (repo-tracked):
- `docs/v2.4-task-plan.md` (NEW; this file).
- `docs/adr/ADR-18-mvp-surface-freeze.md` (Amendments D + E appended).
- `docs/adr/ADR-18-amendment-d-draft.md` (DELETED; staging file
  retired per Amendment D §Acceptance).
- `docs/v2.4-next-steps.md` (§"P0 frame" checklist ticked with this
  PR cross-link).

Files changed (local-only auto-memory, NOT in this PR — per
Amendment B pre-flight; absolute paths kept for operator/reviewer
verifiability only):
- `~/.claude/projects/C--Users-SeanHoppe-vs-streamManager/memory/project_v10_rl_track.md` (UPDATED).
- `~/.claude/projects/C--Users-SeanHoppe-vs-streamManager/memory/project_v10_p4_hold_lifted.md` (UPDATED).

Repo-tracked LOC delta: docs only.

ADR-18 Amendment B self-applies: this PR carries the memory pre-
flight stamp + the stale-memory updates.

ADR-18 Amendment D self-applies (docs side): this PR carries the
relocated amendment body verbatim from staging draft (header date
2026-05-18 preserved). Cycle-conditional acceptance items
(bandit.py `is_ready_for_shadow_v10_1()`, train.py
`promotion_gate` envelope keys, phase-5 prompt re-mint, shadow
harness `--mode=v10.1` suffix, `check_criteria` filter,
`shadow_episodes.soak_run_id` schema verification,
`project_v10_p5_gate_deadlock.md` status flip to
AMENDMENT-LANDED) all DEFER to v2.5 because v2.4 is a
consolidation cycle and does not elect Path-D synthetic-fixture
P5 implementation (Seed v2.4-C deferred per §"P1 fire-order"
row 3).

ADR-18 Amendment E self-applies: this PR carries the Rule 5
cycle-handoff exemption clause with self-application breakdown.

External action (paired with this PR, executed at P0 merge):
- `gh issue close 111 --reason completed` with body linking PR
  #176 (cf7d003) + PR #179 (Amendment D landing) — Seed v2.4-B
  housekeeping.

## PHASE P1 — Sonnet-DIP investigation

Single prompt this cycle (consolidation gate excludes Path-D P5
+ CLI-timeout audit per fire-order conditional rows).

Prompt: `docs/prompts/v2.4-orchestration/task-sonnet-dip-
investigation.md` (TBD mint — may share P0 PR or follow-on PR).

Scope: identify which alignment-eval rows flipped v2.2 stable-
passing → v2.3 stable-failing. Output: 1 markdown report at
`reports/sonnet-dip-v23-v24.md` + recommendation (FREEZE-on-content
or FIRE-fix at v2.5). Disambiguate corpus-rot vs Sonnet-drift vs
CLI-degrade artefacts.

## PHASE P2 — ship-gate finalize

Prompt: `docs/prompts/v2.4-orchestration/phase-2-ship-gate-
finalize.md`. Fires after Sonnet-DIP investigation lands.

Folds Seed v2.4-H (lever-ledger scope binding) into ship-gate PR
body per `docs/v2.4-next-steps.md` fire-order row 7 footnote.

## DoD (this PR)

- [x] `docs/v2.4-task-plan.md` minted with all 7 operator
      decisions recorded.
- [x] Amendment D body relocated from staging → ADR-18 §Amendments
      (verbatim, with cycle-conditional acceptance items marked
      DEFERRED v2.5).
- [x] Amendment E minted in ADR-18 §Amendments with self-
      application breakdown (8 cap-counted / 6 exempt).
- [x] Staging file `docs/adr/ADR-18-amendment-d-draft.md` deleted.
- [x] Memory pre-flight stamp included (above).
- [x] Stale memories updated (`project_v10_rl_track.md` +
      `project_v10_p4_hold_lifted.md`).
- [x] `docs/v2.4-next-steps.md` §"P0 frame" checklist ticked
      with PR # cross-link (#179; substituted in follow-up commit
      `f8e5dbd` before review).
- [ ] Issue #111 auto-closes via PR body `Closes #111` at merge.
- [ ] Single PR `chore(v2.4-p0):` against `main` (trivially true
      at merge).

## P2 close-out (this PR — ship-gate finalize)

Tier-3 soak + alignment-eval landed 2026-05-19. Ship verdict: **PASS**.

- Soak report: `reports/soak-20260519T085540Z.md` (runtime 32.1 min,
  emitted=60 received=383, RSS drift +1.12 MB, canary degrade_count=0,
  verdict PASS).
- Alignment-eval: `reports/alignment-eval-20260519T101249Z.md` +
  `.json` sidecar; `--ci-gate` exit **0**. Sonnet **0.8261** (v2.3 =
  0.8182; STILL DIPPED, FREEZE-on-content recommendation HOLDS per
  S4 disposition for 0.80–0.85 band; Seed v2.4-D re-opens as v2.4-Q
  v2.5 seed). Haiku **1.0** (v2.3 = 0.9412; STABLE far off floor).
- 4-cycle Sonnet trajectory: 0.8636 → 0.9474 → 0.8182 → 0.8261.
  Stability denominator: 22 → 19 → 22 → 23.
- 4-cycle Haiku trajectory: 0.85 → 0.9412 → 1.0 (v2.3 piggybacked
  fresh post-RECOVERED baseline; v2.4 marks a second cycle off floor).
- Cycle-discipline LOC (Amendment C binding, manual bucket-scoped
  `git diff b35e982..HEAD -- src tests tools dashboard`): **+0 / -0 /
  +0 net** = **PASS** (consolidation gate ≤ 0). Narrative
  predecessor-tag diff `b00473d..HEAD` (same bucket): +216 / 0 /
  +216 (PR #176 v10 P4 trainer test files landed at predecessor
  tip; not load-bearing against gate).
- Soak summary's auto-rendered dual-anchor block recorded a false
  **BLOCK** (+747 / -1 / +746 vs cycle-tip) because
  `tools/soak_driver.py` `_git_diff_loc` uses an unscoped repo-wide
  `git diff --shortstat`. Bug latent since v2.3 Seed 4 landing
  (feature-cycle 2250 tolerance masked it). Filed as **Seed v2.4-O**
  in `docs/v2.4-next-steps.md` §"NEW v2.4 ship-gate seeds"; fix
  defers to v2.5 per P2 do-not-touch guard (no code edits at P2).
  Ship verdict on bucket-scoped manual computation, per P2 prompt
  S3 "Mismatch = report bug" clause.
- Seed v2.4-H scope binding: option (a) PRODUCTION SCOPE CANONICAL.
  `WIRED_LEVER_LEDGER_COUNT` = 1 (v2.3 Seed 6 JsonlTailWorker wire
  unchanged). Soak-scope ledger remains internal soak-driver
  artefact. No ADR-18 amendment minted.
- ADR-5 v2.4 baseline append: **SKIPPED** per P2 prompt §S7 default
  (consolidation cycle + cycle-tip bucket-scoped LOC delta = 0 + no
  latency lever wired this cycle). Soak per-band p95 within
  noise/recovery of v2.3 (overall p95 10.518 s vs 10.584 s; Δ
  −0.07 s; Seeds v2.4-E + v2.4-F watch).
- v10 P4 corpus piggyback: 240 (entering v2.4) → **360** (post v2.4
  P2 soak). `BRIDGE_RL_LOGGER_ENABLED=1` honored. +120 episodes.
- v2.4-next-steps.md row-by-row pass: 8 closed (Seeds v2.4-A, -B,
  -D, -H landed; -C, -G deferred; -E, -F watch); 6 Amendment E
  exempt (Seeds v2.4-I..N).
- 3 NEW v2.4 ship-gate seeds filed (Seed v2.4-O whole-repo bug
  🔴; Seed v2.4-P S1-wipe glob 🟡; Seed v2.4-Q Seed v2.4-D
  re-open 🟡) carry forward to v2.5.

Files changed (this PR):
- `docs/v2.4-task-plan.md` (this section appended).
- `docs/v2.4-next-steps.md` (row markers ticked + close-out tables
  filled + NEW v2.4 ship-gate seeds appended).
- `docs/v2.4-backlog.md` (MINT — carry-forwards from v2.4 to v2.5).
- `CHANGELOG.md` (`## [2.4.0]` appended).
- `reports/soak-20260519T085540Z.md` (Tier-3 soak artifact).
- `reports/alignment-eval-20260519T101249Z.md` +
  `reports/alignment-eval-20260519T101249Z.json`.

Files NOT changed (per P2 do-not-touch guard):
- `docs/adr/ADR-5-latency-budget.md` — skipped per S7 default.
- `tools/soak_driver.py` — Seed v2.4-O fix defers to v2.5 (production
  bucket touch out of P2 scope).
- All FROZEN surface — untouched.

Memory (local-only auto-memory, NOT in this PR):
- `~/.claude/projects/.../memory/project_v24_cycle_close.md` (MINT).
- `~/.claude/projects/.../memory/MEMORY.md` (index entry append).

## Refs

- `docs/v2.4-next-steps.md` (PR #178, the comparison anchor).
- `docs/prompts/v2.4-orchestration/phase-0-cycle-frame.md`.
- `docs/prompts/v2.4-orchestration/phase-2-ship-gate-finalize.md`
  (this PR's procedure source, PR #182).
- `docs/adr/ADR-18-mvp-surface-freeze.md` Amendments A / B / C /
  D / E.
- `project_v23_cycle_close.md`.
- `project_v10_p5_gate_deadlock.md`.
- Issue #177 (v10 P5 entry-gate deadlock; closed by Amendment D
  landing).
- Issue #111 (v10 P4 trainer; closed at P0 merge per Seed v2.4-B).
- Issue #112 (v10 P5 phase; remains blocked on Path-D P5 impl
  deferred to v2.5).
- Issue #131 (v10.x cycle frame; remains gated).
