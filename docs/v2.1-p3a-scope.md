# v2.1 P3a — PPP Layer 3 follow-ups (PR #145 review drain + ADR-18 Rule 4 amendment)

**Cycle:** v2.1 (feature)
**Phase:** P3a (sub-phase of P3 — drain review findings from PR #145 + retroactive LOC overage amendment per ADR-18 Rule 4)
**Branch:** `feat/v2.1-p3a-ppp-p3-followups`
**Base SHA:** `<P3 merge SHA>` (`origin/main` after PR #145 merged)
**Authoring date:** 2026-05-11
**Parent scope:** `docs/v2.1-p3-scope.md`

---

## 1. ADR-18 Rule 4 amendment (retroactive)

P3 (PR #145) landed at **1071 net LOC add** vs the 700 cap (371
over). Per ADR-18 Rule 4 the overage is recorded retroactively in
`docs/v2.1-p3-scope.md` §LOC tracker; the 3 review findings from
the cavecrew-reviewer pass are hoisted into a P3a sub-phase. P3a
does NOT count as a new phase against the v2.1 P1/P2/P3/P4 phase
budget (sub-phase of P3 per the pattern established in
`docs/v2.1-p1a-scope.md` and the v1.8/v1.9 P1a phase prompts).

Driver of the P3 overage: 594 LOC of security-critical test floor
across 5 test files (avg 119 LOC/file — self-monitor + decoy +
hallucination surfaces). Non-test code was 477 LOC, well under the
500-LOC implicit ceiling. The overage is test floor, not feature
scope creep.

Future tightening lever (cross-cycle, NOT in P3a): consolidate
decoy/PPP test fixtures into a shared `tests/conftest.py` PPP
fixture group (≈ −80 LOC). Already noted in
`docs/v2.1-p3-scope.md` §LOC tracker as P3 future work.

## 2. Items + R-register cross-reference

| # | Item | R-ref | Severity | Files |
|---|---|---|---|---|
| 1 | `register_decoy_stream` returns stale-row probe_id on re-register conflict; PR body claims "returns the existing row's metadata" but code silently returns fresh-minted probe_id while WAL still holds the original. Fix: on `first_write=False`, fetch existing row (`SELECT probe_id, registered_at, hmac_sig FROM provenance_decoys WHERE jsonl_path=?`) and return that authentic tuple. | R-decoy-idem | 🔴 bug | `src/stream_manager/governance.py:1488` |
| 2 | `write_provenance_decoy` returns bare `bool`; conflict-path caller has no atomic path to fetch the existing row. Expand return to `tuple[bool, str \| None, float \| None, str \| None]` (first_write, probe_id, registered_at, hmac_sig) so item 1 can serve authentic WAL data without a second round-trip. | R-decoy-bus-sig | 🟡 risk | `src/stream_manager/message_bus.py:1009` |
| 3 | Re-register test asserts only `first2 is False`; never asserts probe_id identity or registration-dict equality. Would have caught item 1. Add `assert probe_id2 == probe_id` + `assert reg2 == reg` + a separate test that re-register with a different probe_id-input still returns the WAL's original probe_id. | R-decoy-test-gap | 🟡 risk | `tests/test_audit_decoy_register.py:108-111` |
| 4 | Update `docs/v2.1-p3-scope.md` §LOC tracker with retroactive ADR-18 Rule 4 amendment (1071 actual vs 700 cap; 371 over). Mirror the entry style from `docs/v2.1-p1-scope.md` §LOC tracker post-P1 (which records the 1078/700 overage with the same retroactive-amendment note). | R-loc-amend | docs | `docs/v2.1-p3-scope.md` |

## 3. LOC budget

Soft target: ≤ 100 LOC net add. P3a is a bug-fix sub-phase, not a
new feature surface:
- Item 1: ~15 LOC (helper SELECT + branch in `register_decoy_stream`)
- Item 2: ~10 LOC (return-tuple expansion + 1 caller in
  `_record_decoy_envelopes` if it inspects the return; otherwise 0)
- Item 3: ~30 LOC (assertions + 1 new test fn)
- Item 4: docs-only (~15 LOC scope doc edit)

Total estimate: ~70 LOC net add. Hard ceiling 100 LOC; if exceeded,
file a P3b (NOT a P4 spillover — P4 is the ship-gate and must stay
ship-gate-only per cycle-frame).

## 4. Surface freeze guard

ADR-18 surface freeze remains in force. P3a touches ONLY:

- `src/stream_manager/governance.py` — item 1; additive (new
  helper) + one in-place branch in `register_decoy_stream`. The
  branch is a behavior-fix on the conflict path (currently returns
  garbage); not a non-additive seam change since callers that
  receive the previously-broken probe_id were broken to begin with.
  Disclose in PR body anyway per
  `feedback_subagent_stale_mental_model.md`.
- `src/stream_manager/message_bus.py` — item 2; signature widening
  of `write_provenance_decoy`. **This IS a non-additive seam
  change** (return-type narrowing → widening at the type level).
  Disclose up-front. Audit every call site:
  - `governance.py:1496` — only production caller; updated in
    item 1.
  - `tools/cassette_record.py:395` — cassette caller; check if it
    inspects the return.
  - `tests/test_audit_decoy_register.py:32,46,50,62,71` — test
    callers; update to unpack the tuple.
- `tests/test_audit_decoy_register.py` — item 3; additive
  assertions on existing test fn + 1 new test fn.
- `docs/v2.1-p3-scope.md` — item 4; docs only.

NO edits to other src/ files. NO test additions outside item 3.
NO new dashboard endpoints, no new envelope dataclasses, no new
WAL tables, no new `audit.*` types.

### Pre-flight grep

```
grep -nE 'write_provenance_decoy|register_decoy_stream' src/ tests/ tools/ dashboard/
```

Before P3a edits: hits in P3 scope only (governance.py:1488/1496,
message_bus.py:1009, tools/cassette_record.py:395, dashboard/server.py:1303,
tests/test_audit_decoy_register.py × 5, tests/test_audit_hallucination_detect.py × 4).
After P3a edits: same set; no new hits.

## 5. DOD

- [ ] All 4 items landed in one PR
- [ ] Full pytest suite green (target: 720 + 1 new = 721 passed)
- [ ] Re-register test asserts `probe_id2 == probe_id` AND `reg2 == reg`
- [ ] New test fn: re-register with a freshly-minted probe_id at
      the governance layer still returns the WAL's original probe_id
      (verifies item 1 fix end-to-end, not just at the bus layer)
- [ ] `write_provenance_decoy` signature widened; all 3 call-site
      classes updated (governance, cassette, tests)
- [ ] `docs/v2.1-p3-scope.md` §LOC tracker amended (item 4)
- [ ] FROZEN-seam diff guard: ONE disclosed non-additive hunk
      (`message_bus.py` return-type widening) in PR body
- [ ] LOC ≤ 100 net add
- [ ] Conventional commit prefix `fix(ppp):` (not `feat` — this is
      a bug-fix sub-phase)
- [ ] Single PR against `main`
- [ ] PR body cross-references each R-register entry resolved
- [ ] Cross-PR seam review: writer (`write_provenance_decoy`) ↔
      readers (governance, cassette, tests) all updated atomically
      per `feedback_cross_pr_seam_review.md`

## 6. P4 unblock

P3a is the last gate before P4 (ship-gate finalize). P4 prompt is
minted at P3a close-out per the just-in-time prompt-minting
pattern in `docs/v2.1-task-plan.md` §"P2 / P3 / P4 prompts minted
just-in-time".

P4 ship-gate inherits two carry-forward items from P3:
1. **Dormant observer decision** — `JsonlTailWorker.start()` still
   not wired into dashboard startup (carried from PR #143 + #145).
   P4 either wires it OR records the dormancy in
   `project_v21_cycle_close.md` with a v2.2 backlog seed. P3a does
   NOT touch this — out of scope.
2. **Tier 3 soak with PPP auto-probe default-on** — flip from
   P1's opt-in to default-on at the ship-gate. P3a does NOT
   touch this — out of scope.
