# v1.2 soak finalization plan

**Status:** v1.2.0 tagged at `69965aa` 2026-05-03 *without* Tier 3 ship-gate soak. ADR-5 latency numbers still reflect v1.1 baseline (`reports/soak-20260503T101758Z.md`). This doc tracks the work to close that gap and re-baseline ADR-5 against v1.2 code.

**Companion docs:**
- `docs/adr/ADR-17-soak-tiers.md` — three-tier model (replay / record-cassette / ship-gate)
- `docs/adr/ADR-5-latency-budget.md` — current latency budget (v1.1 re-baseline; will receive v1.2 §)
- `docs/v1.2-task-plan.md` — predecessor cycle (Tasks A–G)
- `docs/v1.2-followup.md` — code-quality ledger; non-blocking for soak finalization

**Action prompts:** `docs/prompts/v1.2-soak-finalize/task-m{1..6}-*.md` — hand each verbatim to a fresh Claude Code session.

---

## CRITICAL: do-not-touch list (v1.1 + v1.2 protected symbols)

These symbols are load-bearing and MUST NOT be modified during soak finalization. Diff PR head vs base on hot files before merge (memory: `feedback_subagent_stale_mental_model.md`).

| Symbol / file | Owning task | Cycle |
|---|---|---|
| `cli_pool`, `CliPool`, `CliWorker`, `.bridge/cli-pool.pids` | Task J | v1.1 |
| `_install_lazy_hydrator`, `GovernanceEngine.hydrated`, `tools/perf_probe.py` | Task I | v1.1 |
| `_run_sse`, `_consume_sse_stream`, `transport` kwarg, `/api/commands/stream` | Task K | v1.1 |
| `hitl_pending.matched_hash`, `dispatch_resolution` matched_hash read | Task L | v1.1 |
| `EngineRegistry.start_refresh/stop_refresh/refresh_all/refresh_status`, `last_refresh_ts`, `_refresh_timer` | Task M | v1.1 |
| `wirecli.py` module + `WireProtocolError`/`WireSchemaVersionError`/`WireTransportError` | Task N | v1.1 |
| `tools/soak_driver.py` `--cli-replay` flag, `src/stream_manager/cli_replay_pool.py`, `tools/cassette_record.py` | Task A | v1.2 |
| `LifecycleBridge` + `/api/lifecycle/jobs` + dashboard lifecycle pane | Task C | v1.2 |
| `_VALID_TRANSPORTS = frozenset({"sse"})`, long-poll removal in `desktop_command_consumer.py` | Task D | v1.2 |
| `Transport = Literal["wirecli"]`, `_JSON_REMOVED_MSG`, json transport refusal | Task E | v1.2 |

**Rule:** soak-finalize work is *measurement only*. No production code changes except (a) ADR-5 documentation update and (b) cassette fixture refresh. Any code regression surfaced by M1/M3 → file as separate fix PR, do not bundle.

---

## Task summary

| ID | Task | Tier | Effort | Depends on | Action prompt |
|---|---|---|---|---|---|
| M1 | Tier 1 replay smoke vs committed cassette | P0 | ½ session | — | `task-m1-replay-smoke.md` |
| M2 | Tier 2 cassette refresh on Haiku | P0 | ½ session | M1 PASS | `task-m2-cassette-refresh.md` |
| M3 | Tier 3 ship-gate soak (default model, --cli-pool-size 2) | P0 | 1 session | M2 PASS | `task-m3-shipgate-soak.md` |
| M4 | ADR-5 v1.2 re-baseline | P0 | ¼ session | M3 PASS | `task-m4-adr5-update.md` |
| M5 | Cycle-close commit + memory update | P0 | ¼ session | M4 merged | `task-m5-cycle-close.md` |
| M6 | Followup ledger triage → v1.3 backlog | P2 | ½ session | parallel | `task-m6-followup-triage.md` |

**Order:** strict M1 → M2 → M3 → M4 → M5. M6 may run anytime in parallel.

---

## TASK M1 — Tier 1 replay smoke

- Shipped 2026-05-03 @ `6350b96` (PR #48); report: `reports/replay-20260503T143136Z.md`

```
Run the existing committed cassette through soak_driver in replay mode
to confirm v1.2 plumbing (Tasks C, D, E) does not regress vs the
recorded baseline. This is the cheap gate before burning Haiku quota
on M2 or default-model quota on M3.

Reference:
- tools/soak_driver.py — has --cli-replay flag (Task A)
- tests/fixtures/soak_cassette_2026-05-03.jsonl — committed cassette
- src/stream_manager/cli_replay_pool.py — ReplayPool / ReplayWorker
- reports/soak-20260503T101758Z.md — v1.1 ship-gate baseline format

DO NOT modify any production code. M1 is measurement only.
If replay surfaces a regression, file a separate fix PR and stop M1.

Scope:

1. Run:
   python tools/soak_driver.py \
     --cli-replay tests/fixtures/soak_cassette_2026-05-03.jsonl

2. Capture report → reports/replay-<UTC-ts>.md
   Report header MUST record mode=replay, cassette path, miss-counter.

3. Verify in report:
   - Overall PASS
   - miss-counter == 0 (else cassette is stale → M2 required first)
   - Lifecycle bridge (Task C): bus envelopes for `claude_code_*` event
     types appear, no orphan start/end keys
   - SSE-only consumer (Task D): no long-poll fallback path hit
   - JSON transport (Task E): no `transport=json` envelopes in cassette
     should reach engine; if any present, ReplayPool returns canned
     response (cassette is pre-Task-E, OK)
   - p95 within ±5% of cassette recorded p95 (RELATIVE signal only)
   - RSS drift < 50 MB

4. Commit report under reports/. No other file changes.

DOD:
- reports/replay-<ts>.md PASS
- Comment on next-step PR (M2) linking the report
- If FAIL: file fix PR, do not proceed to M2
```

---

## TASK M2 — Tier 2 cassette refresh

- Shipped 2026-05-03 @ `2f8bdbe` (PR #49); cassette: `tests/fixtures/soak_cassette_2026-05-03-v12.jsonl`; latest pointer repointed; sanity replay: `reports/replay-20260503T144324Z.md`

```
Refresh the soak cassette on Haiku so it covers v1.2 surface (Tasks
C, D, E merged after the 2026-05-03 cassette was recorded). Output
is the new fixture committed to the repo + repointed `latest`
symlink/pointer file.

Reference:
- tools/cassette_record.py — recorder wrapper (Task A)
- tests/fixtures/soak_cassette_2026-05-03.jsonl — current pinned cassette
- tests/fixtures/soak_cassette_latest.jsonl — pointer file (if exists)
- docs/adr/ADR-17-soak-tiers.md §"Tier 2 — record-cassette"

DO NOT modify production code. M2 is fixture refresh only.
The cassette covers Haiku — its latencies are NOT comparable to
ADR-5 absolute targets (memory: see ADR-17 RELATIVE warning).

Scope:

1. Confirm `claude` CLI on PATH and BRIDGE_API_GOV=1 viable.
2. Run:
   python tools/cassette_record.py
   (forces --cli-pool-size 2, --model claude-haiku-4-5-20251001)

3. New cassette path: tests/fixtures/soak_cassette_<YYYY-MM-DD>.jsonl
4. Repoint tests/fixtures/soak_cassette_latest.jsonl → new file.
5. Run replay against NEW cassette:
   python tools/soak_driver.py --cli-replay tests/fixtures/soak_cassette_latest.jsonl
   Confirm PASS + miss-counter == 0.
6. Run pytest tests/test_soak_replay.py — must pass.

7. Commit:
   - tests/fixtures/soak_cassette_<date>.jsonl (NEW)
   - tests/fixtures/soak_cassette_latest.jsonl (POINTER)
   - reports/replay-<post-refresh-ts>.md (sanity replay)
   Commit msg: `chore(v1.2): refresh soak cassette for Tasks C/D/E surface`

DOD:
- New cassette includes lifecycle bridge envelopes (Task C)
- New cassette has zero `transport=json` envelopes (Task E)
- Pytest replay test green against new fixture
- M3 (ship-gate) NOT YET run — explicit gate before quota burn
```

---

## TASK M3 — Tier 3 ship-gate soak

- Shipped 2026-05-03 @ `a0dba4d` (PR #50); report: `reports/soak-20260503T145124Z.md`; verdict PASS, p50 4.134 s / p95 10.396 s / max 12.082 s; v1.2 latency improvement vs v1.1 across p50/p95/max

```
Run the v1.2 ship-gate soak. This is the SOLE source of truth for
ADR-5 absolute latency numbers. 30-min duration, default model, real
CLI subprocess pool. ~60 real `claude -p` calls.

Reference:
- tools/soak_driver.py — ship-gate path
- reports/soak-20260503T101758Z.md — v1.1 baseline (target format)
- docs/adr/ADR-5-latency-budget.md §"v1.1 ship-gate re-baseline"
- docs/adr/ADR-17-soak-tiers.md §"Tier 3"
- memory: feedback_soak_cli_pool_flag.md (--cli-pool-size 2 mandatory)

DO NOT modify production code. M3 is measurement only.

Pre-flight (subagent stale-mental-model defense):
1. git diff v1.2.0..HEAD -- src/stream_manager/cli_pool.py \
     src/stream_manager/governance.py \
     src/stream_manager/wirecli.py \
     src/stream_manager/desktop_command_consumer.py
   Confirm no protected-symbol drift since tag.
2. Confirm `claude` CLI on PATH.
3. Confirm BRIDGE_API_GOV=1 environment.

Scope:

1. Run:
   python tools/soak_driver.py --cli-pool-size 2
   (mandatory --cli-pool-size 2 — default 0 reproduces v1.0 cold-start
    regression per memory feedback_soak_cli_pool_flag.md)

2. Capture report → reports/soak-<UTC-ts>.md (auto-generated by driver).

3. Acceptance gates (FAIL any → see escalation below):
   - Overall PASS
   - 100% events via SSE
   - RSS drift < 50 MB
   - No uncaught exceptions in server log
   - ALLOW p95 within ADR-5 §v1.1 numbers (~6s pool-bound floor)
   - L2/L3 trigger p95 within ADR-5 §v1.1 numbers (~6s)
   - L4 alignment p95 ≤ 13.5s
   - NEW v1.2 expectation: lifecycle bridge events visible in
     dashboard log; no orphan start/end keys in `LifecycleBridge._seen`

4. Commit report. No other file changes in this PR.
   Commit msg: `chore(v1.2): ship-gate soak baseline for ADR-5 re-bind`

Escalation if FAIL:
- Latency miss → either (a) propose ADR-5 re-baseline (option B
  precedent from v1.1) with explicit justification, or (b) revert
  offending v1.2 task. Do NOT silently widen budget.
- Plumbing FAIL (RSS drift, exceptions, SSE gap) → file fix PR
  against main, DO NOT merge M3 report. Re-run after fix.

DOD:
- reports/soak-<ts>.md PASS with all gates green
- Report committed to main
- M4 unblocked
```

---

## TASK M4 — ADR-5 v1.2 re-baseline

- Shipped 2026-05-03 @ `7b7dc64` (PR #51); ADR-5 §"v1.2 ship-gate baseline" added citing M3 report; v1.1 budget table carried forward unchanged for v1.2 (measured ≤ budget with margin); REQUIREMENTS.md NOT bumped (untouched in v1.2 cycle; FR-OG-X session-picker drift flagged for M6)

```
Update ADR-5 with v1.2 ship-gate latency numbers from M3 report.
Cross-reference the M3 report path. Bump REQUIREMENTS.md spec version
pin if applicable.

Reference:
- docs/adr/ADR-5-latency-budget.md — current budget doc
- reports/soak-<m3-ts>.md — M3 output (source of truth)
- REQUIREMENTS.md — spec version pin
- docs/adr/ADR-17-soak-tiers.md §"Cassette is a *relative* signal"
  (only Tier 3 feeds ADR-5)

Scope:

1. Add new section to docs/adr/ADR-5-latency-budget.md:
   §"v1.2 ship-gate baseline" containing:
   - Source: reports/soak-<m3-ts>.md
   - Date, runtime, total events
   - p50 / p95 / max overall
   - Per-trigger split: ALLOW, L2/L3, L4 alignment
   - Delta vs v1.1 baseline (regression / improvement / parity)
   - Status: ACCEPTED as v1.2 budget

2. Status field at top of ADR-5: append v1.2 update line.

3. REQUIREMENTS.md: bump spec version pin if v1.2 added/changed any
   FR-OG-* entries (Tasks B/C added picker + lifecycle pane).

4. Commit msg: `docs(adr-5): re-baseline latency budget for v1.2`

DOD:
- ADR-5 §v1.2 section merged
- REQUIREMENTS.md version pin reflects v1.2 if needed
- Single commit, no code touched
```

---

## TASK M5 — Cycle-close commit

```
Final v1.2 close-out. Updates memory note that flagged the
"ship-gate NOT run" caveat, and marks v1.2 cycle fully shipped.

Reference:
- C:\Users\SeanHoppe\.claude\projects\C--Users-SeanHoppe-VS-streamManager\memory\project_v12_cycle_close.md
- docs/v1.2-soak-finalize.md (THIS doc)
- M3 report + M4 ADR-5 update (must be merged)

Scope:

1. Update memory file project_v12_cycle_close.md:
   - Remove "ship-gate Tier 3 soak NOT run" caveat
   - Add "ADR-5 re-baselined for v1.2 at <commit-sha>" line
   - Reference reports/soak-<m3-ts>.md

2. Update MEMORY.md index entry one-liner if hook changes.

3. Optional: tag v1.2.1 if any code shipped post-69965aa during
   M1–M4. Otherwise leave v1.2.0 as the cycle tag and note the
   close-out commits target main directly.

4. Commit msg: `chore(v1.2): cycle close — ship-gate soak verified`

DOD:
- Memory updated, no stale caveat
- Cycle marked complete
- v1.3 cycle unblocked
```

---

## TASK M6 — Followup ledger triage

```
Triage docs/v1.2-followup.md entries. Move 🔵 (low) items to a new
v1.3 backlog file. Keep 🟡 (medium) items if still open. Parallel-
safe with M1–M5.

Reference:
- docs/v1.2-followup.md — current ledger
- docs/v1.2-task-plan.md §"Task summary" — for v1.3 framing template

Scope:

1. Create docs/v1.3-backlog.md with sections:
   - "Carried from v1.2 followup" (🔵 items)
   - "Carried from v1.2 followup" (🟡 items still open)

2. Items to carry:
   - 🟡 list_active_jobs LIMIT * 4 overfetch (PR #43) — windowed query
   - 🔵 HookFolderPoller st_ino/st_mtime on Windows (PR #43)
   - 🔵 _record_event_type tool_result+background heuristic (PR #43)
   - 🔵 bus._conn.execute private attribute reach in tests (PR #43)
   - 🔵 CommandConsumer._process_one underscore-prefix test seam (PR #44)
   - 🔵 _VALID_TRANSPORTS + transport==long-poll dual check (PR #44)
   - 🔵 Transport = Literal["wirecli"] degenerate single-value (PR #45)
   - 🔵 wirecli_soak_compare.py output filename rename (PR #45)

3. Update docs/v1.2-followup.md:
   - Mark each carried item with `→ v1.3` link
   - Do NOT delete history (file is running ledger per its convention)

4. Commit msg: `docs(v1.3): seed backlog from v1.2 followup ledger`

DOD:
- docs/v1.3-backlog.md created
- v1.2-followup.md entries cross-linked to v1.3
- No items lost
```

---

## Convention

When an M-task ships, append outcome line under its header above:
`- Shipped <date> @ <commit-sha>; report: reports/<...>.md`. Do not
delete task blocks — file is the running ledger of the v1.2 close-out.
