# v2.7 task plan — feature cycle (Seed v2.6-G step (2) timeout-tighten lever-wire)

**Cycle theme: feature.** Operator-declared at P0 fire 2026-05-21. Soft
LOC target ≤ **1500** vs cycle-tip (v2.7 P0-merge SHA
`4902cca440b33c14fddd9357116923ae5fe1fa4b`), production bucket binding
per ADR-18 Amendment A 3-bucket measurement. BLOCK at 1.5× = **2250**.
Anchor per **Amendment C** = cycle-tip, NOT predecessor tag (v2.6.0 =
`c3a964c` retained as narrative comparator only).

**Cycle-type framing.** v2.6 was feature (Seed v2.5-G step (1)
instrumentation lever-wire; ledger 1 → 2; 3-cycle dormancy broken).
v2.7 is **2-in-a-row feature** — alternation hygiene argues
consolidation, but ADR-18 Amendment A §"Default lean rationale" permits
2-consecutive-feature when a ready FIRE candidate justifies the choice.
Seed v2.6-G step (2) timeout-tighten is evidence-ready: J2 audit
(`docs/seed-v2.6-g-step2-timeout-tighten-audit.md`, this PM-mint
session) recommends primary cap = **30 s** based on measured Sonnet
n=192 p99 = 25.048 s from v2.6 P1 wall-clock instrumentation. Step (2)
qualifies as production-bucket lever-wire on its own — and it is the
**first FROZEN-surface lever ever wired**
(`src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS`
re-classified for this single value-change per the J2 audit's
ADR-18-aware framing). Operator pick at this P0 fire records the
2-in-a-row election with rationale.

Headline scope this cycle:

1. **Seed v2.6-G step (2) timeout-tighten** — change
   `src/stream_manager/cli_governance.py:49`
   `TIMEOUT_SECONDS = 25.0` → `TIMEOUT_SECONDS = 30.0`. ≤ 20 LOC
   `src/` change (the constant edit + comment + any DoD test
   adjustments). Production-bucket lever-wire under Amendment A
   3-bucket measurement. First FROZEN-surface lever ever wired in
   any v1.x/v2.x cycle. Lever ledger BUMP **2 → 3** (production-
   bucket canonical) at v2.7 P1 merge. Closes Seed v2.6-A-T
   mechanically (5.04 s headroom above row-10 single-row p99
   24.96 s, well above 2 s close threshold) once Seed v2.6-A n=12
   re-measure fires at P2.

2. **Seed v2.6-A n=12 re-measure at v2.7 P2** — instrumented single-
   row alignment-eval of `frog7-wirecli-module-10` under the new
   30 s cap (post-P1 merge). Decision tree per
   `docs/seed-v2.6-a-row10-remeasure-protocol.md` §"Stability check":
   ≥ 9/12 INTERVENE → STABLE-CONTENT-DRIFT → golden-update path
   (Seed v2.6-A CLOSED); 6–8/12 INTERVENE → DIP-watch hold; < 6/12
   INTERVENE → spawn new seed; ≥ 6/12 NONE → halt for Seed v2.6-A-T
   (unlikely under new cap with ≥ 5 s headroom). Re-measure runs
   inside the v2.7 P2 alignment-eval window (~3.2–5 min add-on
   under existing soak envelope).

Seed v2.6-G step (3) env-split CARRIES independently to v2.8+ (J2
audit default given step (2) cap = 30 s: prod-vs-eval divergence
only +5 s does not justify env-split ops complexity). Seed v2.6-C
Path-D synthetic-fixture P5 (~600 LOC) **DEFERS to v2.8** (5th
consecutive deferral; lever-budget bound — single-lever fire keeps
the cycle envelope tight). Becomes **Seed v2.7-C** in
`docs/v2.7-backlog.md` at v2.7 P2 mint. v10 chain #112 → #131 →
#124 + #125 stays blocked on Path-D landing.

**P1/P2 fire-order (recorded at P0).** Per `docs/v2.7-next-steps.md`
fire-order table with feature-cycle dispositions applied:

1. **P0 cycle frame (this PR)** — operator-decisions recorded + this
   task-plan minted + `docs/v2.7-next-steps.md` decision blocks
   ticked + `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md`
   skeleton operator-pick blocks filled.
2. **v2.7 P1 — Seed v2.6-G step (2) timeout-tighten.** Prompt:
   `docs/prompts/v2.7-orchestration/phase-1-cli-timeout-tighten.md`
   (TBD mint ahead-of-fire per v2.4 PR #182 / v2.6 PR #195 / PR #197
   precedent). Scope: change `TIMEOUT_SECONDS = 25.0` →
   `TIMEOUT_SECONDS = 30.0`; ≤ 20 LOC `src/` change. First FROZEN-
   surface lever wire — DoD MUST cite the J2 audit recommendation,
   the v2.6 P1 instrumentation evidence, and the v2.6-A-T close-out
   path.
3. **Seed v2.6-C Path-D P5 — DEFERRED to v2.8** (5th consecutive
   deferral; lever-budget bound; v10.3 stochastic-propensities
   dependency still not in hand).
4. **v2.7 P2 — ship-gate finalize.** Prompt:
   `docs/prompts/v2.7-orchestration/phase-2-ship-gate-finalize.md`
   (TBD mint ahead-of-fire). S4 alignment-eval branches on n=3 vs
   n=6 per `feedback_alignment_eval_stability_window.md`: prior
   cycle v2.6 P2 Sonnet `pass_rate = 0.9412` ≥ 0.85 → default
   `--runs 3` applies (subject to unstable-count + per-row CLI-
   timeout-rate escape hatches per skeleton §"Alignment-eval n=6
   stability rule"). Seed v2.6-A n=12 re-measure fires inside the
   P2 alignment-eval window (single-row add-on under new cap).
   Seed v2.4-E (overall p95) + Seed v2.4-F (L4 half) close-votes
   fire at P2.

**Phase count.** P0 (frame) + **1 work phase** (P1 Seed v2.6-G step
(2) timeout-tighten) + P2 (ship-gate) = **3 numbered phases**, **1
work phase**. ADR-18 Rule 4 caps work-phase count at ≤ 3 — v2.7
sits well under at 1.

**v10.x slot.** Per #131 trigger cond 2, v10.x cycle blocked while
v2.x is in P0–P3 numbered phases. v2.7 numbered-phase span = P0 →
P2. v10.x slot also blocked on #112 (v10 P5) which remains blocked
at the **implementation side** (Seed v2.6-C deferred again in v2.7
— 5th consecutive). #131 remains gated.

**Companion docs:**

- `docs/v2.7-next-steps.md` — **the comparison anchor**. P2
  ship-gate walks this file row-by-row.
- `docs/v2.6-backlog.md` — backlog ground-truth at v2.6 P2 close
  (verified reconciled at this PR; no drift vs P0 skeleton
  §"v2.6 carry-forward dispositions" table).
- `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` — J2 evidence
  audit (this PM-mint session); cap value 30 s primary
  recommendation source. Cited by v2.7 P1 prompt at mint.
- `docs/seed-v2.6-a-row10-remeasure-protocol.md` — J3 re-measure
  protocol (this PM-mint session); fixture + runner invocation +
  decision tree + expected runtime. Cited by v2.7 P2 prompt at mint.
- `docs/seed-v2.5-a-row10-diagnosis.md` — v2.6 P2 S6.5 verdict
  (CONTENT-DRIFT); Seed v2.6-A + v2.6-A-T mint origin.
- `docs/v10-mvp-status.md` — J1 ledger refreshed in this PM-mint
  bundle (last-refresh date 2026-05-19 → 2026-05-21; v2.6 ship
  reflection; Seed v2.5-C → v2.6-C rename; deferral count 3 → 4).
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendments A–E (no new
  amendment minted this cycle).
- `docs/v2.6-task-plan.md` — predecessor cycle task plan.
- `docs/v2.6-next-steps.md` — predecessor cycle anchor.

**Reference docs (always available, never inlined):**

- `REQUIREMENTS.md` — no new FR row this cycle.
- `docs/adr/ADR-5-latency-budget.md` — v2.3 baseline current; v2.7
  P2 will likely append baseline because step (2) cap-tighten is a
  latency-surface change (production-path worst-case user wait
  moves 25 s → 30 s on the rare tail). Operator decides at P2 fire.
- `docs/adr/ADR-17-soak-tiers.md` — Tier 3 ship-gate at P2.

## Operator decisions recorded at P0 fire (2026-05-21)

Per `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` skeleton
operator-fill blocks:

1. **Cycle type: FEATURE.** 2-in-a-row feature permitted by
   Amendment A §"Default lean rationale" when a ready FIRE candidate
   justifies the choice. Seed v2.6-G step (2) is evidence-ready
   (J2 audit recommends cap = 30 s based on measured Sonnet n=192
   p99 = 25.048 s from v2.6 P1 instrumentation). Soft LOC ≤ 1500
   vs cycle-tip per Amendment A; BLOCK at 1.5× = 2250.

2. **`WIRED_LEVER_LEDGER_COUNT` posture: WIRE +1 at v2.7 P1.** Seed
   v2.6-G step (2) `TIMEOUT_SECONDS` cap-tighten qualifies as
   lever-wire under Amendment A 3-bucket production measurement.
   Target end posture: **3 production / 0 soak** (v2.3 Seed 6
   JsonlTailWorker wire unchanged + v2.6 Seed v2.5-G step (1) wire
   unchanged + Seed v2.6-G step (2) wire NEW). Soak-scope ledger
   remains internal artefact per Seed v2.4-H production-scope-
   canonical binding. **First FROZEN-surface lever ever wired** —
   `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS`
   single-value re-classified for this cycle per the J2 audit's
   ADR-18-aware framing.

3. **Rule 5 backlog cap reading: ACCEPT 6-CAP** (Amendment E §"Self-
   application"). Cap-counted = 6 (Seeds v2.6-A + v2.6-A-T + v2.6-C
   + v2.4-E + v2.4-F + v2.6-G). Exempt = 6 (Seeds v2.4-I..N,
   promotion-criterion-bound + demand-bound). Total open = 12 seeds
   entering v2.7 P0. No Rule 5 clarification amendment minted this
   cycle. Reading verified against `docs/v2.6-backlog.md`
   §"Carry-forwards from v2.6"; no drift.

4. **Seed v2.6-G step (2) fire decision: FIRE at v2.7 P1 with cap
   value 30 s.** J2 audit primary recommendation. Per
   `docs/seed-v2.6-g-step2-timeout-tighten-audit.md`
   §"Recommendation":
   - Clears measured Sonnet n=192 p99 (25.048 s) with ~5 s
     headroom (≈20% margin).
   - Closes Seed v2.6-A-T mechanically (5.04 s margin above row-10
     single-row p99 24.96 s; well above 2 s close threshold).
   - Substantial false-timeout-NONE reduction expected; the 14/32
     unstable Sonnet rows in v2.6 P2 should shrink to mostly
     content-divergent cases (the domain of J3's row-10 re-measure).
   - Eval-runtime worst-case +16 min absorbable (realistic Δ near
     zero — central tendency is far from cap).
   - Production-path worst-case user wait moves 25 s → 30 s only
     on the rare tail; soaks show this tail almost never fires
     (`degrade_count = 0` across all post-v2.4 soaks).
   - 30 s is conservative-but-credible end of the J2 30–45 s band;
     leaves room for v2.8+ tightening (e.g. back toward 28 s) if
     production data warrants.
   - Implementation: change `src/stream_manager/cli_governance.py:49`
     `TIMEOUT_SECONDS = 25.0` → `TIMEOUT_SECONDS = 30.0`. ≤ 20 LOC
     `src/` change including any DoD test adjustments.

5. **Seed v2.6-G step (3) env-split fire decision: CARRY
   independently.** J2 audit default given step (2) cap = 30 s.
   Rationale: at 30 s the prod-vs-eval cap divergence is only +5 s;
   ops complexity (two env vars, two code paths, two soak readings
   to validate) outweighs the user-wait benefit. Step (3) becomes
   v2.8+ candidate once step (2)'s effect on stability has settled.
   Step (3) NOT on the v2.7 critical path.

6. **Seed v2.6-C deferral: DEFER another cycle (5th consecutive).**
   Lever-budget bound — single-lever fire (Seed v2.6-G step (2)
   alone) keeps the feature-cycle LOC envelope tight (~20 LOC `src/`
   vs ~620 LOC if Path-D bundled). v10.3 stochastic-propensities
   dependency still not in hand. Seed v2.6-C becomes **Seed v2.7-C**
   in `docs/v2.7-backlog.md` (minted at v2.7 P2 ship-gate). Phase-5
   prompt re-mint per Amendment D gate-split also defers to v2.8 P0
   alongside the implementation. Re-evaluate at v2.8 P0 with v10.3
   stochastic-propensities status in hand.

7. **Seed v2.6-A re-measure decision: FIRE n=12 re-measure at v2.7
   P2** (J3 protocol default). Re-measure runs under the new 30 s
   cap (post-P1 merge) — cleaner read for both content-drift verdict
   AND Seed v2.6-A-T boundary-watch close-vote. Fixture re-uses
   `reports/seed-v2.5-a/row10-fixture.jsonl`; output to
   `reports/seed-v2.6-a/alignment-eval-<UTC>Z.{md,json}`. Single-row
   n=12 add-on at ~3.2–5 min fits inside the v2.7 P2 alignment-eval
   window without extending Tier-3 soak envelope.

8. **Amendment C cycle-tip anchor verbatim citation.** v2.7 P2
   ship-gate prompt cites
   `4902cca440b33c14fddd9357116923ae5fe1fa4b..HEAD` for the
   feature-cycle net-LOC gate measurement. Diff command:

   ```
   git diff 4902cca440b33c14fddd9357116923ae5fe1fa4b..HEAD --stat -- src tests tools dashboard
   ```

   Predecessor-tag narrative diff `c3a964c..HEAD` (same bucket)
   reported alongside for cycle-impact context but does NOT gate.

9. **Memory pre-flight (Amendment B).** Performed 2026-05-21 against
   current `main` at `c3a964c` (v2.6.0 ship). Stamp below.

## Memory pre-flight stamp (Amendment B)

Pre-flight performed 2026-05-21 against current `main` at `c3a964c`
(PR #198 squash-merge — v2.6 P2 ship-gate finalize). 6 load-bearing
memories verified per v2.7 P0 cycle-frame skeleton minimum re-read
list:

| Memory | Status | Action |
|--------|--------|--------|
| `feedback_certportal_dev_firewall.md` | FRESH (verified `.claude/settings.local.json` deny patterns + `CLAUDE.md` firewall language unchanged; system-reminder flagged 9-day age — content re-verified against current state, no drift) | None |
| `feedback_no_self_monitor.md` | FRESH (polarity-flip rule + `BRIDGE_SM_PROJECT_SLUGS` filter pattern unchanged in CLAUDE.md; 9-day age re-verified) | None |
| `project_v26_cycle_close.md` | FRESH (minted at v2.6 P2 ship-gate 2026-05-20; includes Seed v2.5-A CLOSED CONTENT-DRIFT + NEW Seed v2.6-A + v2.6-A-T facts + Sonnet n=192 p99 = 25.048 s reading + lever ledger BUMP 1 → 2 + 608-episode v10 P4 corpus piggyback) | None |
| `feedback_alignment_eval_stability_window.md` | FRESH (n=6 mandate rule body matches `--runs 6` branching in P0 skeleton §"Alignment-eval n=6 stability rule"; v2.7 P2 entry condition prior_sonnet = 0.9412 ≥ 0.85 → default `--runs 3` applies unless escape-hatches fire) | None |
| `feedback_soak_cli_pool_flag.md` | FRESH (`--cli-pool-size 2` flag still required; cited in P0 skeleton §"Canonical S2 env block" invocation pattern unchanged; 18-day age re-verified) | None |
| `feedback_glob_narrowing_no_op.md` | FRESH (PR #184 cassette; `git clean -df` pattern + tracked-status filter recorded; pattern unchanged) | None |

Memory-system 7-day age warning surfaced for
`feedback_certportal_dev_firewall.md` + `feedback_no_self_monitor.md`
(9 days) and `feedback_soak_cli_pool_flag.md` (18 days) per running
memory hygiene — text body re-verified against current repo state;
firewall + polarity-flip + cli-pool-size patterns unchanged; no
update required.

## Cycle-discipline guardrails

- **LOC budget (Amendment A + Amendment C).** Anchor = cycle-tip
  (v2.7 P0-merge SHA `4902cca440b33c14fddd9357116923ae5fe1fa4b`).
  Feature gate: **soft ≤ 1500 / BLOCK at 1.5× = 2250** across
  production bucket (`src/` + `tests/` + `tools/` + `dashboard/`).
  Docs bucket advisory per Amendment A 3-bucket measurement.

- **Production-bucket commitment.** v2.7 P0 (this PR) carries zero
  production-bucket delta (docs-only frame). P1 lever-wire
  commitment: Seed v2.6-G step (2) ~20 LOC `src/` change
  (`src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS`
  constant edit + any DoD test adjustments). P2 ship-gate adds
  zero production-bucket delta beyond the P1 work-phase scope
  (Seed v2.6-A n=12 re-measure is docs/reports only).

- **Rule 5 backlog cap.** Effective cap reading = **6** (cap-counted:
  Seeds v2.6-A + v2.6-A-T + v2.6-C + v2.4-E + v2.4-F + v2.6-G). 6
  exempt carry-forwards (v2.4-I..N) per Amendment E §"Self-
  application". v2.7 cycle work that opens any NEW backlog item
  beyond these 6 must close or graduate ≥ 1 existing seed before
  merging — OR add the new seed as cap-counted (2-cycle drift clock
  starts at first carry-forward).

- **Rule 6 memory pre-flight.** Performed at P0 (above stamp). All
  6 load-bearing memories FRESH; no updates required in this PR.

- **Lever ledger.** Entering posture **2 production / 0 soak** (v2.3
  Seed 6 JsonlTailWorker wire + v2.6 Seed v2.5-G step (1) wire).
  Target end posture **3 production / 0 soak** at v2.7 P2 close
  (Seed v2.6-G step (2) `TIMEOUT_SECONDS` cap wire NEW; first
  FROZEN-surface lever ever wired). Seed v2.4-H scope binding
  remains in force (production scope canonical).

- **Surface freeze.** ADR-18 Rule 1 in force.
  `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS = 25.0`
  is re-classified for this cycle per the Seed v2.6-G step (2)
  value-change (FROZEN-surface single-value lever-wire under the
  J2-audit ADR-18-aware framing). All other FROZEN surface
  untouched.

## PHASE P0 — this PR (docs frame)

Files changed (repo-tracked):

- `docs/v2.7-task-plan.md` (NEW; this file).
- `docs/v2.7-next-steps.md` (NEW; comparison anchor minted in this
  PM-mint bundle; P0 frame decision blocks ticked per operator
  picks above).
- `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` (NEW;
  skeleton minted in this PM-mint bundle; operator-decision blocks
  ticked + operator-pick rationale lines filled).
- `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` (NEW; J2
  evidence audit; cap = 30 s primary recommendation source).
- `docs/seed-v2.6-a-row10-remeasure-protocol.md` (NEW; J3 re-
  measure protocol; n=12 fixture + runner + decision tree).
- `docs/v10-mvp-status.md` (UPDATED; J1 ledger refresh — last-
  refresh date 2026-05-19 → 2026-05-21; v2.6 ship reflection; Seed
  v2.5-C → v2.6-C rename; deferral count 3 → 4; Run 7 piggyback
  608 episodes).
- `docs/2026-05-21-task-list.md` (NEW; this PM-mint session task
  list, internal hygiene).
- `docs/2026-05-21-implementation-notes.md` (NEW; this PM-mint
  session implementation notes, internal hygiene).

Files NOT changed (per ADR-18 surface freeze + cycle-discipline):

- `src/stream_manager/cli_governance.py` — `TIMEOUT_SECONDS = 25.0`
  STAYS FROZEN through P0; v2.6-G step (2) value-change lands at
  v2.7 P1.
- All other FROZEN surface — untouched per ADR-18 Rule 1.
- `tools/`, `tests/` production-bucket paths — zero delta this PR
  (Seed v2.6-G step (2) lands at v2.7 P1, NOT this P0 PR).

Repo-tracked LOC delta: docs only.

ADR-18 Amendment B self-applies: this PR carries the memory pre-
flight stamp (above).

ADR-18 Amendment C self-applies: cycle-tip anchor =
`4902cca440b33c14fddd9357116923ae5fe1fa4b` (v2.7 P0-merge SHA,
backfilled post-merge).

ADR-18 Amendment E self-applies: cap-counted = 6 reading recorded
above; verbatim external-trigger citations live in
`docs/v2.7-next-steps.md` (this PM-mint pre-satisfied).

## PHASE P1 — Seed v2.6-G step (2) timeout-tighten

Prompt: `docs/prompts/v2.7-orchestration/phase-1-cli-timeout-
tighten.md` (TBD mint ahead-of-fire per v2.4 PR #182 / v2.6 PR #195
/ PR #197 precedent).

Scope:

- Change `src/stream_manager/cli_governance.py:49`
  `TIMEOUT_SECONDS = 25.0` → `TIMEOUT_SECONDS = 30.0`. ≤ 20 LOC
  `src/` change (the constant edit + comment + any DoD test
  adjustments).
- Production-bucket lever-wire under Amendment A 3-bucket
  measurement; qualifies as lever-wire on its own.
- **FIRST FROZEN-surface lever ever wired** in any v1.x/v2.x cycle.
  The J2-audit framing treats this single value-change as ADR-18-
  aware (the FROZEN classification covers the symbol's existence
  and semantics; the value is the lever the audit was specifically
  designed to inform).
- Step (3) env-split (`BRIDGE_CLI_TIMEOUT` / `BRIDGE_CLI_TIMEOUT_EVAL`)
  CARRIES independently per P0 pick; NOT bundled this phase.
- Closes Seed v2.6-A-T mechanically once Seed v2.6-A n=12 re-measure
  at v2.7 P2 confirms row-10 p99 ≥ 2 s under new 30 s cap
  (expected: 5.04 s margin based on v2.6 P2 row-10 single-row
  p99 = 24.96 s).

DoD at P1 (preview):

- `TIMEOUT_SECONDS = 30.0` landed at line 49 of `cli_governance.py`.
- Cite J2 audit (`docs/seed-v2.6-g-step2-timeout-tighten-audit.md`)
  + v2.6 P1 instrumentation evidence
  (`reports/alignment-eval-20260520T205842Z.json`) +
  Seed v2.6-A-T close-out path in PR body.
- Lever ledger updated 2 → 3 (production-bucket canonical).
- Any unit/integration tests asserting the cap value updated to
  30.0 (or made cap-agnostic).
- ADR-18 surface-classification note: the J2-audit framing for
  this value-change is documented in the PR body (no new amendment
  required per skeleton §"ADR-18 reference block").

## PHASE P2 — ship-gate finalize

Prompt: `docs/prompts/v2.7-orchestration/phase-2-ship-gate-
finalize.md` (TBD mint ahead-of-fire per v2.4 PR #182 / v2.6 PR #197
precedent).

P2 procedure mirrors v2.6 P2 pattern:

- S1 wipe + S1.1 assertion (`git clean -df reports/` per
  `feedback_glob_narrowing_no_op.md`).
- S2 Tier-3 soak (Amendment C cycle-tip anchor binding) — canonical
  env block per P0 skeleton §"Canonical S2 env block" exports
  `BRIDGE_RL_LOGGER_ENABLED=1` + `BRIDGE_LOC_PATHSPEC=src/,tests/,
  tools/,dashboard/`. `--cli-pool-size 2` mandatory per
  `feedback_soak_cli_pool_flag.md`. Soak runs with new
  `TIMEOUT_SECONDS = 30.0` (production path under elevated cap;
  expect `degrade_count = 0` unchanged).
- S3 invariant + dual-anchor.
- S4 alignment-eval — `--runs 3` default (prior cycle v2.6 P2
  Sonnet `pass_rate = 0.9412` ≥ 0.85); escape-hatches per skeleton
  §"Alignment-eval n=6 stability rule" trigger n=6 if unstable-
  count or row-level CLI-timeout-rate exceed thresholds. With new
  cap = 30 s, false-timeout-NONE rate is expected to drop
  substantially; unstable_sonnet should shrink toward content-
  divergent cases only.
- S4.5 **Seed v2.6-A n=12 re-measure** — instrumented single-row
  alignment-eval of `frog7-wirecli-module-10` under new 30 s cap.
  Fixture `reports/seed-v2.5-a/row10-fixture.jsonl`; output
  `reports/seed-v2.6-a/alignment-eval-<UTC>Z.{md,json}`. Decision
  tree per `docs/seed-v2.6-a-row10-remeasure-protocol.md`
  §"Stability check".
- S5 LOC delta verify (Amendment C cycle-tip; dual-anchor with
  predecessor-tag narrative `c3a964c..HEAD`).
- S6 lever-ledger bump 2 → 3 (production; Seed v2.6-G step (2)
  `TIMEOUT_SECONDS` cap).
- S6.5 **Seed v2.6-A diagnosis verdict** — record golden-update
  vs DIP-watch hold vs verdict-diversity vs timeout-escalate per
  J3 decision tree.
- S6.6 **Seed v2.6-A-T close-vote** — record p99 under new cap
  and headroom; close if ≥ 2 s margin.
- S7 ADR-5 baseline append — operator decides at P2 fire (Seed
  v2.6-G step (2) is a latency-surface change; default disposition
  APPEND).
- S8 CHANGELOG append.
- S9 tag v2.7.0.
- S10 compare-back — walk `docs/v2.7-next-steps.md` row-by-row.
- S11 close memory — mint `project_v27_cycle_close.md`.
- S12 lifetime — default; v2.7 prompts persist as historical
  record.
- S13 mint v2.8 P0 frame ahead-of-fire (PM-mint precedent).
- Seed v2.4-E overall-p95 close-vote (≤ 8.2 s → close; ≤ 10 s for
  3rd consecutive cycle → downgrade narrative carry; > 10.156 s →
  regression-flag).
- Seed v2.4-F L4 half close-vote (≤ 17 s → close; ≤ 22 s → carry
  with reduced concern; ≥ 22 s → promote 🔴).
- v10 P4 corpus piggyback (Run 8) if `BRIDGE_RL_LOGGER_ENABLED=1`.
- `docs/v2.7-backlog.md` mint at P2 (carry-forwards to v2.8).

## P2 close-out (this PR)

<filled at v2.7 P2 ship-gate fire>

## Refs

- `docs/prompts/v2.7-orchestration/phase-0-cycle-frame.md` — this
  P0 skeleton (operator-decision blocks filled at this PR).
- `docs/v2.7-next-steps.md` — comparison anchor (decision blocks
  ticked at this PR).
- `docs/seed-v2.6-g-step2-timeout-tighten-audit.md` — J2 evidence
  audit; cap = 30 s primary recommendation source.
- `docs/seed-v2.6-a-row10-remeasure-protocol.md` — J3 re-measure
  protocol; n=12 fixture + runner + decision tree.
- `docs/v10-mvp-status.md` — J1 ledger (refreshed in this PM-mint).
- `docs/v2.6-task-plan.md` — predecessor cycle task plan.
- `docs/v2.6-next-steps.md` — predecessor cycle anchor.
- `docs/v2.6-backlog.md` — predecessor cycle backlog ground-truth.
- `docs/adr/ADR-18-mvp-surface-freeze.md` — Amendments A–E.
- `project_v26_cycle_close.md` — v2.6 cycle close memory (memory
  pre-flight reference).
- `feedback_alignment_eval_stability_window.md` — n=6 mandate
  memory (P2 S4 entry-condition).
- `feedback_certportal_dev_firewall.md` — SM dev firewall.
- `feedback_no_self_monitor.md` — polarity-flip rule.
- `feedback_soak_cli_pool_flag.md` — `--cli-pool-size 2` required.
- `feedback_glob_narrowing_no_op.md` — S1 wipe pattern.
- Precedent PRs: PR #178 (v2.4 PM-mint); PR #191 (v2.6 PM-mint);
  PR #193 (v2.6 P0 fire); PR #194 (v2.6 P0 SHA backfill).
