# v2.0 task plan — consolidation cycle

**Cycle theme: consolidation.** Zero new modules. Net-LOC delta target
**≤ 0** (deletion-positive). Three work phases. Two outstanding levers
disposed (revive-or-rip).

**Companion docs:**
- `docs/adr/ADR-18-mvp-surface-freeze.md` — minted in P0; FROZEN /
  EVOLVING / EXPERIMENTAL classification + falsify-before-extend rule
  + LOC budget + phase budget + backlog hard cap. Load-bearing for
  every other v2.0 phase.
- `docs/v2.0-backlog.md` — pre-seeded in v1.9 P0 with two carry-forward
  stubs (CLI pool sizing >2, PPP audit harness) plus 🟡 P1b cli_pool
  A/B + 🟡 Tier 1.5 smoke soak codification. P1b graduates to v2.0 P1;
  Tier 1.5 graduates to v2.0 P2. CLI pool sizing >2 disposes via
  ADR-18 Rule 5 (3-cycle cap → ADR disposition).
- `docs/v1.9-backlog.md` — predecessor (closed-out: v1.9 cycle).
- `docs/v1.9-task-plan.md` — format template.

**Reference docs (always available, never inlined):**
- `REQUIREMENTS.md` — current spec
- `docs/adr/ADR-5-latency-budget.md` — latency budget; v1.9 ship-gate
  baseline at HEAD. v1.9 §"Caveats" + lever-effect ledger is the
  load-bearing reference for P1 + P3 disposition.
- `docs/adr/ADR-17-soak-tiers.md` — three-tier soak model. v2.0 P2
  extends with Tier 1.5 + trigger matrix (additive only).
- `docs/adr/ADR-18-mvp-surface-freeze.md` — see above.
- `reports/soak-20260507T084933Z.md` — v1.9 ship-gate report (32.3
  min, `--cli-pool-size 2`, baseline for v2.0 lever-effect comparison:
  overall p95 11.064 s, ALLOW p95 ~6.5 s, L4 alignment p95 17.40 s
  (n=4), LM p95 15.11 s, `cli_pool_send_ms` p95 ~6.5 s,
  `cli_dispatch_fallback_ms` p95 0.00 ms, fallback fire rate 0% =
  DORMANT-2)
- `reports/p1a-corpus-haiku-verdicts-20260507T083813Z.md` — v1.9 P1a
  fresh-process Haiku probe: 100% BLOCK on wrapped destructive corpus
  at confidence ≥ 0.85; 96% BLOCK on bare probe. Falsifies
  corpus-weakness hypothesis; isolates dormancy cause to cli_pool
  warm-process reuse (untested in P1a).
- `src/stream_manager/cli_pool.py` — `CliPool`, `CliWorker`,
  `CliWorker.send`, worker reuse semantics. v2.0 P1 extends with
  optional `worker_recycle_every_n: int | None = None` kwarg only.
- `src/stream_manager/cli_governance.py` — fallback retry path; v1.7
  / v1.8 / v1.9 wiring. v2.0 P3 either keeps (revival) or rips.
- `src/stream_manager/governance.py` — `_evaluate_inner_core`
  pre-CLI dispatch site; v1.7 P2 fastpath unread here. v2.0 P3
  either wires (revival via P1b lever revival proves fallback works,
  fastpath gets revived in same cycle) or rips alongside.
- `src/stream_manager/model_router.py` — `RoutingDecision` field
  set; v1.7 P2 `fallback_model_id`. v2.0 P3 may delete this field if
  fastpath rips.
- `tools/soak_driver.py` — `--cli-pool-size`, `_L2_L3_TRIGGER`. v2.0
  P1 extends with `--worker-recycle-every-n` kwarg pass-through; v2.0
  P2 adds Tier 1.5 invocation shape.
- `tools/p1a_haiku_probe.py` — fresh-process probe; v2.0 P1 reuses
  the `_scrubbed_env` + `_wrap_user_prompt` helpers as the A/B "fresh"
  arm.
- Memory: `feedback_subagent_stale_mental_model.md`,
  `feedback_no_self_monitor.md`, `feedback_cli_over_sdk.md`,
  `feedback_soak_cli_pool_flag.md`,
  `feedback_cassette_must_cover_new_envelopes.md`,
  `feedback_cross_pr_seam_review.md`,
  `feedback_subagent_escape_hatches.md`,
  `feedback_subagent_long_task_abandonment.md`,
  `feedback_monitoring_live_sessions.md`,
  `project_v19_cycle_close.md`, `project_v10_rl_track.md`.

**Format:** mirrors `docs/v1.9-task-plan.md` — each phase block is
self-contained, hand verbatim to a fresh Claude Code session via the
matching prompt under `docs/prompts/v2.0-orchestration/`.

**Prior baseline:** v1.9.0 ship at `a7d0666`. Soak source of truth:
`reports/soak-20260507T084933Z.md`. Major findings:
1. Fallback lever (confidence + verdict) DORMANT-2 (0% fire rate two
   consecutive cycles).
2. Haiku fastpath router DORMANT-3 (0% fire rate three consecutive
   cycles since v1.7.0; flag computed but never read at pre-CLI
   dispatch site).
3. P1a probe falsifies corpus-weakness; isolates remaining hypothesis
   to cli_pool warm-process reuse.
4. ~2800 LOC delta in v1.9 — over the soft target now codified in
   ADR-18 Rule 3.

---

## CRITICAL: do-not-touch list — read from ADR-18

Per ADR-18 Rule 1, the FROZEN module list is the do-not-touch list.
Re-stating the v2.0-impacting rows here for fast reference; full list
lives in ADR-18 §"Initial classification".

| Symbol / file | State | Cycle source |
|---|---|---|
| `cli_pool`, `CliPool`, `CliWorker`, `CliWorker.send`, `.bridge/cli-pool.pids` | FROZEN | v1.1 Task J |
| `cli_governance.py` retry path + `BRIDGE_L4_FALLBACK_*` constants + `governance_fallback_routed` envelope | FROZEN | v1.7 / v1.8 / v1.9 |
| `governance._evaluate_inner_core` content-detection helpers (`_looks_ambiguous_block`, `is_ambiguous_block` / `is_hitl_synthesis` flag computation + `route()` kwargs) | FROZEN | v1.8 P1 |
| `model_router.route` band priority + `RoutingDecision` field set | FROZEN | NFR-M1-M5 + v1.7 P2 |
| `tools/soak_driver.py` `--cli-replay`, `--cli-pool-size`, `_L2_L3_TRIGGER`, `_ALLOW_PHASE_ORDER`, `_format_allow_phase_breakout` | FROZEN | v1.2 / v1.4-1.8 |
| `_last_phase_timings_ms` keys (v1.4 + v1.5 + v1.6 + v1.7 sets) | FROZEN | v1.4-1.7 (extend, never remove or rename) |
| All bus envelope types from v1.1-v1.9 | FROZEN | metadata-only extensions |

**Rule:** if a v2.0 phase needs to modify any FROZEN row, the
modification must be additive (new optional kwarg, new metadata field,
new sub-band that does not change priority order) per the existing
do-not-touch convention. Non-additive changes require an ADR-18
amendment **before** the phase opens.

**Rip exceptions:** ADR-18 Rule 2 (DORMANT-3 forces rip) overrides
FROZEN status for the specific lever being ripped. v2.0 P3 disposes
the Haiku fastpath router (DORMANT-3 entering v2.0). v2.0 P1 A/B
produced 0% fire rate at all four arms
(`reports/v2-p1-cli-pool-ab-20260507T141200Z.md`); revival
hypothesis falsified, granting anticipatory rip authority under
ADR-18 Rule 2 §"What counts as a strike". P3 rips both levers
(Haiku fastpath + verdict-fallback) in the same phase. Rip = remove
dead code, dead flags, dead instrumentation keys, AND the matching
do-not-touch entries; the ADR-18 classification table updates in P3.

---

## Task summary

| Phase | Title | Effort | Depends on | Action prompt |
|---|---|---|---|---|
| P0 | Cycle frame (THIS doc + ADR-18 + 5 phase prompts) | ¼ session | — | `phase-0-cycle-frame.md` |
| P1 | cli_pool worker A/B (per-turn isolation lever revival) | 1 session | P0 + ADR-18 merged | `phase-1-cli-pool-ab.md` |
| P2 | Tier 1.5 smoke soak codification + trigger matrix (ADR-17 amendment) | ½ session | P0 | `phase-2-tier-15-codify.md` |
| P3 | Haiku fastpath rip (DORMANT-3) + verdict-fallback rip (DORMANT-2 + P1 falsification → anticipatory rip authority) | ½ session | P1 merged | `phase-3-haiku-fastpath-disposition.md` |
| P4 | v2.0 ship-gate + ADR-5 v2.0 baseline + LOC delta gate + DORMANT-N WARN/BLOCK signal codification | ½ session | P1 + P2 + P3 merged | `phase-4-ship-gate-finalize.md` |

**Order:** P0 lands first (must include ADR-18). P2 (ADR-17 amendment)
is independent of P1 and may land in parallel. P1 must complete and
merge before P3 opens (P3's rip decision depends on P1 fire-rate
outcome). P4 cuts only after P1 + P2 + P3 are merged.

**P2-before-P1 preference:** P2 is half-session docs; P1 is full
session + ~128 min soak. Prefer landing P2 first so the trigger
matrix exists when P1 ships. If P1 lands first, P1 PR cites
matrix-exemption (P1 is the matrix's first canonical Tier 1.5
trigger; matrix is forward-looking from P2 merge per
`phase-2-tier-15-codify.md`).

**Phase budget:** 3 work phases (P1, P2, P3) — exactly at the ADR-18
Rule 4 cap. No P1a / P2a sub-phases without an explicit cycle-frame
amendment.

**LOC budget:** ≤ 0 net add (deletion-positive). P1 adds an optional
kwarg + ~50 LOC of A/B harness; P2 is docs-only (ADR-17 amendment +
trigger-matrix doc); P3 rips both Haiku fastpath router (~200 LOC
including `RoutingDecision.fallback_model_id` if unused after rip)
and verdict-fallback retry path (`cli_governance.py` retry branches +
`BRIDGE_L4_FALLBACK_*` constants + envelope emission +
`cli_dispatch_fallback_ms` key + dedicated test files). Net target:
~−700 LOC (per P1 report estimate at
`reports/v2-p1-cli-pool-ab-20260507T141200Z.md` §recommendation 3).

**Abandonment rule (RESOLVED):** P1 A/B produced 0% fire rate at all
four recycle cadences (status-quo / N=1 / N=5 / N=10). cli_pool
warm-process-reuse hypothesis falsified. Outcome documented at
`reports/v2-p1-cli-pool-ab-20260507T141200Z.md`; ADR-5 lever-effect
ledger updated. P3 rips both levers under ADR-18 Rule 2 anticipatory
rip authority. No P5 sub-phase minted — phase budget at cap; new
corpus-framing investigation lever lands in v2.1 backlog seed
(P4 §S8). `CliPool.__init__` `worker_recycle_every_n` default stays
`None` permanently (FROZEN signature per ADR-18 Rule 1; no flip).
P4 ship-gate soak invokes `--cli-pool-size 2` only — no
`--worker-recycle-every-n` (no cadence to promote).

If P2 trigger matrix introduces a Tier 1.5 gate that blocks
non-ship-gate PRs unexpectedly, ABORT the matrix landing and ship
Tier 1.5 as opt-in only.

---

## PHASE P0 — Cycle frame

**Pain point:** v1.9 cycle closed with two dormant levers (fallback
DORMANT-2, fastpath DORMANT-3), ~2800 LOC delta with no codified cap,
backlog rolling 🟢 *CLI pool sizing >2* for a third cycle, and a
do-not-touch list that grows linearly per cycle without consolidation.
v2.0 must mint the surface-freeze + falsify-before-extend rules
**before** any feature work, and frame v2.0 as a consolidation cycle.

**References:**
- `docs/v1.9-task-plan.md` — format template
- `docs/v2.0-backlog.md` — pre-seeded in v1.9 P0
- `reports/soak-20260507T084933Z.md` — v1.9 ship-gate (baseline context for P1)
- `reports/p1a-corpus-haiku-verdicts-20260507T083813Z.md` — falsification reference for P1 hypothesis
- Memory: `project_v19_cycle_close.md`, `feedback_cross_pr_seam_review.md`

**Do-not-touch guard:** P0 is docs only. Zero code edits.

**Scope:**
1. Create `docs/adr/ADR-18-mvp-surface-freeze.md` with FROZEN /
   EVOLVING / EXPERIMENTAL classification table + falsify-before-extend
   rule + LOC budget + phase budget + backlog hard cap.
2. Create `docs/v2.0-task-plan.md` (THIS doc).
3. Create `docs/prompts/v2.0-orchestration/phase-{0,1,2,3,4}-*.md` —
   five prompt files.
4. Confirm `docs/v2.0-backlog.md` exists and the two pre-seeded items
   match the cycle plan (P1b → v2.0 P1, Tier 1.5 → v2.0 P2). Do NOT
   mutate it (frozen-emoji rule).

**DOD:**
- [ ] ADR-18 created with full classification table covering all
      symbols on the v1.9 do-not-touch list
- [ ] `docs/v2.0-task-plan.md` created with P0 / P1 / P2 / P3 / P4
      blocks + LOC budget + phase budget declarations
- [ ] Five orchestration prompts created
- [ ] `docs/v2.0-backlog.md` UNCHANGED
- [ ] PR scope is docs-only — `git --no-pager diff origin/main..HEAD --stat -- src tests tools dashboard` empty
- [ ] Single PR against `main`

**Action prompt:** `docs/prompts/v2.0-orchestration/phase-0-cycle-frame.md`

---

## PHASE P1 — cli_pool worker A/B (per-turn isolation lever revival)

**Pain point:** v1.9 P1a falsified the corpus-weakness hypothesis (a
fresh-process Haiku BLOCKs 100% of wrapped destructive prompts at
confidence ≥ 0.85). The remaining hypothesis: cli_pool's long-lived
stream-json workers, reused across ~30 turns per soak, bias Haiku
toward conversational ALLOW on later destructive prompts despite
per-turn `session_id` rotation. v1.9 ship-gate still measured
`cli_dispatch_fallback_ms` p95 = 0.00 ms (DORMANT-2). v2.0 P1 tests
the hypothesis directly via worker recycle cadence A/B.

**References:**
- `docs/v2.0-backlog.md` §"🟡 v1.9 P1b — cli_pool worker per-turn
  isolation A/B" — full investigation scope, decision criteria,
  measurement plan
- `src/stream_manager/cli_pool.py` — `CliWorker` lifecycle, send loop;
  add optional `worker_recycle_every_n: int | None = None` kwarg to
  `CliPool` constructor only
- `src/stream_manager/cli_governance.py` — fallback retry path; UNCHANGED
- `tools/soak_driver.py` — accepts `--worker-recycle-every-n` and
  forwards to `CliPool`; `_L2_L3_TRIGGER` UNCHANGED
- `tools/p1a_haiku_probe.py` — `_scrubbed_env` + `_wrap_user_prompt`
  helpers; reuse via import for the A/B "fresh-process control" arm
- `reports/p1a-corpus-haiku-verdicts-20260507T083813Z.md` — fresh-process
  baseline (100% BLOCK at confidence ≥ 0.85)
- ADR-18 Rule 1 (FROZEN cli_pool surface — additive kwarg only) +
  Rule 2 (DORMANT-N counter)

**Do-not-touch (additive only):** `CliPool.__init__` kwargs extend with
`worker_recycle_every_n: int | None = None` (default `None` = status
quo). `CliWorker.send` signature UNCHANGED. Bus envelope schemas
UNCHANGED. Recycle decision lives inside `CliPool` (worker pool
manager), not inside `CliWorker.send`.

**Scope:**
1. Add `worker_recycle_every_n` kwarg to `CliPool.__init__`. When set,
   `CliPool` recycles the worker (kill + respawn fresh subprocess) after
   every N successful sends. Default `None` preserves v1.1 / v1.9 behaviour.
2. Forward kwarg through `tools/soak_driver.py --worker-recycle-every-n`.
3. Run four soak arms at ship-gate-comparable scale (60 events,
   `--cli-pool-size 2`):
   - Arm A (control): `--worker-recycle-every-n` unset (status quo).
   - Arm B: `--worker-recycle-every-n 1` (recycle every turn).
   - Arm C: `--worker-recycle-every-n 5`.
   - Arm D: `--worker-recycle-every-n 10`.
4. Measurement per arm: `cli_dispatch_fallback_ms` p95 + fire rate,
   `cli_pool_send_ms` p95 + spawn overhead delta, overall p95, ALLOW
   p95, L2/L3 verdict distribution on `_L2_L3_TRIGGER` corpus.
5. Author `reports/v2-p1-cli-pool-ab-<timestamp>.md` with the four-arm
   table + decision recommendation.
6. **Outcome (recorded):** all four arms produced 0% fallback fire
   rate. cli_pool warm-process-reuse hypothesis falsified;
   anticipatory rip authority granted under ADR-18 Rule 2. P3 rips
   both Haiku fastpath router + verdict-fallback retry path. Report
   at `reports/v2-p1-cli-pool-ab-20260507T141200Z.md`.

**DOD:**
- [ ] `CliPool` accepts `worker_recycle_every_n` kwarg; default
      preserves v1.9 byte-identical behaviour
- [ ] `tools/soak_driver.py --worker-recycle-every-n` forwards to pool
- [ ] Four-arm soak completes; report authored
- [ ] ADR-5 lever-effect ledger updated with v2.0 P1 outcome (per-arm
      fire rate)
- [ ] Tests cover the `None` default = status quo invariant + at least
      one non-None recycle case
- [ ] PR LOC delta ≤ 100 net add (kwarg plumbing + harness only)

**Action prompt:** `docs/prompts/v2.0-orchestration/phase-1-cli-pool-ab.md`

---

## PHASE P2 — Tier 1.5 smoke soak codification + trigger matrix

**Pain point:** ADR-17 defines three soak tiers but lacks (a) a
fast-soak variant between Tier 1 (replay, 0 quota) and Tier 3 (32 min,
full ship-gate), (b) an enforcement gate, and (c) a trigger matrix
mapping PR-touch paths to required tier. v1.9 plan-agent flagged this
as the cheapest reduction in soak-cost / regression-coverage gap.

**References:**
- `docs/v2.0-backlog.md` §"🟡 Tier 1.5 smoke soak — codification +
  trigger matrix" — proposed shape, 90s wall-clock, ~6 Haiku calls
- `docs/adr/ADR-17-soak-tiers.md` — additive amendment only (extend
  with Tier 1.5 section; do not modify Tier 1 / 2 / 3 definitions)
- `tools/soak_driver.py` — already supports `--total-events` +
  `--total-seconds`; Tier 1.5 is a parameter-set, not a new flag
- ADR-18 Rule 1 (ADR-17 is FROZEN; this is an additive amendment)

**Do-not-touch:** ADR-17 Tier 1 / 2 / 3 sections UNCHANGED. ADR-5
baseline source-of-truth UNCHANGED (Tier 3 only). `tools/soak_driver.py`
flag set UNCHANGED.

**Scope:**
1. Amend ADR-17 with §"Tier 1.5 — smoke soak (per-PR; pool-warmup gate)"
   section. Codify invocation: `python tools/soak_driver.py
   --cli-pool-size 2 --total-events 6 --total-seconds 120`. Codify
   binary gate: pool warmed AND clean shutdown — NOT a latency gate.
2. Author trigger matrix (PR-touch path → tier) and land at
   `docs/soak-trigger-matrix.md` (new doc, NOT a phase-prompts file).
   Mapping: changes to `src/stream_manager/cli_pool.py`,
   `src/stream_manager/cli_governance.py`,
   `src/stream_manager/governance.py` → Tier 1.5 required; ship-gate
   PR (release tag cut) → Tier 3; pure docs / tests / `tools/` →
   Tier 1 only.
3. Estimate token reduction at typical-cycle scale and record in the
   ADR-17 amendment.
4. **Do NOT** add a CI gate that runs Tier 1.5 automatically. Codify
   the matrix; enforcement is operator-driven this cycle. (CI gate
   lands as v2.1 backlog item if matrix proves stable.)

**DOD:**
- [ ] ADR-17 amended with Tier 1.5 section (additive)
- [ ] `docs/soak-trigger-matrix.md` created
- [ ] Token-reduction estimate recorded in ADR-17 amendment
- [ ] PR scope is docs-only — zero code hunks
- [ ] PR LOC delta ≤ 0 net add in `src/` + `tools/`

**Action prompt:** `docs/prompts/v2.0-orchestration/phase-2-tier-15-codify.md`

---

## PHASE P3 — Haiku fastpath rip + verdict-fallback rip

**Pain point:** Haiku fastpath router (`is_ambiguous_block` /
`is_hitl_synthesis` content-detection at the pre-CLI dispatch site)
has been wired-but-unread since v1.7.0 — DORMANT-3 entering v2.0.
ADR-18 Rule 2 mandates rip-or-revive at DORMANT-3. P1 A/B falsified
the cli_pool warm-process-reuse revival hypothesis for the
verdict-fallback lever (0% fire rate at all four cadences); ADR-18
Rule 2 §"What counts as a strike" grants anticipatory rip authority
in the same cycle. P3 rips both levers.

**References:**
- `docs/adr/ADR-18-mvp-surface-freeze.md` Rule 1 (FROZEN list) +
  Rule 2 (DORMANT-N + anticipatory rip authority) + lever ledger
- `reports/v2-p1-cli-pool-ab-20260507T141200Z.md` — REQUIRED
  CITATION
- `src/stream_manager/governance.py` — `_evaluate_inner_core`; site
  where `is_ambiguous_block` / `is_hitl_synthesis` are computed but
  never read at pre-CLI dispatch
- `src/stream_manager/cli_governance.py` — verdict-fallback retry
  branch; `BRIDGE_L4_FALLBACK_*` constants; envelope emission sites
- `src/stream_manager/model_router.py` — `RoutingDecision.fallback_model_id`
- `src/stream_manager/governance.py` — `_last_phase_timings_ms` dict (sole owner; no separate `engine.py`)
- ADR-5 §lever-effect ledger — append both rip events
- ADR-18 §"Initial classification" — move both rows to new
  §"Decommissioned" subsection

**Do-not-touch (rip exception):** ADR-18 Rule 2 grants rip authority
for both levers. Symbols ripped:
- Haiku fastpath: unread `route()` kwarg consumer at pre-CLI
  dispatch site (content-detection helpers stay — FROZEN per v1.8 P1,
  may be reused by future work).
- Verdict-fallback: `cli_governance.py` retry trigger logic
  (verdict + confidence branches), `_fallback_confidence_floor()`,
  `BRIDGE_L4_FALLBACK_CONFIDENCE`, `BRIDGE_L4_FALLBACK_MODE`,
  `governance_fallback_routed` + `governance_envelope_missing_confidence`
  envelope emission (envelope schemas stay on disk — append-only),
  `cli_dispatch_fallback_ms` key from `_last_phase_timings_ms`.

`cli_dispatch_fallback_ms` removal is the FIRST subtractive change
to `engine._last_phase_timings_ms` (FROZEN per ADR-18 Rule 1).
Requires explicit ADR-18 amendment authorising the change, co-located
in the same PR.

`cli_pool` UNCHANGED. Bus envelope schemas UNCHANGED (no removals).

**Scope:**
1. **Haiku fastpath rip:** delete unread `route()` kwarg consumer at
   pre-CLI dispatch site. `git grep fallback_model_id` to decide
   `RoutingDecision.fallback_model_id` survival (delete only if no
   other caller reads it).
2. **Verdict-fallback rip:** delete retry trigger branches, helper,
   env constants, envelope emission sites, `cli_dispatch_fallback_ms`
   key. Update `tools/soak_driver.py` `_ALLOW_PHASE_ORDER` if it
   lists the key.
3. **ADR-18 amendment** (co-located in same PR): authorise first
   subtractive change to `_last_phase_timings_ms`; document precedent
   that subtractive timing-key change is allowed ONLY for ripped
   levers under Rule 2.
4. **Lever ledger updates:** ADR-18 §"Initial classification" → move
   both rows to §"Decommissioned" with rip-date + rip-PR. Decrement
   `<!-- WIRED_LEVER_LEDGER_COUNT: 2 -->` HTML comment to `0`.
5. **ADR-5 lever-effect ledger:** append v2.0 P3 entries (both rip
   events; cite P1 report path).
6. **Search-and-update history references:** v1.7 / v1.8 / v1.9 task
   plan + phase-prompt do-not-touch rows annotated as "ripped at v2.0
   P3 — see ADR-18 §Decommissioned" (preserve history rows; do not
   delete).
7. **Net deletion target:** ~−700 LOC (per P1 report §recommendation
   3 estimate).

**DOD:**
- [ ] `reports/v2-p1-cli-pool-ab-20260507T141200Z.md` cited in PR
      description with verbatim quote of "anticipatory rip
      authority" sentence
- [ ] Ripped symbols removed from code + tests + docs (no orphan
      references; `git grep` clean for ripped names)
- [ ] ADR-18 §"Decommissioned" subsection added (both rows)
- [ ] ADR-18 `WIRED_LEVER_LEDGER_COUNT` HTML comment is `0`
- [ ] ADR-18 §"Amendments" entry authorising subtractive
      `_last_phase_timings_ms` change
- [ ] ADR-5 lever-effect ledger appended (both rip events)
- [ ] Net LOC delta strongly negative; target ~−700 LOC
- [ ] Alignment-eval `--ci-gate` exit 0 (rips must not regress
      alignment)
- [ ] Tier 1.5 smoke soak passes (per ADR-17 amendment from P2)
- [ ] No FROZEN-list non-additive change beyond authorised
      `_last_phase_timings_ms` key removal

**Action prompt:** `docs/prompts/v2.0-orchestration/phase-3-haiku-fastpath-disposition.md`

---

## PHASE P4 — v2.0 ship-gate + ADR-5 v2.0 baseline + LOC delta gate

**Pain point:** v2.0 must validate (a) the consolidation cycle's
LOC-delta target was met, (b) the lever rips did not regress
alignment or latency, (c) the DORMANT-N rule from ADR-18 has codified
WARN/BLOCK signal in the ship-gate script.

**References:**
- ADR-18 Rule 3 (LOC budget) + Rule 2 (DORMANT-N WARN/BLOCK signal)
- ADR-5 — append v2.0 ship-gate baseline section
- `tools/soak_driver.py` — Tier 3 invocation unchanged
- `tools/alignment_eval.py` — `--ci-gate` invocation unchanged
- v2.0 P1 + P2 + P3 reports

**Do-not-touch:** Tier 3 ship-gate invocation, alignment-eval gate,
ADR-5 baseline format. ADR-18 amendments only via explicit decision
(no silent edits).

**Scope:**
1. Run Tier 3 ship-gate soak: `python tools/soak_driver.py
   --cli-pool-size 2`. **No** `--worker-recycle-every-n` — P1
   falsified worker-recycle revival; no cadence to promote.
   `CliPool.__init__` default stays `None` (FROZEN per ADR-18
   Rule 1). Compute v2.0 baseline metrics; compare against v1.9
   baseline. Confirm soak report omits `cli_dispatch_fallback_ms`
   cleanly (key removed in P3).
2. Compute LOC delta:
   `git --no-pager diff a7d0666..HEAD --stat -- src tests tools dashboard | tail -1`.
   Target: ≤ 0. With P3's ~−700 LOC rip, delta lands deeply
   negative. If positive, ABORT ship and either land an ADR-18
   amendment or open v2.1 as forced consolidation cycle.
3. Codify DORMANT-N WARN/BLOCK in `tools/soak_driver.py` post-soak
   summary. Hard-code `WIRED_LEVER_LEDGER` dict; after P3 rips both
   levers it is empty. Empty-ledger output line:
   `Lever ledger: 0 wired levers — DORMANT-N gate inert`. Schema for
   future re-introductions (DORMANT-1 INFO / DORMANT-2 WARN /
   DORMANT-3 BLOCK + non-zero exit) documented as code comment;
   no active enforcement code while ledger empty. Additive output
   only — no behavioural change to soak driver itself. Drift-detection
   test asserts `len(WIRED_LEVER_LEDGER)` matches ADR-18
   `WIRED_LEVER_LEDGER_COUNT` HTML comment.
4. Update ADR-5 with v2.0 ship-gate baseline section. Append lever
   ledger with v2.0 P1 falsification + P3 rip outcomes.
5. Update CHANGELOG.md with v2.0.0 entry. Cycle close-out: tag
   v2.0.0; update memory `project_v20_cycle_close.md` (mint).
6. Seed v2.1 backlog with 🟡 corpus-framing parity (P1a fresh-process
   BLOCK 100% vs soak ALLOW 100% gap; cite P1 report §"What this A/B
   does not falsify").

**DOD:**
- [ ] Tier 3 soak verdict PASS; alignment-eval `--ci-gate` exit 0;
      0 FR-OG-7 regressions; 0 haiku regressions vs sonnet
- [ ] LOC delta ≤ 0 OR explicit ADR-18 amendment
- [ ] ADR-5 v2.0 baseline section appended
- [ ] DORMANT-N gate codified in soak driver post-soak summary
- [ ] CHANGELOG.md v2.0.0 entry written
- [ ] Memory mint: `project_v20_cycle_close.md`
- [ ] v2.0.0 tag on a `main` commit; ship PR merged
- [ ] v2.1 backlog created with carry-forwards if any

**Action prompt:** `docs/prompts/v2.0-orchestration/phase-4-ship-gate-finalize.md`
