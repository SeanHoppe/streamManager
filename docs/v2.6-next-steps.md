# v2.6 next steps — itemized fire-order

> Minted 2026-05-20 as part of v2.6-pm-mint PR (pre-P0). Mirrors
> `docs/v2.5-next-steps.md` + `docs/v2.4-next-steps.md` pattern.
>
> This file is the **comparison anchor** for every v2.6 fired prompt.
> When a v2.6 PR lands, the operator (or follow-up SM dev session)
> walks this file row-by-row, marks ✅ LANDED / ⏸ DEFERRED / ❌ DROPPED,
> and records the landing PR # next to the row.
>
> Predecessor: `docs/v2.5-next-steps.md` §"v2.5 P2 ship-gate close-out"
> + §"NEW v2.5 ship-gate seeds" + carry-forward dispositions. v2.5.1
> P2 closed Seed v2.4-Q RECOVERED at n=6 0.9375; NEW Seed v2.5-A
> filed. Entering count = **11 open going into v2.6 P0** (cap-counted
> 5 + exempt 6).
>
> ADR-18 Amendment C is law for v2.6 P2: cycle-discipline LOC gate
> binds at the **v2.6 P0-merge tip → ship-gate HEAD**. Predecessor
> tag `c1e9070` (v2.5.1) is narrative comparator only.
>
> **NEW headline scope this cycle (default-lean feature):**
> Seed v2.5-G CLI-timeout instrumentation step-(1) (the
> measurement-protocol opener; resolves Seed v2.5-A diagnosis blocker
> in lockstep), optionally bundled with Seed v2.5-C Path-D
> synthetic-fixture P5 if operator sequences both. Operator picks the
> final composition at v2.6 P0 fire — this PM-mint records the
> default lean and the fire-order skeleton.

---

## Fire-order (operator-bound at P0 mint; default-lean recorded at PM-mint 2026-05-20)

| # | Prompt file                                          | Sev | Default disposition                                | Compare-back row |
|---|------------------------------------------------------|-----|----------------------------------------------------|------------------|
| 1 | `phase-0-cycle-frame.md`                             | —   | FIRE FIRST (P0 fire PR; this PM-mint is pre-P0)    | §P0 frame        |
| 2 | `phase-1-cli-timeout-instrumentation.md` (TBD mint)  | 🔴  | FIRE iff cycle=feature AND Seed v2.5-G fire=YES    | §Seed v2.5-G     |
| 3 | `phase-1-shadow-synthetic.md` (TBD mint, P1a slot)   | 🟡  | FIRE iff cycle=feature AND Seed v2.5-C fire=YES    | §Seed v2.5-C     |
| 4 | `phase-2-ship-gate-finalize.md` (TBD mint)           | —   | FIRE LAST (after P1 / P1a merge; ahead-of-fire OK) | §P2 ship-gate    |

P0 = first. P2 = last. **Default feature composition:** 1 work
phase (Seed v2.5-G P1 only). **Maximal feature composition:** 2
work phases (Seed v2.5-G P1 + Seed v2.5-C P1a). Operator may also
elect 0 work phases (consolidation override) at P0 fire — see
§"Cycle-type fallback" below.

Footnote on rows 2 / 3 / 4: per v2.4 PR #182 / v2.5.1 PR #188
precedent, work-phase + ship-gate prompts may be minted ahead-of-fire
in a PM PR (not in this PM-mint). Operator decides mint timing at
P0 merge.

---

## Seeds (11 open going into v2.6 P0; ID-stable per v2.5.1 close)

### Seed v2.5-A — 🟡 `frog7-wirecli-module-10` 100% Sonnet timeout opacity (CARRY)

**Source:** `docs/v2.5-backlog.md` §"Seed v2.5-A";
`docs/v2.5.1-sonnet-floor-investigation.md` §"n=6 re-measure summary".

**Fact:** v2.5.1 P1 n=6 re-measure recorded `cli governance timeout
(>25.0s); degrading` on **6/6** Sonnet runs on row
`frog7-wirecli-module-10` (100% rate). The row's "stable NONE"
Sonnet majority is therefore 100% timeout-degradation artefact, NOT
content drift. Single-row evidence drove v2.5.1 P1 verdict-A
classification.

**Disposition v2.6:** ⏸ **WATCH 🟡**; resolves when Seed v2.5-G
timeout-instrumentation lands. Close-out path: instrumented row
runner re-measures row 10; verdict = content-drift OR
timeout-attributable. If §"Seed v2.5-G fire decision" = FIRE this
cycle, resolves same cycle.

**Compare-back row marker:** `[ ] Seed v2.5-A — re-measured at v2.6
P2 ship-gate (instrumented row runner): row 10 verdict (___);
content-drift / timeout-attributable. Carries to v2.7 if Seed
v2.5-G defers.`

### Seed v2.5-C — 🟡 Path-D synthetic-fixture P5 implementation (CARRY)

**Source:** `docs/v2.5-backlog.md` §"Seed v2.5-C"; issue #177
§"Disposition options" Path D; `project_v10_p5_gate_deadlock.md`.

**Fact:** P5 shadow-harness + ship-criteria checker land against
synthetic-fixture manifest (engineered `is_ready_for_shadow()=True`),
NOT against live-data manifest (gated on v10.3 per Amendment D).
~600 LOC; requires feature classification per Amendment A. Deferral
count entering v2.6: **2 cycles** (v2.4 + v2.5).

**Disposition v2.6:** TBD per §"Seed v2.5-C fire decision" in P0
frame. Default-lean (operator-bound at P0 fire): FIRE if Seed v2.5-G
also fires (2 work phases); DEFER if Seed v2.5-G alone fires (1 work
phase) — keeps LOC budget tight.

**Compare-back row marker:** `[ ] Seed v2.5-C — FIRE (v2.6 P1a PR
#___) / DEFERRED v2.7 per consolidation cycle choice / DEFERRED v2.7
per LOC budget. Becomes Seed v2.6-C if deferred.`

### Seed v2.4-E — 🟢 Overall p95 watch (CARRY)

**Source:** `docs/v2.5-backlog.md` §"Seed v2.4-E";
`reports/soak-20260520T115443Z.md` §"Latency".

**Fact:** v2.5.1 P2 p95 = 9.656 s (v2.4 P2 = 10.518 s, Δ −0.862 s
improvement; also Δ −3.501 s vs v2.5 P2 first-soak 13.157 s — which
now reads as transient variance outlier). NOT ≤ 8.2 s closure
threshold. Mild improvement direction holds.

**Disposition v2.6:** ⏸ **WATCH continues 🟢**. Re-confirm at v2.6
P2 ship-gate. If holds 1 more cycle ≤ 10 s, downgrade candidate
toward close. Closure still bound on ≤ 8.2 s threshold per Seed
v2.4-E original spec.

**Compare-back row marker:** `[ ] Seed v2.4-E — re-measured at v2.6
P2 ship-gate: p95 = ___ s; (closure / continue / regress) per ≤ 8.2 s
threshold + ≤ 10 s downgrade band.`

### Seed v2.4-F — 🟡 L4 + LM categorize p95 small-n watch + regression-flag (CARRY)

**Source:** `docs/v2.5-backlog.md` §"Seed v2.4-F";
`reports/soak-20260520T115443Z.md` §"Per-band latency".

**Fact:** v2.5.1 P2 L4 21.64 s (n=4; v2.4 P2 = 15.36 s, **Δ +6.28 s
regression-flag**); LM 25.26 s (n=10; v2.4 P2 = 13.60 s, **Δ +11.66 s
regression-flag**). Both regressed at the same small n. Not
autonomously promoted to 🔴 — small-n sample variance (L4 max=23.39 s
anchors p95) makes single reading insufficient.

**Disposition v2.6:** ⏸ **WATCH continues 🟡** with regression-flag.
v2.6 P2 ship-gate re-measure decides:

- If v2.6 reading returns to v2.4-P2 band (L4 ≤ 17 s, LM ≤ 15 s),
  retroactively mark v2.5.1 P2 as sample-variance outlier; downgrade
  to 🟢 or close.
- If v2.6 reading holds at v2.5.1 P2 elevated band, **promote to 🔴**
  and operator decides v2.7 latency-lever scope.

**Compare-back row marker:** `[ ] Seed v2.4-F — re-measured at v2.6
P2 ship-gate: L4 ___ s (n=___), LM ___ s (n=___); (downgrade / hold
🟡 regression-flag / promote 🔴) verdict.`

### Seed v2.5-G — 🔴 CLI governance timeout instrumentation (CARRY; FIRE candidate)

**Source:** `docs/seed-v2.4-g-cli-timeout-audit.md` (J2 evidence
audit, v2.5 P0); `docs/v2.5-backlog.md` §"Seed v2.5-G"; Seed v2.5-A
row-10 evidence.

**Fact:** `src/stream_manager/cli_governance.py:49` `TIMEOUT_SECONDS
= 25.0` shows real timeouts on alignment-eval probe path.
Measurement-protocol three-step plan recorded at v2.5 P0:

1. **Instrument first** — add per-run wall-clock timing to alignment-
   eval row runner (~30 LOC tooling); no FROZEN-surface touches.
2. **Then tighten** — pick `TIMEOUT_SECONDS` value in 30–45 s range
   based on measured eval p99.
3. **Then split** — `BRIDGE_CLI_TIMEOUT` (prod) +
   `BRIDGE_CLI_TIMEOUT_EVAL` (eval override) env-readable.

v2.5.1 P1 added empirical reinforcement: row `frog7-wirecli-module-10`
6/6 timeout (Seed v2.5-A namesake row). The strongest single-row
signal yet for the measurement-protocol stance.

**Disposition v2.6:** TBD per §"Seed v2.5-G fire decision" in P0
frame. **Default-lean (operator-bound at P0 fire): FIRE step (1)**
as v2.6 P1 (~30 LOC tooling; lever-wire that breaks consolidation
gate but well under feature soft cap). Resolves Seed v2.5-A
diagnosis blocker.

**Compare-back row marker:** `[ ] Seed v2.5-G — step (1)
instrumentation FIRE (v2.6 P1 PR #___) / DEFERRED v2.7 per
consolidation cycle choice. If FIRED: step (2) timeout-tighten
defers to v2.7 + measured eval p99 input; step (3) env-split may
land same phase or defers v2.7.`

### Seed v2.4-I — 🟢 Project-context rank + 400-token budget invariant test (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-I".

**External-trigger citation (verbatim per ADR-18 Amendment E
§"Acceptance"):** Promotion-criterion-bound — fires when project-
context ranking algorithm changes OR 400-token budget is altered.
Trigger has NOT fired in v2.4, v2.5, or v2.6 PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT). 2-cycle drift clock NOT started (trigger has
not fired).

**Compare-back row marker:** `[ ] Seed v2.4-I — promotion criterion
NOT fired in v2.6; carry forward to v2.7 (Amendment E EXEMPT,
promotion-criterion-bound).`

### Seed v2.4-J — 🟢 Learn-mode ALLOW-promotion regression coverage (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-J".

**External-trigger citation:** Promotion-criterion-bound — fires
when learn-mode ALLOW-promotion logic is touched in `src/`. Trigger
has NOT fired in v2.4, v2.5, or v2.6 PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT).

**Compare-back row marker:** `[ ] Seed v2.4-J — promotion criterion
NOT fired in v2.6; carry forward to v2.7 (Amendment E EXEMPT).`

### Seed v2.4-K — 🟢 Topology-discovery coverage (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-K".

**External-trigger citation:** Promotion-criterion-bound — fires
when topology-discovery code path is touched in `src/`. Trigger has
NOT fired in v2.4, v2.5, or v2.6 PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT).

**Compare-back row marker:** `[ ] Seed v2.4-K — promotion criterion
NOT fired in v2.6; carry forward to v2.7 (Amendment E EXEMPT).`

### Seed v2.4-L — 🟢 Out-of-scope guard scans (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-L".

**External-trigger citation:** Promotion-criterion-bound — fires
when out-of-scope guard logic is touched OR a new monitored project
slug is added. Trigger has NOT fired in v2.4, v2.5, or v2.6 PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT).

**Compare-back row marker:** `[ ] Seed v2.4-L — promotion criterion
NOT fired in v2.6; carry forward to v2.7 (Amendment E EXEMPT).`

### Seed v2.4-M — 🟢 "Governs messages, not transitions" boundary regression (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-M".

**External-trigger citation:** Promotion-criterion-bound — fires
when governance-boundary code (messages vs transitions distinction)
is touched in `src/`. Trigger has NOT fired in v2.4, v2.5, or v2.6
PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT).

**Compare-back row marker:** `[ ] Seed v2.4-M — promotion criterion
NOT fired in v2.6; carry forward to v2.7 (Amendment E EXEMPT).`

### Seed v2.4-N — 🟡 Remote-CLI monitoring extension (CARRY, EXEMPT)

**Source:** `docs/v2.5-next-steps.md` §"Seed v2.4-N".

**External-trigger citation:** Demand-bound — fires when a concrete
use case for remote-CLI monitoring surfaces (operator runs a
governed session on a non-local machine). Trigger has NOT fired in
v2.4, v2.5, or v2.6 PM-mint.

**Disposition v2.6:** ⏸ **NOT FIRED.** Carry forward to v2.7
(Amendment E EXEMPT).

**Compare-back row marker:** `[ ] Seed v2.4-N — no concrete use case
surfaced in v2.6; carry forward to v2.7 (Amendment E EXEMPT,
demand-bound).`

---

## P0 frame — operator-bound decisions (recorded at P0 fire; PM-mint default-lean below)

P0 cycle-frame fire PR records each of these. `docs/prompts/v2.6-
orchestration/phase-0-cycle-frame.md` skeleton (this PM-mint) bounds
the surface; this section is the **comparison checklist**. Decisions
verbatim in `docs/v2.6-task-plan.md` §"Operator decisions recorded
at P0 fire" (minted at P0 fire, not this PM-mint).

- [ ] **Cycle type.** **Feature** (default lean per PM-mint preamble;
      Seed v2.5-G + Seed v2.5-A 2-for-1 + alternation hygiene argues
      feature) OR **consolidation** (override; operator records why).
- [ ] **`WIRED_LEVER_LEDGER_COUNT` posture.** Currently 1 production /
      0 soak (v2.3 Seed 6 JsonlTailWorker production wire, held
      through v2.4 + v2.5 + v2.5.1). Feature cycle classification
      requires ≥ 1 new lever wired OR retains existing wired-lever
      count without rip. Default-lean: **WIRE +1** at Seed v2.5-G
      step (1) (production-bucket tooling counts as wire iff env-
      readable split lands same phase; operator confirms at P0 fire).
- [ ] **ADR-18 Rule 5 backlog cap.** ACCEPT 5-CAP reading per
      `docs/v2.5-backlog.md` §"Rule 5 backlog hard cap" reconciliation
      at v2.5.1 P2 close. Cap-counted = 5 (v2.5-A + v2.5-C + v2.4-E +
      v2.4-F + v2.5-G). Exempt = 6 (v2.4-I..N).
- [ ] **Seed v2.5-G fire decision.** Default-lean: FIRE step (1)
      instrumentation as v2.6 P1.
- [ ] **Seed v2.5-C fire decision.** Default-lean: TBD by operator.
      FIRE if both work phases sequenced; DEFER if Seed v2.5-G alone.
- [ ] **Seed v2.5-A carry-forward.** Resolves in lockstep with Seed
      v2.5-G fire; no standalone fix path.
- [ ] **Amendment C cycle-tip anchor verbatim citation.** Recorded in
      `docs/v2.6-task-plan.md` §"Operator decisions" #7; SHA
      `<v2.6 P0-merge SHA>` (backfilled post-merge per v2.5 P0
      precedent PR #186).
- [ ] **Memory pre-flight (Amendment B).** Performed at v2.6 P0 fire;
      stamp in `docs/v2.6-task-plan.md` §"Memory pre-flight stamp".
      Minimum re-read list per P0 frame § "Memory pre-flight" (6
      memories; +1 for newly-minted `feedback_alignment_eval_
      stability_window.md` from v2.5.1 P1).
- [ ] **v2.5-backlog reconciliation.** Verify
      `docs/v2.5-backlog.md` §"Carry-forwards" matches the
      §"v2.5 carry-forward dispositions" table in P0 frame; reconcile
      any drift before opening P0 fire PR.

---

## Cycle-type fallback (consolidation override)

If operator overrides default-lean to **consolidation** at P0 fire,
the following dispositions apply:

- Both work-phase rows (`phase-1-cli-timeout-instrumentation.md` +
  `phase-1-shadow-synthetic.md`) become **DEFERRED v2.7**.
- Seed v2.5-G + Seed v2.5-C both rename to Seed v2.6-G / v2.6-C and
  carry to v2.7 P0.
- Cycle becomes 2-numbered-phase (P0 + P2 ship-gate), 0 work phases.
- Lever ledger HOLD at 1 production / 0 soak (unchanged).
- Operator records the override rationale in `docs/v2.6-task-plan.md`
  §"Operator decisions" #1 (typical: scope uncertainty, dependency
  on v10.3 stochastic propensities not yet available, lever-budget
  bound, etc.).

This fallback exists because alternation hygiene is NOT law; ADR-18
Amendment A §"Default lean rationale" permits two-cycle-in-a-row of
the same type when the candidate-lever backlog has no ready entry.
Seed v2.5-G is ready; Seed v2.5-C is ready iff v10.3 dependencies
are accounted for. Operator judges at P0 fire time.

---

## Compare-back protocol (per-landing)

When a v2.6 PR lands:

1. Find the seed row this PR addresses (use compare-back row marker).
2. Mark `[x]` and append `LANDED PR #N (<merge-SHA>)`.
3. If the PR addressed a seed that was NOT in the fire-order, append
   `OFF-SCOPE — operator-bound; recorded for narrative only`.
4. If the PR closed a 🔴 row → drop the 🔴 prefix in the v2.6 backlog
   carry-forward at v2.6 P2 close-out.
5. At v2.6 P2 ship-gate close memory, re-run row-by-row pass: every
   row must be ✅ LANDED, ⏸ DEFERRED v2.7 with rationale, or ❌
   DROPPED with rationale.

Strict invariant: **no v2.6 PR is "done" until this file is updated**.

---

## ADR-18 hygiene at mint time (this file)

- `docs/` bucket only. Not load-bearing under Amendment A.
- No `src/` or `tests/` change. No surface delta.
- Memory pre-flight stamp NOT required for this PM-mint per v2.4
  PR #178 precedent (memory stamp lives in P0 fire PR, not PM-mint).
- Amendment E §"Acceptance" final row pre-satisfied: verbatim
  external-trigger citations recorded for every exempt seed
  (v2.4-I..N) at PM-mint time.

---

## Refs

- `docs/prompts/v2.6-orchestration/phase-0-cycle-frame.md` (this PM-
  mint; the skeleton).
- `docs/v2.5-next-steps.md` (predecessor comparison anchor).
- `docs/v2.5-backlog.md` (v2.5.1 P2 close-out backlog ground truth).
- `docs/v2.5-task-plan.md` §"v2.5.1 P2 close-out" (predecessor P2
  closeout reference).
- `docs/seed-v2.4-g-cli-timeout-audit.md` (J2 audit; Seed v2.5-G
  evidence, renamed from v2.4-G).
- `docs/v2.5.1-sonnet-floor-investigation.md` (Seed v2.5-A row-10
  evidence; v2.5.1 P1 verdict-A investigation).
- `docs/adr/ADR-18-mvp-surface-freeze.md` Amendments A / B / C / D
  / E.
- `project_v25_cycle_close.md` (v2.5.1 cycle close memory).
- `feedback_alignment_eval_stability_window.md` (n=6 mandate memory).
- `feedback_certportal_dev_firewall.md` (dev firewall).
- `feedback_no_self_monitor.md` (polarity-flip rule).
- `feedback_soak_cli_pool_flag.md` (`--cli-pool-size 2` required).
- Precedent PM-mint PR: PR #178 (v2.4 PM-mint).
