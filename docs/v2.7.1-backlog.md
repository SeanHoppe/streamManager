# v2.7.1 backlog

Carry-forwards from v2.7.1 P2 ship-gate (2026-05-22) into v2.8. v2.7.1
was a **corrective sub-cycle** (P1 row-05 Haiku-stabilisation re-eval +
P2 ship-gate re-fire under Hatch B per-row exclusion). v2.7.0 NEVER
tagged (parent v2.7 P3 BLOCKED at S4 CI gate); v2.7.1 is the carry tag
(v1.3.1 / v2.5.1 precedent). Cycle-tip anchor INHERITED from v2.7 P0
PR #200 merge SHA `4902cca440b33c14fddd9357116923ae5fe1fa4b` per
ADR-18 Amendment C + v2.5.1 precedent (no re-pin at sub-cycle). Cycle-
tip production-bucket delta vs `4902cca` under Amendment C: **+42 /
−22 / +20 net LOC** (binding gate PASS at soft ≤ 1500). PASS.
`WIRED_LEVER_LEDGER_COUNT` **HOLD at 3 production / 0 soak** through
v2.7.1 (re-measure is measurement, not wire; no golden flip under
Hatch B).

Severity emoji convention is **frozen at original review time** per
the `docs/v1.3-backlog.md` convention — re-triage by adding a v2.8
task, not by editing the emoji.

---

## Carry-forwards from v2.7.1 (dispositions at v2.7.1 P2 ship-gate)

Per `docs/v2.7.1-next-steps.md` + this ship-gate phase §S10/S11.
Items surfaced through v2.7 + v2.7.1 cycles that v2.8 inherits:

1. ⏸ **Seed v2.6-C (🟡 Path-D synthetic-fixture P5 implementation)** —
   DEFERRED through v2.7.1 (6th-consecutive deferral; v2.4 + v2.5 +
   v2.5.1 + v2.6 + v2.7 + v2.7.1 all skipped). ~600 LOC requires
   feature classification per Amendment A. PROMOTED candidate for
   v2.8 P1 FIRE under Convergence-cycle proposal (status doc
   `docs/2026-05-22-status.md` §"Proposal — Convergence cycle"
   landing #1). Amendment D v10.1-mode entry gate covers fire under
   v10.3 stochastic-propensity uncertainty. Re-evaluate at v2.8 P0.

2. ⏸ **Seed v2.6-G step (3) env-split** — DEFERRED through v2.7.1.
   step (2) timeout-tighten landed at v2.7 P1 PR #203 (`28a89c4`,
   ledger BUMP 2 → 3); step (3) env-split splits
   `cli_governance.TIMEOUT_SECONDS` into `BRIDGE_CLI_TIMEOUT` (prod,
   default 30 s) + `BRIDGE_CLI_TIMEOUT_EVAL` (eval, default 60 s).
   ~50 LOC `src/stream_manager/cli_governance.py` + tests. PROMOTED
   candidate for v2.8 P2 FIRE under Convergence-cycle proposal
   (landing #2). Falsified J2 audit at v2.7 P2 + cap-clip
   corroboration at v2.7.1 P1 row-05 reading (Sonnet 2/12 NONE
   timeouts) reinforce the structural-fix need.

3. ⏸ **Seed v2.6-A (🟡 `frog7-wirecli-module-10` Sonnet content
   drift)** — CARRY from v2.7 P2 STILL-100%-TIMEOUT-ESCALATE verdict
   (PR #206 `59bee5c`). v2.7.1 P2 inherits unchanged. v2.8 P2
   Convergence cap-clip re-measure at eval-cap=60s expected to
   falsify content-drift verdict (root cause = cap-clip not drift).
   CLOSE candidate at v2.8 P2 re-measure outcome.

4. ⏸ **Seed v2.6-A-T (🟡 cap-tighten margin)** — CARRY jointly with
   Seed v2.6-A. Margin −0.092 s at v2.7 P2 (NEGATIVE — the cap was
   tightened but the row STILL clipped at the new cap). v2.8 P2
   re-measure at eval-cap=60s expected to make margin moot (cap-
   clip not real signal). CLOSE jointly with v2.6-A at re-measure
   verdict.

5. ⏸ **Seed v2.7-A-CLIP (🔴 corpus-wide cap-clip artefact)** —
   ELEVATED at v2.7 P3 from "hypothesised at v2.7 P2" to "confirmed
   corpus-wide"; CORROBORATED on a 5th row (row-05) at v2.7.1 P1
   reading (Sonnet 2/12 NONE timeouts at p95 = 30.054 s / p99 =
   30.061 s). PROMOTED candidate for v2.8 P2 FIRE under
   Convergence-cycle proposal (landing #3). Corpus re-measure at
   eval-cap=60s surfaces true Sonnet response distribution.
   Protocol: `docs/seed-v2.7-a-clip-corpus-protocol.md` (J5 PM-mint
   at PR #208).

6. ⏸ **Seed v2.7-B (🟡 `frog7-phase-timings-keys-05` Haiku-
   stabilisation regression) — CARRY-WITH-EXCLUSION from v2.7.1 P1
   Hatch B disposition.** n=12 re-measure (PR <P1-PR-#>,
   `<merge-SHA>`) yielded:

   - **Sonnet:** SUGGEST=7 / ALLOW=3 / NONE=2 (n=12). Counted-
     majority SUGGEST matches golden `expected_verdict=SUGGEST`.
     Unstable (7/12 in 6–8 range). 2/12 cap-clip NONE timeouts
     (Sonnet-side cap-clip corroborates v2.7.1 P1 sister finding
     for Seed v2.7-A-CLIP).
   - **Haiku:** ALLOW=7 / SUGGEST=4 / GUIDE=1 (n=12). Counted-
     majority ALLOW diverges from golden SUGGEST. Unstable (7/12
     in 6–8 range). 0/12 cap-clip (well under 30 s cap).
   - **Hatch verdict B** per J1 + J3 stability-count semantics
     (Haiku stable-count 7/12 in 6–8 → per-row exclusion). v2.7 P3
     n=6 unanimous Haiku-ALLOW reading was the high-side tail of
     an underlying unstable distribution, not stable server-side
     drift.

   Disposition at v2.7.1 P2: row-05 EXCLUDED from S4 gate
   denominator under hatch (4); NO golden flip; CARRY-WITH-
   EXCLUSION to v2.8. Structural resolution (B-i golden
   recalibration with new `haiku_known_diverge` field, ~+10 LOC
   `tools/alignment_eval.py`) DEFERRED to v2.8 P2 (rides on top of
   step (3) env-split PR).

7. ⏸ **Seed v2.4-E (overall p95 watch)** — RE-CLASSIFY 🟡 at v2.7.1
   P2 reading **10.480 s** (Δ +0.554 s vs v2.6 P2 9.926 s; ABOVE
   regression-flag threshold 10.156 s by +0.324 s). 5-cycle
   trajectory: 10.584 → 10.518 → 9.656 → 9.926 → 10.480 s. The
   sub-10 streak ends at v2.7.1. Note: v2.7 P3 overnight soak
   reported 13.957 s — but that was dominated by a single LM-band
   outlier (max 6974 s; n=10 in LM band amplifies one outlier into
   ~5594 s p95 contribution). This v2.7.1 P2 soak shows LM p95
   12.37 s (well-behaved), so the 10.480 s reading is the "real"
   post-LM-exclusion overall p95.

   Disposition v2.8 P3: WATCH continues; **regression-flag is
   tripped this cycle** (10.480 > 10.156). Operator may classify
   this as either (a) ADR-18 Amendment A's "regression-flag
   tripped → close-vote re-opens in next cycle" or (b) sample-
   variance + Convergence-cycle absorption (the step (3) env-split
   may move the regression cause as a side effect because the eval-
   cap widening exposes longer Sonnet responses that would
   otherwise be cap-clipped). Decision at v2.8 P3 ship-gate read.

8. ⏸ **Seed v2.4-F (L4 half watch)** — RE-CLASSIFY 🟡 at v2.7.1 P2
   reading **22.49 s** (n=4). Δ vs v2.4-P2 baseline 15.36 s = +7.13
   s elevated. **promote-🔴 line ≥ 22 s is breached this cycle**.
   v2.7 P3 overnight reading was 12.66 s recovered — but the n=4
   sample size is small enough to bounce between cycles. Closure
   threshold ≤ 17 s not met.

   Disposition v2.8 P3: WATCH continues at 🟡; **promote-🔴 is
   tripped this cycle** (22.49 ≥ 22). Operator may classify as
   sample-variance (n=4 is small) and absorb into Convergence
   cycle's step (3) env-split side-effect window OR file a
   stand-alone investigation seed. Decision at v2.8 P3 ship-gate
   read.

9. 🟢 **Seed v2.4-F (LM half)** — CLOSED RECOVERED at v2.6 P2
   (per `docs/v2.6-backlog.md` §3). v2.7.1 P2 soak LM p95 12.37 s
   confirms LM band stayed recovered (within ≤ 15 s carry band).
   v2.7 P3 overnight LM reading (5593 s) is now positively
   confirmed as single-outlier sampling artefact per status doc
   OQ-2 hypothesis. No further v2.4-F LM tracking needed.

10. ⏸ **OQ-1 (soak-side lever ledger reads 0 vs docs ledger 3)** —
    v2.7.1 P2 soak (this cycle) again reports `Lever ledger: 0
    wired levers — DORMANT-N gate inert`. Docs ledger is 3
    production. Status doc §"Open follow-ups" OQ-1 carry-forward
    confirms instrumentation gap (soak driver's lever-counter does
    not detect the FROZEN-surface wires landed at v2.3 PR #157 +
    v2.6 P1 PR #196 + v2.7 P1 PR #203). v2.8 carry-forward;
    diagnostic file as standalone v2.8 P2 or v2.9 P1 candidate
    (instrumentation only; ≤ 20 LOC `tools/soak_driver.py`).

11. ⏸ **OQ-2 (LM band single-outlier hypothesis)** —
    CONFIRMED at v2.7.1 P2 soak (LM p95 = 12.37 s well-behaved
    vs overnight 5593 s). No further v2.8 carry; investigation
    CLOSED.

12. ⏸ **OQ-3 (gauge horizon)** + **OQ-4 (PR #154 schedule weight)**
    — UNCHANGED v2.7.1 P2; defer to v2.8+. PR #154 still DRAFT 9
    days old; non-blocking; operator-bound disposition only.

---

## Cycle-tip LOC delta (Amendment C narrative)

```
git diff 4902cca440b33c14fddd9357116923ae5fe1fa4b..HEAD --stat -- src tests tools dashboard
```

5 files changed; **+42 / −22 net +20 LOC** production-bucket.

| File                                     | Delta             | Phase / PR                |
|------------------------------------------|-------------------|---------------------------|
| `src/stream_manager/cli_governance.py`   | +1 / −1           | v2.7 P1 PR #203 (`28a89c4`) |
| `src/stream_manager/latency_budgets.py`  | +2 / −2           | v2.7 P1 PR #203           |
| `tests/test_cli_governance.py`           | +1 / −1           | v2.7 P1 PR #203           |
| `tests/test_alignment_eval_timing.py`    | +4 / −2           | v2.7 P1 PR #203           |
| `tools/ship_gate_runner.py`              | +34 / −16         | v2.7 P3 BLOCK PR #207 (`2a014ac`) stub-fold |

v2.7.1 itself: 0 production-bucket LOC (P1 re-measure + P2 ship-gate
docs/memory + tests/golden HOLD unchanged because Hatch B path).

PASS feature-cycle gate (net ≤ 1500 soft / 2250 BLOCK).

---

## WIRED_LEVER_LEDGER (canonical: production-bucket only)

| Cycle  | Wire                                                                      | Bucket    | Source           |
|--------|---------------------------------------------------------------------------|-----------|------------------|
| v1.x   | (pre-canonical; instrumentation-only wires not counted)                   | —         | —                |
| v2.3   | Seed 6 — JsonlTailWorker production wiring                                | `src/`    | PR #157          |
| v2.6   | Seed v2.5-G step (1) — alignment-eval wall-clock instrumentation          | `tools/` + `src/` | PR #196          |
| v2.7   | Seed v2.6-G step (2) — `TIMEOUT_SECONDS` cap-tighten (FROZEN-surface lever) | `src/`    | PR #203 (`28a89c4`) |
| v2.7.1 | (none — measurement only)                                                 | —         | —                |

**Posture exiting v2.7.1: 3 production / 0 soak (HOLD).**

---

## v10 P4 corpus piggyback (post-v2.7.1)

| Run    | Cycle / Source                                  | Cumulative episodes | Δ           |
|--------|-------------------------------------------------|---------------------|-------------|
| Run 8  | v2.7 P3 BLOCK soak (`soak-20260521T235321Z`)    | TBD                 | TBD         |
| Run 9  | v2.7.1 P2 soak (`soak-20260522T090102Z`)        | **see close memory** | TBD (BRIDGE_RL_LOGGER_ENABLED=1 set) |

(Episode counts fill at close-memory mint from rl_episodes.db query.
v2.8 P1 / P2 piggyback runs continue the corpus growth toward the
v10.3 stochastic-propensity stage.)

---

## Refs

- `docs/v2.7.1-task-plan.md` — sub-cycle frame + P1 LANDED row.
- `docs/v2.7.1-next-steps.md` — fire-order + per-seed disposition.
- `docs/v2.6-backlog.md` — predecessor backlog (carry-forward
  source for v2.7).
- `docs/2026-05-22-status.md` §"Proposal — Convergence cycle" —
  v2.8 frame source.
- `docs/prompts/v2.8-orchestration/phase-0-cycle-frame.md` — v2.8
  P0 frame skeleton (minted at v2.7.1 P2 S13).
- `reports/soak-20260522T090102Z.md` — v2.7.1 P2 Tier-3 soak (this
  cycle's binding measurement).
- `reports/v2.7.1-seed-v2.7-b/alignment-eval-20260522T085144Z.{md,json}`
  — v2.7.1 P1 row-05 n=12 evidence.
- `reports/v2.7.1-ship-gate/alignment-eval-<UTC>Z.{md,json}` —
  v2.7.1 P2 S4 alignment-eval evidence (filled at S4 completion).
- ADR-18 Amendments A–E (inherited; no new amendment minted in
  v2.7.1).
- v2.5 → v2.5.1 precedent: PR #75 / PR #188 / PR #189 / PR #190.
- v1.3 → v1.3.1 precedent: `ad372d7` (PR #75 Path-A).
