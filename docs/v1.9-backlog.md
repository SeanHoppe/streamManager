# v1.9 backlog

Seed list of items deferred out of v1.8 scope.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v1.9 task,
not by editing the emoji.

---

## Seed items

_None at v1.8 P0 cycle frame. Items will be appended by v1.8 P2 ship-gate
finalize if the soak / alignment-eval / lever-effect measurement produce
new caveats per the data-driven mint rules in `docs/v1.8-task-plan.md`
PHASE P2._

Carry-forward candidates (un-promoted from v1.8 — the v1.8 cycle did not
land them; re-open here only if v1.8 P2 explicitly defers). Promotion
decisions are made at v1.8 P2 close-out, NOT at this P0 frame — these
items stay in `docs/v1.8-backlog.md` until P2 explicitly re-homes them:

- 🟢 CLI pool sizing >2 — concurrent-burst fallback lever (still
  carried; v1.7 P3 disposition: NOT promoted; v1.8 P2 re-evaluates).
- 🟢 PPP audit harness — Provenance Probe Protocol; blocked-by sync-comms
  v1.0 HITL panel landing first; carries forward.

---

## Convention

When picked up in a v1.9 task, link the new task ID and the originating
phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
