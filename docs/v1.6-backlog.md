# v1.6 backlog

Seed list of items deferred out of v1.5 scope.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v1.6 task,
not by editing the emoji.

---

## Seed items

### 🟡 v1.6 P1 — `_evaluate_inner` CLI residue instrumentation

Source: v1.5 ship-gate (`reports/soak-20260504T201714Z.md`) + ADR-5 §"v1.5 ship-gate baseline" §"Caveats".

The v1.4 sub-phase-tail hypothesis is FALSIFIED: the five v1.5 sub-phases (`og7_check`, `fast_precheck`, `graph_classify`, `hydrator_state_read`, `routing_dispatch`) sum to 0.13 ms p95 vs `evaluate_inner` p95 = 5599 ms. ~99.998% of the ALLOW p95 tail lives in code paths NOT covered by v1.5 instrumentation, most plausibly the synchronous `cli_pool` round-trip on the escalation branch.

Scope: extend timing capture into `_maybe_cli_evaluate` (governance.py) and `CliGovernor.evaluate` (cli_governance.py) with five new sub-phase keys: `cli_setup_ms`, `cli_dispatch_ms`, `cli_pool_acquire_ms`, `cli_pool_send_ms`, `cli_parse_ms`. Render as a third soak-report block: `### ALLOW _evaluate_inner CLI residue breakout (v1.6)`. Verdict path byte-identical; v1.4 + v1.5 keys unchanged.

Goal: localize the ALLOW p95 tail to a specific component (model wall-clock vs pool acquisition vs CLI parse) so v1.7 can choose a defensible lever (Haiku fastpath vs pool sizing vs other).

Owner: v1.6 P1. Action prompt: `docs/prompts/v1.6-orchestration/phase-1-cli-residue-instrumentation.md`.

---

## Convention

When picked up in a v1.6 task, link the new task ID and the originating
phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
