# v1.4 backlog

Seed list of items deferred out of v1.3 scope. Each item links back to
the originating phase via `docs/v1.3-task-plan.md`.

Severity emoji convention is **frozen at original review time** per the
`docs/v1.3-backlog.md` convention — re-triage by adding a v1.4 task,
not by editing the emoji.

---

## Seed items (carry-forward from v1.3 scoping)

- 🟢 **Learn Mode: slide button (UI toggle)**
  Add a slide-toggle UI control on the dashboard header to enable/disable
  Learn Mode at runtime (per-session). Persists to localStorage. Wires
  through to the Sonnet categorizer worker's enable flag. Visual state
  must be obvious (off = grey, on = green pulsing).
  Originating: v1.3 P5 — Learn Mode shipped advisory-only; runtime
  kill-switch is a v1.4 ergonomic add.

- 🟢 **Testing scripts/scenarios library**
  Build out the `tests/scenarios/`, `tests/beacons/`, `tests/probes/`
  artifact libraries with a comprehensive set of canned operator
  scripts covering the v1.0–v1.3 surface (governance L1–L4 ladder,
  HITL queue, lifecycle bridge, SSE transport, WireCLI, Learn Mode
  categorizer). Goal: a fresh contributor can run
  `python tools/scenario_runner.py --all` and validate the entire SM
  surface without ad-hoc Desktop traffic.
  Originating: v1.3 P0 — testing methodology defined in
  `docs/v1.3-testing.md`; library buildout deferred.

---

## Convention

Items here are seeded from v1.3 phase scoping. When picked up in a v1.4
task, link the new task ID and the originating phase. Severity emoji:

- 🔴 critical — blocks ship gate
- 🟡 medium — should ship in cycle if effort allows
- 🟢 low — quality-of-life, ergonomic, opt-in
- 🔵 (rare) backlog hygiene — informational

Severity is set when the item lands here and is NOT re-triaged by edit.
