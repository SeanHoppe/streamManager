# v2.1 task plan — feature cycle (PPP audit harness)

**Cycle theme: feature.** Land the PPP (Provenance Probe Protocol)
audit harness in three work phases. Net-LOC delta target: feature
cycle (no cap). Three work phases + P0 docs frame + P4 ship-gate
= ADR-18 Rule 4 phase budget exactly.

**Cycle-type framing:** feature cycle (operator decision recorded at
P0). v2.0 was the deletion-positive consolidation cycle. v2.1 has no
DORMANT-N lever queued — `WIRED_LEVER_LEDGER` is empty post-v2.0 P3
rip — so DORMANT-N gate stays inert this cycle. Any v2.1 lever
introduction must bump `WIRED_LEVER_LEDGER` dict + ADR-18
`WIRED_LEVER_LEDGER_COUNT` HTML comment in the same PR (PPP envelope
pair is additive, not a DORMANT-N candidate).

**P1 lever pivot (recorded at P0):** the stub
`docs/prompts/v2.1-orchestration/phase-0-cycle-frame.md` (minted at
v2.0 P4 ship-gate, commit `3a2d9f7`) recommended 🟡 corpus-framing
parity as P1 candidate. At P0 review the operator declined that
lever (gap is structural curiosity, no fresh content-detection
consumer per backlog promotion criterion). The fallback candidate —
sync-comms v1.0 HITL panel — was found ALREADY SHIPPED in v1.0–v1.2
(Tasks D/E/F/K) when ground-truth-walked at P0; the
`project_sync_comms.md` memory was 5+ days stale and described
pre-ship state. With sync-comms v1.0 done, the 🟢 PPP audit harness
seed (carried v1.7 → v1.8 → v1.9 → v2.0 → v2.1, blocker
"sync-comms v1.0 HITL panel landing first") naturally unblocks. v2.1
P1 = PPP Layer 1 stream disambiguation. P2 = Layer 2 canary echo.
P3 = Layer 3 negative-control + self-monitor guard.

**Companion docs:**
- `docs/adr/ADR-18-mvp-surface-freeze.md` — cycle-discipline rules
  inherit unchanged. v2.1 P0 expects no ADR-18 amendment (purely
  additive envelope pair + new WAL table).
- `docs/v2.1-backlog.md` — 6 seeds at hard cap. P0 does NOT mutate
  the backlog (frozen-emoji rule per `docs/v1.3-backlog.md`
  convention; re-triage by adding a v2.2 task, not by editing
  emoji). PPP seed stays 🟢 in backlog; cycle ownership recorded
  here in task-plan instead.
- `docs/v1.7-backlog.md` §"🟢 PPP audit harness — Provenance Probe
  Protocol" — original seed (v1.6 ship-gate finalize S2 audit
  conversation); load-bearing scope reference for P1/P2/P3.
- `docs/v2.0-task-plan.md` — format template (immediate predecessor).

**Reference docs (always available, never inlined):**
- `REQUIREMENTS.md` — current spec; PPP harness adds new FR-PPP §
  block in P1.
- `docs/adr/ADR-5-latency-budget.md` — v2.0 ship-gate baseline at
  HEAD. PPP envelope pair is advisory (auto every 30min during soak,
  on-demand otherwise) and must NOT regress p95 — the per-30-min
  cadence is sparse enough that ADR-5 budgets stay observed.
- `docs/adr/ADR-14-desktop-command-sse.md` — SSE transport landed
  Task K; PPP HITL question rides this transport (no new transport).
- `docs/adr/ADR-17-soak-tiers.md` (+ Tier 1.5 amendment from v2.0
  P2) — Tier 3 ship-gate soak inherits PPP probe firing every 30min
  in P4.
- `src/stream_manager/desktop_commands.py` — `surface_hitl` kind
  already in allowlist (line 45); P1 adds `audit_probe` kind +
  governed-side handler emits `audit.probe_ack`.
- `src/stream_manager/desktop_command_consumer.py` — extends with
  `audit_probe` handler (additive; see Task E shape).
- `src/stream_manager/cross_session_hydrator.py` — provenance
  assertion writeback path passes through here (HMAC-signed assertion
  is logically a cross-session-pattern peer; same WAL writer pattern).
- `src/stream_manager/hitl.py` — P1 wires `audit_probe` as a new
  HITL trigger reason; pattern matches existing
  `cross_session_flag` precedent at `governance.py:1231`.
- `src/stream_manager/jsonl_tail.py` — P2 canary-echo observer
  reads from this; no signature change in P1.
- `src/stream_manager/session_watcher.py` — candidate-list source
  for P1 disambiguation question; P3 self-monitor guard filters by
  brain_id here.
- `src/stream_manager/message_bus.py` — `desktop_commands` /
  `hitl_pending` WAL tables; P1 adds `provenance_assertions` table
  (additive).
- `tools/cassette_record.py` + `tools/soak_driver.py` — REQUIRED
  same-cycle envelope coverage per
  `feedback_cassette_must_cover_new_envelopes.md`. P1 extends
  cassette with `audit.probe` / `audit.probe_ack` envelopes.
- Memory: `feedback_no_self_monitor.md`,
  `feedback_cassette_must_cover_new_envelopes.md`,
  `feedback_cross_pr_seam_review.md`,
  `feedback_subagent_stale_mental_model.md`,
  `feedback_parallel_undisclosed_deviations.md`,
  `project_sync_comms.md` (now SHIPPED status),
  `project_v20_cycle_close.md`.

**Format:** mirrors `docs/v2.0-task-plan.md`. Each phase block is
self-contained, hand verbatim to a fresh Claude Code session via
the matching prompt under `docs/prompts/v2.1-orchestration/`.

**Prior baseline:** v2.0.0 ship at `401ae47`. Soak source of truth:
`reports/soak-20260507T174051Z.md` (v2.0 ship-gate). Lever ledger
empty (DORMANT-N gate inert).

---

## CRITICAL: do-not-touch list — read from ADR-18

Per ADR-18 Rule 1, the FROZEN module list is the do-not-touch list.
v2.0 P3 ripped Haiku fastpath router + verdict-fallback retry path;
those rows are now in ADR-18 §"Decommissioned" (do not re-introduce
without explicit revival ADR). Re-stating the v2.1-impacting rows
for fast reference; full list lives in ADR-18 §"Initial
classification" + §"Decommissioned".

| Symbol / file | State | Cycle source |
|---|---|---|
| `cli_pool`, `CliPool`, `CliWorker`, `CliWorker.send`, `.bridge/cli-pool.pids` | FROZEN | v1.1 Task J |
| `model_router.route` band priority + `RoutingDecision` field set | FROZEN | NFR-M1-M5 |
| `tools/soak_driver.py` `--cli-replay`, `--cli-pool-size`, `_L2_L3_TRIGGER`, `_ALLOW_PHASE_ORDER`, `_format_allow_phase_breakout` | FROZEN | v1.2 / v1.4-1.8 |
| `_last_phase_timings_ms` keys (v1.4 + v1.5 + v1.6 + v1.7 sets minus v2.0-ripped `cli_dispatch_fallback_ms`) | FROZEN | v1.4-1.7 (extend, never remove or rename) |
| All bus envelope types from v1.1-v2.0 | FROZEN | metadata-only extensions |
| `desktop_commands` WAL table + `desktop_command` kind allowlist | FROZEN | v1.0–v1.2 Tasks D/E/K (extend kinds additively) |
| `cross_session_hydrator` + `patterns.cross_session` column + `cross_session_flag` HITL trigger | FROZEN | v1.0–v1.2 Task F |

**Rule:** if a v2.1 phase needs to modify any FROZEN row, the
modification must be additive (new kind in `desktop_command`
allowlist, new WAL table peer, new HITL trigger reason peer) per the
existing do-not-touch convention. Non-additive changes require an
ADR-18 amendment **before** the phase opens.

**Expected additive surface in v2.1:**
- New `desktop_command` kind: `audit_probe` (ack via existing
  `desktop_command_ack` envelope, no schema change).
- New WAL table: `provenance_assertions` (id, session_id,
  jsonl_path, brain_id, prompt_hash, signed_at, expires_at,
  hmac_sig). Peer to `desktop_commands` table (same migration
  pattern).
- New HITL trigger reason: `audit_probe`. Peer to
  `cross_session_flag` at `governance.py:1231`. Resolution sets
  provenance_assertion row, NOT a cross_session flag.
- New bus envelope pair: `audit.probe` / `audit.probe_ack`.
  Cassette MUST cover same-cycle.

---

## Task summary

| Phase | Title | Effort | Depends on | Action prompt |
|---|---|---|---|---|
| P0 | Cycle frame (THIS doc + phase-0 update + P1 prompt) | ¼ session | — | `phase-0-cycle-frame.md` |
| P1 | PPP Layer 1 — stream disambiguation + envelope pair + HMAC provenance assertion | 2 sessions | P0 merged | `phase-1-ppp-stream-disambiguation.md` |
| P2 | PPP Layer 2 — canary echo (nonce emit + JSONL observation + 10s timeout) | 1 session | P1 merged | `phase-2-ppp-canary-echo.md` (mint at P1 close-out) |
| P3 | PPP Layer 3 — negative-control fake stream + self-monitor brain_id guard | 1 session | P2 merged | `phase-3-ppp-negative-control.md` (mint at P2 close-out) |
| P4 | v2.1 ship-gate + ADR-5 v2.1 baseline + CHANGELOG + tag (PPP probe fires auto every 30 min during Tier 3 soak) | ½ session | P1 + P2 + P3 merged | `phase-4-ship-gate-finalize.md` (mint at P3 close-out) |

**Order:** P0 lands first (this PR — task plan + phase-0 update +
P1 prompt). P1 → P2 → P3 strictly serial (each layer depends on
previous WAL row + envelope shape). P4 cuts only after P3 merged.

**P2 / P3 / P4 prompts minted just-in-time** at the close-out of the
preceding phase. Pattern matches v10 P3 → P4 prompt minting (P4
prompt minted at P3 close-out, not at P0). Rationale: each layer's
shape may inform the next layer's prompt (e.g. envelope schema
finalised in P1 propagates verbatim into P2's nonce-emit envelope).

**Phase budget:** 3 work phases (P1/P2/P3) — exactly at ADR-18 Rule
4 cap. No P1a / P2a sub-phases without an explicit cycle-frame
amendment.

**LOC budget:** feature cycle (no cap). P1 estimated ~400-600 LOC
net add (envelope pair + WAL table + HITL trigger + dashboard panel
section + cassette coverage + tests). P2 ~200-400 LOC (canary observer
+ tests + cassette extension). P3 ~150-300 LOC (decoy stream
generator + self-monitor brain_id filter + tests). Total v2.1 ~750–
1300 LOC net add. Compare v1.9 ~+2800 LOC, v2.0 −1031 LOC; v2.1 lands
in the middle. If P1 alone exceeds 700 LOC, split work to a P1a
sub-phase (requires cycle-frame amendment per Rule 4).

**Backlog hard cap:** 6 seeds in `docs/v2.1-backlog.md` at P0 entry.
P0 does NOT mutate backlog (frozen-emoji rule). PPP seed remains 🟢
in backlog with cycle ownership noted ONLY in this task plan, not by
emoji edit. If v2.1 cycle work surfaces new findings worth seeding,
those become v2.2 backlog items via the v2.1 P4 ship-gate.

**Abandonment rule:** if P1 surfaces that the
`audit_probe`/`audit.probe_ack` envelope pair conflicts with an
existing FROZEN envelope (cassette coverage gap, schema collision),
ABORT P1 and downgrade v2.1 to consolidation cycle ripping the
🟢 CLI pool sizing >2 seed (3-cycle ADR-18 Rule 5 disposition trigger
fires automatically at v2.1 cycle frame).

---

## PHASE P0 — Cycle frame

**Pain point:** v2.1 phase-0 stub at `3a2d9f7` (minted at v2.0 P4
ship-gate) recommended 🟡 corpus-framing parity as P1 candidate.
Operator-review at P0 declined that lever and the immediate
fallback candidate (sync-comms v1.0 HITL panel) was found shipped
in v1.0–v1.2 (Tasks D/E/F/K). PPP audit harness seed (carry-forward
since v1.7 backlog) naturally unblocks with sync-comms v1.0 done;
v2.1 P0 records the pivot here, mints the P1 prompt, and updates
the phase-0-cycle-frame stub to match the operator decision.

**References:**
- `docs/prompts/v2.1-orchestration/phase-0-cycle-frame.md` — stub
  to be elaborated by this PR
- `docs/v2.1-backlog.md` — 6 seeds at hard cap; PPP not in this
  backlog (lives in v1.7 backlog as a 🟢 carry-forward)
- `docs/v1.7-backlog.md` §"🟢 PPP audit harness" — original scope
- `project_sync_comms.md` — UPDATED at P0 to record SHIPPED status
- Memory: `project_v20_cycle_close.md`,
  `feedback_subagent_stale_mental_model.md` (was load-bearing for
  the ground-truth walk that surfaced sync-comms-shipped),
  `feedback_cross_pr_seam_review.md`

**Do-not-touch guard:** P0 is docs only. Zero code edits.

**Scope:**
1. Create `docs/v2.1-task-plan.md` (THIS doc).
2. Mint `docs/prompts/v2.1-orchestration/phase-1-ppp-stream-disambiguation.md`.
3. Update `docs/prompts/v2.1-orchestration/phase-0-cycle-frame.md`
   to record:
   - Cycle type decision: feature
   - Primary lever (P1): PPP Layer 1 (replaces stub's
     corpus-framing recommendation)
   - Pivot rationale + sync-comms-shipped finding
4. Confirm `docs/v2.1-backlog.md` UNCHANGED (frozen-emoji rule;
   PPP cycle ownership recorded in task plan).
5. Confirm ADR-18 UNCHANGED (no v2.1 P0 amendment expected; PPP
   surface is purely additive).
6. Memory updates already landed pre-PR: `project_sync_comms.md`
   marked SHIPPED v1.0–v1.2; `MEMORY.md` index line updated.

**DOD:**
- [ ] `docs/v2.1-task-plan.md` created with P0 / P1 / P2 / P3 / P4
      blocks + LOC budget + phase budget + backlog cap declarations
- [ ] `docs/prompts/v2.1-orchestration/phase-1-ppp-stream-disambiguation.md`
      created
- [ ] `docs/prompts/v2.1-orchestration/phase-0-cycle-frame.md`
      updated with cycle-type decision + P1-lever decision +
      pivot rationale (preserve original stub content as
      §"Original P0 stub (superseded)" appendix)
- [ ] `docs/v2.1-backlog.md` UNCHANGED (`git diff` shows zero
      hunks against this file)
- [ ] `docs/adr/ADR-18-mvp-surface-freeze.md` UNCHANGED
- [ ] PR scope is docs-only —
      `git --no-pager diff origin/main..HEAD --stat -- src tests tools dashboard`
      empty
- [ ] Single PR against `main`
- [ ] Conventional commit prefix `docs(v2.1):`

**Action prompt:** `docs/prompts/v2.1-orchestration/phase-0-cycle-frame.md`

---

## PHASE P1 — PPP Layer 1 — stream disambiguation

**Pain point:** SM today binds gov decisions to a JSONL tail + bus
stream by path + project_slug. There is no runtime check that the
stream SM watches is the one the user is actually driving. Failure
modes are silent: wrong-session bind, mirror-frame collision (now
possible with sync-comms v1.0 multi-session brains), self-monitor
leak (per `feedback_no_self_monitor.md`), parser hallucination on
stale buffers. Manual audit at v1.6 ship-gate surfaced this gap;
v1.7→v2.0 deferred PPP behind sync-comms v1.0 HITL panel landing.
With sync-comms v1.0 shipped (Tasks D/E/F/K), Layer 1 unblocks.

**References:**
- `docs/v1.7-backlog.md` §"🟢 PPP audit harness" — three-layer scope
  + envelope shape + cassette requirement
- `src/stream_manager/desktop_commands.py:45` — extend allowlist
  with `audit_probe` kind
- `src/stream_manager/desktop_command_consumer.py` — Task E pattern
  for handler registration; add `audit_probe` handler
- `src/stream_manager/hitl.py:57` — `cross_session_flag` trigger
  reason precedent; add `audit_probe` peer
- `src/stream_manager/governance.py:1221,1231` — peer pattern for
  emitting `audit_probe` HITL queue entry on `/sm-probe` invocation
- `src/stream_manager/session_watcher.py` — candidate-list source
  (top-K streams currently watched)
- `src/stream_manager/message_bus.py` — WAL writer pattern;
  add `provenance_assertions` table migration
- `dashboard/static/index.html:715` — HITL queue panel; add
  audit-probe row variant rendering candidate-list radio buttons
- `tools/cassette_record.py` — extend with `audit.probe` /
  `audit.probe_ack` envelope coverage
- `tools/soak_driver.py` — auto-probe every 30 min during soak;
  flag remains opt-in this phase, becomes default in P4
- `feedback_cassette_must_cover_new_envelopes.md` — REQUIRED
  same-cycle cassette extension
- ADR-14 (SSE transport) — PPP HITL question rides existing SSE,
  no new transport

**Do-not-touch (additive only):** `desktop_commands.py` allowlist
extends with `"audit_probe"` (additive); `hitl.py` enum extends
with `AUDIT_PROBE = "audit_probe"` (additive); `governance.py`
emits new HITL queue entry via existing peer-pattern (additive call
site, no signature change); WAL adds new `provenance_assertions`
table (peer to `desktop_commands`, no schema change to existing
tables); cassette extends with new envelope pair (additive). Bus
envelope schemas for existing types UNCHANGED. `_last_phase_timings_ms`
keys UNCHANGED. CLI surface UNCHANGED.

**Scope:**
1. **Envelope pair `audit.probe` / `audit.probe_ack`** — add to bus
   message types. `audit.probe` payload:
   `{candidate_streams: [{slug, jsonl_path, brain_id, last_event_ts, prompt_hash}], ttl_seconds, hmac_sig}`.
   `audit.probe_ack` payload: `{selected_jsonl_path | none, signed_at, expires_at, hmac_sig}`.
2. **WAL `provenance_assertions` table:** id, session_id,
   jsonl_path, brain_id, prompt_hash, signed_at, expires_at,
   hmac_sig. Migration peer to `desktop_commands` table.
3. **`/sm-probe` endpoint** — on-demand HITL question with
   candidate-list. Responds with current cached assertion if not
   expired (TTL ~30 min); else emits new probe.
4. **HITL panel section** — render audit-probe row with
   candidate-list radio buttons + "none" option. Pattern matches
   existing `cross_session_promotion` row variant.
5. **Auto-probe cadence** — opt-in flag at P1; default-on at P4
   ship-gate. 30 min cadence during soak.
6. **Cassette coverage** — extend `tools/cassette_record.py` with
   the new envelope pair. Soak driver replay must cover
   `audit.probe` / `audit.probe_ack` round-trip.
7. **Tests:**
   - `tests/test_audit_probe_envelope.py` — schema validation,
     HMAC sig round-trip, TTL expiry.
   - `tests/test_audit_probe_hitl.py` — HITL queue entry emitted
     with `trigger_reason="audit_probe"`; resolution writes
     `provenance_assertions` row.
   - `tests/test_audit_probe_cassette.py` — cassette captures both
     envelopes; soak driver replay round-trips.
   - `tests/test_audit_probe_self_monitor.py` — candidate list
     EXCLUDES SM's own brain_id (deferred guard preview; full
     guard lands in P3, but P1 must not surface SM's own JSONL in
     the candidate list — defense-in-depth).

**DOD:**
- [ ] Envelope pair `audit.probe` / `audit.probe_ack` defined +
      tested
- [ ] `provenance_assertions` WAL table migrates cleanly + tested
- [ ] `audit_probe` kind in `desktop_commands.py` allowlist
- [ ] `audit_probe` HITL trigger reason in `hitl.py` enum
- [ ] `/sm-probe` endpoint returns assertion or fires new probe
- [ ] HITL panel renders audit-probe row variant
- [ ] Cassette coverage extended same-cycle (per
      `feedback_cassette_must_cover_new_envelopes.md`)
- [ ] Auto-probe is opt-in at P1 (default-on slips to P4 ship-gate)
- [ ] All 4 test files added; full suite green
- [ ] LOC budget ≤ 700 net add (split to P1a if exceeded)
- [ ] Single PR against `main`
- [ ] Conventional commit prefix `feat(ppp):`
- [ ] REQUIREMENTS.md FR-PPP-1..N appended
- [ ] Sub-cycle close-out diff PR head vs base on FROZEN seam
      files (`desktop_commands.py`, `hitl.py`, `governance.py`,
      `message_bus.py`) per `feedback_subagent_stale_mental_model.md`
- [ ] Cross-PR seam review (writer↔reader pairs) per
      `feedback_cross_pr_seam_review.md`: `audit.probe` writer
      (`/sm-probe` endpoint) + reader (`desktop_command_consumer`)
      + cassette + dashboard renderer all match the v1.7-backlog
      design table

**Action prompt:** `docs/prompts/v2.1-orchestration/phase-1-ppp-stream-disambiguation.md`

---

## PHASE P2 — PPP Layer 2 — canary echo

**Mint prompt at P1 close-out.** Scope sketch (per v1.7 backlog
§Layer 2):

- SM emits a nonce in active session.
- User types nonce in active session.
- SM observes nonce in claimed JSONL within 10s ⇒ binding proven
  in both directions.
- 10s timeout fallback: emits `audit.probe_failure` envelope;
  re-fires Layer 1 disambiguation HITL question.

**Effort:** 1 session. Builds on P1 envelope pair + WAL table.

---

## PHASE P3 — PPP Layer 3 — negative-control + self-monitor guard

**Mint prompt at P2 close-out.** Scope sketch (per v1.7 backlog
§Layer 3):

- Synthetic JSONL never written; if dashboard reports activity on
  it, parser is hallucinating.
- Self-monitor guard: candidate-list filter (P1 preview) elevated
  to hard guard — any JSONL whose `brain_id` matches SM's own
  brain_id is excluded from `/sm-probe` candidates.

**Effort:** 1 session. Wraps the harness with the safety net per
`feedback_no_self_monitor.md`.

---

## PHASE P4 — v2.1 ship-gate + ADR-5 v2.1 baseline + CHANGELOG + tag

**Mint prompt at P3 close-out.** Scope sketch:

1. Tier 3 ship-gate soak: `python tools/soak_driver.py --cli-pool-size 2`
   with PPP auto-probe enabled (default-on flip from P1's opt-in).
   Probe fires every 30 min; soak duration ~32 min ⇒ at least one
   probe fires during the gate.
2. Alignment-eval `--ci-gate` exit 0 (PPP is advisory; must not
   regress alignment).
3. ADR-5 v2.1 baseline section appended. Note PPP cadence does
   NOT shift p95 (envelope pair is sparse + advisory).
4. CHANGELOG.md v2.1.0 entry written.
5. v2.1.0 tag on a `main` commit; ship PR merged.
6. Memory mint: `project_v21_cycle_close.md`.
7. v2.2 backlog seed (if PPP harness surfaces new findings).
   Backlog rolls forward — close-out 🟢 PPP seed in v1.7 backlog
   chain, append v2.1 graduation note.
8. DORMANT-N gate stays inert (`WIRED_LEVER_LEDGER` empty post-
   v2.0 P3 rip; PPP envelope pair is additive, not a lever).

**DOD:** mirrors v2.0 P4 DOD with PPP-specific items. Detail at
P3 close-out.

---

## Cross-cutting risks

1. **Stale-memory recurrence** — v2.1 P0 caught
   `project_sync_comms.md` 5+ days stale; the PPP pivot would have
   been masked if the memory was trusted. Apply
   `feedback_subagent_stale_mental_model.md` rule: ground-truth
   walk `src/` before scoping any phase whose lever depends on a
   memory claim of "shipped" or "frozen".
2. **Cassette coverage drift** — PPP envelope pair MUST land in
   cassette same-cycle as P1 per
   `feedback_cassette_must_cover_new_envelopes.md`. v1.3 nearly
   tagged without LM coverage; same risk for PPP if cassette
   extension lags P1 PR.
3. **Self-monitor leak** — P1 deferred-guard preview must filter SM's
   own brain_id. Risk: P1 ships without the filter and the
   negative-control test in P3 only catches hallucination, not
   SM-watching-itself. P1 DOD includes
   `tests/test_audit_probe_self_monitor.py` as defense-in-depth.
4. **Sub-phase escape hatch** — if P1 exceeds 700 LOC, the abandonment
   rule says split to P1a. `feedback_subagent_escape_hatches.md`:
   flag any "deferred to follow-up" phrasing in subagent returns;
   demand authorization or in-PR landing.
