# 2026-05-24 task list — POC live-monitor fleet dispatch + v2.8 P2 prep

> **Source:** Operator `/goal` (2026-05-23 21:xx local): "what is next for Sean (operator)
> — have coordinator determine how to take operator's list and make a subagent do it.
> Also update status line to show what the subagents are doing and what they're doing
> next. + Create a task list .md file to show what the subagents are doing today.
> As items drain, add new ones."
>
> **Predecessor:** [`docs/2026-05-22-task-list.md`](2026-05-22-task-list.md) — POC
> fleet design (C0..C11) + INTENT.md conformance mapping. Goal-append §3 fleet roster
> + §4 INTENT mapping are the dispatch plan for today.
>
> **State at fire (2026-05-24):** v2.8 P1 Path-D MERGED at `07ee05c` (PR #214).
> POC fleet stubs minted under `.claude/agents/` (untracked operator state — pending
> bundle PR or staged here). v2.8 P2 (step (3) env-split + Seed v2.7-A-CLIP corpus
> re-measure) + v2.8 P3 ship-gate queued.
>
> **Coordinator:** main-thread (this Claude session). Subagents = dispatch targets,
> NOT dispatchers. `poc-coordinator.md` defines the workflow shape; main thread
> executes it.
>
> **Lifetime:** Discardable at POC ship verdict OR end-of-day, whichever first.
> Drain-queue model — completed rows struck through; new rows appended at bottom.

---

## Statusline tracking

Statusline reads `.claude/state/subagent-activity.txt` (single line, mtime-cached).
Main thread updates between dispatches. Format:

```
<current-subagent> | next: <next-subagent>
```

Empty file → no active dispatch (statusline shows branch only).

---

## Operator queue (source = §1-4 of 2026-05-22 task list + carry-forwards)

| Pri | Item | Owner | Status |
| --- | --- | --- | --- |
| **P0** | F-POC-1: Coordinator dispatches POC fleet C1..C11 (live-monitor smoke) | main-thread | in flight |
| **P0** | F-POC-2: Aggregate C1..C11 PASS/FAIL → `reports/poc-live-monitor-<UTC>.md` | main-thread | pending |
| **P0** | F-POC-3: Verdict review → SHIP / NO-SHIP <blocker> | operator | blocked on F-POC-2 |
| P1 | v2.8 P2 prep: step (3) env-split + Seed v2.7-A-CLIP corpus re-measure prompt mint | main-thread | pending |
| P1 | v2.8 P3 ship-gate prompt mint | main-thread | pending |
| P2 | Sonnet-DIP Seed v2.4-Q v2.5 carry-forward re-open (already CLOSED RECOVERED) | none | informational |
| P2 | POC fleet stub PR (bundle `.claude/agents/*.md` + `tools/poc_*.ps1` + `docs/poc/`) | operator | parallel to F-POC |

---

## POC subagent fleet — dispatch ledger (today's drain queue)

Source: `docs/2026-05-22-task-list.md` §3 + §4. Each row = one subagent dispatch
with main-thread-tracked status. Rows drain top-to-bottom.

| # | Subagent | Dispatch status | Verdict | Evidence path |
| --- | --- | --- | --- | --- |
| C1 | session-target-scout | **DONE 17:30Z** | **NO-TARGET** | inline above |
| C8 | firewall-auditor (baseline) | **DONE 11:33Z** | **BASELINE PASS** | inline above |
| C6 | robin (Path-D shadow side, synthetic-fixture) | **DONE** | **PASS** | 24/24 pytest 32.41s; constants + budget + isolation + FROZEN-diff all green |
| C10 | safety-priority-injector (cassette DRY-RUN) | **DONE** | **GAP** (4 blockers) | inline below |
| C2 | env-bootstrap-validator | **BLOCKED on C1** | — | — |
| C3 | tail-emitter-prober | **BLOCKED on C1** | — | — |
| C4 | governance-trace-verifier | **BLOCKED on C1** | — | — |
| C5 | dashboard-surface-prober | **BLOCKED on C1** | — | — |
| C7 | learn-mode-bias-prober | **BLOCKED on C1** (needs live envelopes) | — | — |
| C11 | context-reload-prober | **BLOCKED on C1** (needs governed-project target) | — | — |
| C9 | e2e-smoke-runner | **BLOCKED on C1** + needs operator turn | — | — |

**Gate logic (mirrors `.claude/agents/poc-coordinator.md` §"Per-stage gate logic"):**

- C1 FAIL → halt all; report `NO-SHIP no-target-locked`.
- C2 FAIL → halt C3/C4/C5/C9; C6/C7/C8/C10/C11 still fire (independent surfaces).
- C3 FAIL → skip C4/C5/C9; continue C6/C7/C8/C10/C11.
- C4 FAIL → skip C5/C9; continue C6/C7/C8/C10/C11.
- C5 FAIL → skip C9; continue C6/C7/C8/C10/C11.
- C8 FAIL at any point → ABORT all; verdict = `NO-SHIP firewall-breach <evidence>`.
- C6/C7/C10/C11 FAIL → record; do not halt other lanes.

---

## Drain log (append-only, newest at bottom)

> Format: `<UTC-ts> | <subagent> | <verdict> | <one-line evidence>`

```
2026-05-24T17:30Z | C1 session-target-scout | NO-TARGET | 7 live sessions; 4 SM-self + 3 certPortal-cwd → 0 survivors after filter
2026-05-24T11:33Z | C8 firewall-auditor (baseline) | BASELINE PASS | hot-zone clean; deny list intact; admissible textual refs only
2026-05-24T??:??Z | C10 safety-priority-injector (DRY-RUN) | GAP | p1-p4 cassette-ready in spec; p5 needs env override; pack not recorded; replay helper absent; role-binding schema gap (decisions table + governance_decision envelope lack agent_role/agent_profile_slug)
2026-05-24T??:??Z | C10-pre-req | DONE | safety pack 5 envelopes written to tests/cassettes/safety/p{1..5}-*.json (dry-run, failures=0)
2026-05-24T??:??Z | C6 robin Path-D shadow side | PASS | 24/24 pytest 32.41s; ship constants + non-invasion budget + DB isolation + FROZEN-diff all green
```

**BLOCKER surfaced to operator:** all live non-SM CLI sessions have cwd
rooted in `C:\Users\SeanHoppe\vs\certPortal\oversight\` which is the
FIREWALLED repo path. C2-C5/C7/C9/C11 are unblocked the moment operator
starts ONE non-SM, non-certPortal-repo `claude -p` session in ANY other
project root (candidates per C1 enumeration: `stedi`, `selfImprove`, or
any new project dir). Coordinator has a session-arrival monitor armed;
will resume C1 lock the moment an eligible session appears.

---

## New artefacts minted today

- `.claude/agents/poc-watch.md` — POC fleet drain-queue lifecycle owner (sibling to robin; reads task-list + state file + reports; emits NEXT-DISPATCH plan or WAIT action; main thread executes). Coexists with robin (disjoint scopes; C6 is the named delegation seam).

---

## Carry-in queue (added items appended here)

> Operator may append rows here for the coordinator to pick up next.

```
~~2026-05-24 | C10-derived | Record safety pack: `python tools/cassette_record_safety.py --dry-run` from main thread → emits tests/cassettes/safety/p{1..5}-*.json (≤ 60 s). PRE-REQ for C10 LIVE fire.~~ **DONE 2026-05-24** — 5 envelopes written, failures=0.
2026-05-24 | C10-derived | Decide replay path: (a) build `tools/cassette_replay.py` skeleton (≤ 50 LOC) OR (b) inline replay fixture in `tests/test_safety_pack_replay.py`. Coordinator picks (a) for re-use across C10 + future safety regressions.
2026-05-24 | C10-derived | Role-binding observability gap → file issue for ADR-18-compliant `governance_decision` envelope extension (add `agent_role` + `agent_profile_slug` as metadata-only fields per FROZEN amendment 2026-05-12). Decisions-table column add is a hot-zone migration (message_bus.py); defer unless P5 cycle slot opens.
2026-05-24 | C10-derived | p5 timeout simulation → record under `BRIDGE_FALLBACK_LATENCY_BUDGET_MS=1` to capture authentic OBSERVE-degradation; `--dry-run` produces synthetic OBSERVE not exercising real path.

### p5 re-record plan (closes C10 GAP blocker #4)

**Goal**: replace dry-run synthetic p5 envelope with one captured under a forced API timeout so `engine.evaluate` actually hits the OBSERVE-degradation path (not a synthetic envelope where `decision.action == expected_verdict` by construction).

**Concrete recipe**:

```powershell
# 1. Pre-flight: ensure live `claude` CLI on PATH (one-shot non-SM CLI session)
where.exe claude

# 2. Force the fallback budget to 1 ms so any L4 CLI call times out
$env:BRIDGE_FALLBACK_LATENCY_BUDGET_MS=1

# 3. Re-record ONLY p5 (preserve p1-p4 from existing pack)
#    --allow-overwrite is required because tests/cassettes/safety/p5-api-timeout-observe.json exists
python tools/cassette_record_safety.py --allow-overwrite

# 4. Reset env
Remove-Item Env:\BRIDGE_FALLBACK_LATENCY_BUDGET_MS

# 5. Verify p5 envelope reflects degraded path (NOT _dry_run:true, reasoning mentions exception)
type tests\cassettes\safety\p5-api-timeout-observe.json | jq '.decision, ._dry_run // null'
```

**Expected delta**: `decision.action == "OBSERVE"`, `decision.reasoning` contains `engine.evaluate raised` (per `tools/cassette_record_safety.py:179-196` exception-catch path), `recorded_latency_ms` ≈ 1-5 ms (timeout forces fast failure), no `_dry_run` field.

**Re-record DoD**:
- [ ] p5 cassette `_dry_run` key absent.
- [ ] `decision.reasoning` contains `engine.evaluate raised`.
- [ ] `recorded_latency_ms` ≤ `BRIDGE_FALLBACK_LATENCY_BUDGET_MS + 5_000` (5s slack for process spawn).
- [ ] `decision.action == "OBSERVE"`.
- [ ] p1-p4 envelopes unchanged (use `git diff tests/cassettes/safety/p[1-4]*` = empty).

**Caveat**: this fire requires a LIVE `claude` CLI on PATH (`tools/cassette_record_safety.py:149-151` refuse-if-absent guard). Operator runs from main thread; NOT a subagent task (per `docs/poc/next-steps.md` item 6 — "NOT a subagent — too touchy for delegation").

**When to fire**: after the POC bundle PR lands (this PR), as part of the C10 follow-up sub-cycle OR bundled into a later POC ship-gate fire-PR.
```

---

## Refs

- `docs/2026-05-22-task-list.md` §3 fleet roster + §4 INTENT mapping (binding).
- `.claude/agents/poc-coordinator.md` — workflow contract.
- `.claude/agents/{session-target-scout,env-bootstrap-validator,tail-emitter-prober,governance-trace-verifier,dashboard-surface-prober,robin,learn-mode-bias-prober,firewall-auditor,e2e-smoke-runner,safety-priority-injector,context-reload-prober}.md` — fleet definitions.
- `INTENT.md` (binding for §4 conformance).
- `docs/v10-mvp-status.md` §2 phase ledger.
- `feedback_subagent_long_task_abandonment.md` — no `>5min` Bash from any subagent.
- `feedback_certportal_dev_firewall.md` — firewall scope (repo source denied, session-transcript admissible).
- `feedback_no_self_monitor.md` — polarity-flip refusal at any wire site.
- `feedback_session_monitor_target.md` — C1 lock-in rule.
