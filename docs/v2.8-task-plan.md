# v2.8 task plan — Convergence cycle (skeleton)

> **Minted 2026-05-22 at v2.7.1 P2 S13** per v2.7.1 P2 ship-gate
> prompt + `docs/2026-05-22-status.md` §"Proposal — Convergence
> cycle to move the needle, not the margin".
>
> **Cycle classification: FEATURE (recommended); operator picks
> at v2.8 P0 fire** per `docs/prompts/v2.8-orchestration/phase-0-
> cycle-frame.md` §"Cycle-type call" decision block.
>
> **Skeleton scope.** Frames cycle posture + bundle order + LOC
> envelope. Phase details fill at each phase fire-PR open.

## Cycle posture entering v2.8

- **Predecessor tag:** `v2.7.1` at `5e971b4` (resolved at v2.8 P0
  fire 2026-05-22 from `git rev-parse v2.7.1`).
- **Cycle-tip anchor:** `70e23e5` (v2.8 P0 merge of PR #211;
  Amendment C; fresh mint; backfilled at v2.8 P0 backfill PR per
  v2.7 P0 / PR #201 precedent).
- **WIRED_LEVER_LEDGER entering:** 3 production / 0 soak.
- **WIRED_LEVER_LEDGER exiting (expected):** 4 production / 0 soak
  (step (3) env-split bumps ledger 3 → 4 — first FROZEN-surface
  classification widening since v2.7 P1).
- **Backlog cap-count entering:** 7 cap-counted (6 carry-forwards
  + Seed v2.7-A-CLIP NEW).
- **Backlog cap-count exiting (expected):** 2 cap-counted (v2.4-E
  + v2.4-F watch-only); 5-seed deflation in one cycle.
- **Monitor target (Q-D BOUND 2026-05-22):** certPortal sessions;
  peer SM EXCLUDED per polarity-flip. Env-var mandate (encoded dir
  form for jsonl_tail, joint encoded+short for refusal layers):
  `BRIDGE_PROJECT_SLUG=C--Users-SeanHoppe-VS-certPortal` +
  `BRIDGE_SM_PROJECT_SLUGS=<12 encoded SM dirs ∪ streamManager>`
  per phase fire that runs JsonlTailWorker. SM-side wire site
  `dashboard/server.py:292-331` + `src/stream_manager/jsonl_tail.py:178`
  + `rl/episode_logger.py:45,110`; no code change required at any
  v2.8 phase (pure runtime-config). Scope: binding redirects
  JsonlTailWorker live tail only; v10 P4 piggyback retains
  self-exclusion semantics. Refer
  `docs/prompts/v2.8-orchestration/phase-0-cycle-frame.md`
  §"Monitor target" §"Convention split" §"Scope clarification" for
  full env-var table + pre-launch checklist (incl. step 0 dashboard
  restart) + firewall reaffirmation.

## Phases (recommended bundle order)

### P1 — Seed v2.6-C Path-D synthetic-fixture P5

- **Prompt:** `docs/prompts/v2.8-orchestration/phase-1-seed-v2.6-c-
  path-d.md` (minted at v2.8 P1-prep PM-mint PR).
- **Scope:** ~+600 LOC in `rl/` + tests; synthetic-fixture P5
  shadow A/B + ship criteria infrastructure landing under v10.1-
  mode entry gate (Amendment D split).
- **Effect:** Unblocks issue #112 (v10 P5 shadow + ship criteria),
  which unblocks #131 (v10.x cycle frame freeze-lift), which
  unblocks #124 + #125.
- **Lever ledger:** HOLD 3/0 (Path-D touches `rl/` surface which is
  EVOLVING-not-FROZEN; not a v1.x/v2.x lever wire).
- **Cycle-tip LOC at P1 close (expected):** ~+600.

### P2 — Seed v2.6-G step (3) env-split + Seed v2.7-A-CLIP corpus re-measure

- **Prompt:** `docs/prompts/v2.8-orchestration/phase-2-env-split-
  plus-clip-remeasure.md` (minted at v2.8 P2-prep PM-mint PR).
- **Scope (env-split):** ~+50 LOC in
  `src/stream_manager/cli_governance.py` + tests. Splits
  `TIMEOUT_SECONDS` into prod-path (`BRIDGE_CLI_TIMEOUT`, default
  30 s) vs eval-path (`BRIDGE_CLI_TIMEOUT_EVAL`, default 60 s).
- **Scope (cap-clip re-measure):** 0 production LOC; runs corpus-
  wide n=12 alignment-eval against full
  `tests/golden/l4_alignment.jsonl` (32 rows) at eval-cap = 60 s
  per `docs/seed-v2.7-a-clip-corpus-protocol.md`. Output to
  `reports/v2.8-seed-v2.7-a-clip/`.
- **Lever ledger:** BUMP 3 → 4 (step (3) is FROZEN-surface
  classification widening on `cli_governance.py:49` line family).
- **Cycle-tip LOC at P2 close (expected):** ~+650 cumulative.

### P3 — ship-gate

- **Prompt:** `docs/prompts/v2.8-orchestration/phase-3-ship-gate.md`
  (minted at v2.8 P2 ship-gate PM-mint PR).
- **Scope:** S1–S13 ship-gate procedure (v2.6 P2 / v2.7 P3 /
  v2.7.1 P2 template). Cycle-tip LOC verify; ship-gate
  alignment-eval at `--runs 6` default (escape-hatch reading TBD
  per P2 cap-clip re-measure outcome); ADR-5 append; CHANGELOG;
  tag v2.8.0; close memory; v2.9 P0 mint.
- **Tag target:** **v2.8.0** (clean feature tag; parent path).

## Carry-forward state entering v2.8 (from v2.7.1-backlog)

| Seed              | Disposition entering v2.8 P0 frame                                                  |
|-------------------|-------------------------------------------------------------------------------------|
| v2.6-C Path-D     | **PROMOTED to P1 FIRE** (6th-consecutive defer avoided by Convergence pick)         |
| v2.6-G step (3)   | **PROMOTED to P2 FIRE** (env-split)                                                 |
| v2.6-A            | CLOSED by Seed v2.7-A-CLIP P2 re-measure (root-cause = cap-clip not content drift)  |
| v2.6-A-T          | CLOSED jointly with v2.6-A at P2 re-measure verdict                                 |
| v2.7-A-CLIP       | **PROMOTED to P2 FIRE** (corpus re-measure at eval-cap=60s)                         |
| v2.7-B            | CARRY-WITH-EXCLUSION from v2.7.1 P2; structural fix (`haiku_known_diverge` field)   |
|                   | also lands at P2 IF Hatch-B disposition holds at v2.8 P2 measurement                |
| v2.4-E (overall p95)  | watch-only; close-vote at v2.8 P3 ship-gate                                     |
| v2.4-F (L4 half)      | watch-only; close-vote at v2.8 P3 ship-gate                                     |

## LOC envelope verification (at P0 fire)

Re-run when v2.8 P0 anchor SHA is known:

```
git diff 70e23e5..HEAD --stat -- src tests tools dashboard rl
```

Expected cumulative cycle-tip delta:

- P1 Path-D:  ~+600 LOC (`rl/` + tests).
- P2 step (3) env-split:  ~+50 LOC (`src/` + tests).
- P2 cap-clip re-measure:  0 production LOC.
- P3 ship-gate:  0 production LOC.

**Total expected:** ~+650 LOC (~43 % of soft ≤ 1500 / 29 % of BLOCK
2250). Well inside envelope.

## Refs

- `docs/prompts/v2.8-orchestration/phase-0-cycle-frame.md` —
  companion P0 cycle-frame prompt.
- `docs/v2.8-next-steps.md` — operator-bound decisions; row-by-row
  disposition walk.
- `docs/v2.7.1-backlog.md` — carry-forward source (minted at
  v2.7.1 P2 close-out).
- `docs/2026-05-22-status.md` §"Proposal — Convergence cycle" —
  origin proposal text + LOC + velocity reasoning.
- `docs/seed-v2.7-a-clip-corpus-protocol.md` — J5 protocol for P2
  cap-clip re-measure.
- ADR-18 Amendment A + Amendment C + Amendment D.
- `project_v271_cycle_close.md` — v2.7.1 ship reference.
